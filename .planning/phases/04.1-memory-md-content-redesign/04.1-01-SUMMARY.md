---
phase: 04.1-memory-md-content-redesign
plan: "01"
subsystem: core/schema
tags: [schema, migration, V18, epoch-precision, lesson-substrate]
requires: [V17 user_version baseline]
provides: [V18 schema, shape_vocabulary, shape_candidates, normalizeEpochMs]
affects: [src/core/schema.ts, src/core/migrations.ts, src/core/migration-steps.ts, src/shared/epoch-utils.ts]
tech-stack:
  added: []
  patterns: [view-aware idempotent re-init, mixed-precision read-time normalization]
key-files:
  created:
    - src/shared/epoch-utils.ts
    - src/tests/shared/epoch-utils.test.ts
    - src/tests/core/migration-v17-v18.test.ts
  modified:
    - src/core/schema.ts
    - src/core/migrations.ts
    - src/core/migration-steps.ts
    - src/tests/core/curated-context.test.ts
    - src/tests/core/migration-v2v3.test.ts
    - src/tests/core/migration/v17-reopen.test.ts
    - src/tests/core/sqlite-vec-loader.test.ts
    - src/tests/embeddings/embed-pipeline.test.ts
    - src/tests/mcp/recall-server.test.ts
key-decisions:
  - "Two new tables (shape_vocabulary canonical + shape_candidates pending) with composite PKs"
  - "multi_project_count stored in artifact.data JSON, not as a critical_rules column (V17 view-over-artifact convention)"
  - "Read-time normalization (normalizeEpochMs) — no data migration; legacy 10-digit-second rows handled at read"
  - "V17 view-active detection in initializeSchema gates SCHEMA_V3 re-run safely"
  - "Demotion-prevention floor raised 16→18 to prevent silent demotion on re-open"
requirements-completed:
  - STOR-09
  - CUR-14
duration: 35 min
completed: 2026-04-29
---

# Phase 04.1 Plan 01: V18 schema migration + epoch-utils Summary

V18 schema migration shipped with shape_vocabulary + shape_candidates substrate tables for the Phase 4.1 lesson taxonomy, plus the read-time epoch-ms normalization helper required by the timestamp-precision lock.

## Files

**Created (3):**
- `src/shared/epoch-utils.ts` (19 lines) — `normalizeEpochMs(value)` helper
- `src/tests/shared/epoch-utils.test.ts` (38 lines) — 7 unit tests
- `src/tests/core/migration-v17-v18.test.ts` (123 lines) — 7 migration tests

**Modified (3 source + 6 test):**
- `src/core/schema.ts` — added `SHAPE_VOCABULARY_SCHEMA` constant
- `src/core/migrations.ts` — `TARGET_VERSION 16→18`, V17 view detection, demotion floor 18
- `src/core/migration-steps.ts` — added `migrateV17toV18` step
- 6 existing test files updated to assert `user_version=18`

## Migration version after this plan: 18

## Where `normalizeEpochMs` is consumed

**None yet.** Plan 02 fixes the transcript-chunker writer (one-line `* 1000` upscale at write time, eliminating the seconds-precision class for new rows). Read-time consumers (Plan 05/07/etc.) wire as needed for retrieval paths that compare across mixed-kind rows.

## `data.multi_project_count` access path

Confirmed JSON-extract via `json_extract(data, '$.multi_project_count')`. **No DDL change to critical_rules view.** The V17 view-over-artifact pattern stores all extension fields in the JSON `data` column; Plan 07 will UPDATE `artifact.data` directly (not via the critical_rules view) and read via the COALESCE-with-1 SELECT.

## Test count

**14 new tests:**
- 7 epoch-utils unit tests (ms passthrough, seconds upscale, null/undefined, non-finite, boundary, zero)
- 7 migration-v17-v18 tests (fresh DB, idempotency, V16-stub upgrade, direct call, index existence, table columns + PKs)

## Regression bumps

5 existing test files updated for `user_version` assertion 16→18:
- `curated-context.test.ts` (1)
- `migration-v2v3.test.ts` (1)
- `sqlite-vec-loader.test.ts` (1)
- `embed-pipeline.test.ts` (1)
- `recall-server.test.ts` (1)

Plus `v17-reopen.test.ts` test renamed to reflect the new ceiling: V17 DB auto-promotes to V18 on open (not "stays at 17").

## Tasks Completed

| # | Task | Files | Commit |
|---|------|-------|--------|
| 1 | Add SHAPE_VOCABULARY_SCHEMA + migrateV17toV18 | schema.ts, migration-steps.ts | 783d5d9 |
| 2 | Wire V17→V18 + bump TARGET_VERSION 16→18 | migrations.ts | 783d5d9 |
| 3 | Create normalizeEpochMs + 7 unit tests | epoch-utils.ts + .test.ts | 95347e5 |
| 4 | Migration regression tests V17→V18 + user_version bumps | 7 test files | 7d28d1a |

## Verification

- `bun run build` passes (esbuild + 24 hook smoke tests)
- All 14 new tests pass
- 0 non-llama regressions (20 pre-existing llama-server failures unchanged per STATE.md)
- Migration tested on fresh in-memory DB and on stub V16/V17 DBs
- v17-reopen integration test confirms post-V17 → V18 promotion does not throw

## Deviations from Plan

**[Rule 2 — Missing Critical] schema_versions table guard on post-V17 path** — Found during: Task 2 verification | Issue: post-V17 path skips SCHEMA_V3 (which contains schema_versions DDL); the version-record INSERT downstream then fails with "no such table: schema_versions" on partial v2 DBs upgraded in-process | Fix: Added explicit `CREATE TABLE IF NOT EXISTS schema_versions` before the INSERT in initializeSchema | Files modified: src/core/migrations.ts | Verification: integration/v2-compat.test.ts now passes | Commit: 783d5d9

**[Rule 2 — Missing Critical] V17-views-active gate for SCHEMA_V3 re-run** — Found during: Task 2 verification | Issue: SCHEMA_V3 contains `CREATE INDEX ON project_curated_context` which throws "views may not be indexed" on a fully-migrated V17 DB (where the table has been replaced by a view) | Fix: Detect via `sqlite_master WHERE type='view' AND name='learnings'`. If views are active, skip SCHEMA_V3; otherwise (legacy v2/partial-v3 DB), run SCHEMA_V3 to fill in v3 tables | Files modified: src/core/migrations.ts | Verification: v17-reopen.test.ts + v2-compat.test.ts both pass | Commit: 783d5d9

**[Rule 1 — Bug] Demotion-prevention floor stale at 16** — Found during: Task 2 verification | Issue: `if (currentUv < 16) db.pragma('user_version = 16')` would demote a fresh-V18 DB back to 16 if hit during initializeSchema | Fix: Raised floor to 18 to match new TARGET_VERSION | Files modified: src/core/migrations.ts | Commit: 783d5d9

**Total deviations: 3 auto-fixed (3 bugs/missing-critical).** **Impact:** All within Phase 4.1 scope; each was a downstream consequence of the TARGET_VERSION bump, caught by existing test suite, fixed inline per Rule 1/2 auto-policy.

## Issues Encountered

None blocking. Test count target (≥13) exceeded (14 new + 5 regression updates).

## Next Phase Readiness

**Ready for Plan 02** (transcript-chunker timestamp-precision fix + reach investigation). Plan 02 depends on Plan 01's `normalizeEpochMs` for read-time consumers (deferred to consumer plans per Plan 01 scope) and on V18 baseline for any downstream migration testing.

## Self-Check: PASSED

- 3 commits on master: `git log --oneline --grep="04.1-01"` → 783d5d9, 95347e5, 7d28d1a
- Files created on disk: `ls src/shared/epoch-utils.ts src/tests/shared/epoch-utils.test.ts src/tests/core/migration-v17-v18.test.ts` → all exist
- Build passes; new tests pass; no non-llama regressions
