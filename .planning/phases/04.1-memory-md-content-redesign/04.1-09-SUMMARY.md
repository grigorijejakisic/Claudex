---
phase: 04.1-memory-md-content-redesign
plan: "09"
subsystem: tests/integration + scripts
tags: [live-fire, behavioral-gate, SC#3, perceptual-probes, soak, phase-closure]
requires: [Plans 01-08 all complete]
provides: [behavioral live-fire test, SC#3 content-quality scorer, perceptual probe set, soak script]
affects: [src/tests/integration/, scripts/]
tech-stack:
  added: []
  patterns: [Jaccard substrate-adequacy probes, mechanical SC#3 scorer with parsing-gate, end-to-end soak]
key-files:
  created:
    - src/tests/integration/phase-4-1-live-fire-behavioral.test.ts
    - src/tests/integration/phase-4-1-perceptual-similarity-probes.test.ts
    - src/tests/integration/phase-4-1-content-quality-scorer.test.ts
    - scripts/phase-4-1-soak.ts
  modified:
    - build.ts (added scripts/phase-4-1-soak.ts to optional entry points)
key-decisions:
  - "5 live-fire tests cover the full behavioral gate (density-3 promotion, density-2 negative, multi=2, +2 boost, full-pipeline)"
  - "4 perceptual-similarity probes assert substrate adequacy (Jaccard ≥0.4, session_arc overlap, shape match-or-abstain, duration-invariant)"
  - "SC#3 scorer is mechanical (parsing_ok + 4 component scores averaged)"
  - "Soak script ships as compiled CJS in dist/scripts/phase-4-1-soak.cjs (build.ts entry-point added)"
requirements-completed:
  - CUR-09
  - CUR-10
  - CUR-11
  - CUR-12
  - CUR-13
  - CUR-14
  - CUR-15-precision (reach deferred per Plan 02)
  - STOR-09
duration: 50 min
completed: 2026-04-29
---

# Phase 04.1 Plan 09: Live-fire gate + SC#3 + probes + soak Summary

Phase 4.1 closes with the behavioral live-fire integration test exercising every Plan 01-08 deliverable, plus the substrate-validation probe set for Phase 6.5, plus the SC#3 content-quality scorer and an operational soak script.

## Files

**Created (3 integration tests + 1 soak):**
- `src/tests/integration/phase-4-1-live-fire-behavioral.test.ts` (5 tests)
- `src/tests/integration/phase-4-1-perceptual-similarity-probes.test.ts` (4 tests)
- `src/tests/integration/phase-4-1-content-quality-scorer.test.ts` (7 tests)
- `scripts/phase-4-1-soak.ts` (operational, compiles to dist/scripts/)

**Modified:**
- `build.ts` — added `scripts/phase-4-1-soak.ts` to optionalEntryPoints

## Test count

**16 new tests, all passing.**

Live-fire (5):
1. density-3 → critical_rules promotion in single project
2. density-2 → NO promotion
3. multi_project_count = 2 after 2-project bridge
4. +2 scoring boost via assembleCriticalReminders
5. Full pipeline smoke (3 lessons → critical_rule → sidecar → scorer)

Perceptual-similarity probes (4) — for Phase 6.5 hand-off:
1. Canonical + 5 variants share Jaccard ≥0.4 on user_framing_tokens
2. Variants share at least one session_arc segment
3. Shape handles match across variants OR are abstained either side
4. Duration variation does not break framing-token adequacy

SC#3 content-quality scorer (7):
1. Corrupted fixture (duplicate header) → score 0
2. Well-formed fixture with 5 lesson pointers → ≥80
3. Bad pointer specificity (raw text instead of lesson filenames) → <80
4. Bad topic quality (session-IDs in titles) → <100
5-7. Plan 06 fixtures (Lacuna/Oracle/Nexus) post-migration: parsing_ok=true

## Soak verification

Ran `node dist/scripts/phase-4-1-soak.cjs` — exit 0, output:
```
Phase 4.1 Soak — PASS

  [PASS] critical_rules count = 1 per project
         projectA=1, projectB=1
  [PASS] multi_project_count = 2 in sidecar
         mpA=2, mpB=2
  [PASS] assembleCriticalReminders renders system-promoted rule with boost
         section length: 80
```

## Pass/fail report

**Live-fire behavioral gate: PASSING.** All 5 invariants hold (>80% per CONTEXT.md gate criterion):
- density-3 promotion fires
- density-2 does not
- multi_project_count cross-project tracking works
- +2 boost applied in scorer
- end-to-end pipeline runs clean

**SC#3 mechanical scoring: PASSING (rubric implemented).** Scorer correctly distinguishes corrupted vs well-formed; Plan 06 fixtures pass parsing_ok=true. Content score totals depend on managed-section content (Active Projects + Lessons populated by DB state); the gate is parsing_ok and component scores not the absolute total.

**Substrate-validation probes: PASSING.** All 4 probes hold; substrate carries enough signal that Phase 6.5's matcher will be able to group variants of the same experience.

## Phase 4.1 closure

Every locked deliverable from CONTEXT.md is exercised end-to-end:
- ✅ Three lesson types with frontmatter (Plan 04 + tests in Plan 09 live-fire)
- ✅ Layered storage: telemetry handles + shape handles + narrative body (Plan 04 schema, exercised in Plan 09 probes)
- ✅ MEMORY.md restructure: drop entities/threads, add lessons (Plan 05 + tested in heartbeat regression)
- ✅ Vocabulary growth protocol: candidate→canonical at density 3 (Plan 07 shape-vocab-promoter)
- ✅ Trajectory preservation: process_* with 2-of-5 trigger (Plan 08 process-trigger)
- ✅ Feedback → critical_rules promotion bridge (Plan 07 feedback-promoter; gated by Plan 09 live-fire)

## Tasks Completed

| # | Task | Files | Commit |
|---|------|-------|--------|
| 1 | Behavioral live-fire integration test | phase-4-1-live-fire-behavioral.test.ts | b6726ad |
| 2 | Perceptual-similarity probe set (6.5 hand-off) | phase-4-1-perceptual-similarity-probes.test.ts | b6726ad |
| 3 | Mechanical SC#3 content-quality scorer | phase-4-1-content-quality-scorer.test.ts | b6726ad |
| 4 | Soak script for operational e2e validation | scripts/phase-4-1-soak.ts + build.ts | b6726ad |

## Verification

- `bun run build` passes (24 hook smoke tests + soak script compiles)
- `bun run test src/tests/integration/phase-4-1*.test.ts` — 16 tests pass
- `node dist/scripts/phase-4-1-soak.cjs` — exit 0, all 3 invariants PASS

## Deviations from Plan

**[Rule 1 — Bug] Bun cannot load better-sqlite3** — Found during: Task 4 verification | Issue: `bun run scripts/phase-4-1-soak.ts` failed with "better-sqlite3 is not yet supported in Bun" | Fix: Added scripts/phase-4-1-soak.ts to build.ts optionalEntryPoints; users run via `node dist/scripts/phase-4-1-soak.cjs` instead. Documented in script header. | Files modified: build.ts | Commit: b6726ad

**Total deviations: 1 (Rule 1 — runtime workaround for bun-vs-node).**

## Issues Encountered

None blocking. Bun limitation worked around by compiling to CJS via existing build pipeline.

## Phase 4.1: COMPLETE

All 9 plans landed. Phase ships with:
- New schema: V18 (shape_vocabulary, shape_candidates, critical_rules_multi_project sidecar)
- Lesson taxonomy: feedback / project / process types with locked frontmatter
- MEMORY.md restructure: entities/threads dropped, ## Lessons added with foreground/background tier
- Bridge: feedback density-3 → system-promoted critical_rules + multi-project +2 boost
- Curation: /endsession lesson proposer with 2-of-5 process_* trigger gating
- Live-fire gate: behavioral assertion all hold

## Self-Check: PASSED

- 1 commit on master: `git log --oneline --grep="04.1-09"` → b6726ad
- 4 files created on disk (3 tests + 1 script)
- Build passes; 16 integration tests pass; soak PASS
- Phase 4.1 closure note documents every CONTEXT.md deliverable mapping
