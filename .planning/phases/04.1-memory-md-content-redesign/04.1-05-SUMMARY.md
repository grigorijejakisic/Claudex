---
phase: 04.1-memory-md-content-redesign
plan: "05"
subsystem: angel/memory-md-writer
tags: [MEMORY.md, restructure, Lessons-section, CUR-09, CUR-10]
requires: [Plan 03 line-anchored marker, Plan 04 lesson-reader]
provides: [renderLessons function, ## Lessons section in MEMORY.md output]
affects: [src/angel/memory-md-writer.ts]
tech-stack:
  added: []
  patterns: [filesystem-sourced pointer-line index, foreground/background tier filter, attention-budget trim chain]
key-files:
  created:
    - src/tests/angel/memory-md-writer-lessons-section.test.ts
  modified:
    - src/angel/memory-md-writer.ts
    - src/tests/angel/memory-md-writer.test.ts
key-decisions:
  - "Drop ## Entities and ## Recent Threads from MEMORY.md output (sources of noise: entity:-, entity:--2--1, session-IDs as topics)"
  - "Add ## Lessons — pointer-line index sourced from lesson files (Plan 04 substrate)"
  - "Section order: preamble → Active Projects → Lessons → Handoff → How to Query"
  - "Foreground filter (tier === 'foreground' OR undefined) — background tier on disk but not in MEMORY.md"
  - "20-entry foreground cap; ≤140-char total pointer line length"
  - "renderEntities/renderRecentThreads kept as dead exports for now (cleanup deferred)"
requirements-completed:
  - CUR-09
  - CUR-10
  - CUR-12 (covered by Plan 06)
duration: 35 min
completed: 2026-04-29
---

# Phase 04.1 Plan 05: MEMORY.md restructure (## Lessons section) Summary

`renderLessons` added to memory-md-writer.ts, sourced via `listLessonsForProject` from Plan 04. `curateMemoryMd` section assembly updated to drop entities/threads and add lessons. `enforceSizeCap` trim chain reordered. Existing tests updated to reflect the new schema (Entities/Recent Threads dropped, Lessons added).

## Files

**Created:**
- `src/tests/angel/memory-md-writer-lessons-section.test.ts` (8 tests, ~190 lines)

**Modified:**
- `src/angel/memory-md-writer.ts`:
  - Added `MAX_LESSONS_FOREGROUND = 20` and `POINTER_LINE_MAX_CHARS = 140` constants
  - Added `import { listLessonsForProject } from './lesson-reader.js'`
  - Added `renderLessons(project): string` exported function
  - Added internal `extractLessonSalience(body): string` helper
  - Updated `curateMemoryMd` section assembly: dropped entities/threads, added lessons
  - Updated `enforceSizeCap` signature and trim chain: lessons → projects → handoff
- `src/tests/angel/memory-md-writer.test.ts`:
  - Updated 3 existing tests to assert new schema (Entities/Recent Threads absent, Lessons present)
  - User-tail preservation test now uses Active Projects diff to drive write trigger

## Test count

**8 new tests, 77 total memory-md-writer + lesson-related green** (34 baseline + 9 marker-bug + 9 migration + 8 lessons-section + 17 lesson writer/reader).

renderLessons direct (6):
1. Empty-state: header + 'No lessons captured yet.'
2. Foreground rendering with task-pattern + salience extraction
3. "unclassified" fallback when shape abstained
4. ≤140 char pointer line cap on pathological long body
5. Background-tier exclusion
6. 20-entry foreground cap with last_fired_at_epoch DESC sort

curateMemoryMd integration (2):
7. Output has no `## Entities` and no `## Recent Threads`; has `## Lessons`
8. Section ordering: Lessons → Handoff → How to Query

## Dead exports list

`renderEntities` and `renderRecentThreads` remain in `memory-md-writer.ts` as exported but unused. CONTEXT.md authorized this defer ("just remove them from the `sections` literal in `curateMemoryMd`. They become dead exports — Plan 09 (or a follow-up cleanup phase) can remove them after green-soak").

## Section reorder verified

Verified via the integration test: in the rendered output, `## Lessons` appears AFTER `## Active Projects` and BEFORE `## Handoff`. Order: preamble → Active Projects → Lessons → Handoff → How to Query.

## Tasks Completed

| # | Task | Files | Commit |
|---|------|-------|--------|
| 1 | Add renderLessons + update curateMemoryMd assembly + enforceSizeCap | memory-md-writer.ts | 0167c70 |
| 2 | Tests for ## Lessons + section reorder + existing-test updates | memory-md-writer*.test.ts | 0167c70 |

## Verification

- `bun run build` passes
- `bun run test src/tests/angel/memory-md-writer*.test.ts src/tests/angel/lesson-*.test.ts` — 77 tests pass
- Section ordering verified
- Foreground filter + 20-cap + 140-char cap all enforced

## Deviations from Plan

**[Rule 1 — Bug] require()-style lazy import broke at runtime** — Found during: Task 1 verification | Issue: my initial implementation used `const { listLessonsForProject } = require('./lesson-reader.js')` for lazy loading; this errored at runtime in vitest because the path resolution differs from the compiled-CJS one | Fix: Changed to a static `import` at the top of memory-md-writer.ts | Files modified: src/angel/memory-md-writer.ts | Commit: 0167c70

**Total deviations: 1 auto-fixed (Rule 1).**

## Issues Encountered

None blocking. Three existing memory-md-writer tests required updates to reflect the new schema (Entities/Recent Threads dropped, Lessons added). Updates were straightforward — no test logic regressed, just adjusted assertions.

## Next Phase Readiness

**Wave 3 partner Plan 08 ready** (lesson proposer, depends on 04 only — already complete). After Plan 08, **Plan 07** (bridge sweeps, depends on 01+04+05) can run.

## Self-Check: PASSED

- 1 commit on master: `git log --oneline --grep="04.1-05"` → 0167c70
- File created on disk: lessons-section.test.ts; modifications applied to memory-md-writer.ts
- Build passes; 77 tests pass
