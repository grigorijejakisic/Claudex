---
phase: 04.1-memory-md-content-redesign
plan: "04"
subsystem: angel/lesson
tags: [lesson, types, writer, reader, frontmatter, STOR-09, CUR-10]
requires: [Plan 01 V18 baseline]
provides: [LessonType, LessonFrontmatter, ParsedLesson, writeLesson, parseLessonFile, listLessonsForProject]
affects: [src/angel/lesson-types.ts, src/angel/lesson-writer.ts, src/angel/lesson-reader.ts]
tech-stack:
  added: []
  patterns: [layered storage frontmatter (telemetry required + shape abstain-allowed); atomic write tmp+rename; non-throwing parser]
key-files:
  created:
    - src/angel/lesson-types.ts
    - src/angel/lesson-writer.ts
    - src/angel/lesson-reader.ts
    - src/tests/angel/lesson-writer.test.ts
    - src/tests/angel/lesson-reader.test.ts
  modified: []
key-decisions:
  - "Slug separator: hyphen over underscore (planner discretion per CONTEXT.md)"
  - "Shape block omitted entirely when all fields are undefined (abstain-allowed)"
  - "Reader is non-throwing — every malformed-input branch returns null"
  - "Filename ↔ frontmatter.type mismatch refuses parsing (defends against rename collisions)"
  - "Inline confidence comments on shape values are stripped at parse time"
requirements-completed:
  - STOR-09
  - CUR-10
duration: 35 min
completed: 2026-04-29
---

# Phase 04.1 Plan 04: Lesson types/writer/reader substrate Summary

Three new source files + two test files build the Phase 4.1 lesson type system substrate. Writer enforces locked frontmatter schema (telemetry required, shape abstain-allowed, ms-precision epoch). Reader is non-throwing and handles inline confidence comments.

## Files

**Created (3 source + 2 test):**
- `src/angel/lesson-types.ts` (89 lines) — 6 type/interface exports
- `src/angel/lesson-writer.ts` (~140 lines) — `writeLesson`, `computeLessonFilePath`, `renderLessonFrontmatter`
- `src/angel/lesson-reader.ts` (~180 lines) — `parseLessonFile`, `listLessonsForProject`
- `src/tests/angel/lesson-writer.test.ts` (9 tests)
- `src/tests/angel/lesson-reader.test.ts` (8 tests)

## Test count

**17 new tests, all passing.**

Writer (9):
1. Feedback lesson round-trip
2. Project lesson with shape ABSENT (abstain) round-trips with shape undefined
3. Process lesson with triggered_by + full shape
4. Idempotency: 2nd write same content produces no change
5. Empty body rejection
6. Missing telemetry array fields rejection
7. Pre-1e12 epoch rejection (CUR-14 wedge)
8. Slug whitespace/uppercase rejection
9. Render: shape block omitted when all fields undefined

Reader (8):
1. Non-existent file → null
2. No frontmatter delimiters → null
3. Filename ↔ frontmatter.type mismatch → null
4. Round-trip: writer → reader produces same fields
5. Inline confidence comments stripped from shape values
6. Empty memory directory → []
7. Sorted list, malformed files skipped, non-lesson `*.md` ignored
8. tier + last_fired_at_epoch fields parsed

## Tasks Completed

| # | Task | Files | Commit |
|---|------|-------|--------|
| 1 | Define lesson types | lesson-types.ts | 03d1653 |
| 2 | Implement lesson-writer | lesson-writer.ts | 03d1653 |
| 3 | Implement lesson-reader | lesson-reader.ts | 03d1653 |
| 4 | Round-trip tests for writer + reader | lesson-{writer,reader}.test.ts | 03d1653 |

## Verification

- `bun run build` passes
- `bun run test src/tests/angel/lesson-*.test.ts` — all 17 tests pass
- Schema lock enforced at writer (CUR-14 ms-precision, telemetry required, slug validation)
- Round-trip preserves abstention (shape: undefined in → shape: undefined out)

## Deviations from Plan

None. Plan 04 executed as written.

**Total deviations: 0.**

## Issues Encountered

None.

## Next Phase Readiness

**Ready for Plan 05** (MEMORY.md restructure with ## Lessons section — depends on 03+04) and **Plan 07** (bridge sweeps — depends on 01+04+05) and **Plan 08** (lesson proposer — depends on 04). No callers wired yet; Plans 05/07/08 wire them.

## Self-Check: PASSED

- 1 commit on master: `git log --oneline --grep="04.1-04"` → 03d1653
- Files created on disk: lesson-types.ts, lesson-writer.ts, lesson-reader.ts, lesson-writer.test.ts, lesson-reader.test.ts
- Build passes; 17 tests pass; no regressions on sibling tests
