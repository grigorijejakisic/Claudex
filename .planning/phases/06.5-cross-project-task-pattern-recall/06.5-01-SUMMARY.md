---
phase: 06.5-cross-project-task-pattern-recall
plan: 01
subsystem: schema-migration
tags: [migration, sidecar, classifier, heartbeat, backfill]
requires: []
provides: [V21-schema, artifact_task_pattern-table, task-pattern-classifier]
affects: [src/core/schema.ts, src/core/migrations.ts, src/core/migration-steps.ts, src/angel/lesson-writer.ts, src/angel/heartbeat.ts]
tech-stack:
  added: []
  patterns: [sidecar-table, INSERT-OR-IGNORE, regex-first-with-abstain, LEFT-JOIN-filter]
key-files:
  created:
    - src/angel/task-pattern-classifier.ts
    - src/tests/core/migrations-v21.test.ts
    - src/tests/angel/task-pattern-classifier.test.ts
    - .planning/phases/06.5-cross-project-task-pattern-recall/06.5-01-BACKUP.md
  modified:
    - src/core/schema.ts
    - src/core/migrations.ts
    - src/core/migration-steps.ts
    - src/angel/lesson-writer.ts
    - src/angel/heartbeat.ts
    - src/tests/core/migrations-v20.test.ts
    - src/tests/core/migrations-v19.test.ts
    - src/tests/core/migration-v17-v18.test.ts
    - src/tests/core/migration-v2v3.test.ts
    - src/tests/core/curated-context.test.ts
    - src/tests/mcp/recall-server.test.ts
    - src/tests/core/sqlite-vec-loader.test.ts
    - src/tests/embeddings/embed-pipeline.test.ts
    - src/tests/core/migration/v17-reopen.test.ts
key-decisions:
  - "Sidecar pattern (CREATE TABLE artifact_task_pattern) NOT ALTER on artifact view — same approach Phase 4.1 used for critical_rules_multi_project; V17 view-over-artifact path doesn't compose cleanly with column ALTERs"
  - "Task pattern row keyed by lesson_pointer.id for filesystem-based lessons; separate writeLessonWithTaskPattern wrapper rather than mutating writeLesson signature"
  - "Topology rules carry confidence floor 0.85 baked-in; Jaccard >0.5 boosts above floor; abstain rows write '__abstain__' sentinel so LEFT JOIN filter prevents heartbeat reprocessing"
  - "Backfill uses regex-only classifier (no LLM) — bounded reach 200 rows/tick"
requirements-completed:
  - RETR-06
duration: 14 min
completed: 2026-04-29
---

# Phase 6.5 Plan 01: V21 Schema Substrate Summary

V21 schema bump shipping the `artifact_task_pattern` sidecar table + telemetry CHECK enum extension (`cross_project_ambiguous`, `cross_project_query_expansion`), plus the regex-first task-pattern classifier with write-time integration and heartbeat backfill — closing Phase 6.5 deliverable #1 (substrate side) and #6 substrate (cross-project content equivalence — function side ships in Plan 02).

**Duration:** 14 min  
**Start:** 2026-04-29T21:12:33Z  
**End:** 2026-04-29T21:26:55Z  
**Tasks:** 3 (all autonomous, atomic commits per task)  
**Files:** 4 created + 14 modified

## Task-by-task

### Task 1 — V21 migration + schema bump (commit 3b7df19)

- `ARTIFACT_TASK_PATTERN_SCHEMA` constant added to `src/core/schema.ts` near `SHAPE_VOCABULARY_SCHEMA` block.
  - PK on `artifact_id`, secondary index on `task_pattern`.
  - `classifier_source` CHECK enum: `('write_time', 'heartbeat_backfill')`.
- `TELEMETRY_SCHEMA` `event_kind` CHECK enum extended with `'cross_project_ambiguous'` and `'cross_project_query_expansion'`.
- `migrateV20toV21` in `src/core/migration-steps.ts` mirrors V20's rebuild-and-copy pattern with stub-DB and pre-V19-shape guards. Idempotent via the `telemetryAcceptsCrossProjectEnums(db)` probe.
- `migrations.ts`: `TARGET_VERSION 20→21`; new `[20, () => migrateV20toV21(db)]` step; demote-protection ceiling raised; `db.exec(ARTIFACT_TASK_PATTERN_SCHEMA)` inserted in both fresh-DB and post-V17 paths after `POINTER_RECALL_SCHEMA`.
- 8 V21 unit tests; 16 prior V20-floor assertions updated to V21 across 9 test files.
- Live DB copy migration verified: 8942 artifacts + 14394 telemetry rows preserved; sidecar table+index present; both new event_kinds accepted.

### Task 2 — Task-pattern classifier + write-time integration (commit d8ca71a, part 1)

- `src/angel/task-pattern-classifier.ts` (new) — three exports:
  - `classifyTaskPattern(db, handles, shape?, source?='write_time')` — three-stage classifier: direct shape match → Jaccard fallback → topology rules → abstain.
  - `writeTaskPattern(db, artifactId, result)` — INSERT OR IGNORE; null `task_pattern` is a no-op.
  - `backfillTaskPatternsBatch(db, batchSize=200)` — heartbeat batch path; LEFT JOIN filter; sentinel-write for abstains.
- `src/angel/lesson-writer.ts` — adds `writeLessonWithTaskPattern(db, params)` wrapper:
  - Calls `writeLesson` (lesson file).
  - Resolves `lesson_pointer.id` via `ensurePointerId`.
  - Classifies and writes task_pattern row.
  - Failures swallowed (non-fatal — lesson is source of truth, fingerprint is metadata).
- 20 unit tests (6 classifyTaskPattern, 3 writeTaskPattern, 5 backfillTaskPatternsBatch, 2 lesson-writer integration, 4 split across categories).

### Task 3 — Heartbeat backfill phase wiring (commit d8ca71a, part 2)

- `src/angel/heartbeat.ts`: import `backfillTaskPatternsBatch`; insert new "Phase 4b.5: task-pattern fingerprint backfill" between retention sweep (4b) and cross-project consolidation (4c).
- Single line of substantive work, wrapped in try/catch — non-fatal.
- No telemetry pressure (per CONTEXT.md scope-lock — V21 enum extends only for cross_project_*).

## Soak verification

Soak script ran 200 ticks (200 rows/tick) against a copy of the live DB:

| Reading | Value |
|---------|-------|
| Ticks to convergence | 45 |
| Relevant artifacts | 8686 |
| Sidecar rows total | 8686 (100% coverage) |
| Real fingerprints | 2755 (~32% hit rate) |
| Abstain sentinels | 5931 |
| Post-convergence tick | `{classified: 0, abstained: 0, alreadyDone: 0}` |

Convergence in 45 ticks (≈ 8686 / 200 expected). Idempotent: re-running after convergence produces zero work.

The ~32% hit rate is expected for regex-only classification on free-form historical text — most artifacts are conversational chunks without strong task-shape signals. New lessons written via `writeLessonWithTaskPattern` carry the `shape.task_shape` field directly (1.0 confidence path) and so will hit ~100% of intended targets.

## Test gate

| Suite | Result |
|-------|--------|
| `migrations-v21.test.ts` | 8/8 PASS |
| `migrations-v20.test.ts` | 6/6 PASS (after V21 floor update) |
| `migrations-v19.test.ts` | 7/7 PASS (after V21 floor update) |
| `migration-v17-v18.test.ts` | 7/7 PASS |
| `migration-v2v3.test.ts` | 11/11 PASS |
| `task-pattern-classifier.test.ts` | 20/20 PASS |
| `phase-4-1-perceptual-similarity-probes.test.ts` | 4/4 PASS (regression) |
| `phase-5-full-gate.test.ts` | 7/7 PASS |
| `phase-6-mcp-surface-unchanged.test.ts` | 9/9 PASS |
| `phase-6-rif-spread-retained.test.ts` | 8/8 PASS |
| Pre-existing llama baseline failures | 20 (unchanged) |

**0 non-llama regressions.**

## Backup gate

`.planning/phases/06.5-cross-project-task-pattern-recall/06.5-01-BACKUP.md`:
- Path: `C:\Users\Grigorije\.claudex\backups\pre-v4-P65-1777497210.db`
- 350.94 MiB, integrity_check=ok, user_version=20
- 8940 artifacts, 990 sessions

Restoration: `cp ~/.claudex/backups/pre-v4-P65-1777497210.db ~/.claudex/db/claudex.db`.

## Deviations from Plan

**[Rule 1 — Bug] V20-floor assertions in 9 unrelated test files**

- Found during: Task 1 verification (full `bun run test`).
- Issue: 16 tests across 9 files hardcoded `expect(uv).toBe(20)` against the V20 TARGET_VERSION. Phase 6.5's `TARGET_VERSION 20→21` raise broke these assertions.
- Fix: Updated each assertion to `.toBe(21)`; updated descriptive comments to reference Phase 6.5.
- Files modified: `migrations-v20.test.ts`, `migrations-v19.test.ts`, `migration-v17-v18.test.ts`, `migration-v2v3.test.ts`, `curated-context.test.ts`, `recall-server.test.ts`, `sqlite-vec-loader.test.ts`, `embed-pipeline.test.ts`, `migration/v17-reopen.test.ts`.
- Verification: Re-ran each test file; all pass post-fix.
- Commit: 3b7df19.

This is the same TARGET_VERSION-bump-cascade pattern Phase 6 had to handle (V19→V20 raise broke the same tests). Tracking forward: any future TARGET_VERSION bump should grep for `expect(...).toBe(<previous>)` and update in the same commit.

**Total deviations:** 1 auto-fixed (Rule 1 — Bug). **Impact:** zero regression; correctness restored. The deviation captures recurring maintenance cost of the TARGET_VERSION literal scattered across tests — could be addressed by importing a `CURRENT_TARGET_VERSION` constant from `migrations.ts` in tests, but that's a Phase 8 cross-cutting cleanup, not a Phase 6.5 deliverable.

## Authentication Gates

None. Pure local schema + classifier work.

## Issues Encountered

None.

## Next Phase Readiness

Plan 02 is unblocked. The substrate (V21 schema + sidecar + classifier + write-time + heartbeat backfill) is fully shipped. Plan 02 will consume:
- `artifact_task_pattern` rows (both write-time and backfilled)
- `shape_vocabulary` canonical values
- `lesson_pointer` JOIN path for lesson-based candidates

**Ready for 06.5-02-PLAN.md** (HYBRID equivalence + Experience Tier scorer + tier-utils.ts extraction).
