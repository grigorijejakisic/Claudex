---
phase: 07-v4-coexistence-migration-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assembly/assembler.ts
  - src/intelligence/trigger-engine.ts
  - src/intelligence/outcome-tracker.ts
  - src/intelligence/contradiction-detector.ts
  - src/mcp/recall-server.ts
  - src/angel/heartbeat.ts
  - src/adapters/cc-hooks/stop.ts
  - src/embeddings/sqlite-vec-backend.ts
  - src/embeddings/embed-pipeline.ts
autonomous: true
requirements:
  - AR-01
  - AR-02
  - MIG-03
  - MIG-04
must_haves:
  truths:
    - "All 10 occurrences of `// new INSERTs (V28 trigger blocks them). Phase 7 owns retirement direction` are replaced with the steady-state documentation comment defined in CONTEXT decision 2"
    - "No remaining grep hits for `Phase 7 owns retirement direction` in src/"
    - "Each replacement preserves the exact reader code below it byte-for-byte (mechanical text replacement only — no behavioral change)"
    - "`bun run build` exits 0 after the replacement"
    - "`bun run test` post-replacement pass count matches the pre-replacement baseline byte-for-byte (no test regressions — comments are not behavior)"
  artifacts:
    - path: "src/assembly/assembler.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~215"
      contains: "rows persist for as long as their content is useful"
    - path: "src/intelligence/trigger-engine.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~112"
      contains: "rows persist for as long as their content is useful"
    - path: "src/intelligence/outcome-tracker.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~71"
      contains: "rows persist for as long as their content is useful"
    - path: "src/intelligence/contradiction-detector.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~71"
      contains: "rows persist for as long as their content is useful"
    - path: "src/mcp/recall-server.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~494"
      contains: "rows persist for as long as their content is useful"
    - path: "src/angel/heartbeat.ts"
      provides: "Steady-state legacy comment on the experience_patterns readers at line ~382 and ~1035 (heartbeat has 2 occurrences)"
      contains: "rows persist for as long as their content is useful"
    - path: "src/adapters/cc-hooks/stop.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~370"
      contains: "rows persist for as long as their content is useful"
    - path: "src/embeddings/sqlite-vec-backend.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~394"
      contains: "rows persist for as long as their content is useful"
    - path: "src/embeddings/embed-pipeline.ts"
      provides: "Steady-state legacy comment on the experience_patterns reader at line ~207"
      contains: "rows persist for as long as their content is useful"
  key_links:
    - from: "all 10 reader sites"
      to: "Phase 4 retirement direction (forever-legacy)"
      via: "uniform steady-state comment text"
      pattern: "rows persist for as long as their content is useful"
---

<objective>
Mechanically downgrade the 10 occurrences (across 9 files; `heartbeat.ts` has 2) of the Phase 4 reader-site comment from a migration TODO into a steady-state documentation comment per CONTEXT decision 2 ("Forever-legacy reads with no fade").

The Phase 4 comment was forward-looking: "Phase 7 owns retirement direction". v5 is substrate-only post-reframe (multi-handle thesis dropped, density abstraction dropped). Nothing replaces these readers; they keep working as-is. The right semantic is *"reads pre-Phase-4 legacy table; new INSERTs blocked at V28 trigger; rows persist for as long as their content is useful"* — a steady-state acknowledgement, not a deferred TODO.

Purpose: closes AR-01 (catalog of readers — already done by Phase 4) and AR-02 (each reader's policy named) for v5.0.0 ship discipline. After this plan, no reader-site comment in `src/` references "Phase 7 owns retirement direction" — the phrase belongs only to historical .planning artifacts.

Pure mechanical text replacement. Zero behavioral change. Zero tests regress (comments are not behavior).
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-v4-coexistence-migration-ship/07-CONTEXT.md
@.planning/phases/04-angel-reduction/04-08-PLAN.md
@.planning/phases/04-angel-reduction/04-08-SUMMARY.md
@src/assembly/assembler.ts
@src/intelligence/trigger-engine.ts
@src/intelligence/outcome-tracker.ts
@src/intelligence/contradiction-detector.ts
@src/mcp/recall-server.ts
@src/angel/heartbeat.ts
@src/adapters/cc-hooks/stop.ts
@src/embeddings/sqlite-vec-backend.ts
@src/embeddings/embed-pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the 10 reader sites and the exact existing comment text via grep</name>
  <files></files>
  <action>
First, run grep to confirm the exact text that exists today at all 10 sites:

```
grep -n "Phase 7 owns retirement direction" src/
```

Expected hits (10 total, in 9 files; heartbeat has 2):
- `src/assembly/assembler.ts:215`
- `src/intelligence/trigger-engine.ts:112`
- `src/intelligence/outcome-tracker.ts:71`
- `src/intelligence/contradiction-detector.ts:71`
- `src/mcp/recall-server.ts:494`
- `src/angel/heartbeat.ts:382`
- `src/angel/heartbeat.ts:1035`
- `src/adapters/cc-hooks/stop.ts:370`
- `src/embeddings/sqlite-vec-backend.ts:394`
- `src/embeddings/embed-pipeline.ts:207`

If the grep returns a different number of hits or different files, STOP and report the divergence. CONTEXT acknowledges the count "may be 10 by latest grep; final list determined at plan time." A different count means Phase 4's reader catalog has drifted — investigate which file changed and update CONTEXT before proceeding.

Read 2-3 lines of context around EACH hit to capture the exact existing comment block. Phase 4 added a uniform multi-line comment shape; the typical block is:

```typescript
// reads pre-Phase-4 experience_patterns table — write surface deleted, no
// new INSERTs (V28 trigger blocks them). Phase 7 owns retirement direction
// (drop / project / keep). See .planning/reframes/2026-05-05-multi-handle-kill.md.
```

(Exact wording may vary slightly per site — Phase 4's plan 04-08 wrote a uniform but-not-byte-identical shape.)
  </action>
  <verify>
Grep confirms 10 hits across 9 files. Each hit has 2-3 lines of comment context immediately above the reader code.
  </verify>
  <done>
The 10 sites are confirmed and their existing comment shape is recorded. If the count is not exactly 10, the divergence is reported and CONTEXT is updated before continuing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the comment block at all 10 sites with the steady-state shape</name>
  <files>src/assembly/assembler.ts, src/intelligence/trigger-engine.ts, src/intelligence/outcome-tracker.ts, src/intelligence/contradiction-detector.ts, src/mcp/recall-server.ts, src/angel/heartbeat.ts, src/adapters/cc-hooks/stop.ts, src/embeddings/sqlite-vec-backend.ts, src/embeddings/embed-pipeline.ts</files>
  <action>
At each of the 10 sites, replace the Phase 4 forward-looking comment block with the steady-state version. Preserve the indentation level of the original comment exactly. The replacement comment text:

```typescript
// reads pre-Phase-4 experience_patterns table — write surface deleted, no
// new INSERTs (V28 trigger blocks them). Rows persist for as long as their
// content is useful. See .planning/reframes/2026-05-05-multi-handle-kill.md.
```

Three semantic changes from the Phase 4 wording:
1. "Phase 7 owns retirement direction" → "Rows persist for as long as their content is useful." (forward-looking TODO → steady-state statement)
2. The "(drop / project / keep)" clause is dropped (those options were Phase 7 deliberation; the decision is preserve-as-legacy).
3. The reframe artifact reference stays — it carries the reasoning.

Use the Edit tool's `old_string` / `new_string` shape — do NOT use `replace_all` since each site's surrounding indentation/context differs. Read each file, locate the exact existing block (using the grep output from Task 1 as the line-number anchor), and produce a precise replacement.

For `src/angel/heartbeat.ts`, both occurrences (line ~382 and line ~1035) get the same replacement. They're independent edits — the surrounding code at each site differs, so the Edit tool's old_string uniqueness requirement is satisfied without `replace_all`.

DO NOT modify the reader code below the comment. DO NOT add new logic, error handling, or feature-flag checks. The only diff in each file is the comment block.

Special case for `src/embeddings/sqlite-vec-backend.ts:394` and `src/embeddings/embed-pipeline.ts:207`: these are embedding-pipeline reader sites, not assembler/intelligence sites. The comment may say "experience_patterns" but the surrounding code reads from the embedding/vector index over those rows. The replacement comment is the same text — the table name in the comment is the source-of-truth ("reads pre-Phase-4 experience_patterns table"), regardless of the access path below.
  </action>
  <verify>
After all 10 edits:
1. `grep -n "Phase 7 owns retirement direction" src/` returns ZERO hits.
2. `grep -n "rows persist for as long as their content is useful" src/` returns 10 hits across the 9 expected files.
3. `bun run build` exits 0.
4. `git diff --stat` shows exactly 9 files modified, with line-counts dominated by comment changes (no large diffs).
5. `git diff` shows ONLY comment-line changes — no code lines changed.
  </verify>
  <done>
All 10 sites carry the steady-state legacy comment; no `src/` file references "Phase 7 owns retirement direction"; build is clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run the full test suite to confirm zero behavioral regressions</name>
  <files></files>
  <action>
Run `bun run test`. Comments are not behavior; the post-edit pass count must match the pre-edit baseline byte-for-byte.

Capture and record the pre-edit baseline by running `git stash && bun run test 2>&1 | tail -20 && git stash pop` BEFORE Task 2's edits — but since Tasks 1-2 are sequential and Task 2 is a single atomic edit per site, simpler: capture the post-edit pass-count and compare against the Plan 07-01 SUMMARY.md's published baseline (Plan 07-01 ships first per the `wave: 1` ordering with no dependency on this plan, so its baseline is the right reference).

If the post-edit pass count differs from Plan 07-01's baseline by ANY amount, stop and investigate. The 27 pre-existing failures (`llama-client`, `llama-server-supervisor`, `phase-5-full-gate`) MUST remain unchanged.

Also run `bun run vesna` and confirm the suite still gates pass (existing 18 functional probes — the 3 new probes from Plan 07-04 don't exist yet at this wave). Vesna pass rate must be byte-for-byte unchanged.
  </action>
  <verify>
`bun run test` pass count equals Plan 07-01's published baseline (or, if executed first in the wave, equals the master baseline). `bun run vesna` aggregate is unchanged. The 27 pre-existing failures persist unchanged.
  </verify>
  <done>
Zero test regressions; comment downgrades are confirmed mechanical-only.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "Phase 7 owns retirement direction" src/` — zero hits.
2. `grep -rn "rows persist for as long as their content is useful" src/` — exactly 10 hits across the 9 expected files.
3. `bun run build` exits 0.
4. `bun run test` pass count unchanged from Plan 07-01's post-merge baseline; 27 pre-existing failures unchanged.
5. `bun run vesna` 18/18 GATED PASS unchanged.
6. `git diff --stat` shows 9 files modified, comment-only diffs.
</verification>

<success_criteria>
The reader-comment surface across the 9 files (10 occurrences) reflects v5's steady-state truth: forever-legacy, no fade, no replacement; the rows that exist persist for as long as their content is useful. AR-01 and AR-02 are closed for v5.0.0 ship discipline. The phrase "Phase 7 owns retirement direction" is gone from `src/` (it remains in `.planning/` history as the Phase 4 closeout artifact, which is correct — historical record, not active TODO). Zero behavioral change.
</success_criteria>

<output>
After completion, create `.planning/phases/07-v4-coexistence-migration-ship/07-02-SUMMARY.md` summarizing: 10 sites edited, exact replacement text, post-merge `bun run test` pass count (matching Plan 07-01's baseline), zero behavioral change confirmation. Cite the baseline explicitly per Phase 4's 04-01-SUMMARY.md:79 convention.
</output>
