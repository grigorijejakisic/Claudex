---
phase: 07-v4-coexistence-migration-ship
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
files_created: []
autonomous: false
requirements:
  - VAL-06
must_haves:
  truths:
    - "All 7 ship gates plus the substrate-health gate (`bun run doctor`) pass per CONTEXT decision 4"
    - "CHANGELOG.md '[Unreleased]' section is filled with a v5.0.0 block in Keep-a-Changelog format with `### Added`, `### Removed`, `### Changed`, `### Coverage` sub-sections; reframe artifact gets a callout at the top of the entry"
    - ".planning/STATE.md is updated to reflect Phase 7 SHIPPED + v5 milestone closed"
    - ".planning/ROADMAP.md Phase 7 status flips from `[ ]` to `[x]` with the SHIPPED-date stamp"
    - "v5.0.0 annotated git tag is created on master with the annotation message defined in CONTEXT specifics, message LEADS with the kill"
    - "`git push origin master --tags` is presented as the FINAL operator-confirm step (NOT executed by the plan; user-approval-gate per CLAUDE.md rule 1)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v5.0.0 release notes in Keep-a-Changelog format"
      contains: "## [5.0.0]"
    - path: ".planning/STATE.md"
      provides: "Updated state — Phase 7 SHIPPED + v5 milestone closed"
      contains: "Phase 7"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 status flipped to [x] SHIPPED with date"
      contains: "Phase 7"
  key_links:
    - from: "git tag v5.0.0"
      to: "annotation message defined in 07-CONTEXT.md specifics"
      via: "git tag -a v5.0.0 -F <tag-message-file>"
      pattern: "v5.0.0"
---

<objective>
Final close-out plan for v5.0.0. Runs the seven must-pass ship gates plus the substrate-health gate (`bun run doctor`); fills CHANGELOG.md `[Unreleased]` → `[5.0.0]`; updates STATE.md + ROADMAP.md; creates the annotated `v5.0.0` git tag with the message defined in CONTEXT specifics; STOPS short of `git push --tags` per CLAUDE.md rule 1 (user confirms the public ship gate).

Purpose: closes VAL-06 (one-turn handoff pickup probes) by re-running them in the suite, verifies all four v5 SC gates pass, and ships v5.0.0 — the substrate-only milestone with the honest negative result on the multi-handle/density-fusion thesis as one of its most important outputs (CONTEXT decision 4: "tag note LEADS with the kill, not buries it").

This plan is `autonomous: false` because the v5.0.0 tag is a one-way operation visible to the public — explicit user confirmation is required before `git push origin master --tags`. Pre-tag work (running gates, filling CHANGELOG, creating the tag locally) IS autonomous; the final push is not.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-v4-coexistence-migration-ship/07-CONTEXT.md
@.planning/phases/07-v4-coexistence-migration-ship/07-01-SUMMARY.md
@.planning/phases/07-v4-coexistence-migration-ship/07-02-SUMMARY.md
@.planning/phases/07-v4-coexistence-migration-ship/07-03-SUMMARY.md
@.planning/phases/07-v4-coexistence-migration-ship/07-04-SUMMARY.md
@.planning/reframes/2026-05-05-multi-handle-kill.md
@CHANGELOG.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 7 ship gates plus the substrate-health gate</name>
  <files></files>
  <action>
Per CONTEXT decision 4, run each gate and record the result. ALL must pass; ANY failure stops the plan and routes to the failed plan's owner for diagnosis.

| # | Gate | Command | Expected |
|---|---|---|---|
| 1 | Vesna full suite | `bun run vesna` | `21/21 PASS at 100%`, `gated=true`, `AGGREGATE: 100% — GATED PASS` |
| 2 | Phase-7 vitest tests | `bun run test src/tests/integration/phase-7-learnings-provenance.test.ts src/tests/integration/phase-2-1-kill-regression.test.ts src/tests/integration/phase-6-crash-resilience.test.ts` | All three files green |
| 3 | Build | `bun run build` | exit 0 (~70ms) |
| 4 | Test pass-count diff | `bun run test` | No NEW regressions vs Plan 07-04's post-merge baseline; the 27 pre-existing failures (`llama-client`, `llama-server-supervisor`, `phase-5-full-gate`) persist unchanged |
| 5 | MEMORY.md content quality | `bun run sc3` | ≥80% across active projects |
| 6 | Handoff pickup probes | already in `bun run vesna` (3 handoff-pickup probes) | 3/3 PASS |
| 7 | CLI bundle smoke | `bun run test src/tests/integration/cli-bundle-smoke.test.ts` | 7/7 PASS |
| + | Substrate health | `bun run doctor` | exit 0 (reranker:7439, Ollama, hooks, Angel all green) |

Record each gate's pass status + relevant numbers (pass-count, percentage, exit code). If gate 4 has a regression, identify the failing test and the introducing plan (07-01 / 02 / 03 / 04) — this is why each prior plan published its post-merge baseline.

If any gate fails, stop and report. DO NOT proceed to Task 2 until all 8 gates are green.
  </action>
  <verify>
8 gates × all pass. Numbers recorded for SUMMARY.md citation.
  </verify>
  <done>
All ship gates pass; numbers recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill CHANGELOG.md '[Unreleased]' → '[5.0.0]' release entry</name>
  <files>CHANGELOG.md</files>
  <action>
Replace the existing `[Unreleased]` section in CHANGELOG.md with a `[5.0.0]` release entry plus a fresh empty `[Unreleased]` block above it, in Keep-a-Changelog format. The reframe artifact gets a callout at the top of the entry per CONTEXT specifics.

Structure (substitute concrete bullet content based on the actual SUMMARYs of plans 01-04):

```markdown
## [Unreleased]

### Added

- _Nothing yet._ Track v5.1+ milestone planning at `.planning/STATE.md` once it kicks off.

## [5.0.0] — <YYYY-MM-DD ship date>

**Substrate-only milestone.** Three load-bearing legs proposed at v5 start; legs 2 and 3 (recall-by-any-modality via fusion, abstraction-from-density) killed empirically by Phase 2/2.1 (3 KILL bound measurements at `.planning/aggregates/multi-handle.json`). Leg 1 (provenance-tagged episode substrate) shipped + extended to learnings. Reframe artifact: [`.planning/reframes/2026-05-05-multi-handle-kill.md`](.planning/reframes/2026-05-05-multi-handle-kill.md).

Methodology promoted to v5 standard practice: pre-committed decision rule, locked corpus, multiple bound measurements, append-only aggregator, Wilson/Newcombe CI binding.

Not delivered: improved retrieval. v4 `hybrid-retrieval.ts` is unchanged in production; future milestones may revisit on the substrate this milestone built.

### Added

- **Phase 1 — Episode substrate (V25):** new `episodic_events` table with structured row schema and provenance enum (`organic | injected | tool_result | environmental`), dual-write helpers from CC hooks, V25 forward-only migration, 60+ EPI-tagged tests including a stub-extractor proof of EPI-07 Mem0-trap-impossibility. Operator README + environmental audit at `.planning/phases/01-episode-substrate/`.
- **Phase 4 — Angel reduction (V28):** three extraction-time-pattern-creation sites deleted (`pattern-extractor.ts`, `experience-scoring.ts` step 1, `heartbeat.ts` synthesis loop). V28 BEFORE INSERT trigger blocks new `experience_patterns` rows structurally via TEMP `session_pragmas` sidecar. Three-layer cutoff: JSDoc tombstones + `extraction-deleted.test.ts` regression guard + V28 trigger. `classifySessionDomains` relocated to `domain-classifier.ts`. ~1100 lines deleted, ~700 added — pure shrinkage on production path.
- **Phase 6 — Crash-resilient episode boundary (V29):** `episode_boundary_cursor` table + `sessions.last_heartbeat_ts` + `sessions.last_jsonl_write_ts`. New `src/angel/boundary/` module surface (thresholds, pid-liveness, jsonl-watcher, composition-rule, cursor, boundary-detector). chokidar 5.0.0 runtime dep. 5 CC hooks bump heartbeat ts; SessionEnd emits `clean_endsession` close marker atomically with cursor advance. Angel.heartbeatTick runs `runBoundaryTick` on every cadence; Angel boot starts JSONL watcher with degraded-mode fallback. 55 boundary tests + 7 V29 migration tests + 6 hook regression tests.
- **Phase 7 — v4 coexistence + Vesna update + ship (V30):** `learnings.provenance` column with closed-enum CHECK matching `episodic_events`; `captureInsightsAsLearnings` filters wrapper-tagged content via Phase 1's `parseWrappers` (KNOWN_WRAPPER_TAGS source-of-truth at `src/extraction/wrapper-parser.ts`). 10 reader-site comments downgraded from forward-looking TODOs to steady-state legacy documentation. Vesna grows 18 → 21 functional probes (`episodic-recall-001`, `episodic-recall-002`, `learnings-injected-guard-001`). Three new vitest integration tests (`phase-7-learnings-provenance`, `phase-2-1-kill-regression`, `phase-6-crash-resilience`) cover the four v5 SC gates at substrate level.

### Removed

- **Phase 3 — Multi-handle retrieval cutover** — DROPPED 2026-05-05. Premised on the multi-handle/density-fusion thesis killed by Phase 2/2.1. v4's `hybrid-retrieval.ts` (semantic + FTS + reranker) stays in production unchanged.
- **Phase 5 — Density-based abstraction** — DROPPED 2026-05-05. Same dead thesis. Intra-project density measured at 0.2418 across both Phase 2.1 tiers (threshold 0.30).
- **Requirements RET-01..05 + ABS-01..04** — closed with the dropped phases.
- **Mem0 fix from commit `0d0fbca`** — deleted as structurally obsolete (Phase 4 deletes the upstream extraction sites that fed it).

### Changed

- **Multi-handle thesis status** — empirically rejected at our scale. The thesis is dead; the methodology that killed it is alive and standard.
- **Phase 4 reader-site comments** — 10 occurrences (across 9 files) downgraded from "Phase 7 owns retirement direction (drop / project / keep)" to steady-state legacy documentation: "Rows persist for as long as their content is useful." Forever-legacy reads with no fade.

### Coverage

- `bun run vesna` 21/21 GATED PASS at 100%
- `bun run test` — <pass-count> passing tests, +<delta> over v4.1.2's 3211 baseline; 27 pre-existing failures unchanged (`llama-client`, `llama-server-supervisor`, `phase-5-full-gate`)
- `bun run sc3` — ≥80% MEMORY.md content quality across active projects
- `bun run doctor` exit 0
```

Substitute `<YYYY-MM-DD ship date>` and `<pass-count>` with concrete values from Task 1's recorded numbers and `date +%F` at execution time.

The `### Added` bullets per phase are not exhaustive — they're the user-visible-shipping summary. The exhaustive change record lives in each plan's SUMMARY.md.

The `### Removed` block is the most distinctive part of the v5.0.0 entry — it surfaces the kill explicitly, in keeping with CONTEXT decision 4 ("tag note LEADS with the kill, not buries it"). The CHANGELOG mirrors that discipline.
  </action>
  <verify>
`grep -n "## \[5.0.0\]" CHANGELOG.md` matches one line. The entry includes `### Added`, `### Removed`, `### Changed`, `### Coverage` sub-sections. The reframe artifact callout is at the top of the entry. Concrete pass-count + ship-date are filled in.
  </verify>
  <done>
CHANGELOG.md ships v5.0.0 in Keep-a-Changelog format; the kill is surfaced explicitly per CONTEXT decision 4.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/STATE.md and .planning/ROADMAP.md to reflect Phase 7 SHIPPED</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**STATE.md:**
1. Update the "Current Position" block: change "Phase 6 SHIPPED ... Phase 7 next" to "v5 milestone CLOSED <date>; Phase 7 SHIPPED via /auto-execute-phase. v5.0.0 tag annotated on master."
2. Add a `Phase 7` row to the verdict log mirroring the shape of the `Phase 6` row: SHIPPED date, what shipped (V30 + provenance filter + 10 reader-comment downgrades + 3 Vesna probes + 3 vitest tests + CHANGELOG + tag), ship gate numbers (Vesna 21/21, test pass-count, doctor exit 0).
3. Update the "Last activity" line to today's date.
4. In the "v5 Phase Structure" table, flip Phase 7 status from `pending` to `SHIPPED <date>`.

**ROADMAP.md:**
1. In the summary list (line 94), flip `- [ ] **Phase 7: v4 coexistence / migration / ship**` to `- [x] **Phase 7: v4 coexistence / migration / ship**` and add a `_(type: engineering)_` line ending with `— SHIPPED <date>`.
2. Below the existing Phase 7 prose (lines 96-104), append an `**Outcome:**` block matching the shape of Phase 4 / Phase 6 outcome blocks. Include: V30 migration shape, 10 reader-comment downgrades, 3 Vesna probes added, 3 vitest tests added, ship gates passed (numbers from Task 1), v5.0.0 tag annotated.
3. The list of plans (`- [x] 07-01-PLAN.md` etc.) goes under the outcome block, mirroring Phase 6's structure.

DO NOT modify the historical Phase 1/2/2.1/3/4/5/6 blocks. DO NOT modify the "Validation criteria" section unless any of SC-V5-1 through SC-V5-4 has shifted (they haven't — Phase 7 closes them as planned).

DO NOT modify the "Out of Scope for v5" block in REQUIREMENTS.md.
  </action>
  <verify>
`grep -n "Phase 7" .planning/STATE.md | head -5` shows the Phase-7-shipped row in the verdict log + the "Last activity" + "v5 milestone CLOSED" lines. `grep -n "Phase 7" .planning/ROADMAP.md | head -5` shows the `[x]` flip + the new outcome block.
  </verify>
  <done>
STATE.md and ROADMAP.md reflect v5 closure; mirror the shape Phase 6's close-out used.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create the v5.0.0 annotated git tag locally (NOT pushed)</name>
  <files></files>
  <action>
Per CONTEXT specifics, the tag annotation message is:

```
v5.0.0 — Bound Multi-Modal Episodes (Substrate-Only)

Substrate-only milestone. Three load-bearing legs proposed at v5
start; legs 2 and 3 (recall-by-any-modality via fusion, abstraction-
from-density) killed empirically by Phase 2/2.1 (3 KILL bound
measurements, .planning/aggregates/multi-handle.json). Leg 1
(provenance-tagged episode substrate) shipped + extended to learnings.

Phases shipped: 1 (substrate), 2 (multi-modal seed), 2.1 (corpus
rerun), 4 (Angel reduction), 6 (crash-resilient boundary), 7
(coexistence + Vesna update + v5 tag).
Phases dropped: 3 (multi-handle cutover), 5 (density abstraction).
Reframe artifact: .planning/reframes/2026-05-05-multi-handle-kill.md.

Methodology promoted to v5 standard practice: pre-committed decision
rule, locked corpus, multiple bound measurements, append-only
aggregator, Wilson/Newcombe CI binding.

Not delivered: improved retrieval. v4 hybrid-retrieval.ts unchanged
in production; future milestones may revisit on the substrate this
milestone built.
```

Steps:

1. Confirm working tree is clean: `git status` — should report only the changes from Tasks 2 and 3 (CHANGELOG.md, STATE.md, ROADMAP.md). If other unstaged changes exist, stop and investigate.

2. Stage the Phase 7 doc updates and the CHANGELOG entry, plus all Phase 7 plan SUMMARY.md files written by Plans 07-01 through 07-04:
   ```
   git add CHANGELOG.md .planning/STATE.md .planning/ROADMAP.md \
           .planning/phases/07-v4-coexistence-migration-ship/
   ```

3. Commit with a Phase 7 close-out message (single commit per the v4-final-archive precedent):
   ```
   git commit -m "$(cat <<'EOF'
   phase(07): close — v5.0.0 substrate-only milestone shipped

   - V30 schema migration: learnings.provenance column with closed-enum CHECK
   - captureInsightsAsLearnings filters wrapper-tagged content via parseWrappers
   - 10 reader-site comments downgraded to steady-state legacy
   - Vesna grows 18 -> 21 functional probes (episodic-recall-001/002 + learnings-injected-guard-001)
   - 3 new vitest integration tests (phase-7-learnings-provenance, phase-2-1-kill-regression, phase-6-crash-resilience)
   - CHANGELOG v5.0.0 entry in Keep-a-Changelog format
   - STATE/ROADMAP flipped to SHIPPED

   Ship gates verified:
   - bun run vesna 21/21 GATED PASS at 100%
   - bun run test <pass-count> passing; 27 pre-existing failures unchanged
   - bun run build clean
   - bun run sc3 >= 80%
   - bun run doctor exit 0

   Reframe artifact: .planning/reframes/2026-05-05-multi-handle-kill.md
   EOF
   )"
   ```
   (Substitute `<pass-count>` with the actual number from Task 1.)

4. Create the annotated tag using the message above. Pass via heredoc (Windows-safe via Bash tool):
   ```
   git tag -a v5.0.0 -m "$(cat <<'EOF'
   v5.0.0 — Bound Multi-Modal Episodes (Substrate-Only)

   Substrate-only milestone. Three load-bearing legs proposed at v5
   start; legs 2 and 3 (recall-by-any-modality via fusion, abstraction-
   from-density) killed empirically by Phase 2/2.1 (3 KILL bound
   measurements, .planning/aggregates/multi-handle.json). Leg 1
   (provenance-tagged episode substrate) shipped + extended to learnings.

   Phases shipped: 1 (substrate), 2 (multi-modal seed), 2.1 (corpus
   rerun), 4 (Angel reduction), 6 (crash-resilient boundary), 7
   (coexistence + Vesna update + v5 tag).
   Phases dropped: 3 (multi-handle cutover), 5 (density abstraction).
   Reframe artifact: .planning/reframes/2026-05-05-multi-handle-kill.md.

   Methodology promoted to v5 standard practice: pre-committed decision
   rule, locked corpus, multiple bound measurements, append-only
   aggregator, Wilson/Newcombe CI binding.

   Not delivered: improved retrieval. v4 hybrid-retrieval.ts unchanged
   in production; future milestones may revisit on the substrate this
   milestone built.
   EOF
   )"
   ```

5. Verify the tag locally: `git tag -v v5.0.0` (or `git tag -l --format='%(contents)' v5.0.0`) — confirm the annotation message is present and intact.

6. STOP. DO NOT run `git push origin master --tags`. The push is the operator's gate per CLAUDE.md rule 1 (destructive, public, hard-to-reverse). Plan 07-05 is `autonomous: false` precisely so this final step waits for user confirmation.

Print the final operator-confirm step:

```
Ship gates: 8/8 PASS.
v5.0.0 commit + annotated tag created locally.

Next step (operator): git push origin master --tags
```
  </action>
  <verify>
`git log -1 --oneline` shows the close-out commit. `git tag -l v5.0.0` returns `v5.0.0`. `git tag -l --format='%(contents)' v5.0.0` includes "Substrate-Only" and "killed empirically by Phase 2/2.1" verbatim. `git status` is clean (no staged or unstaged changes left over).
  </verify>
  <done>
Local commit + annotated tag exist; the push is queued for operator confirmation.
  </done>
</task>

</tasks>

<verification>
1. All 8 ship gates pass — numbers recorded in 07-05-SUMMARY.md.
2. CHANGELOG.md `[5.0.0]` entry is filled with all four sub-sections; reframe artifact callout at top.
3. STATE.md + ROADMAP.md reflect Phase 7 SHIPPED.
4. Local commit on master with the close-out message.
5. Local annotated tag `v5.0.0` with the annotation message defined in CONTEXT specifics.
6. `git push origin master --tags` is NOT executed (operator-confirm step).
</verification>

<success_criteria>
v5.0.0 is shipped locally and queued for the public push. The substrate-only milestone closes with an honest negative result on the multi-handle/density-fusion thesis surfaced front-and-center in both the CHANGELOG and the tag annotation. All four v5 SC gates are validated (SC-V5-1 / -2 / -3' via Vesna + vitest; SC-V5-4 at substrate level with Vesna form deferred to v6+). The methodology that produced the kill is promoted to v5 standard practice.

After operator confirms `git push origin master --tags`, Claudex v5.0.0 is public.
</success_criteria>

<output>
After completion, create `.planning/phases/07-v4-coexistence-migration-ship/07-05-SUMMARY.md` summarizing:
- All 8 ship gate results with concrete numbers
- CHANGELOG entry shape (link to the new section)
- STATE.md / ROADMAP.md update diff summary
- Local commit SHA + tag verification output
- Final operator-confirm step echoed verbatim
- Note that the optional `bun run kill-regression` script (CONTEXT specifics) is NOT shipped in v5.0.0 per the plan-phase decision (vitest assertion in `phase-2-1-kill-regression.test.ts` is the mandatory form; the script remains a documented v5.1+ optional rigor add).
</output>
