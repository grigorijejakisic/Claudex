---
phase: 11-p9-final-validation
plan: 07
type: execute
wave: 5
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified:
  - CLAUDE.md
  - README.md
  - .planning/phases/11-p9-final-validation/11-V4-VALIDATION.md
  - .planning/phases/11-p9-final-validation/11-CLOSE-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/v4.1-distribution/STUB.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "V4_VALIDATION.md ties SC#1-#4 verdicts to evidence files (Plans 01-04 result files), with overall PASS/FAIL"
    - "CLAUDE.md updated to reflect v4 architecture; benchmark language softened (numbers retained as archival, not framed as gates); v4 banner added"
    - "README.md updated with v4 banner: 'v4.0 is internal infrastructure. v4.1 = Distribution will make it installable by strangers.'"
    - "git tag v4.0.0 created on the v4-ship commit and pushed; tag message references V4_VALIDATION.md"
    - ".planning/STATE.md marks v4 SHIPPED; ROADMAP.md marks Phase 11 closed"
    - "v4.1 Distribution stub committed at .planning/v4.1-distribution/STUB.md"
    - "Full bun run test passes; bun run vesna passes; bun run sc3 passes; assembler.cache-stability.test.ts passes"
    - "Phase 11 close commit + v4.0.0 tag commit are clearly named and traceable"
  artifacts:
    - path: .planning/phases/11-p9-final-validation/11-V4-VALIDATION.md
      provides: "Final v4 ship-gate validation tying SC#1-#4 to evidence"
      contains: "## SC#1 — Vesna"
    - path: .planning/phases/11-p9-final-validation/11-CLOSE-SUMMARY.md
      provides: "Phase 11 close summary"
      contains: "v4.0.0 shipped"
    - path: .planning/v4.1-distribution/STUB.md
      provides: "v4.1 Distribution milestone stub"
      contains: "v4.1 — Distribution"
  key_links:
    - from: .planning/phases/11-p9-final-validation/11-V4-VALIDATION.md
      to: .planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md
      via: "V4_VALIDATION cites SC#1 evidence"
      pattern: "11-04-SC1-RESULT.md"
---

<objective>
Final phase-close: synthesize SC#1-#4 verdicts into V4_VALIDATION.md, update docs (CLAUDE.md, README.md), commit the v4.1 Distribution stub, tag `v4.0.0`, and close Phase 11 + the v4 milestone.

This plan is the ship moment. All earlier plans must be PASS (or explicitly accepted-failure-with-rationale) before this plan executes. If any SC verdict is FAIL or HITL-PENDING without resolution, this plan BLOCKS — Phase 11 does not close until the SC gates clear.

Per CONTEXT.md (lines 19, 73-81):
- Updated docs reflect v4 architecture; benchmark language removed
- Banner: "v4.0 is internal infrastructure. v4.1 = Distribution will make it installable by strangers."
- Ship sequence: SC#1-#4 PASS → full test suite green → optional vesna re-run → vibe-check → doc updates → git tag v4.0.0 → push → v4.1 stub commit
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-p9-final-validation/11-CONTEXT.md
@.planning/phases/11-p9-final-validation/11-01-PLAN.md
@.planning/phases/11-p9-final-validation/11-02-PLAN.md
@.planning/phases/11-p9-final-validation/11-03-PLAN.md
@.planning/phases/11-p9-final-validation/11-04-PLAN.md
@.planning/phases/11-p9-final-validation/11-05-PLAN.md
@.planning/phases/11-p9-final-validation/11-06-PLAN.md
@CLAUDE.md
@README.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthesize V4_VALIDATION.md tying SC#1-#4 to evidence</name>
  <files>.planning/phases/11-p9-final-validation/11-V4-VALIDATION.md</files>
  <action>
**Hard precondition check** before writing the file:

```bash
# All four SC result files must exist
for f in 11-01-SC3-RESULT.md 11-02-SC2-RESULT.md 11-03-SC4-RESULT.md 11-04-SC1-RESULT.md; do
  test -f ".planning/phases/11-p9-final-validation/$f" || { echo "MISSING: $f — Plan 07 cannot execute"; exit 1; }
done
echo "All SC result files present"
```

**If any are missing, STOP** — block this plan and route back to the missing plan(s) for completion. Do NOT write V4_VALIDATION.md against incomplete inputs.

Read each SC result file and extract its verdict. Refuse to ship if any is FAIL or HITL-PENDING-without-resolution. The escape hatch is explicit operator override (the orchestrator decides; the executor does not silently accept failure).

Write `.planning/phases/11-p9-final-validation/11-V4-VALIDATION.md`:

```markdown
# v4 Final Validation Against SC#1-#4

**Date:** YYYY-MM-DD
**Commit:** <git rev-parse HEAD>
**Overall verdict:** PASS — v4 ready to ship / FAIL — blocked

## SC#1 — Vesna probe pass ≥80% (full suite, every category, every active project)

- Evidence: [.planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md]
- Aggregate: NN%
- Per-category: [paste table from 11-04-SC1-RESULT.md]
- Per-project: <linked or pasted>
- Verdict: PASS / FAIL

## SC#2 — Token budget ≤500 cache-stable (3-layer test)

- Evidence: [.planning/phases/11-p9-final-validation/11-02-SC2-RESULT.md]
- Layer 1 (TOK-01): NN tokens ≤500 — PASS / FAIL
- Layer 2 (CACH-01): SHA-256 byte-identical — PASS / FAIL
- Layer 3 (CACH-02): invariance verified — PASS / FAIL
- Verdict: PASS / FAIL

## SC#3 — MEMORY.md content-quality ≥80% (all 5+ active projects, mechanical scoring)

- Evidence: [.planning/phases/11-p9-final-validation/11-01-SC3-RESULT.md]
- Per-project pass: [paste table]
- Verdict: PASS / FAIL

## SC#4 — One-turn handoff pickup (3 cold-start trials, 3 different projects)

- Evidence: [.planning/phases/11-p9-final-validation/11-03-SC4-RESULT.md]
- Trial 1: <project> — PASS / FAIL
- Trial 2: <project> — PASS / FAIL
- Trial 3: <project> — PASS / FAIL
- Verdict: PASS (3/3) / FAIL

## Ancillary closures

- STOR-04 (legacy `_old` tables): DROP / DEFER — see 11-05-OLD-TABLES-AUDIT.md
- Benchmark vibe-check: LongMemEval NN%, LoCoMo NN% — see 11-06-BENCHMARK-VIBE-CHECK.md (archival, non-gating)

## Test suite gates

- bun run test: ✓
- bun run vesna: ✓ (gated true)
- bun run sc3: ✓ (every project ≥80%)
- assembler.cache-stability.test.ts: ✓
- bun run build: ✓

## Decision

If overall verdict is PASS: proceed to Tasks 2-5 (doc updates, tag, stub, close commit).
If overall verdict is FAIL: HALT. Open the relevant Phase 11.x sub-plan(s); v4 does not ship until all four SC verdicts are PASS.
```

This file is the single source of truth for "did v4 ship cleanly?" — it's what someone three months later reads to understand the ship moment.
  </action>
  <verify>
- 11-V4-VALIDATION.md exists with each SC verdict tied to an evidence file
- Overall verdict is explicit (no "mostly passing" — it's PASS or FAIL)
- If FAIL: this plan halts and the orchestrator is informed
  </verify>
  <done>V4_VALIDATION.md is written with truthful SC roll-up; if PASS, ready for doc updates and tag.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and README.md for v4</name>
  <files>CLAUDE.md, README.md</files>
  <action>
**CLAUDE.md updates** (per CONTEXT.md line 19 and the v4 audit corrective):

1. **What This Is section**: keep largely intact — the architecture description is correct.
2. **Benchmarks section**: rebind from a "this is what we do" into a "this is what we logged at v4 ship". Concretely:
   - Keep the actual numbers (LongMemEval 90.6%, LoCoMo 55.5%) — they're true and useful as historical record.
   - Add a one-liner above them: "These are archival v4-ship-time numbers; not used as ship gates per the v4 audit. The Vesna behavioral probe suite (`bun run vesna`) and the four success criteria in `.planning/phases/11-p9-final-validation/11-V4-VALIDATION.md` are the canonical ship gate."
   - Keep the "don't cite the old 90.8% LoCoMo number" warning — that's still true and load-bearing.
3. **Critical Safety Rules**: no changes — they're load-bearing for hooks/reranker/auth.
4. **Build & Test section**: add `bun run vesna` and `bun run sc3` to the listed scripts (these are the ship-gate runners going forward).

**README.md updates** (CONTEXT.md line 79 verbatim):

Add a banner at the top (or update if a banner exists):

```markdown
> **v4.0 is internal infrastructure. v4.1 = Distribution will make it installable by strangers.**
```

Then ensure README.md describes:
- What Claudex v4 is (one-paragraph, derived from CLAUDE.md "What This Is")
- How to run the ship-gate locally (`bun run test`, `bun run vesna`, `bun run sc3`)
- Where to look for the v4 validation record (link to .planning/phases/11-p9-final-validation/11-V4-VALIDATION.md)

Do NOT rewrite README.md from scratch — update the banner + link, leave the rest. v4.1 Distribution will own the documentation deep-rewrite (per CONTEXT lines 109-111).

**Anti-pattern to avoid**: do NOT add benchmark numbers to README.md. The v4 audit corrective says benchmarks aren't ship-defining; the README banner says v4 is internal infrastructure. Both speak the same language: stop selling green numbers.
  </action>
  <verify>
- CLAUDE.md Benchmarks section reframed as archival
- CLAUDE.md "Build & Test" lists bun run vesna and bun run sc3
- README.md has the v4 banner from CONTEXT line 79 verbatim
- README.md links to V4_VALIDATION.md
- No benchmark numbers in README.md
- bun run build passes (no markdown breaks anything)
  </verify>
  <done>Docs match v4 reality: behavioral validation as the gate, benchmarks as archival.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the v4.1 Distribution stub</name>
  <files>.planning/v4.1-distribution/STUB.md</files>
  <action>
Per CONTEXT.md line 26 ("v4.1 Distribution scaffolding beyond `.planning/` next-cycle stub commit" is OUT OF SCOPE) and ROADMAP success criterion 10 ("v4.1 = Distribution milestone scaffolded — `.planning/` next-cycle stub committed"):

Create `.planning/v4.1-distribution/STUB.md`:

```markdown
# v4.1 — Distribution

**Status:** Stub — milestone planning starts post-v4 ship.
**Created:** YYYY-MM-DD (Phase 11 close, v4.0.0 ship)

## Headline

v4.0 is internal infrastructure. v4.1 makes it installable by strangers.

## Carried-forward from v4 (deferred items)

[At Phase 11 close, scan REQUIREMENTS.md for `[ ]` items not closed by v4 SC gates. List them here as starting input for v4.1 planning.]

- STOR-06 (transcript_chunk session_id/turn_range/topic_label/embedding) — owned by Phase 4.1, deferred to v4.1
- EXTR-04 held-out recall measurement (target ≥0.85) — partial-B at v4 ship, finish in v4.1
- EXTR-06 transcript chunking via LLM topic-segmentation at /endsession — Phase 4.1 partial, complete in v4.1
- INJ-01 through INJ-07 — most wired, but a final audit at v4.1 kickoff is sensible
- LoCoMo benchmark improvement (55.5% known-WIP) — active improvement target
- Per-project Vesna scoping (Phase 11 Plan 04 deferral if applicable) — v4.1 harness work
- Branch protection rule for the Vesna CI gate (manual GitHub UI step from Phase 10) — v4.1 setup item
- [include any STOR-04 deferred-caller list from Phase 11 Plan 05 if DEFER path was taken]
- [include SC#1/2/3/4 follow-ups if any were accepted-with-caveats at v4 ship]

## Themes (initial — to be refined when /gsd:new-milestone is run)

1. **Installability**: stranger sets up Claudex from a clone in <30 minutes with no insider knowledge.
2. **Documentation**: README is the entry point, not CLAUDE.md.
3. **Onboarding cold-start**: first-session experience is intentional, not improvised.
4. **External integrations**: Claude Code is one harness; v4.1 considers others (Cursor, Zed, etc.) — scope TBD.
5. **Open issues from v4 deferrals**: see "Carried-forward" above.

## Next steps

- After v4.0.0 tag pushes, run `/gsd:new-milestone` for v4.1 to start the requirements + roadmap cycle.
- Do NOT pre-plan phases here — this is a stub. v4.1 planning is its own milestone.
```

This stub is intentionally minimal — it carries forward open items and themes, but does NOT pre-commit to phases. The next milestone cycle owns that.

**Where to scan for deferred items**: 
- `.planning/REQUIREMENTS.md` — open `[ ]` items as of v4 ship
- Phase 11 SUMMARY files for any "v4-deferred" notes
- Phase 11 SC result files if any accepted-with-caveats decisions were logged
  </action>
  <verify>
- .planning/v4.1-distribution/STUB.md exists
- Carried-forward items list is populated by scanning REQUIREMENTS.md and Phase 11 SUMMARY files (not blank)
- Themes are listed but not deeply scoped — it's a stub, not a roadmap
  </verify>
  <done>v4.1 Distribution stub committed; carries forward what v4 left open.</done>
</task>

<task type="auto">
  <name>Task 4: Update STATE.md, ROADMAP.md, REQUIREMENTS.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
**STATE.md**: locate the file (might be at `.planning/STATE.md` — confirm via `ls .planning/`). Update:
- last_completed_phase: 11
- v4 status: SHIPPED on YYYY-MM-DD at commit <hash> tag v4.0.0
- next milestone: v4.1 Distribution (stub at .planning/v4.1-distribution/STUB.md)

If the file's shape differs (some projects use frontmatter, some use a markdown summary), match the existing convention — don't restructure it.

**ROADMAP.md**: under Phase 11, set status to closed and add the v4.0.0 tag reference. Mark v4 milestone as SHIPPED.

**REQUIREMENTS.md**: this is more delicate. Walk the open `[ ]` items and decide for each:
- Closed by v4 SC gates → flip to `[x]` with reference to the closing plan/result file.
- Deferred to v4.1 (in the v4.1-distribution stub's carried-forward list) → mark `[~]` and append "→ v4.1" or similar marker per project convention; do NOT delete.
- Genuinely orphaned (e.g. invalidated by Phase 9 deletion) → strike through or mark `[~ deprecated]` with a one-liner explaining why.

Do NOT close requirements that lack evidence. The audit's whole point was that "green numbers feel like progress" — flipping a checkbox without evidence is the same anti-pattern.

For each requirement closed in this task, the SC#1-#4 result files OR a Phase 11.x SUMMARY must be the citation. If you can't cite, leave it open and put it in the v4.1 stub's carried-forward list.
  </action>
  <verify>
- STATE.md reflects v4 SHIPPED + v4.1 next
- ROADMAP.md Phase 11 closed, v4 milestone SHIPPED
- REQUIREMENTS.md changes are evidence-cited per requirement
- No requirements flipped to `[x]` without an evidence link
  </verify>
  <done>State files reflect v4 ship; carried-forward items are explicit.</done>
</task>

<task type="auto">
  <name>Task 5: Phase close commit + v4.0.0 tag + push</name>
  <files>.planning/phases/11-p9-final-validation/11-CLOSE-SUMMARY.md</files>
  <action>
Step 1 — write `.planning/phases/11-p9-final-validation/11-CLOSE-SUMMARY.md`:

```markdown
# Phase 11 Close Summary — v4 Final Validation, v4.0.0 Tagged

**Closed:** YYYY-MM-DD
**Plans:** 7 (11-01 through 11-07)
**Requirements addressed:** SC#1-#4 verified; STOR-04 closed (DROP) or deferred (DEFER)

## What shipped

v4.0.0 — internal-infrastructure release of Claudex. Tagged at commit <hash>.

## Validation summary

See [11-V4-VALIDATION.md](./11-V4-VALIDATION.md) for the full SC roll-up.

- SC#1 (Vesna behavioral): PASS at NN% aggregate, ≥80% every category
- SC#2 (token + cache-stable): PASS — NN/500 tokens, byte-identical, volatile-state-invariant
- SC#3 (MEMORY.md quality): PASS — every active project ≥80%
- SC#4 (handoff pickup): PASS 3/3 cold-start trials

## Vibe-check (archival)

LongMemEval Oracle: NN.N% (vs 90.6% baseline)
LoCoMo: NN.N% (vs 55.5% baseline)
Non-gating per CONTEXT.md axiom.

## What's next

v4.1 = Distribution. Stub at `.planning/v4.1-distribution/STUB.md`. Run `/gsd:new-milestone` to start the next cycle.

## Behavioral discipline (the why, restated)

The audit's diagnosis was "green numbers feel like progress while artifacts regress." v4 corrected by replacing benchmark gates with behavioral (SC#1) + structural (SC#2) + content-quality (SC#3) + continuity (SC#4) gates. Phase 11 is the moment that corrective went live. Future milestones should not relapse into benchmark gating.
```

Step 2 — final test run before commit:

```bash
bun run test                      # full suite
bun run build                     # TS clean
bun run vesna                     # gate green
bun run sc3                       # gate green
bun run vitest run src/tests/assembly/assembler.cache-stability.test.ts  # SC#2
```

If any fail, do NOT commit/tag. Open a sub-plan and resolve.

Step 3 — phase close commit:

```bash
git add -A   # only after confirming `git status` shows expected files (use `git status` first to review)
git commit -m "$(cat <<'EOF'
phase(11): close — v4 final validation, all SC gates cleared

SC#1 Vesna behavioral: NN% aggregate, ≥80% every category
SC#2 token budget ≤500 cache-stable: NN/500, byte-identical, invariant
SC#3 MEMORY.md quality: every active project ≥80%
SC#4 handoff pickup: 3/3 cold-start trials PASS

Archival vibe-check: LongMemEval Oracle NN.N% (vs 90.6%); LoCoMo NN.N% (vs 55.5%).
Non-gating per CONTEXT.md axiom.

STOR-04 legacy `_old` tables: <DROP via V24 / DEFER to v4.1>.
v4.1 Distribution stub at .planning/v4.1-distribution/STUB.md.

Closes Phase 11. v4.0 ships as internal infrastructure.
EOF
)"
```

Step 4 — tag v4.0.0:

```bash
git tag -a v4.0.0 -m "$(cat <<'EOF'
v4.0.0 — Claudex internal infrastructure release

Validation: see .planning/phases/11-p9-final-validation/11-V4-VALIDATION.md

SC#1 Vesna behavioral: PASS — NN%
SC#2 token budget ≤500 cache-stable: PASS
SC#3 MEMORY.md quality: PASS
SC#4 handoff pickup: PASS 3/3

v4.0 is internal infrastructure. v4.1 = Distribution will make it installable by strangers.
EOF
)"
```

Step 5 — push commit + tag (per CLAUDE.md "Push freely when asked" — this is the asked moment, but **the orchestrator should still confirm the push** since it's a permanent public action affecting the GitHub repo):

```bash
git push origin master
git push origin v4.0.0
```

If the push prompts for credentials or fails, surface the error to the orchestrator — do not retry blind. Per CLAUDE.md, the relevant GitHub account here is whichever the project remote is configured for; check `git remote -v` and confirm with the orchestrator if ambiguous.

Step 6 — final reporting:

After push succeeds, the executor reports back: "Phase 11 closed. v4.0.0 tagged at <commit>. v4 SHIPPED." with a link to V4_VALIDATION.md. The TaskList "Execute Phase 11 — Final validation + v4 tag" can be marked completed by the orchestrator.

**If the push fails or is denied**: leave the commit + tag local, write the failure into the SUMMARY, and surface to orchestrator. Do NOT delete the tag.
  </action>
  <verify>
- 11-CLOSE-SUMMARY.md exists
- bun run test, build, vesna, sc3, cache-stability all pass before commit
- Phase 11 close commit exists with the SC verdicts in the body
- v4.0.0 tag exists annotated with V4_VALIDATION.md reference
- git push completed (or failure surfaced to orchestrator with commit+tag intact locally)
  </verify>
  <done>v4.0.0 is tagged and pushed; Phase 11 is closed; STATE/ROADMAP/REQUIREMENTS reflect v4 SHIPPED.</done>
</task>

</tasks>

<verification>
- 11-V4-VALIDATION.md exists with all 4 SC verdicts cited to evidence files
- CLAUDE.md and README.md updated per CONTEXT.md
- v4.1 Distribution stub exists
- STATE.md, ROADMAP.md, REQUIREMENTS.md updated to reflect v4 SHIPPED
- Phase close commit + v4.0.0 tag + push (or push-failure-surfaced) complete
- Full ship-gate test suite passes (bun run test + vesna + sc3 + cache-stability)
- 11-CLOSE-SUMMARY.md is the human-readable record of the ship moment
</verification>

<success_criteria>
- v4.0.0 is tagged on a commit where SC#1-#4 all PASS with evidence
- Docs match v4 reality (no benchmark gating language; behavioral suite is the ship gate)
- v4.1 Distribution is set up to start cleanly without re-discovery
- A future maintainer can read 11-V4-VALIDATION.md and understand exactly what was true at v4 ship
</success_criteria>

<output>
On completion, write `11-07-SUMMARY.md` pointing to 11-CLOSE-SUMMARY.md and 11-V4-VALIDATION.md.
</output>
