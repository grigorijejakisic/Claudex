---
phase: 02-multi-modal-index-seeds-density-check
plan: 05
type: execute
wave: 3
depends_on: [02-03, 02-04]
files_modified:
  - src/benchmark/episodic-density/verdict.ts
  - src/benchmark/episodic-density/runner.ts
  - src/benchmark/episodic-density/cli.ts
  - src/benchmark/vesna/probes/episodic-fingerprint-fires.json
  - src/benchmark/vesna/probes/fusion-non-regression.json
  - src/benchmark/vesna/probes/.disabled/episodic-fingerprint-fires.json
  - src/benchmark/vesna/probes/.disabled/fusion-non-regression.json
  - src/tests/benchmark/episodic-density/verdict.test.ts
  - .planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md
  - .planning/phases/02-multi-modal-index-seeds-density-check/02-results.json
  - src/shared/constants.ts
autonomous: true
requirements: [IDX-02, IDX-04]
must_haves:
  truths:
    - "A verdict module exists at src/benchmark/episodic-density/verdict.ts that consumes HarnessRunResult.decision_rule_inputs and returns one of three verdicts: GREEN_LIGHT, SCOPE_DOWN, KILL"
    - "The verdict logic implements CONTEXT item 5 EXACTLY (cited verbatim in code AND in 02-RESULTS.md): GREEN_LIGHT requires all three criteria on the held-out test set; KILL on criterion 1 OR 2 failure; SCOPE_DOWN otherwise"
    - "The verdict runs ONLY on harness output; it is non-mutating; it returns a structured object with the three criterion checks each as { passed: boolean, observed: number, threshold: number, evidence: string }"
    - "The runner CLI runs the harness, computes the verdict, writes 02-RESULTS.md and 02-results.json, then — and only then — applies the verdict-driven side effects (Vesna probe (de)activation, feature flag default)"
    - "Verdict ordering enforced: verdict computed and persisted to 02-results.json BEFORE any code/config that depends on the verdict is mutated (team-lead non-negotiable #4)"
    - "On GREEN_LIGHT: two new Vesna probes (episodic-fingerprint-fires, fusion-non-regression) are activated by being placed in src/benchmark/vesna/probes/ alongside the 17 existing probes; bun run vesna picks them up automatically"
    - "On SCOPE_DOWN or KILL: the same two probe JSON files are placed in src/benchmark/vesna/probes/.disabled/ (not in the main probes directory) so bun run vesna does NOT include them in the gate; CONTEXT item 7 negative-result handling enforced"
    - "On KILL or SCOPE_DOWN: src/shared/constants.ts DEFAULT_CONFIG.features.error_fingerprint is flipped from true (Plan 02-02 default) to false; existing fingerprinted rows and sidecar data are RETAINED (CONTEXT item 7: 'Backfilled rows retained — destructive cleanup is not the answer')"
    - "On GREEN_LIGHT: error_fingerprint flag default stays true"
    - "02-RESULTS.md is human-readable, includes the decision rule cited verbatim, the three criterion checks with observed values, the latency table, the per-origin metric tables, and the final verdict"
    - "02-results.json is the machine-readable mirror of RESULTS.md, consumable by future re-runs and Phase 3 baseline comparison"
    - "Re-running the runner is safe: results files are overwritten in place; verdict is recomputed; flag and probe state are reconciled to the latest verdict"
  artifacts:
    - path: "src/benchmark/episodic-density/verdict.ts"
      provides: "Pure verdict module — consumes decision_rule_inputs, returns Verdict"
      contains: "export function computeVerdict"
      min_lines: 80
    - path: "src/benchmark/episodic-density/runner.ts"
      provides: "Top-level runner — open DB, run harness, compute verdict, write results, apply side effects (in that order)"
      contains: "export async function runFullPhase2Measurement"
      min_lines: 100
    - path: "src/benchmark/episodic-density/cli.ts"
      provides: "Extended CLI: bun run src/benchmark/episodic-density/cli.ts measure"
      contains: "runFullPhase2Measurement"
    - path: "src/benchmark/vesna/probes/.disabled/episodic-fingerprint-fires.json"
      provides: "Vesna probe — known matched pair retrieval ≥ threshold (CONTEXT item 3)"
      min_lines: 20
    - path: "src/benchmark/vesna/probes/.disabled/fusion-non-regression.json"
      provides: "Vesna probe — RRF-fused recall@10 not measurably worse than semantic-only (CONTEXT item 3)"
      min_lines: 20
    - path: "src/tests/benchmark/episodic-density/verdict.test.ts"
      provides: "Unit tests covering all three verdict branches; ordering test that mutating side effects can't run before the verdict is persisted"
      min_lines: 80
    - path: ".planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md"
      provides: "Human-readable measurement report — decision rule verbatim, criterion checks, metric tables (pooled + per-origin), latency table, density signal, verdict"
      min_lines: 100
    - path: ".planning/phases/02-multi-modal-index-seeds-density-check/02-results.json"
      provides: "Machine-readable mirror; Phase 3 baseline"
      min_lines: 30
  key_links:
    - from: "src/benchmark/episodic-density/runner.ts"
      to: "src/benchmark/episodic-density/harness.ts"
      via: "import { runHarness } and consume HarnessRunResult"
      pattern: "runHarness"
    - from: "src/benchmark/episodic-density/runner.ts"
      to: "src/benchmark/episodic-density/verdict.ts"
      via: "import { computeVerdict } and call BEFORE applying side effects"
      pattern: "computeVerdict"
---

<objective>
Land the verdict logic that turns Plan 02-04's `HarnessRunResult.decision_rule_inputs` into one of three verdicts (GREEN_LIGHT / SCOPE_DOWN / KILL) per CONTEXT item 5, then build the runner that orchestrates: open DB → run harness → compute verdict → persist verdict to disk → apply verdict-driven side effects (Vesna probe activation/disablement and feature-flag default flip). The ordering is non-negotiable (team-lead #4): the verdict is decided and persisted BEFORE any code or config the verdict depends on is mutated.

Decision rule (CONTEXT item 5; cited verbatim in `verdict.ts` JSDoc AND in 02-RESULTS.md):

> **GREEN-LIGHT Phase 3 — proceed with full multi-handle retrieval cutover:**
>
> ALL three must hold on the **held-out test set**:
> 1. RRF-fusion has measurable improvement over semantic-only — minimum **+5pp on either precision@5 OR recall@10**, AND the **Wilson 95% CI lower bound on the delta is ≥ 0** (i.e., the improvement is not statistically indistinguishable from zero at our sample size). The AND-CI-bound is the discipline that prevents green-lighting on noise — at n≈40-60 pairs, raw point-deltas of +5pp can be inside the CI of zero.
> 2. Density at scale produces signal — ≥30% of high-similarity pairs (per #4) are intra-project recurrent.
> 3. Latency p99 of fused retrieval < 2× semantic-only baseline. Cost discipline: a marginally-better signal that doubles tail latency is not worth shipping.
>
> **SCOPE-DOWN to advisory — Phase 3 ships, but lighter than originally planned:**
> Improvement exists on specific subsets (e.g. only Python stack traces, only one project) but not broadly. Phase 3 ships an **advisory-only surface** ("you've hit a similar error before, see episode X") without aggressive RRF fusion in the production retrieval path. Phase 5 density abstraction is de-scoped accordingly (advisory, not abstraction).
>
> **KILL — pivot or stop:**
> No measurable improvement (criteria 1 fails on held-out CI bound) OR density is pure noise (criteria 2 fails). Phase 3 plan is rewritten or the multi-handle thesis is reconsidered at the milestone level.

Output: 02-RESULTS.md + 02-results.json on disk; Vesna probe state reconciled to verdict; `error_fingerprint` flag default flipped iff verdict is KILL or SCOPE_DOWN; verdict.test.ts proving the ordering.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-modal-index-seeds-density-check/02-CONTEXT.md
@.planning/phases/02-multi-modal-index-seeds-density-check/02-04-PLAN.md
@src/benchmark/episodic-density/harness.ts
@src/benchmark/episodic-density/wilson.ts
@src/benchmark/vesna/probes/handoff-pickup-active.json
@src/shared/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verdict module — pure function from harness output to verdict</name>
  <files>src/benchmark/episodic-density/verdict.ts</files>
  <action>
Create `src/benchmark/episodic-density/verdict.ts` with the following at the top:

1. **JSDoc comment block at the top of the file containing CONTEXT item 5 cited VERBATIM**, word-for-word, no paraphrase. (This is team-lead non-negotiable #1.) Wrap the citation in a `/** ... */` block; do not condense, summarize, or "improve" the language.

2. Required exports:

```typescript
export type VerdictKind = 'GREEN_LIGHT' | 'SCOPE_DOWN' | 'KILL';

export interface CriterionCheck {
  name: 'criterion_1_fusion_improvement' | 'criterion_2_density_signal' | 'criterion_3_latency_budget';
  passed: boolean;
  observed: number;     // primary observed value
  threshold: number;    // the locked threshold from CONTEXT item 5
  evidence: string;     // human-readable: e.g. "delta_p5=+0.064, CI lower=0.012, n=42 pairs"
  applies_to: 'criterion_1' | 'criterion_2' | 'criterion_3';
}

export interface Verdict {
  kind: VerdictKind;
  criteria: {
    criterion_1: CriterionCheck;
    criterion_2: CriterionCheck;
    criterion_3: CriterionCheck;
  };
  reasoning: string;     // one paragraph human-readable; cites the criteria
  computed_at_ts_epoch: number;
  decision_rule_quote: string; // pasted verbatim from CONTEXT item 5
}

export function computeVerdict(inputs: HarnessRunResult['decision_rule_inputs'], opts?: { ts_epoch?: number }): Verdict;
```

**Verdict logic (implements CONTEXT item 5 exactly):**

- **Criterion 1 — fusion improvement on held-out test set:**
  - PASS iff: (max(`fused_p5_minus_semantic_p5.delta`, `fused_r10_minus_semantic_r10.delta`) >= 0.05) AND (corresponding `ci_lower >= 0` for the metric whose delta cleared 0.05). The CONTEXT phrasing is "minimum +5pp on either precision@5 OR recall@10, AND the Wilson 95% CI lower bound on the delta is ≥ 0" — both half-clauses are required AND the CI check binds to the same metric whose point-delta cleared 5pp. (If P@5 cleared 5pp but its CI lower < 0 while R@10 did not clear 5pp, criterion 1 FAILS — the CI discipline prevents shopping for the metric whose point estimate happens to be high.)
  - `observed` = the larger of the two deltas; `threshold` = 0.05; `evidence` includes both metrics' deltas + CI lowers + sample n.
  - **REQUIRED CODE COMMENT at or near the criterion-1 evaluation in `verdict.ts`** (per checker-02-04 verification — Note 3): include a comment with the following load-bearing prose:

    > // INVARIANT (enforced by 02-04 schema): the metric whose `delta` cleared 5pp
    > // and the metric whose `ci_lower` we check are co-located in the SAME
    > // fused_*_minus_semantic_* object (one for p@5, one for r@10). Cross-metric
    > // drift is structurally impossible by construction — we read both fields
    > // from the same struct, so `(delta_p5 >= 0.05 AND ci_lower_r10 >= 0)` is
    > // literally unwritable. Therefore the criterion-1 logic is: for each metric
    > // object independently, test (delta >= 0.05 AND ci_lower >= 0); criterion
    > // PASSES iff at least one metric object passes BOTH halves. Do NOT rewrite
    > // this as a max-then-pick — the per-object structure is the discipline.

    The pairing is structural (each metric's `delta`, `ci_lower`, `ci_upper` live in one struct), but the comment is non-negotiable so future maintainers can't accidentally refactor into a max-pick form that would re-introduce cross-metric ambiguity.

- **Criterion 2 — density signal:**
  - PASS iff: `intra_project_share >= 0.30`.
  - `observed` = `intra_project_share`; `threshold` = 0.30; `evidence` includes the noise floor and sigma.

- **Criterion 3 — latency budget:**
  - PASS iff: `p99_fused_over_p99_semantic < 2.0`.
  - `observed` = the ratio; `threshold` = 2.0; `evidence` includes both p99 absolute values.

**Verdict assembly:**

- All three pass → `GREEN_LIGHT`.
- Criterion 1 FAILS OR Criterion 2 FAILS → `KILL`. (CONTEXT phrasing: "No measurable improvement (criteria 1 fails on held-out CI bound) OR density is pure noise (criteria 2 fails)" → KILL.)
- Otherwise (criterion 1 and 2 pass but criterion 3 FAILS, OR partial improvement with one of 1/2 borderline) → `SCOPE_DOWN`. CONTEXT phrasing: "Improvement exists on specific subsets... not broadly. Phase 3 ships an advisory-only surface."

**reasoning** field: one paragraph, no boilerplate, mentioning each criterion's pass/fail and the values that drove it. Example: "Criterion 1 PASSED (+5.4pp on R@10, CI lower 0.011, n=46). Criterion 2 PASSED (intra-project share 0.41). Criterion 3 FAILED (p99 ratio 2.31 — fused tail is 2.31× semantic baseline, exceeding the 2.0 budget). Verdict: SCOPE_DOWN — fusion has signal but the cost discipline blocks full cutover; advisory-only path forward per CONTEXT item 5."

**Pure function**: no I/O, no time-of-day dependency (allow `opts.ts_epoch` injection — default to `Math.floor(Date.now()/1000)` only when opts.ts_epoch is undefined).

**Avoid:**
- Loosening criterion 1's CI binding ("if either metric's delta cleared 5pp regardless of which one's CI is positive") — CONTEXT explicitly says the CI check IS the discipline against shopping.
- Adding a fourth criterion or re-weighting any of the three — moving goalposts is the named failure mode.
- Mutating any state (DB, files) from this module — verdict is a pure compute step.
  </action>
  <verify>
1. `bun run build` clean.
2. Tests in Task 4 pass.
  </verify>
  <done>computeVerdict implements CONTEXT item 5 exactly; cites the rule verbatim in JSDoc; pure function with structured criterion-check output and a human-readable reasoning paragraph.</done>
</task>

<task type="auto">
  <name>Task 2: Vesna probe definitions in .disabled/</name>
  <files>src/benchmark/vesna/probes/.disabled/episodic-fingerprint-fires.json, src/benchmark/vesna/probes/.disabled/fusion-non-regression.json</files>
  <action>
Create the parent directory `src/benchmark/vesna/probes/.disabled/` (use `mkdir -p`). Author both probe JSON files in there. The runner in Task 3 moves them to `src/benchmark/vesna/probes/` ONLY on GREEN_LIGHT verdict.

**Probe shape**: read an existing probe (e.g. `src/benchmark/vesna/probes/handoff-pickup-active.json`) to confirm the JSON schema: `{ id, description, setup_steps, user_prompt, expected_recall, ...}`. Match the schema exactly — Vesna's loader is strict.

**A. `episodic-fingerprint-fires.json`** — assertion: a known-matched pair (a, b) from the held-out test set is retrieved by Variant B (fingerprint-only) when query=a. The probe seeds the test DB with two synthetic events sharing exception type and ≥3 frames in different sessions; expects the fingerprint retrieval to return event b in the top-k of a query for event a's content.

**B. `fusion-non-regression.json`** — assertion: RRF-fused recall@10 is not measurably worse than semantic-only on a small synthetic corpus. The probe seeds a corpus where semantic retrieval already finds the right answer; the assertion is that fusion does not push the right answer below rank-10. CONTEXT item 3 phrasing: "RRF-fused recall@10 not measurably worse than semantic-only."

**Activation contract**: probes live in `.disabled/` until the runner moves them. Vesna's loader (`src/benchmark/vesna/loader.ts`) discovers `*.json` in `src/benchmark/vesna/probes/` only — confirm by reading the loader; if it recurses, place the disabled probes in a directory the loader EXCLUDES (its glob may already exclude `.`-prefixed dirs; if not, use a non-glob directory such as `src/benchmark/vesna/probes-disabled/` and adapt the runner accordingly. Verify the actual loader behavior at execution time.)

**Avoid:**
- Activating the probes at file-creation time — they MUST start in `.disabled/`.
- Writing probe content that depends on a real ~/.claudex/db DB — Vesna probes have their own setup_steps for the test DB. Match existing probe conventions.
- Asserting precision-level threshold without a confidence margin — Vesna probes are 3-trial pass/fail; small synthetic corpora minimise variance.
  </action>
  <verify>
1. Both files parse as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/benchmark/vesna/probes/.disabled/episodic-fingerprint-fires.json'))"` does not throw.
2. The Vesna loader, when invoked normally (`bun run vesna --list` or equivalent), DOES NOT include either probe (confirming the .disabled location works).
3. After the operator manually copies one probe into `src/benchmark/vesna/probes/`, `bun run vesna --list` (or equivalent) DOES include it (confirming the activation pattern works in both directions).
  </verify>
  <done>Both probe JSONs exist in .disabled/; Vesna loader excludes them by default; the activation/deactivation move is mechanical for the runner in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Top-level runner — orchestrate harness → verdict → persistence → side effects (in that order)</name>
  <files>src/benchmark/episodic-density/runner.ts, src/benchmark/episodic-density/cli.ts</files>
  <action>

**A. `src/benchmark/episodic-density/runner.ts`** — required export:

```typescript
export interface RunSummary {
  verdict: Verdict;
  results_md_path: string;
  results_json_path: string;
  vesna_probe_state: 'activated' | 'disabled';
  flag_state: 'true_default' | 'false_default';
}

export async function runFullPhase2Measurement(db: Database, opts?: { seed?: number; ts_epoch?: number }): Promise<RunSummary>;
```

Pipeline (ordering is mechanical — DO NOT reorder):

1. **Run harness**: `const harness = await runHarness(db, { seed: opts?.seed });`
2. **Compute verdict**: `const verdict = computeVerdict(harness.decision_rule_inputs, { ts_epoch: opts?.ts_epoch });`
3. **Persist machine-readable results**: write `02-results.json` with shape `{ harness, verdict }`. Atomic write: write to a temp file then `fs.rename` to final name. If this step fails, abort with a clear error — DO NOT proceed to side effects.
4. **Render and write 02-RESULTS.md** (Task 5 builds the renderer; runner just calls it). Atomic write same pattern.
5. **Side-effect gate**: ONLY now, after results are durably on disk, mutate code/config:
   - **GREEN_LIGHT** → move both probe JSONs from `.disabled/` to `src/benchmark/vesna/probes/`. Leave `DEFAULT_CONFIG.features.error_fingerprint = true` unchanged in `src/shared/constants.ts`.
   - **SCOPE_DOWN** → keep both probe JSONs in `.disabled/`. Flip `DEFAULT_CONFIG.features.error_fingerprint = false` in `src/shared/constants.ts` (per CONTEXT item 7: "Ship the implementation behind a feature flag, off by default" if KILL or SCOPE_DOWN).
   - **KILL** → keep probes disabled; flip flag default to false (same as SCOPE_DOWN).
6. Return RunSummary.

**Idempotency**: re-running the runner reproduces the side-effect state from the latest verdict. The renderer/writer overwrites results files. The probe move is "ensure-state" not "transfer-once" — if a probe is already in the right location, the move is a no-op.

**Failure modes**:
- If `runHarness` throws (e.g. corpus floor not met), the runner catches, writes a `02-results.json` with `{ verdict: { kind: 'BLOCKED', reason: <message> } }` (extend the Verdict type with a fourth `'BLOCKED'` kind for the corpus-floor case), writes a minimal RESULTS.md explaining the blockage, and returns. NO side effects fire on BLOCKED.

**B. `src/benchmark/episodic-density/cli.ts`** — extend the existing CLI from Plan 02-03 with a `measure` subcommand:

```bash
bun run src/benchmark/episodic-density/cli.ts measure [--seed N]
```

Open DB, run migrations (defensive), call `runFullPhase2Measurement`, print the RunSummary including the verdict reasoning paragraph, exit 0 on GREEN_LIGHT/SCOPE_DOWN, exit 0 on KILL (this is a successful empirical-phase outcome, not a failure — CONTEXT item 7), exit 1 on BLOCKED (corpus floor / runtime error).

**Avoid:**
- Mutating any side effect BEFORE results are persisted to disk. The team-lead non-negotiable #4 is mechanical: results write → side-effects, no overlap.
- Mutating files with non-atomic writes (write directly without rename) — `fs.writeFileSync('foo.md', body); fs.renameSync` is the cheap atomic pattern.
- Implementing a "force GREEN_LIGHT" override flag — moving goalposts is the named failure mode.
- Caching the verdict between runs — every `measure` invocation recomputes from the live DB corpus. Phase 5 (the second empirical phase) reuses the harness shape.
  </action>
  <verify>
1. `bun run build` clean.
2. `bun run test src/tests/benchmark/episodic-density/` — all tests pass; new ordering test (Task 4) green.
3. Manual: against a fixture DB ≥ floor, `bun run src/benchmark/episodic-density/cli.ts measure` produces the two results files and reconciles probe + flag state to the verdict.
4. Manual: re-run is a no-op observationally — same verdict, same files (modulo `computed_at_ts_epoch`), same probe location, same flag default.
  </verify>
  <done>Runner orchestrates harness → verdict → persistence → side effects in that exact order; mechanically idempotent; exits 0 on KILL (negative result is success); exits 1 on BLOCKED.</done>
</task>

<task type="auto">
  <name>Task 4: Verdict + ordering tests</name>
  <files>src/tests/benchmark/episodic-density/verdict.test.ts</files>
  <action>
Create the test file. Cover:

1. **GREEN_LIGHT branch**: feed inputs where all three criteria pass (e.g. delta_p5=+0.07 with ci_lower=0.02, intra_project_share=0.42, p99 ratio=1.4) → assert `kind === 'GREEN_LIGHT'`, all three criterion `passed === true`, `decision_rule_quote` contains the verbatim CONTEXT phrasing.
2. **KILL on criterion 1 failure (CI bound)**: delta_p5=+0.07 BUT ci_lower=-0.01 (point estimate clears 5pp but CI doesn't exclude zero) → criterion 1 FAILS, verdict KILL.
3. **KILL on criterion 1 failure (point estimate)**: delta_p5=+0.03 with ci_lower=0.01 (CI excludes zero but point delta is below 5pp threshold) AND delta_r10 also below threshold → criterion 1 FAILS, verdict KILL.
4. **KILL on criterion 2 failure**: criterion 1 + 3 pass; intra_project_share=0.18 → criterion 2 FAILS, verdict KILL. (CONTEXT explicitly says "OR density is pure noise (criteria 2 fails)".)
5. **SCOPE_DOWN on criterion 3 failure**: criteria 1 + 2 pass; p99_fused/p99_semantic = 2.4 → criterion 3 FAILS, verdict SCOPE_DOWN. (CONTEXT: latency-only failure is scope-down, not kill.)
6. **CI binding discipline**: delta_p5=+0.06 with ci_lower=-0.02 (point clears 5pp but CI doesn't), delta_r10=+0.04 with ci_lower=0.01 (CI excludes zero but point doesn't clear 5pp) → criterion 1 FAILS. (Neither metric satisfies BOTH halves of the AND; verdict KILL.)
7. **CI binding satisfaction**: delta_p5=+0.06 with ci_lower=0.01 → criterion 1 PASSES even if delta_r10=+0.02 with ci_lower=-0.03. (One metric satisfying both halves is sufficient.)
8. **Determinism + verbatim quote**: pass same inputs twice with identical `ts_epoch` → byte-equal Verdict; `decision_rule_quote` contains the substrings `"GREEN-LIGHT Phase 3"`, `"+5pp on either precision@5 OR recall@10"`, `"Wilson 95% CI lower bound on the delta is ≥ 0"`, `"≥30% of high-similarity pairs"`, `"Latency p99 of fused retrieval < 2× semantic-only baseline"`.
9. **Ordering test (runner-level)**: stub `runHarness` to return a fixed result; mock `fs.writeFile` (or its sync equivalent used by the runner) to track call order; mock `fs.rename` (probe move) similarly; assert that every probe-move and constants.ts mutation call comes AFTER both results-file writes have completed. Non-trivial: spy on the entire fs surface used by the runner; the test fails if any side-effect file write precedes the results writes. (Team-lead non-negotiable #4 made mechanical.)

**Avoid:**
- Mocking `computeVerdict` in the ordering test — exercise the real verdict over fixed inputs.
- Skipping the verbatim-quote assertion — team-lead non-negotiable #1 is the hill this test defends.
- Snapshot-testing the entire Verdict object — assert specific fields.
  </action>
  <verify>
1. `bun run test src/tests/benchmark/episodic-density/verdict.test.ts` — full green, all 9 cases pass.
2. `bun run test` — full suite green.
  </verify>
  <done>9 passing tests; ordering test mechanically enforces the team-lead non-negotiable; CI-binding discipline (the hardest semantic) covered with 4 distinct cases.</done>
</task>

<task type="auto">
  <name>Task 5: 02-RESULTS.md renderer + first persisted snapshot + 02-results.json schema</name>
  <files>.planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md, .planning/phases/02-multi-modal-index-seeds-density-check/02-results.json</files>
  <action>
Implement an inline renderer inside `runner.ts` (do NOT create a separate `render.ts` — the renderer is short and tightly coupled to the runner). Then run the runner once against the operator's DB to produce the first persisted snapshot.

**`02-RESULTS.md` shape (renderer template)**:

```markdown
# Phase 2 Results: Multi-modal index seeds + density-at-scale check

**Generated:** {ISO date}
**Harness seed:** {seed}
**Verdict:** **{KIND}** — {one-line reason}

---

## Decision rule (CONTEXT.md item 5, locked BEFORE measurement)

> {VERBATIM CONTEXT item 5 paste — entire item, no condensation}

---

## Criterion checks (held-out test set)

| # | Criterion | Threshold | Observed | Passed | Evidence |
|---|-----------|-----------|----------|--------|----------|
| 1 | Fusion improvement (max(Δp@5,Δr@10) ≥ +5pp AND CI lower ≥ 0 on the same metric) | 0.05 / CI≥0 | {value} | {Y/N} | {evidence} |
| 2 | Density signal (intra-project share of high-similarity pairs ≥ 30%) | 0.30 | {value} | {Y/N} | {evidence} |
| 3 | Latency budget (p99 fused / p99 semantic < 2.0) | 2.0 | {value} | {Y/N} | {evidence} |

---

## Quality metrics — held-out test set

### Pooled
| Variant | precision@5 (Wilson 95% CI) | recall@10 (Wilson 95% CI) | MRR (mean ± bootstrap CI) | n |
|---------|------------------------------|----------------------------|----------------------------|---|
| A semantic-only | ... | ... | ... | ... |
| B fingerprint-only | ... | ... | ... | ... |
| C RRF-fused (k=60) | ... | ... | ... | ... |

### phase1_organic only
{same table}

### v4_backfill only
{same table}

### Deltas vs A (Newcombe 95% CI)
| Comparison | Δ precision@5 (CI) | Δ recall@10 (CI) | Origin split |
|------------|--------------------|------------------|--------------|
| C - A      | ...                | ...              | pooled |
| C - A      | ...                | ...              | phase1_organic |
| C - A      | ...                | ...              | v4_backfill |
| B - A      | ...                | ...              | pooled |

---

## Latency

| Variant | p50 (ms) | p95 (ms) | p99 (ms) |
|---------|----------|----------|----------|
| A | ... | ... | ... |
| B | ... | ... | ... |
| C | ... | ... | ... |

p99(C) / p99(A) = {ratio} (criterion 3 threshold = 2.0)

---

## Density signal (CONTEXT item 4)

- Random-pair sample size: 1000
- Noise floor (95th percentile of random pair Jaccard): {value}
- Sample stddev (σ): {value}
- Cluster threshold (noise floor + 2σ): {value}
- Weak clusters (K=2..4): {count}
- Strong clusters (K≥5): {count}
- Intra-project share of high-similarity pairs: {value} (CONTEXT item 4 threshold = 0.30)
- Density meaningful: {Y/N}

---

## Corpus

- Total fingerprinted episodes: {N}
- phase1_organic: {N}
- v4_backfill: {N}
- Projects covered: {list}
- Test set size: {N pairs}

See: 02-03-corpus-audit.md for source breakdown and 20-pair spot-check.

---

## Verdict reasoning

{verdict.reasoning paragraph}

---

## Next steps

{branch on verdict kind:}
- **GREEN_LIGHT**: Phase 3 (multi-handle retrieval cutover) proceeds; both Vesna probes activated; flag default stays true.
- **SCOPE_DOWN**: Phase 3 ships advisory-only; Vesna probes remain disabled; flag default flipped to false (CONTEXT item 7); Phase 5 de-scoped to advisory.
- **KILL**: Phase 3 plan rewritten; multi-handle thesis reconsidered at user-approval gate; flag default flipped to false; backfilled rows and harness retained per CONTEXT item 7.

Code retained at flag for future reference. Phase 5 (the second empirical phase) reuses this harness shape.
```

**`02-results.json` schema**:

```json
{
  "schema_version": 1,
  "generated_at_ts_epoch": <int>,
  "harness": <full HarnessRunResult JSON>,
  "verdict": <full Verdict JSON>
}
```

This file is the Phase 3 baseline per CONTEXT item 3 measurement protocol; future re-runs may diff against it.

After running the runner once, the snapshot files are written. Commit them in the same git operation as the source — they are durable artifacts (matching how Phase 1 committed its operator README).

**Avoid:**
- Hand-editing the rendered 02-RESULTS.md after the runner produces it — re-runs overwrite. Operator notes belong in 02-SUMMARY.md instead.
- Inlining the entire HarnessRunResult into 02-RESULTS.md — the JSON file is the canonical record; the .md is a human view.
- Skipping the verbatim CONTEXT item 5 paste — non-negotiable.
  </action>
  <verify>
1. After running `bun run src/benchmark/episodic-density/cli.ts measure` (against the operator's DB or a fixture DB ≥ floor), the two files exist on disk at `.planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md` and `02-results.json`.
2. 02-RESULTS.md contains the full CONTEXT item 5 verbatim block (assert via `grep -F '+5pp on either precision@5 OR recall@10' .planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md`).
3. 02-results.json parses cleanly: `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/02-multi-modal-index-seeds-density-check/02-results.json'))"` returns object with `schema_version`, `harness`, `verdict`.
4. Re-running the runner produces byte-different files only in `generated_at_ts_epoch` and `computed_at_ts_epoch` (verdict is otherwise stable on the same corpus + same seed).
  </verify>
  <done>02-RESULTS.md is the human-readable verdict report with the CONTEXT item 5 verbatim block; 02-results.json is the machine-readable mirror; first snapshot is committed to disk.</done>
</task>

<task type="auto">
  <name>Task 6: Apply verdict-driven side effects (probe location + flag default)</name>
  <files>src/shared/constants.ts, src/benchmark/vesna/probes/.disabled/{episodic-fingerprint-fires,fusion-non-regression}.json, src/benchmark/vesna/probes/{episodic-fingerprint-fires,fusion-non-regression}.json</files>
  <action>
This task is conditional on the verdict produced by Task 5's runner invocation. The runner already has the move + flag-flip side-effect code (Task 3); Task 6 is the operator's checklist for confirming the side effects landed and committing them.

After Task 5's runner run, observe the verdict in 02-RESULTS.md and 02-results.json:

- **GREEN_LIGHT** verdict observed:
  - Verify both probes are in `src/benchmark/vesna/probes/` (NOT in `.disabled/`).
  - Verify `src/shared/constants.ts` `DEFAULT_CONFIG.features.error_fingerprint === true` (unchanged from Plan 02-02).
  - Run `bun run vesna` — should now load 19 probes (17 existing + 2 new) and pass.

- **SCOPE_DOWN** or **KILL** verdict observed:
  - Verify both probes are in `src/benchmark/vesna/probes/.disabled/` (NOT in main probes dir).
  - Verify `src/shared/constants.ts` `DEFAULT_CONFIG.features.error_fingerprint === false` (flipped from Plan 02-02 default).
  - Run `bun run vesna` — should still load 17 probes and pass (no new probes activated).

In all cases:
- Confirm `episodic_index_error_fingerprint` rows from Plan 02-03 backfill are STILL THERE — destructive cleanup is forbidden by CONTEXT item 7.
- Confirm `metadata_json.error_fingerprint` data on Phase 1 organic rows is STILL THERE.

If any of these checks fail, the runner's side-effect code (Task 3) has a bug — fix it, NOT the artifact state.

**Avoid:**
- Manually moving probes or flipping the flag without a fresh verdict from the runner — the flag/probe state is verdict-derived, not operator-driven.
- Deleting backfill data on KILL/SCOPE_DOWN — explicitly forbidden by CONTEXT item 7.
- Editing 02-RESULTS.md to "fix" a verdict the operator dislikes — moving goalposts. The verdict is what it is; if it's wrong, the bug is in the harness/verdict logic, not the artifact.
  </action>
  <verify>
1. The four side-effect checks above all pass on the operator's machine.
2. `bun run vesna` exits 0 with the expected probe count (17 or 19).
3. `node -e "const Database=require('better-sqlite3'); const db=new Database('~/.claudex/db/claudex.db'); console.log(db.prepare('SELECT COUNT(*) as n FROM episodic_index_error_fingerprint').get());"` reports the same row count as before the verdict applied (no destructive cleanup).
  </verify>
  <done>Side-effect state on disk matches the verdict; backfill data preserved; Vesna gate green at the appropriate probe count.</done>
</task>

</tasks>

<verification>
- `bun run build` clean.
- `bun run test src/tests/benchmark/episodic-density/` — all tests pass; verdict ordering test green.
- `bun run vesna` — green at 17 or 19 probes depending on verdict.
- `bun run test` — full suite green.
- 02-RESULTS.md and 02-results.json exist on disk; both contain the CONTEXT item 5 verbatim block (in the .md) or its quoted form (in the JSON's `verdict.decision_rule_quote`).
- Backfill data and metadata_json fingerprints retained regardless of verdict.
</verification>

<success_criteria>
- [ ] `computeVerdict` implements CONTEXT item 5 EXACTLY, with the rule cited verbatim in JSDoc (team-lead #1).
- [ ] CI-binding discipline mechanically enforced — point delta ≥ 5pp AND CI lower ≥ 0 must bind to the SAME metric (test cases 6, 7 cover this hardest semantic).
- [ ] Runner orchestrates harness → verdict → persistence → side effects, in that order; ordering test passes (team-lead #4).
- [ ] 02-RESULTS.md is the durable human-readable artifact; 02-results.json is the machine-readable mirror and Phase 3 baseline.
- [ ] All metric tables in 02-RESULTS.md report pooled AND per-corpus_origin (team-lead #3).
- [ ] On GREEN_LIGHT: probes activated; flag default stays true.
- [ ] On SCOPE_DOWN or KILL: probes stay disabled; flag default flipped to false; backfill data RETAINED (CONTEXT item 7).
- [ ] Re-running the runner is idempotent — same verdict, same side-effect state, files overwritten in place.
- [ ] IDX-02 (measured improvement, ship gate criterion 1), IDX-04 (density at scale, ship gate criterion 2) both addressed.
- [ ] Empirical-phase discipline upheld: negative result (KILL/SCOPE_DOWN) is a successful outcome (exit 0), not a failure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-modal-index-seeds-density-check/02-05-SUMMARY.md` documenting:
- The observed verdict (GREEN_LIGHT / SCOPE_DOWN / KILL / BLOCKED).
- The three criterion check values (the figures that drove the verdict).
- Probe + flag state after side effects.
- Pointer to 02-RESULTS.md and 02-results.json.
- Phase 3 implications: GREEN_LIGHT = full multi-handle cutover; SCOPE_DOWN = advisory-only; KILL = re-plan at user-approval gate.
- Operator notes (deviations from the spec, surprises, anything that informs the Phase 5 empirical-phase rerun of the same shape).
</output>
</content>
</invoke>