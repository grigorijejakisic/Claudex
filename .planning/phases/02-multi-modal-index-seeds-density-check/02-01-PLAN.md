---
phase: 02-multi-modal-index-seeds-density-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/migration-steps.ts
  - src/core/migrations.ts
  - src/tests/adapters/episodic-events/error-fingerprint-migration.test.ts
autonomous: true
requirements: [IDX-01]
must_haves:
  truths:
    - "Running migrations on a V25 DB produces a V26 DB with an episodic_index_error_fingerprint table"
    - "Running migrations on a fresh :memory: DB produces a V26 DB with the sidecar table on first open"
    - "The sidecar has columns (shingle_hash TEXT, episode_event_id INTEGER, ts_epoch INTEGER, project TEXT, corpus_origin TEXT) — corpus_origin is part of the schema, not added later"
    - "The sidecar has indexes on (shingle_hash), (episode_event_id), and (project, ts_epoch)"
    - "Re-running migrations on an already-V26 DB is a no-op (idempotent)"
    - "The episodic_events table is unchanged by the V26 migration — Phase 1's contract that no ALTER TABLE on episodic_events holds"
    - "corpus_origin is constrained to ('phase1_organic','v4_backfill') by a CHECK constraint per CONTEXT item 2"
  artifacts:
    - path: "src/core/migration-steps.ts"
      provides: "migrateV25toV26 function — DDL for episodic_index_error_fingerprint sidecar"
      contains: "export function migrateV25toV26"
    - path: "src/core/migrations.ts"
      provides: "Step-table registration of V26"
      contains: "migrateV25toV26"
    - path: "src/tests/adapters/episodic-events/error-fingerprint-migration.test.ts"
      provides: "V26 migration shape + idempotency + CHECK constraint tests"
      min_lines: 60
  key_links:
    - from: "src/core/migrations.ts"
      to: "src/core/migration-steps.ts"
      via: "import { migrateV25toV26 } and step-table entry [25, () => { migrateV25toV26(db); }]"
      pattern: "migrateV25toV26"
---

<objective>
Land the V26 schema migration that creates the `episodic_index_error_fingerprint` sidecar table — the inverted-index storage Plan 02-02 (fingerprinter), Plan 02-03 (backfill), and Plan 02-04 (measurement harness) read and write. Includes the DDL with the locked column set, the `corpus_origin` CHECK constraint, the three required indexes, registration in the migration runner, and unit tests proving the migration is correct + idempotent + enforces the enum + leaves `episodic_events` untouched.

Purpose: Per CONTEXT.md item 6, Phase 2 introduces the FIRST sidecar pattern in the v5 substrate (one-table-per-modality, inverted index of metadata_json-derived features, no ALTER TABLE on episodic_events). Setting this pattern correctly here informs Phase 3 which will likely build 2-3 more sidecars. Without this table, Plans 02-02, 02-03, 02-04 cannot land.

Output: A V26 DB schema, a passing test file, no behavioral change to existing readers.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-modal-index-seeds-density-check/02-CONTEXT.md
@src/core/schema.ts
@src/core/migration-steps.ts
@src/core/migrations.ts
@src/shared/constants.ts
@src/tests/adapters/episodic-events/schema-migration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migrateV25toV26 + register in runner</name>
  <files>src/core/migration-steps.ts, src/core/migrations.ts</files>
  <action>
1. **`src/core/migration-steps.ts`** — append a new `migrateV25toV26(db: Database): boolean` function at the bottom of the file, following the exemplar pattern of `migrateV24toV25` (which sits at line ~1747 — read it first to match style: idempotency guard via early `hasTable(db, 'episodic_index_error_fingerprint')` returning false, single `db.exec(...)` block with the DDL, return boolean signalling whether work was done).

   The DDL MUST be exactly (column order matters for the test):

   ```sql
   CREATE TABLE IF NOT EXISTS episodic_index_error_fingerprint (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     shingle_hash TEXT NOT NULL,
     episode_event_id INTEGER NOT NULL REFERENCES episodic_events(id),
     ts_epoch INTEGER NOT NULL,
     project TEXT NOT NULL,
     corpus_origin TEXT NOT NULL CHECK (corpus_origin IN ('phase1_organic','v4_backfill')),
     schema_version SMALLINT NOT NULL DEFAULT 1
   );
   CREATE INDEX IF NOT EXISTS idx_epev_efp_shingle      ON episodic_index_error_fingerprint(shingle_hash);
   CREATE INDEX IF NOT EXISTS idx_epev_efp_event        ON episodic_index_error_fingerprint(episode_event_id);
   CREATE INDEX IF NOT EXISTS idx_epev_efp_project_ts   ON episodic_index_error_fingerprint(project, ts_epoch);
   ```

   Function MUST be idempotent (CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS). Wrap in the same try-pattern V24→V25 uses; return `true` if the table was newly created and `false` if it already existed.

   **Reasoning anchored in CONTEXT.md:**
   - `(shingle_hash TEXT, episode_event_id INTEGER, ts_epoch INTEGER, project TEXT)` is the literal column set from item 6 — DO NOT rename or reorder.
   - Indexes `(shingle_hash)`, `(episode_event_id)`, `(project, ts_epoch)` are also from item 6 — DO NOT add or skip any.
   - `corpus_origin` enum is from item 2's known-limitation requirement — closed to two values; Phase 5 (a future empirical phase) may add more in a future migration if it adopts this pattern.

2. **`src/core/migrations.ts`** — add `migrateV25toV26` to the import block (alphabetical, after `migrateV24toV25` at line ~46), and add `[25, () => { migrateV25toV26(db); }]` to the step-table at the position immediately after the V25 step (line ~136). Match the existing entry style (arrow function wrapping the migrate call, ignoring the boolean return).

   Also: search for any standalone fall-through migration block (around line ~333-341 there's a sequential block that explicitly calls `migrateV23toV24(db); migrateV24toV25(db);` — if `migrateV25toV26(db)` belongs in that ordered list, append it there too. Match the existing pattern verbatim.

3. **DO NOT** bump `SCHEMA_VERSION` in `src/shared/constants.ts`. That constant is V2 of the legacy schema-versions row counter (currently 300 — unrelated to step-table V25/V26). Only the step-table registration in `migrations.ts` controls V26 application.

**Avoid:**
- Embedding non-DDL logic. The migration creates the table only; the backfill that populates it is Plan 02-03's domain.
- ALTER TABLE on `episodic_events` — explicitly forbidden by CONTEXT item 6 ("preserves Phase 1 contract").
- Adding readers anywhere — Plan 02-02 introduces the first writer; Plan 02-04 the first reader.
- Adding a vec0 / FTS5 mirror of this table — sidecar is plain SQLite, by design.
- Renaming or modifying any v4 table (artifacts, observations, learning, etc.) — out of scope for this plan and for Phase 2 entirely.
  </action>
  <verify>
1. `bun run build` succeeds with no TypeScript errors.
2. `node -e "const Database = require('better-sqlite3'); const { runMigrations } = require('./dist/core/migrations.cjs'); const db = new Database(':memory:'); runMigrations(db); const cols = db.pragma('table_info(episodic_index_error_fingerprint)'); console.log(JSON.stringify(cols.map(c => c.name)));"` prints exactly `["id","shingle_hash","episode_event_id","ts_epoch","project","corpus_origin","schema_version"]`.
3. Same script with `db.pragma('index_list(episodic_index_error_fingerprint)')` shows the three `idx_epev_efp_*` indexes (plus the auto-generated PK index).
4. Same script with `db.pragma('table_info(episodic_events)')` shows the V25 13-column shape unchanged (Phase 1 contract preserved).
  </verify>
  <done>The episodic_index_error_fingerprint table exists in any newly-opened or migrated DB with the exact column set, indexes, and CHECK constraint from CONTEXT item 6. Step-table registers V26. Phase 1's episodic_events table is byte-identical to its V25 shape.</done>
</task>

<task type="auto">
  <name>Task 2: V26 sidecar migration tests</name>
  <files>src/tests/adapters/episodic-events/error-fingerprint-migration.test.ts</files>
  <action>
Create the test file (mkdir -p the parent directory if not already present from Phase 1). Use the in-memory DB pattern from `src/tests/adapters/episodic-events/schema-migration.test.ts` (Phase 1 plan 01-01) as a reference — match its `import`, `beforeEach`, and assertion style.

The test file MUST cover these cases (one `describe` block, one `it` per case):

1. **Fresh DB has the sidecar table** — open `:memory:`, run migrations, assert `episodic_index_error_fingerprint` table exists with the exact 7 columns from Task 1.
2. **All three indexes present** — assert `index_list(episodic_index_error_fingerprint)` contains `idx_epev_efp_shingle`, `idx_epev_efp_event`, `idx_epev_efp_project_ts`.
3. **CHECK constraint rejects bad corpus_origin** — INSERT with `corpus_origin='phase2_synthetic'` (an invalid value) MUST throw a `SqliteError` matching `/CHECK/i`.
4. **CHECK constraint accepts each enum value** — INSERTs with `'phase1_organic'` and `'v4_backfill'` each succeed (after first writing a parent episodic_events row that the FK can reference).
5. **Idempotent re-migration** — call `runMigrations` twice on a fresh DB; second call MUST NOT throw and MUST NOT duplicate DDL.
6. **episodic_events V25 shape preserved** — after V26 migration, `pragma('table_info(episodic_events)')` returns the exact 13-column V25 shape from Phase 1 plan 01-01 (no columns added, none removed).
7. **schema_version default = 1** — INSERT a sidecar row without specifying `schema_version`, assert the resulting row's `schema_version === 1`.
8. **shingle_hash + episode_event_id are NOT NULL** — INSERT with NULL `shingle_hash` MUST throw NOT NULL; same for `episode_event_id`.
9. **FK references episodic_events** — verify the FK declaration via `pragma('foreign_key_list(episodic_index_error_fingerprint)')` shows a row referencing `episodic_events(id)`. (Don't assert FK enforcement — this codebase runs with FKs off by default; matching the Phase 1 test style.)

Use `import Database from 'better-sqlite3'` and `import { runMigrations } from '../../../core/migrations.js'`.

**Avoid:**
- Testing fingerprint algorithm correctness — that's Plan 02-02's domain.
- Testing backfill — Plan 02-03's domain.
- Testing sidecar query patterns — Plan 02-04's domain.
- Mocking — use the real DB, real migrations.
  </action>
  <verify>
1. `bun run test src/tests/adapters/episodic-events/error-fingerprint-migration.test.ts` — all 9 tests pass.
2. `bun run test` — full suite passes (no regressions in Phase 1 schema-migration.test.ts, dual-write tests, or any existing test).
  </verify>
  <done>9 passing tests in error-fingerprint-migration.test.ts; full suite green; Phase 1's V25 contract is observably preserved by an explicit assertion.</done>
</task>

</tasks>

<verification>
- `bun run build` clean.
- `bun run test` full suite green.
- `bun run test src/tests/adapters/episodic-events/error-fingerprint-migration.test.ts` — 9/9 pass.
- Manual: `node -e "..."` confirms the sidecar table DDL on a freshly-migrated DB and the V25 episodic_events shape is unchanged.
</verification>

<success_criteria>
- [ ] `episodic_index_error_fingerprint` sidecar table exists in V26 schema with the exact 7 columns and 3 indexes from CONTEXT item 6.
- [ ] CHECK constraint enforces the closed `corpus_origin` enum (`'phase1_organic' | 'v4_backfill'`).
- [ ] Migration is idempotent — re-running runMigrations on a V26 DB is a no-op.
- [ ] Phase 1's `episodic_events` table is byte-identical to V25 (no ALTER TABLE).
- [ ] No new readers introduced; no embeddings, no FTS, no vec0; sidecar is plain SQLite.
- [ ] 9 unit tests pass; full vitest suite passes.
- [ ] IDX-01 (sidecar storage layer of error-fingerprint index) addressed in part — Plan 02-02 layers the fingerprinter on top.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-modal-index-seeds-density-check/02-01-SUMMARY.md` documenting:
- Final DDL committed (paste from migration-steps.ts).
- The V25 → V26 transition pattern, in case Phase 3 builds more sidecars (it likely will, per CONTEXT.md specifics).
- Any deviations from the spec and why.
</output>
</content>
</invoke>