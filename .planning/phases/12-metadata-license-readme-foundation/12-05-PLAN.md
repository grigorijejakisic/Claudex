---
phase: 12-metadata-license-readme-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-03
  - 12-04
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
must_haves:
  truths:
    - "README.md at repo root is rewritten — old v3-era framing replaced"
    - "The What+Why sections combined are under 500 words (DOC-01 hard cap)"
    - "What is Claudex section explains what Claudex is and who it's for in plain English (no insider jargon: no Vesna, no SC#1-#4, no V15, no FTS5/sqlite-vec, no advisory voice — those terms are fine in the deeper docs but not above the fold)"
    - "Why Claudex section conveys the v4 thesis: organic memory tool use, not injected imperatives"
    - "Why Claudex section includes the verbatim canonical shadowban example from PROJECT.md (DOC-02 specific requirement)"
    - "README does NOT cite stale v3-era numbers (no '550+ sessions, 30,000+ observations', no 'Persistent memory, incremental checkpointing, and context-aware intelligence' framing)"
    - "README does NOT include a Quick Start with install instructions (Phase 16 territory) — instead points to CHANGELOG / 'coming soon' for installation"
    - "README does NOT include a Troubleshooting section (Phase 16 territory)"
    - "README does NOT reintroduce benchmarks-as-gates language"
    - "README footer links to LICENSE, CHANGELOG.md, CONTRIBUTING.md (which exist at repo root after waves 1+2 complete)"
    - "No emoji unless explicitly authorized"
    - "Tone is direct, technical, no marketing fluff — matches the user's voice in PROJECT.md"
  artifacts:
    - path: README.md
      provides: "Repo-root README — the entry point for human readers landing on github.com/grigorijejakisic/Claudex"
      contains: "Why Claudex"
  key_links:
    - from: README.md
      to: LICENSE
      via: "License footer links to the MIT LICENSE file at repo root"
      pattern: "LICENSE"
    - from: README.md
      to: CHANGELOG.md
      via: "Links section points to CHANGELOG for release history"
      pattern: "CHANGELOG.md"
    - from: README.md
      to: CONTRIBUTING.md
      via: "Links section points to CONTRIBUTING for contributors"
      pattern: "CONTRIBUTING.md"
---

<objective>
Replace the existing v3-era `README.md` with a v4.1-aligned rewrite that explains what Claudex is, who it's for, and why it matters in plain English under 500 words for the What+Why sections combined. The Why section must include the verbatim canonical shadowban example from PROJECT.md.

This is the most consequential single artifact in Phase 12: it is the first thing strangers read on `github.com/grigorijejakisic/Claudex`. The 60-second axiom from CONTEXT.md (line 5) is the success criterion: "Strangers landing on the repo should — within 60 seconds of reading — understand what Claudex is, who it's for, why it matters, and that it's MIT-licensed and documented enough to attempt installation."

CONTEXT.md `<decisions>` lines 59-72 lock the structure, length target, tone, the verbatim shadowban quote, and what to avoid. This plan is in wave 2 because LICENSE, CHANGELOG.md, and CONTRIBUTING.md must exist at repo root before the README can link to them with confidence.
</objective>

<execution_context>
**Current README state (verified, ~210 lines):** Opens with v3-era framing — "Persistent memory that makes LLM agents actually remember", "After 550+ sessions and 30,000+ observations in production use", architecture diagram, etc. Will be REPLACED, not patched.

**The verbatim shadowban example to include in DOC-02 (from PROJECT.md core value, lines 11-13):**

> v4 makes the agent USE Claudex organically as part of how it works in Claude Code. Memory tools (`claudex_search`, `claudex_recall`, `claudex_events`) get reached for the same way `Read` or `Grep` are used — natural extensions of reasoning, not a separate "fetch context" step that has to be remembered. Memory stops acting like rules; the agent stops following injected imperatives and starts using Claudex as a tool of working thought.
>
> **Canonical example (user-articulated 2026-04-27):** if last session we discovered *"60 HTTP polls to backend X = 15-min IP shadowban"*, and this session user says *"investigate another backend for intel gathering,"* the agent should automatically (1) recognize this is rate-limit-research-shaped work, (2) recall the shadowban finding, (3) apply it to scoping — all without being told to query memory.

The shadowban example must appear in the Why section verbatim — italicized phrasing preserved, the three-step enumeration preserved, the "all without being told to query memory" punchline preserved.

**What to AVOID (CONTEXT.md line 72):**
- Vague claims: "revolutionary", "AI-powered", "blazing-fast", "cutting-edge"
- Feature lists without context (no bullet-list of capabilities — show, don't list)
- Install instructions that don't yet work (Phase 16 territory)
- Comparisons to other systems (Mem0, Letta) — separate doc later if desired
- Benchmark-numbers-as-gates language (LongMemEval / LoCoMo numbers belong in CHANGELOG, not README)
- Insider jargon above the fold: Vesna, SC#1-#4, V15 schema, FTS5, sqlite-vec, RRF fusion, BGE-reranker, advisory voice, hybrid retrieval — fine in deeper docs, banned in the front matter
- Emoji (default no per CONTEXT.md line 36)

**Length target (CONTEXT.md line 60):** 500 words for the "what is Claudex" + "why Claudex" sections COMBINED. The total file longer is fine — links, license footer, etc. — but the front-matter (above the fold) must be tight. Treat 500 as a hard ceiling for those two sections, not a target.

**Structure (CONTEXT.md lines 61-69):**
1. Project title (just text — no logo yet)
2. One-line tagline
3. Status badges placeholders — SKIP for v4.1 (CI is on private repo currently; badges populate in Phase 17 once public CI is green; CONTEXT.md line 140 explicitly authorizes skipping)
4. **What is Claudex?** (~150 words, plain English)
5. **Why Claudex?** (~250 words, with the verbatim shadowban example)
6. Quick Start placeholder linking to "Coming soon" / Phase 16 work
7. Links section: CHANGELOG.md, CONTRIBUTING.md, .claude/rules/ for architectural depth
8. License footer: "Claudex is MIT-licensed. See [LICENSE](./LICENSE)."

**Tone (CONTEXT.md line 70):** Direct, technical-literate-but-not-condescending. Match the user's voice in PROJECT.md. Read PROJECT.md before drafting to absorb the voice — clipped sentences, declarative claims with concrete grounding, no hedging.

**Combined-word-count budget plan:**
- "What is Claudex?" — 150 words
- "Why Claudex?" — 250 words
- Buffer — 100 words
- Total — 500 words HARD CAP (DOC-01)
</execution_context>

<tasks>

<task id="12-05-01">
**Subject:** Read PROJECT.md to absorb the user's voice and confirm shadowban quote

**Action:**
Read `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\PROJECT.md` end-to-end (it's the canonical voice reference). Pay specific attention to:
- The core-value paragraph (lines 11-13) — confirm the verbatim shadowban example
- The "Current Milestone: v4.1 — Distribution" section — to align what the README signals about installability with what the milestone actually delivers
- The clipped, declarative sentence rhythm

**Verification:**
- The writing agent has the shadowban quote correct verbatim (3-step enumeration, italicized phrases, "all without being told to query memory" closer)
- The writing agent can match the PROJECT.md sentence rhythm

**Commit:** None (read-only step)
</task>

<task id="12-05-02">
**Subject:** Write the new README.md at repo root, replacing v3-era content

**Action:**
Write `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\README.md` with the structure below. Replace the existing content entirely (do NOT patch — full rewrite).

The What and Why sections combined MUST be under 500 words. Count them after drafting (e.g. `wc -w` on the extracted section text). If over, cut — do not paraphrase the shadowban example to fit.

```markdown
# Claudex

Persistent memory for LLM coding agents — they reach for it the way they reach for Grep.

> **Status:** v4.0 shipped (internal infrastructure). v4.1 — Distribution — is in flight to make Claudex installable by strangers. Until v4.1 lands, install paths are not yet stable; track [CHANGELOG.md](./CHANGELOG.md) for ship dates.

## What is Claudex?

[~150 words explaining what Claudex is and who it's for. Plain English. No
insider jargon. Address the reader as a coding-agent operator who already
knows what Claude Code is.

The shape of the message: "Claudex is a persistent memory system that runs
locally alongside Claude Code. It captures what happens — observations,
decisions, files edited, problems hit — and surfaces the relevant pieces
on the next session, automatically. One SQLite database, one local vector
index, no cloud service. It's for people running long coding sessions
across days or weeks who want the agent to actually remember context
between resets, without manually re-priming each session."

Avoid: feature lists, architecture diagrams, schema details, version
numbers. The point of this section is "what it is and who it's for" —
not how it works.]

## Why Claudex?

[~250 words conveying the v4 thesis: memory tool use should be organic,
not imperative.

Open with: existing memory systems make the agent FOLLOW INSTRUCTIONS to
query memory ("remember to check past decisions before answering"). That
fails because instructions decay — the agent stops querying when the
prompt window stops nagging. v4's bet is that memory tools should be
treated like file-reading tools: reached for as natural extensions of
reasoning when the work shape calls for them.

Then the verbatim shadowban example, formatted as a clear scenario:

  v4 makes the agent USE Claudex organically as part of how it works in
  Claude Code. Memory tools (`claudex_search`, `claudex_recall`,
  `claudex_events`) get reached for the same way `Read` or `Grep` are used
  — natural extensions of reasoning, not a separate "fetch context" step
  that has to be remembered.

  **Canonical example:** if last session we discovered *"60 HTTP polls to
  backend X = 15-min IP shadowban"*, and this session the user says
  *"investigate another backend for intel gathering,"* the agent should
  automatically (1) recognize this is rate-limit-research-shaped work,
  (2) recall the shadowban finding, (3) apply it to scoping — all without
  being told to query memory.

Close with one line: this is the difference v4 measures. Behavioral probes
(SC#1) verify the recall-and-apply pattern landed. The audit history
behind that bet is in [CHANGELOG.md](./CHANGELOG.md).]

## Installation

Coming in v4.1. Until then, this repository is internal infrastructure —
the install path is not yet stable for strangers. Track [CHANGELOG.md](./CHANGELOG.md)
for the v4.1 ship.

## Documentation

- [CHANGELOG.md](./CHANGELOG.md) — release history starting at v4.0.0
- [CONTRIBUTING.md](./CONTRIBUTING.md) — development setup, test commands, commit convention, hook safety
- [`.claude/rules/`](./.claude/rules/) — architectural rules loaded conditionally during work
- [`.planning/PROJECT.md`](./.planning/PROJECT.md) — project intent and current milestone

## License

Claudex is MIT-licensed. See [LICENSE](./LICENSE).

Copyright (c) 2026 Grigorije Jakisic.
```

Replace each `[...]` placeholder block with prose that fits the budget and matches the PROJECT.md voice. Preserve the verbatim shadowban quote — do not rephrase the parenthetical (1)/(2)/(3) enumeration, do not change "all without being told to query memory" to anything else.

After drafting, count the words in just the "What is Claudex?" + "Why Claudex?" sections (header to next header, body only). If combined > 500, cut from the What section first (the Why section's shadowban quote is locked).

**Verification:**
- `README.md` exists at repo root and is fully replaced (not just patched)
- `head -1 README.md` returns `# Claudex`
- `grep -c "What is Claudex?" README.md` returns `1`
- `grep -c "Why Claudex?" README.md` returns `1`
- The verbatim shadowban quote is present:
  - `grep -c "60 HTTP polls to backend X" README.md` returns at least `1`
  - `grep -c "15-min IP shadowban" README.md` returns at least `1`
  - `grep -c "all without being told to query memory" README.md` returns at least `1`
- Combined word count of the What + Why sections is < 500 words. Verify with:
  ```bash
  awk '/^## What is Claudex/,/^## Why Claudex/' README.md > /tmp/what.txt
  awk '/^## Why Claudex/,/^## Installation/' README.md > /tmp/why.txt
  WORDS=$(cat /tmp/what.txt /tmp/why.txt | wc -w)
  test "$WORDS" -lt 500 || { echo "OVER WORD CAP: $WORDS words"; exit 1; }
  ```
- v3-era language removed:
  - `grep -c "550+ sessions\|30,000+ observations\|incremental checkpointing\|context-aware intelligence" README.md` returns `0`
- Insider jargon NOT present above the Installation section:
  - `awk '/^# Claudex/,/^## Installation/' README.md | grep -ic "Vesna\|SC#1\|SC#2\|SC#3\|SC#4\|V15\|FTS5\|sqlite-vec\|RRF\|BGE\|advisory voice\|hybrid retrieval"` returns `0` (one accepted exception: SC#1 may appear in the closing line of "Why Claudex" if framed as the behavioral probe that verifies the v4 thesis — judgment call; otherwise zero)
- No marketing fluff:
  - `grep -i "revolutionary\|blazing\|cutting-edge\|state-of-the-art\|game-chang\|next-gen" README.md` returns `0`
- No benchmarks-as-gates language:
  - `grep -i "benchmark.*gate\|gates.*benchmark\|LongMemEval\|LoCoMo" README.md` returns `0`
- No emoji
- Footer links resolve to existing files: LICENSE, CHANGELOG.md, CONTRIBUTING.md all exist at repo root (created in 12-01, 12-03, 12-04 respectively)
- `bun run build` and `bun run test` unaffected (no code change)

**Commit:**
`phase(12-05): DOC-01,DOC-02 — README rewrite (What+Why under 500 words, verbatim shadowban example)`
</task>

</tasks>

<verification>
After this plan completes:
- `README.md` is rewritten end-to-end (DOC-01, DOC-02 ✓)
- What+Why sections combined are < 500 words
- The verbatim shadowban example appears in Why section, including the (1)/(2)/(3) enumeration and the "all without being told to query memory" closer
- No v3-era claims, no benchmark-as-gate language, no marketing fluff, no emoji
- Quick Start and Troubleshooting are NOT in this README (deferred to Phase 16 per CONTEXT.md)
- Footer links to LICENSE / CHANGELOG.md / CONTRIBUTING.md all resolve
- `bun run build` and `bun run test` unaffected
</verification>
