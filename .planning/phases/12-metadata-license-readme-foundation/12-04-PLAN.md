---
phase: 12-metadata-license-readme-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
autonomous: true
requirements:
  - DOC-06
must_haves:
  truths:
    - "CONTRIBUTING.md exists at repo root as a separate file (not a README section)"
    - "CONTRIBUTING.md covers development setup: clone, bun install, bun run build"
    - "CONTRIBUTING.md explicitly warns that bun run test is the correct test command and bun test (without 'run') invokes Bun's native runner and produces confusing failures"
    - "CONTRIBUTING.md describes the commit convention: Conventional Commits (feat:, fix:, docs:, chore:, test:, phase(NN):); atomic per task"
    - "CONTRIBUTING.md describes the PR workflow including the SC#1 Vesna gate and SC#2/SC#3 cache+content quality gates"
    - "CONTRIBUTING.md states the hook safety rule: never call CC's CLIProxyAPI from a hook (deadlock)"
    - "CONTRIBUTING.md points contributors to .claude/rules/ for deeper architectural context"
  artifacts:
    - path: CONTRIBUTING.md
      provides: "Contributor onboarding guide — dev setup, test commands, commit convention, PR gates, safety rules"
      contains: "bun run test"
  key_links:
    - from: CONTRIBUTING.md
      to: .claude/rules/
      via: "CONTRIBUTING.md references the rules directory as the architectural deep-dive surface for contributors"
      pattern: ".claude/rules/"
    - from: CONTRIBUTING.md
      to: .github/workflows/vesna.yml
      via: "PR workflow section references the Vesna CI workflow as the SC#1 merge gate"
      pattern: "vesna.yml"
---

<objective>
Ship `CONTRIBUTING.md` at the repo root as a separate file (not a README section) covering dev setup, the `bun run test` vs. `bun test` warning, commit convention, PR workflow, and the hook safety rule.

CONTEXT.md `<decisions>` lines 87-95 lock: separate file (not README section — GitHub auto-surfaces it as a top-level community standard), and the coverage list above. The `bun run test` warning is load-bearing: contributors who mistakenly run `bun test` will hit Bun's native runner producing test discovery failures that look like real bugs. The line in CONTRIBUTING.md is the cheapest possible cure.
</objective>

<execution_context>
The repo currently has no `CONTRIBUTING.md`. GitHub's community standards surface this file automatically: it appears as a banner on PRs ("First time contributing? Read our CONTRIBUTING guide") and on the Insights → Community Standards page. Convention is repo root, plain markdown, no frontmatter.

Project-specific testing-tooling fact (from project CLAUDE.md):
> Do NOT use `bun test` — invokes Bun's native runner, not Vitest.

This is non-obvious; Vitest is the test runner; package.json's `"test": "vitest run"` script means `bun run test` works correctly. But `bun test` is a Bun-native command that bypasses npm scripts and runs Bun's own test runner against `*.test.ts` files using a different convention. Contributors will hit this. CONTRIBUTING must explicitly warn.

The `.claude/rules/` directory contains the deeper architectural rules (loaded conditionally during work via Claude Code's CLAUDE.md system). Pointing contributors there avoids cluttering CONTRIBUTING.md with full architectural documentation while still giving them a pointer.

Hook safety rule (from project CLAUDE.md):
> Hook deadlock: Never call CC's CLIProxyAPI from a hook — use Ollama instead.
> Fire-and-forget: Always await in hooks. Only Angel/OpenClaw can fire-and-forget.

Both are non-obvious gotchas that have caused real incidents. Both belong in CONTRIBUTING for contributors who touch hook code.

CI gate references:
- `.github/workflows/vesna.yml` runs SC#1 (Vesna behavioral probe suite) on PRs. Verify this file exists before referencing it; if it does not exist yet, frame the gate as "Vesna probe suite (SC#1) — `bun run vesna` must pass" with a note that CI integration is in progress (Phase 17 territory).
- `bun run test` runs the vitest suite (3000+ tests).
- `bun run sc3` runs the MEMORY.md content-quality scorer (SC#3).
- `bun run vesna` runs the Vesna probe suite locally (SC#1).

Tone: same as PROJECT.md — direct, technical, no marketing fluff, no emoji. Address contributors as people who already know git and TypeScript — don't explain `git clone`, do explain claudex-specific gotchas.
</execution_context>

<tasks>

<task id="12-04-01">
**Subject:** Verify CI workflow files exist for accurate referencing

**Action:**
Check whether `.github/workflows/vesna.yml` (or any vesna-named workflow) exists at the repo root. If yes, reference it by name in the PR workflow section. If no, frame the gate as a local-run requirement (`bun run vesna` must pass before merge) and note that CI integration is Phase 17 territory.

```bash
ls .github/workflows/ 2>/dev/null || echo "no workflows directory"
```

**Verification:**
- The writing agent knows whether `.github/workflows/vesna.yml` exists and adjusts CONTRIBUTING.md text accordingly

**Commit:** None (read-only step)
</task>

<task id="12-04-02">
**Subject:** Write CONTRIBUTING.md at repo root

**Action:**
Write `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\CONTRIBUTING.md` with the following structure (adjust the CI section based on task 12-04-01 finding):

```markdown
# Contributing to Claudex

Thanks for considering a contribution. Claudex is a small, opinionated codebase
— the contribution surface is intentionally narrow. This guide covers the
mechanics; deeper architectural context lives in [`.claude/rules/`](./.claude/rules/)
which Claude Code loads conditionally when editing relevant paths.

## Development setup

```bash
git clone https://github.com/grigorijejakisic/Claudex.git
cd Claudex
bun install
bun run build
```

Requirements:
- **Bun** ≥ 1.3 (the runtime — `bun --version` to confirm)
- An Ollama instance running locally with the `snowflake-arctic-embed2` model
  pulled (used for the bi-encoder fallback path)
- A Claude Code installation if you intend to test hook integration end-to-end

The build is esbuild-based (~70ms) and writes to `dist/`. Hooks register via
`bun run setup` once the build is in place.

## Running tests

**Always use `bun run test`. Never use `bun test`.**

```bash
bun run test           # vitest, 3000+ tests
```

`bun test` (without `run`) invokes Bun's native test runner, which is a
different runner with different conventions. It will discover the wrong files,
report confusing failures, and is not the test gate. The `run` keyword
forwards the command to the npm-style script (`"test": "vitest run"`),
which is what runs in CI.

Other useful commands:

```bash
bun run vesna         # SC#1 — Vesna behavioral probe suite (the merge gate)
bun run sc3           # SC#3 — MEMORY.md content-quality scorer
bun run health        # local health check
```

## Commit convention

Atomic commits using [Conventional Commits](https://www.conventionalcommits.org/):

- `feat: ...` — new capability
- `fix: ...` — bug fix
- `docs: ...` — documentation only
- `chore: ...` — tooling, dependencies, repo plumbing
- `test: ...` — test-only changes
- `refactor: ...` — internal restructuring without behavior change
- `phase(NN-MM): ...` — work tied to a specific GSD phase plan (e.g. `phase(12-03): DOC-05 ...`)

One commit per task; commit messages explain the *why*, not the *what* (the
diff already shows what). When a commit is co-authored, end with a
`Co-Authored-By:` trailer.

## Pull request workflow

Before opening a PR:
1. `bun run build` — must succeed
2. `bun run test` — full vitest suite must pass
3. `bun run vesna` — SC#1 behavioral probe suite must PASS (this is the
   merge gate; do not ship a PR that regresses Vesna)
4. `bun run sc3` — SC#3 MEMORY.md content quality must remain ≥80% per
   project (this catches regressions in the curation pipeline)

[CI section: if `.github/workflows/vesna.yml` exists, write:
"CI runs the Vesna probe suite via `.github/workflows/vesna.yml` on every
PR. The PR cannot merge until Vesna is green."
If it does not exist, write:
"CI integration for the Vesna gate is Phase 17 territory; in the meantime
the local `bun run vesna` run is the gate of record."]

PRs should reference the requirement ID(s) they close (e.g. `Closes LIC-02`)
and link to the GSD phase directory under `.planning/phases/` if the work is
phase-scoped.

## Hook safety

Two non-obvious rules when editing hook code in `src/adapters/cc-hooks/`:

1. **Never call Claude Code's CLIProxyAPI from inside a hook.** Hooks run
   inside the Claude Code process; calling back into its API from a hook
   deadlocks the host. Use Ollama directly when a hook needs an LLM call.
2. **Always `await` in hooks.** Hooks are ephemeral processes — a
   fire-and-forget Promise can be killed mid-flight when the hook process
   exits. Only the long-running components (Angel, OpenClaw bridge) can
   fire-and-forget.

## Architectural context

`.claude/rules/` contains the deeper rule files Claude Code loads
conditionally when editing relevant paths. Read these before making
non-trivial changes to:

- The schema or migrations (`src/storage/schema/`, `src/storage/migrations/`)
- Hook payloads (`src/adapters/cc-hooks/`)
- Angel responsibilities (`src/angel/`)
- The retrieval pipeline (`src/retrieval/`)

The project-level `CLAUDE.md` at the repo root summarizes the runtime
components, schema version, and critical safety rules.

## Issues

Bugs, design questions, and feature requests:
[github.com/grigorijejakisic/Claudex/issues](https://github.com/grigorijejakisic/Claudex/issues)
```

If task 12-04-01 found that `.github/workflows/vesna.yml` exists, replace the bracketed CI section with the first variant. If not, use the second variant.

**Verification:**
- `CONTRIBUTING.md` exists at repo root
- `head -1 CONTRIBUTING.md` returns `# Contributing to Claudex`
- `grep -c "bun run test" CONTRIBUTING.md` returns at least `1`
- `grep -c "bun test" CONTRIBUTING.md` returns at least `1` (the warning)
- `grep -c "Conventional Commits\|conventionalcommits" CONTRIBUTING.md` returns at least `1`
- `grep -c "phase(NN-MM)\|phase(NN" CONTRIBUTING.md` returns at least `1`
- `grep -c "CLIProxyAPI\|deadlock" CONTRIBUTING.md` returns at least `1`
- `grep -c ".claude/rules" CONTRIBUTING.md` returns at least `1`
- `grep -i "revolutionary\|blazing\|powerful\|cutting-edge" CONTRIBUTING.md` returns 0 matches
- No emoji in the file

**Commit:**
`phase(12-04): DOC-06 — CONTRIBUTING.md with dev setup, bun run test warning, commit convention, hook safety`
</task>

</tasks>

<verification>
After this plan completes:
- `CONTRIBUTING.md` exists at repo root as a separate file (DOC-06 ✓)
- Dev setup section covers clone / bun install / bun run build
- `bun run test` vs `bun test` warning is present and load-bearing
- Conventional Commits convention documented including `phase(NN-MM):` pattern
- PR workflow section names SC#1 (Vesna) as the merge gate, SC#3 as content-quality gate
- Hook safety rule (no CLIProxyAPI from hook; always await) documented
- `.claude/rules/` referenced as the deeper architectural surface
- `bun run build` and `bun run test` unaffected (no code change)
</verification>
