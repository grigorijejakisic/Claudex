---
phase: 01-episode-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/schema.ts
  - src/core/migration-steps.ts
  - src/core/migrations.ts
  - src/shared/constants.ts
  - src/tests/adapters/episodic-events/schema-migration.test.ts
autonomous: true
requirements: [EPI-01, EPI-02, EPI-06]
must_haves:
  truths:
    - "Running migrations on a V24 DB produces a V25 DB with an episodic_events table"
    - "Running migrations on a fresh :memory: DB produces a V25 DB with an episodic_events table on first open"
    - "Inserting a provenance value outside the closed enum is rejected by the CHECK constraint"
    - "Existing conversation_turns rows are unmodified by the V25 migration"
    - "Re-running migrations on an already-V25 DB is a no-op (idempotent)"
  artifacts:
    - path: "src/core/migration-steps.ts"
      provides: "migrateV24toV25 function — DDL for episodic_events"
      contains: "export function migrateV24toV25"
    - path: "src/core/migrations.ts"
      provides: "Step-table registration of V25"
      contains: "migrateV24toV25"
    - path: "src/shared/constants.ts"
      provides: "SCHEMA_VERSION bumped to 25"
      contains: "SCHEMA_VERSION = 25"
    - path: "src/tests/adapters/episodic-events/schema-migration.test.ts"
      provides: "Migration shape + idempotency + CHECK constraint tests"
      min_lines: 60
  key_links:
    - from: "src/core/migrations.ts"
      to: "src/core/migration-steps.ts"
      via: "import { migrateV24toV25 } and step-table entry [24, () => migrateV24toV25(db)]"
      pattern: "migrateV24toV25"
    - from: "src/core/migrations.ts"
      to: "src/shared/constants.ts"
      via: "SCHEMA_VERSION compared to highest applied step"
      pattern: "SCHEMA_VERSION"
---

<objective>
Land the V25 schema migration that creates the `episodic_events` table — the substrate Phases 2-7 build on. Includes the DDL with locked column set, the closed `provenance` CHECK constraint, the five required indexes, registration in the migration runner, the SCHEMA_VERSION bump, and unit tests proving the migration is correct + idempotent + enforces the enum.

Purpose: Without the table, no write path can land. This plan is the foundation; Plans 02 and 03 cannot start until it ships.
Output: A V25 DB schema, a passing test file, no behavioral change to existing readers.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-episode-substrate/01-CONTEXT.md
@.planning/phases/01-episode-substrate/01-RESEARCH.md
@src/core/schema.ts
@src/core/migration-steps.ts
@src/core/migrations.ts
@src/shared/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add migrateV24toV25 + register in runner + bump SCHEMA_VERSION</name>
  <files>src/core/migration-steps.ts, src/core/migrations.ts, src/shared/constants.ts, src/core/schema.ts</files>
  <action>
1. **`src/core/migration-steps.ts`** — append a new `migrateV24toV25(db: Database): void` function at the bottom of the file, following the exemplar pattern of the existing `migrateV23toV24` (read it first to match style — idempotency guard via `hasTable(db, 'episodic_events')`, single `db.exec(...)` block with the DDL).

   The DDL MUST be exactly (column order matters for the test):

   ```sql
   CREATE TABLE IF NOT EXISTS episodic_events (
     id INTEGER PRIMARY KEY AUTOINCREMENT,
     session_id TEXT NOT NULL,
     project TEXT NOT NULL,
     ts_epoch INTEGER NOT NULL DEFAULT (unixepoch()),
     turn_number INTEGER,
     type TEXT NOT NULL,
     source TEXT NOT NULL,
     content TEXT NOT NULL,
     provenance TEXT NOT NULL CHECK (provenance IN ('organic','injected','tool_result','environmental')),
     parent_event_id INTEGER REFERENCES episodic_events(id),
     content_hash TEXT NOT NULL,
     metadata_json TEXT,
     schema_version SMALLINT NOT NULL DEFAULT 1
   );
   CREATE INDEX IF NOT EXISTS idx_epev_session_turn_ts ON episodic_events(session_id, turn_number, ts_epoch);
   CREATE INDEX IF NOT EXISTS idx_epev_project_ts     ON episodic_events(project, ts_epoch);
   CREATE INDEX IF NOT EXISTS idx_epev_provenance     ON episodic_events(provenance);
   CREATE INDEX IF NOT EXISTS idx_epev_parent         ON episodic_events(parent_event_id);
   ```

   Function MUST be idempotent (CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS) and non-throwing — wrap any potentially-failing operation but the DDL itself is naturally idempotent.

2. **`src/core/migrations.ts`** — add `migrateV24toV25` to the import block (alphabetical adjacent to `migrateV23toV24`), and add `[24, () => migrateV24toV25(db)]` as a new entry in the step-table at the position after the V24 step. Match the existing entry style.

3. **`src/shared/constants.ts`** — bump `SCHEMA_VERSION` from `24` to `25`. (If the file uses `as const`, preserve it.)

4. **`src/core/schema.ts`** — OPTIONAL: if the file's existing `SCHEMA_V22` etc. constants are used elsewhere as the canonical DDL strings, add an exported `SCHEMA_V25` constant containing the same DDL. If migration-steps.ts inlines the DDL directly (current pattern for many late-version steps), skip this. Choose based on what the existing V24 step does — be consistent.

**Avoid:**
- Embedding any non-DDL logic (no INSERTs, no data migration — Phase 1 is forward-only by mandate).
- Adding readers anywhere — Phase 1's contract is "pure write substrate, no reader."
- Renaming or modifying `conversation_turns` — that table stays intact.
- Adding embeddings, vec0 virtual tables, or FTS5 mirrors — Phase 2/3 own indexing.
  </action>
  <verify>
1. `bun run build` succeeds with no TypeScript errors.
2. `node -e "const Database = require('better-sqlite3'); const { runMigrations } = require('./dist/core/migrations.cjs'); const db = new Database(':memory:'); runMigrations(db); const cols = db.pragma('table_info(episodic_events)'); console.log(JSON.stringify(cols.map(c => c.name)));"` prints exactly `["id","session_id","project","ts_epoch","turn_number","type","source","content","provenance","parent_event_id","content_hash","metadata_json","schema_version"]`.
3. Same script with `db.pragma('index_list(episodic_events)')` shows the four `idx_epev_*` indexes.
  </verify>
  <done>The episodic_events table exists in any newly-opened or migrated DB with the exact column set, indexes, and CHECK constraint specified. SCHEMA_VERSION constant equals 25.</done>
</task>

<task type="auto">
  <name>Task 2: Schema migration tests</name>
  <files>src/tests/adapters/episodic-events/schema-migration.test.ts</files>
  <action>
Create the test file (mkdir -p the parent directory). Use the in-memory DB pattern from `src/tests/angel/heartbeat.test.ts` as a reference.

The test file MUST cover these cases (one `describe` block, one `it` per case):

1. **Fresh DB has the table** — open `:memory:`, run migrations, assert `episodic_events` table exists with the exact 13 columns from Task 1.
2. **All four indexes present** — assert `index_list(episodic_events)` contains `idx_epev_session_turn_ts`, `idx_epev_project_ts`, `idx_epev_provenance`, `idx_epev_parent`.
3. **CHECK constraint rejects bad provenance** — `INSERT ... VALUES (..., 'organis', ...)` (typo) MUST throw a `SqliteError` with `code === 'SQLITE_CONSTRAINT_CHECK'` (or message containing 'CHECK constraint failed'). Use `expect(() => stmt.run(...)).toThrow(/CHECK/i)`.
4. **CHECK constraint accepts each of the four enum values** — INSERTs with `'organic'`, `'injected'`, `'tool_result'`, `'environmental'` each succeed.
5. **Idempotent re-migration** — call `runMigrations` twice; second call MUST NOT throw and MUST NOT duplicate rows or DDL.
6. **Legacy table preserved** — INSERT a row into `conversation_turns` BEFORE running V25 migration on a V24-state DB (simulate by running migrations once on a fresh DB then running again), assert the row is still there afterward, and assert `conversation_turns` schema is unchanged (column count + names match V10 baseline: `id, session_id, project, turn_number, user_text, assistant_text, timestamp_epoch, embedding`).
7. **schema_version default = 1** — INSERT a row without specifying `schema_version`, assert the resulting row's `schema_version === 1`.
8. **parent_event_id is nullable and self-referencing** — INSERT a row with `parent_event_id = NULL` (succeeds), then INSERT a child row with `parent_event_id = <first row's id>` (succeeds).
9. **content_hash is required** — INSERT without content_hash MUST throw NOT NULL constraint error.

Use `import Database from 'better-sqlite3'`, `import { runMigrations } from '../../../core/migrations.js'`, and a `beforeEach` that creates a fresh `:memory:` DB.

**Avoid:**
- Testing write helpers — those are Plan 02/03's domain.
- Testing parent_event_id FK enforcement (SQLite FKs are off by default in this codebase; not a Phase 1 concern).
- Mocking — use the real DB, real migrations.
  </action>
  <verify>
1. `bun run test src/tests/adapters/episodic-events/schema-migration.test.ts` — all 9 tests pass.
2. `bun run test` — full suite passes (no regressions in any existing test from the SCHEMA_VERSION bump or the new migration step).
  </verify>
  <done>9 passing tests in schema-migration.test.ts; full suite green.</done>
</task>

</tasks>

<verification>
- `bun run build` clean.
- `bun run test` full suite green.
- `bun run test src/tests/adapters/episodic-events/schema-migration.test.ts` — 9/9 pass.
- Manual: `node -e "...select sql from sqlite_master where name='episodic_events'..."` shows the expected DDL on a freshly-migrated DB.
</verification>

<success_criteria>
- [ ] `episodic_events` table exists in V25 schema with the exact 13 columns and 4 indexes from CONTEXT.md.
- [ ] CHECK constraint enforces the closed `provenance` enum.
- [ ] Migration is idempotent — re-running runMigrations on a V25 DB is a no-op.
- [ ] Legacy `conversation_turns` is unmodified.
- [ ] No new readers introduced; no embeddings, no FTS, no vec0.
- [ ] 9 unit tests pass; full vitest suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-episode-substrate/01-01-SUMMARY.md` documenting:
- Final DDL committed (paste from migration-steps.ts).
- The V24 → V25 transition pattern, in case Phase 2/3 need a similar migration.
- Any deviations from the spec and why.
</output>
