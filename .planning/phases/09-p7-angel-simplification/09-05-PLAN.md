---
plan: 09-05
phase: 9
sub_phase: 9.5
title: Delete cross-project-consolidator.ts (sub-phase 9.5)
wave: 5
depends_on: [09-04]
files_modified:
  - src/angel/cross-project-consolidator.ts
  - src/angel/heartbeat.ts
  - src/tests/angel/heartbeat.test.ts
autonomous: true
requirements:
  - CUR-07
---

# Plan 09-05 — Delete cross-project-consolidator.ts (sub-phase 9.5)

## Goal

Delete `src/angel/cross-project-consolidator.ts` (488 lines) and its sole
heartbeat callsite (Phase 4c). Per CONTEXT.md `<decisions>.### Module
dependencies (T6-verified)` — *"Phase 6.5 implements cross-project
differently — not via this module"*. The Phase 6.5 cross-project
task-pattern recall (which closed 2026-04-29) uses
`expandSearchCrossProject` and `assembleExperienceTier` paths; the legacy
`runCrossProjectConsolidation` is dead infra.

## Why this is safe — Phase 6.5 superseded the surface

The legacy consolidator did fingerprint-based cross-project knowledge dedup
inside the heartbeat (Phase 4c). Phase 6.5 replaced that with task-pattern
fingerprint sidecar + cross-project query expansion at retrieval time
(`expandSearchCrossProject` in `src/intelligence/...`, plus the
`assembleExperienceTier` path). Those Phase 6.5 surfaces do not call
`runCrossProjectConsolidation`. The legacy module became orphan infra at
Phase 6.5 close.

## Scope

| File | Change |
|---|---|
| `src/angel/cross-project-consolidator.ts` | DELETE (whole file, 488 lines) |
| `src/angel/heartbeat.ts` | Drop the line-49 import; delete Phase 4c block at lines ~975-989 (currently has the `runCrossProjectConsolidation(ctx.db, ctx.config.retention)` call) |
| `src/tests/angel/heartbeat.test.ts` | Drop `runCrossProjectConsolidation: vi.fn().mockReturnValue({...})` mock entry (line ~76-80). |
| Associated tests | If a dedicated `src/tests/angel/cross-project-consolidator*.test.ts` exists, delete it. (Verify — at plan-write time it does not exist; absence preserved at execution time.) |

## Tasks

<task id="09-05-01">
  <subject>Verify zero non-test, non-self consumers</subject>
  <description>
```bash
grep -rnE "cross-project-consolidator|runCrossProjectConsolidation" src/ \
  --include="*.ts" --exclude-dir=node_modules
```

Expected matches:
- `src/angel/cross-project-consolidator.ts` — self
- `src/angel/heartbeat.ts:49, 975-989` — import + Phase 4c callsite
- `src/tests/angel/heartbeat.test.ts:76-80` — mock entry

Verify NO match in `src/intelligence/...` (the Phase 6.5 cross-project
surface lives in intelligence/, not angel/, and uses different function
names — `expandSearchCrossProject`, `assembleExperienceTier`, etc.).
The legacy module's name shape (`cross-project-consolidator`) does not
collide with the Phase 6.5 surface naming.

If any consumer in `src/intelligence/`, `src/core/`, or an MCP handler
appears, STOP and file an amendment.
  </description>
  <verification>
- Grep returns only the three expected files
- No matches in `src/intelligence/`, `src/core/`, `src/adapters/`
  </verification>
</task>

<task id="09-05-02">
  <subject>Delete cross-project-consolidator.ts</subject>
  <description>
`rm src/angel/cross-project-consolidator.ts`. Whole-file deletion — 488 lines.

Verify no test file is orphaned:
```bash
find src/tests -name "cross-project-consolidator*"
```
If empty, no further test deletion. If a test file exists, delete it too.
  </description>
  <verification>
- `test ! -f src/angel/cross-project-consolidator.ts`
- No orphan test files in `src/tests/`
  </verification>
</task>

<task id="09-05-03">
  <subject>Remove heartbeat Phase 4c block + import</subject>
  <description>
In `src/angel/heartbeat.ts`:

1. Drop line 49 import:
   ```ts
   import { runCrossProjectConsolidation } from './cross-project-consolidator.js';
   ```

2. Delete the Phase 4c block (currently around lines 975-989). The block
   has the comment `// Phase 4c: Cross-project knowledge consolidation`
   followed by a `try { ... }` that calls `runCrossProjectConsolidation` and
   may write to `result.cross_project_*` fields. Delete the entire block.

3. Remove any `result.cross_project_*` fields from the heartbeat result-type
   that are written only by this block. (Note: Phase 6.5's task-pattern
   fingerprint backfill is a *separate* block at Phase 4b.5 around line 964
   — leave that alone.)
  </description>
  <verification>
- `grep -n "cross-project-consolidator\|runCrossProjectConsolidation\|Phase 4c" src/angel/heartbeat.ts` returns zero matches
- `grep -n "Phase 4b.5\|Phase 4d" src/angel/heartbeat.ts` returns matches (sibling blocks intact)
- `bun run build` exits 0
  </verification>
</task>

<task id="09-05-04">
  <subject>Update heartbeat.test.ts mock</subject>
  <description>
Drop `runCrossProjectConsolidation: vi.fn().mockReturnValue({...})` (lines
~76-80) from the cross-project-consolidator mock object.

If the entire `vi.mock('../../angel/cross-project-consolidator.js', ...)`
block becomes empty (only this one key was mocked), delete the whole
`vi.mock(...)` call. Otherwise just delete the key.
  </description>
  <verification>
- `grep -n "cross-project-consolidator\|runCrossProjectConsolidation" src/tests/angel/heartbeat.test.ts` returns zero matches
- `bun run test src/tests/angel/heartbeat.test.ts` exits 0
  </verification>
</task>

<task id="09-05-05">
  <subject>Vitest pass + Vesna spot-check + commit</subject>
  <description>
```bash
bun run test
bun run test src/tests/integration/phase-6-multiplier-ablation.test.ts \
              src/tests/integration/phase-6-5-cross-project-vesna.test.ts
```

Vesna spot-check is *especially* worth attention here — Phase 6.5's three
cross-project probes live in
`src/tests/integration/phase-6-5-cross-project-vesna.test.ts` and they MUST
pass after this deletion. If they regress, the deletion was unsafe and the
T6 audit (or the Phase 6.5 close validation) was incomplete; investigate.

Pass rate ≥80% across the spot-check; cross-project probes specifically
must hit 100% (3/3) — they are the canary that the Phase 6.5 surface still
works without the legacy consolidator.

Atomic commit:

```
chore(angel): delete cross-project-consolidator (Phase 9.5)

Dead infra cleanup — Phase 6.5 superseded the cross-project knowledge
surface (task-pattern fingerprint + expandSearchCrossProject in
intelligence/). Heartbeat Phase 4c was the only inbound. Vesna 3/3
cross-project probes pass.

- src/angel/cross-project-consolidator.ts (deleted, 488 lines)
- src/angel/heartbeat.ts: drop Phase 4c block + import (~16 lines)
- src/tests/angel/heartbeat.test.ts: drop mock entry

Phase 9.5 of 9 (sub-phased per-module deletion).
```

Append STATE.md sub-phase entry.
  </description>
  <verification>
- All tests pass
- `phase-6-5-cross-project-vesna.test.ts` 3/3 probes PASS specifically
- Single commit
- STATE.md updated
  </verification>
</task>

## must_haves

- [ ] `src/angel/cross-project-consolidator.ts` does not exist
- [ ] Heartbeat no longer imports or calls `runCrossProjectConsolidation`; Phase 4c gone
- [ ] heartbeat.test.ts mock cleaned
- [ ] `bun run build` + `bun run test` exit 0
- [ ] Vesna spot-check ≥80%; 3/3 cross-project probes PASS specifically
- [ ] Single atomic commit
- [ ] STATE.md updated

## Out of scope

- Phase 6.5 cross-project surface (`expandSearchCrossProject`,
  `assembleExperienceTier`, `cross-project-equivalence.ts`, `tier-utils.ts`)
  — all stays. Phase 9 is dead-infra cleanup, not retrieval surgery.
- Task-pattern fingerprint sidecar table (`artifact_task_pattern`) — stays
- Other 9.X sub-phases

## Risk + rollback

Risk: low. Phase 6.5's close validation (commit f7fb9f2 + earlier) ran the
3 cross-project Vesna probes against the *current* heartbeat (which already
includes the legacy Phase 4c consolidator). Those probes pass primarily via
the new Phase 6.5 retrieval-time path, not the heartbeat-time legacy path.
Deleting the legacy path should not regress those probes.

The CONTEXT.md `<decisions>.### Sub-phase execution discipline` says: *"if
T6 was wrong on any module, bisect catches it; restore + investigate."*
This sub-phase is the highest-confidence T6 verification (Phase 6.5 already
proved the alternative path works), so risk is bounded.

Rollback: `git revert HEAD`.
