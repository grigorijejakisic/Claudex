---
phase: 10-vesna-probe-suite-central-validation
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/benchmark/vesna/probes/handoff-pickup-active.json
  - src/benchmark/vesna/probes/handoff-pickup-paused.json
  - src/benchmark/vesna/probes/handoff-pickup-archived.json
  - src/benchmark/vesna/probes/recall-observability-gap-detection.json
  - src/benchmark/vesna/probes/recall-observability-empty-surface.json
  - src/benchmark/vesna/probes/buffer-001.json
  - src/benchmark/vesna/probes/buffer-002.json
  - src/benchmark/vesna/probes/buffer-003.json
autonomous: true
requirements:
  - VESN-02
must_haves:
  truths:
    - "All 5 existing probes load successfully under the canonical Plan-01 schema"
    - "Each migrated probe has provenance: source_session_id + source_project + scenario filled with the real session that inspired it"
    - "Each migrated probe has lexical_exclusions populated and passes the loader's pre-flight assertion"
    - "3 buffer placeholder probes exist with buffer_placeholder: true so phases 6.5/8.5/etc can claim them later"
    - "Total probe count after this plan: 5 migrated + 3 buffer = 8 (Plans 03/04 add the remaining 12)"
  artifacts:
    - path: src/benchmark/vesna/probes/handoff-pickup-active.json
      provides: "handoff-pickup probe (active status) — migrated to canonical schema"
      contains: "\"category\": \"handoff-pickup\""
    - path: src/benchmark/vesna/probes/handoff-pickup-paused.json
      provides: "handoff-pickup probe (paused status) — migrated"
      contains: "\"category\": \"handoff-pickup\""
    - path: src/benchmark/vesna/probes/handoff-pickup-archived.json
      provides: "handoff-pickup probe (archived status) — migrated"
      contains: "\"category\": \"handoff-pickup\""
    - path: src/benchmark/vesna/probes/recall-observability-gap-detection.json
      provides: "self-instrumented probe (gold-result narration) — migrated"
      contains: "\"category\": \"self-instrumented\""
    - path: src/benchmark/vesna/probes/recall-observability-empty-surface.json
      provides: "self-instrumented probe (empty-surface narration) — migrated"
      contains: "\"category\": \"self-instrumented\""
    - path: src/benchmark/vesna/probes/buffer-001.json
      provides: "Unallocated buffer slot 1"
      contains: "\"buffer_placeholder\": true"
    - path: src/benchmark/vesna/probes/buffer-002.json
      provides: "Unallocated buffer slot 2"
      contains: "\"buffer_placeholder\": true"
    - path: src/benchmark/vesna/probes/buffer-003.json
      provides: "Unallocated buffer slot 3"
      contains: "\"buffer_placeholder\": true"
  key_links:
    - from: src/benchmark/vesna/probes/*.json
      to: src/benchmark/vesna/loader.ts
      via: "loadProbes() reads + validates each"
      pattern: "loadProbes"
---

<objective>
Migrate the 5 existing pre-Phase-10 probes to the canonical schema established in Plan 01, fill in real session-history provenance, and add 3 buffer placeholder probes for downstream phases (6.5, 8.5, etc.) to claim during their own probe authoring.

Purpose: The 5 existing probes were written before the canonical schema existed and use a "runtime: <vitest-test-path>" delegation pattern. CONTEXT.md (lines 53-70) locks a richer schema with provenance + lexical exclusions + auto evaluation. Migration is mechanical but must preserve the behavioral intent of each existing probe.

Output: 5 migrated probe JSON files + 3 buffer placeholder JSON files. Total probe count after this plan: 8. Plans 03/04 add the remaining 12.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-vesna-probe-suite-central-validation/10-CONTEXT.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-01-PLAN.md
@src/benchmark/vesna/probes/handoff-pickup-active.json
@src/benchmark/vesna/probes/handoff-pickup-paused.json
@src/benchmark/vesna/probes/handoff-pickup-archived.json
@src/benchmark/vesna/probes/recall-observability-gap-detection.json
@src/benchmark/vesna/probes/recall-observability-empty-surface.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 3 handoff-pickup probes</name>
  <files>src/benchmark/vesna/probes/handoff-pickup-active.json, src/benchmark/vesna/probes/handoff-pickup-paused.json, src/benchmark/vesna/probes/handoff-pickup-archived.json</files>
  <action>
For each existing handoff probe, rewrite to the canonical schema. PRESERVE the existing behavioral assertion (status-aware handoff surfacing, no body leakage) but lift it into Plan 01's schema shape.

Mining provenance: query Claudex for the source sessions that inspired Phase 7.5's handoff-pickup tests (the phase that originally shipped these probes). Use `claudex_search("phase 7.5 handoff renderHandoff status")` to find the source. If no single session is identifiable, set `source_session_id` to `phase-7.5-design` and `source_project` to `claudex-v3` with a `scenario` that describes the phase's design moment honestly.

Canonical schema for handoff-pickup-active.json:

```json
{
  "id": "handoff-001",
  "category": "handoff-pickup",
  "source_session_id": "<real session id from claudex_search; fallback: phase-7.5-design>",
  "source_project": "claudex-v3",
  "scenario": "Cold-start session with active handoff (status=active, phase=5, topic=phase-5-tier-deletion). User asks an open pickup question; agent should surface the handoff topic on first response without exploring.",
  "user_prompt": "Pick up where things were.",
  "expected_recall": {
    "artifact_id_or_pattern": "context/handoffs/ACTIVE.md",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": [
      "active handoff at phase 5",
      "phase-5-tier-deletion"
    ]
  },
  "lexical_exclusions": ["handoff", "active", "phase 5", "tier-deletion", "pick up", "phase-5"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "handoff",
      "payload": {
        "status": "active",
        "phase": "5",
        "summary": "Continue tier-deletion plan 03 wave 4",
        "topic": "phase-5-tier-deletion",
        "body_what_next": "Run plan 03 wave 4 — Tier A deletion of `Flow + Reference Layer + Materialization` sections."
      }
    }
  ]
}
```

Wait — lexical_exclusions cannot include words from user_prompt. The user_prompt is "Pick up where things were." which is intentionally bland. Lexical_exclusions of `["handoff", "active", "phase 5", "tier-deletion", "phase-5"]` are safe — none appear in "Pick up where things were." DO include `"pick up"` only if it would also need to be absent from the agent's expected response — it should NOT be there because the AGENT is allowed to say "pick up". `lexical_exclusions` test the USER PROMPT, not the agent output. Removing `"pick up"` from exclusions.

Final exclusions for active: `["handoff", "active", "phase 5", "tier-deletion", "phase-5", "ACTIVE.md"]` — none in user_prompt. Pre-flight passes.

For handoff-pickup-paused.json (id `handoff-002`):
- scenario: same shape, status=paused
- user_prompt: "Where did things stop?"
- expected_recall: pattern `"paused handoff at phase"` (status-aware first-line, body NOT leaked)
- setup_steps: status=paused, phase=4, topic=phase-4-context-loop, summary="Paused mid-investigation on context loop"
- lexical_exclusions: `["handoff", "paused", "phase 4", "context-loop", "ACTIVE.md"]`

For handoff-pickup-archived.json (id `handoff-003`):
- scenario: status=archived — agent should NOT surface body content; should treat as "no active handoff"
- user_prompt: "Anything in flight?"
- expected_recall: pattern `"no active handoff"` (or equivalent)
- setup_steps: status=archived, phase=3, topic=phase-3-old-investigation
- lexical_exclusions: `["handoff", "archived", "phase 3", "ACTIVE.md", "in flight"]` — wait, "in flight" IS in user_prompt. Remove `"in flight"` from exclusions; keep the rest.

Pre-flight rule: lexical_exclusions ∩ user_prompt = ∅. Verify by hand for each probe before saving.

Delete the obsolete fields from existing files: `name` (replaced by `id`), `description` (replaced by `scenario`), `tags` (drop), `setup` (replaced by `setup_steps`), `assert` (replaced by `expected_recall`), `fail_signals` (drop — diagnostics emerge from runner), `runtime` (drop — auto evaluation now).
  </action>
  <verify>bun run build succeeds. bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; console.log(loadProbes().filter(p=>p.category==='handoff-pickup').length)" prints 3. No LexicalLeakageError thrown.</verify>
  <done>3 handoff-pickup probes (handoff-001/002/003) match canonical schema, load successfully, pass lexical pre-flight, preserve original behavioral intent (status-aware first-line, no body leakage).</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 2 self-instrumented gap-detection probes</name>
  <files>src/benchmark/vesna/probes/recall-observability-gap-detection.json, src/benchmark/vesna/probes/recall-observability-empty-surface.json</files>
  <action>
Migrate the 2 self-instrumented probes from Phase 8.5 (OBS-02) to canonical schema.

Provenance: these probes trace to Phase 8.5's narration directive design. Use `claudex_search("phase 8.5 narration advisory voice gold checking applying")` to find the source session. Fallback: `source_session_id = phase-8.5-design`, `source_project = claudex-v3`.

Canonical for recall-observability-gap-detection.json (id `self-instrumented-001`):

```json
{
  "id": "self-instrumented-001",
  "category": "self-instrumented",
  "source_session_id": "<real id or phase-8.5-design>",
  "source_project": "claudex-v3",
  "scenario": "Topic has rich prior experience in memory (Mozzart 429 / shadowban). User asks about that topic. Agent should narrate 'checking ... applying' per advisory voice.",
  "user_prompt": "How should I handle the bookmaker rate-limit issue we hit last week?",
  "expected_recall": {
    "artifact_id_or_pattern": "decision:mozzart-shadowban",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": [
      "checking",
      "applying"
    ]
  },
  "lexical_exclusions": ["mozzart", "shadowban", "429", "auto-heal", "per-IP"],
  "evaluation": "auto",
  "setup_steps": [
    { "kind": "narration_directive", "payload": { "silent": false } },
    { "kind": "artifact", "payload": { "kind": "decision", "summary": "rate-limit shadowban detected — Mozzart returns 429 per-IP, 15-min auto-heal", "project": "lacuna-betting" } }
  ]
}
```

Pre-flight check: user_prompt = "How should I handle the bookmaker rate-limit issue we hit last week?". Exclusions `["mozzart", "shadowban", "429", "auto-heal", "per-IP"]` — none in user_prompt. Note `"rate-limit"` IS in user_prompt — do NOT add it to exclusions. The probe's perceptual signal is "bookmaker + rate-limit" surfacing the Mozzart memory; lexical leakage test guards Mozzart-specific tokens, not the generic concept.

For recall-observability-empty-surface.json (id `self-instrumented-002`):
- scenario: Topic has NO prior experience. Agent should narrate "no prior experience on this — going in cold" per OBS-02.
- user_prompt: "What do you know about XZRT-77 protocol decoding?"
- expected_recall: pattern "no prior experience" (or "going in cold")
- setup_steps: `narration_directive { silent: false }` + NO artifacts (empty memory for the topic)
- lexical_exclusions: `["XZRT-77", "decoding", "protocol"]` — none in user_prompt? Wait, user_prompt mentions all three. The exclusions test the user_prompt; if all three are IN the prompt, the loader will throw.

The point of exclusions for empty-surface probes is different: there's nothing to recall, so there's no "memory text" to lexically diverge from. For empty-surface, lexical_exclusions can be `[]` (empty). Set it to `[]` and add a comment in scenario: "No memory exists for this topic; lexical_exclusions empty by design — testing narration of absence, not perceptual recall."

Final empty-surface probe: `lexical_exclusions: []`, `expected_recall.must_contain_phrase_pattern: ["no prior experience", "going in cold"]` (OR-matched? Plan 01 evaluator says ALL patterns must match — verify Plan 01 spec). Re-reading Plan 01 evaluator: "every regex in `expected.must_contain_phrase_pattern` matches `output.agent_text`". That's AND-semantics. Change one of these to a single regex with alternation: `["no prior experience|going in cold"]` so either narration form passes.

Delete obsolete fields from both files (same as Task 1).
  </action>
  <verify>bun run build succeeds. bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; const ps=loadProbes().filter(p=>p.category==='self-instrumented'); console.log(ps.length, ps.map(p=>p.id))" prints `2 [ 'self-instrumented-001', 'self-instrumented-002' ]`. No LexicalLeakageError.</verify>
  <done>2 self-instrumented probes match canonical schema, preserve OBS-02 intent (gold narration vs empty-surface narration), load cleanly, pass pre-flight (or have empty exclusions by design with documented reason).</done>
</task>

<task type="auto">
  <name>Task 3: Add 3 buffer placeholder probes</name>
  <files>src/benchmark/vesna/probes/buffer-001.json, src/benchmark/vesna/probes/buffer-002.json, src/benchmark/vesna/probes/buffer-003.json</files>
  <action>
Create 3 buffer placeholder probes per CONTEXT line 22-23 ("Buffer slots: 3 probes (unallocated; phases claim during their own probe authoring)"). Each is a valid loadable probe but marked `buffer_placeholder: true` so the runner skips it and the gate excludes it from totals (per Plan 01 index.ts logic).

Schema:

```json
{
  "id": "buffer-001",
  "category": "buffer",
  "source_session_id": "phase-10-design",
  "source_project": "claudex-v3",
  "scenario": "Unallocated buffer slot. A downstream phase (e.g., 6.5 cross-project, 8.5 self-instrumented refinement) claims this slot when authoring its own probes. To claim: replace this file's contents with a real probe of any category, set buffer_placeholder: false (or remove the field), update id to <category>-NNN convention.",
  "user_prompt": "[unallocated]",
  "expected_recall": {
    "artifact_id_or_pattern": "[unallocated]",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": []
  },
  "lexical_exclusions": [],
  "evaluation": "auto",
  "buffer_placeholder": true
}
```

Same shape for buffer-002.json (id `buffer-002`) and buffer-003.json (id `buffer-003`). The scenario field on each instructs the future claimant on how to convert the slot.

Verify Plan 01's loader marks these as `buffer_placeholder: true` and the runner SKIPS them rather than executes (since user_prompt is `[unallocated]`). Plan 01 spec line "Skip `buffer_placeholder: true` probes from execution but include them in the loaded list (counted in totals only when populated)." — verify this behavior holds with these 3 placeholders by running `bun run vesna --json` and confirming buffer slots appear in loaded list but not in per-category totals.

Do NOT pre-categorize these (e.g., "buffer-cross-project-001"). Phase 10 ships 3 truly-unallocated slots; downstream phases pick category at claim time.
  </action>
  <verify>bun run build succeeds. `bun run vesna --json` (with all 8 probes loaded) shows: 3 handoff-pickup + 2 self-instrumented in totals, 3 buffer slots NOT in per-category totals, gate evaluated only on the 5 real probes (which should all pass once the harness stub-runs them in trial mode against the test DB).</verify>
  <done>3 buffer placeholder probes exist; loader accepts them; runner skips them; suite report excludes them from totals.</done>
</task>

</tasks>

<verification>
- All 8 probe JSON files load via `loadProbes()` without error
- 5 real probes (3 handoff + 2 self-instrumented) pass lexical pre-flight (or have documented empty exclusions for empty-surface)
- 3 buffer placeholders are skipped by runner, excluded from per-category totals
- No probe references the obsolete fields (`name`, `runtime`, `tags`, `assert`, `fail_signals`, `setup` flat-shape)
- Each migrated probe's `scenario` field documents the original behavioral intent (status-aware handoff for handoff probes; advisory-voice narration for self-instrumented probes)
</verification>

<success_criteria>
- VESN-02 partial: 5 of 17 core slots filled (handoff: 3/3 ✓, self-instrumented: 2/2 ✓). Plans 03 + 04 fill the remaining 12 (entity 0/3, constraint 0/3, cross-project 0/3, lesson-application 0/3).
- 3 buffer slots reserved for downstream phases (per CONTEXT lock).
- All probes consume Plan 01's canonical schema — no schema drift.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vesna-probe-suite-central-validation/10-02-SUMMARY.md` listing: each migrated probe's id + source_session_id + behavioral intent preserved, the 3 buffer slot ids, and any lexical-exclusion adjustments made for pre-flight.
</output>
