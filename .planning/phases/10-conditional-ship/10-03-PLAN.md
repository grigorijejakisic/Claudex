---
phase: 10-conditional-ship
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/benchmark/vesna/probes/deliberation-engagement-a-001.json
  - src/benchmark/vesna/probes/deliberation-engagement-b-001.json
  - src/benchmark/vesna/probes/deliberation-engagement-c-001.json
  - src/benchmark/vesna/probes/deliberation-engagement-d-001.json
  - src/benchmark/vesna/probes/deliberation-engagement-e-001.json
  - src/benchmark/vesna/setup.ts
  - src/benchmark/vesna/loader.ts
  - src/benchmark/vesna/runner.ts
  - src/tests/unit/vesna-loader.test.ts
  - src/tests/unit/vesna-setup.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Vesna probe count grows from 21 to 26 — five new fixtures live in src/benchmark/vesna/probes/, one per P9 drift kind a-e (sample-size shift, threshold-source shift, scope-change, dependency-change, assumption-drift)"
    - "Each new probe exercises the wired routing+assembly path: setup_steps writes a synthetic past-deliberation artifact + companion transcript chunks via the production write surfaces (upsertChunk + the existing artifact creation helper); the user_prompt simulates the drift; expected_recall asserts the deliberation-surfacing section appears with at least one span citation matching the past deliberation"
    - "P9 fixtures stay byte-immutable per probe-set pre-commitment lock — the new probes are NEW files at production-shape scale, never modifications of P9 fixtures (which live under .planning/phases/09-empirical-measurement/probes/)"
    - "Probes for kinds a (sample-size shift) and c (scope-change) are included even though both were FLAT in P9 (Δ=0.000) — they serve as non-regression baseline per CONTEXT decision 4 ('regression discipline requires both lifty and flat kinds covered')"
    - "Probe schema matches the existing src/benchmark/vesna/probes/lesson-application-001.json shape: {id, category, source_session_id, source_project, scenario, user_prompt, expected_recall: {must_surface_within_turns, must_contain_phrase_pattern}, lexical_exclusions, evaluation, setup_steps[]}"
    - "Vesna loader + setup + runner accept the new probes without code changes beyond extending the probe-loading glob (if needed) and the deliberation-surface setup_step kind handler"
    - "`bun run vesna` reports 26/26 PASS at 100% (gated=true, AGGREGATE: 100% — GATED PASS) — the deliberation-surfacing section fires when each new probe is run"
  artifacts:
    - path: "src/benchmark/vesna/probes/deliberation-engagement-a-001.json"
      provides: "Drift kind a (sample-size shift) probe — verifies non-regression baseline for the flat-in-P9 kind"
    - path: "src/benchmark/vesna/probes/deliberation-engagement-b-001.json"
      provides: "Drift kind b (threshold-source shift) probe — exercises one of the lifty kinds from P9 (b/d/e)"
    - path: "src/benchmark/vesna/probes/deliberation-engagement-c-001.json"
      provides: "Drift kind c (scope-change) probe — non-regression baseline for the second flat-in-P9 kind"
    - path: "src/benchmark/vesna/probes/deliberation-engagement-d-001.json"
      provides: "Drift kind d (dependency-change) probe — exercises a lifty-in-P9 kind"
    - path: "src/benchmark/vesna/probes/deliberation-engagement-e-001.json"
      provides: "Drift kind e (assumption-drift) probe — exercises the third lifty-in-P9 kind"
  key_links:
    - from: "src/benchmark/vesna/probes/deliberation-engagement-*.json"
      to: "transcript_chunk_v6 + artifact tables"
      via: "setup_steps[].kind='deliberation_surface' writes synthetic transcript chunks via upsertChunk + the existing artifact-creation helper used by setup_steps[].kind='artifact' (lesson-application-001.json shape)"
      pattern: "deliberation_surface|deliberation-engagement"
    - from: "src/benchmark/vesna/probes/deliberation-engagement-*.json"
      to: "src/assembly/deliberation-surface.ts (Plan 10-02)"
      via: "expected_recall asserts the rendered section text contains the advisory header + at least one span citation"
      pattern: "Deliberation Surfaced|From session"
---

<objective>
Extend the Vesna probe suite from 21 to 26 functional probes by adding one drift-engagement probe per P9 drift kind (a-e) at production-shape scale. Each new probe exercises the wired routing+assembly path landed in Plans 10-01 and 10-02 — setup writes a synthetic past-deliberation (artifact + companion transcript chunks via the production write surfaces), the user_prompt simulates the drift kind, and expected_recall asserts the deliberation-surfacing section appears with at least one labeled span citation matching the past deliberation.

CONTEXT decision 4 locks the probe count and selection discipline:
- 21 → 26: one new probe per drift kind a-e (sample-size, threshold-source, scope-change, dependency-change, assumption-drift)
- Includes kinds a + c which were FLAT in P9 (Δ=0.000) as non-regression baseline — regression discipline requires both lifty and flat kinds covered
- P9 fixtures stay byte-immutable per the probe-set pre-commitment lock — these are NEW files in `src/benchmark/vesna/probes/`, never modifications of P9's fixtures under `.planning/phases/09-empirical-measurement/probes/`
- Probes target the wired retrieval path, not the harness — pass criterion is mechanical wire correctness + behavioral engagement at the agent level (consistent with existing Vesna probe shape)

Vesna's existing probe loader + setup + runner is largely shape-stable — the only code change required is a new `setup_steps[].kind='deliberation_surface'` handler in `src/benchmark/vesna/setup.ts` that writes the synthetic past-deliberation artifact + transcript chunks via the production write surfaces (`upsertChunk` from Phase 8 plan 08-02 + the existing artifact-creation helper used by `kind='artifact'`).

Output: five new probe JSON files; minimal extensions to `setup.ts` (new step kind), `loader.ts` (if any glob pattern hardcodes), and `runner.ts` (if it counts probes anywhere); the existing 21 probes untouched; `bun run vesna` reports 26/26 PASS.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-conditional-ship/10-CONTEXT.md
@.planning/phases/10-conditional-ship/10-01-PLAN.md
@.planning/phases/10-conditional-ship/10-02-PLAN.md
@.planning/phases/09-empirical-measurement/09-RESULTS.md
@.planning/phases/09-empirical-measurement/09-CONTEXT.md

@src/benchmark/vesna/setup.ts
@src/benchmark/vesna/loader.ts
@src/benchmark/vesna/runner.ts
@src/benchmark/vesna/types.ts
@src/benchmark/vesna/probes/lesson-application-001.json
@src/benchmark/vesna/probes/lesson-application-002.json
@src/ingestion/upsert-chunk.ts
@src/ingestion/transcript-chunker-v6.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `deliberation_surface` setup_step kind handler in vesna/setup.ts</name>
  <files>src/benchmark/vesna/setup.ts, src/benchmark/vesna/types.ts</files>
  <action>
Read `src/benchmark/vesna/setup.ts` to find the existing setup_step kind dispatcher (the switch / map that handles `kind='artifact'`, `kind='handoff'`, etc. used by lesson-application-001.json's setup_steps).

**(a) Extend the setup_step type enum in `src/benchmark/vesna/types.ts`:**

Find the existing setup_step kind type (likely `'artifact' | 'handoff' | ...`). Add `'deliberation_surface'` to the union. The new kind's payload shape:

```ts
{
  kind: 'deliberation_surface';
  payload: {
    /** Source artifact (CONTEXT.md decision shape — kind='decision' typically) */
    artifact: {
      kind: string; // 'decision' | 'learning' | 'experience_pattern' | 'mental_model' | 'directive_rule' | 'critical_rule'
      summary: string;
      project: string;
      tags?: string[];
    };
    /** Synthetic past-deliberation transcript chunks that informed the artifact */
    transcript_chunks: Array<{
      session_id: string;
      project_id: string;
      turn_index: number;
      sub_index: number;
      role: 'user' | 'assistant' | 'tool' | 'system';
      provenance: 'organic' | 'injected' | 'tool_result' | 'environmental';
      body: string;
      created_at_epoch_ms: number;
      wrapper_redacted: boolean;
    }>;
  };
}
```

**(b) Add the dispatcher case in `src/benchmark/vesna/setup.ts`:**

```ts
case 'deliberation_surface': {
  const { artifact, transcript_chunks } = step.payload;
  // Reuse the existing artifact-creation helper used by case 'artifact'
  await createArtifact(db, artifact);
  // Write transcript chunks via the production write surface (Phase 8 plan 08-02)
  for (const chunk of transcript_chunks) {
    upsertChunk(db, chunk);
  }
  break;
}
```

Adapt the helper names (`createArtifact`, etc.) to whatever the existing `case 'artifact'` block actually calls. Add the `upsertChunk` import from `../../ingestion/upsert-chunk.js` (path adjusted to setup.ts's location).

The new step kind is the only code change — the loader + runner already iterate over arbitrary `setup_steps[]`. Confirm by inspecting `loader.ts` and `runner.ts` for any hardcoded probe count or hardcoded step-kind allowlist.
  </action>
  <verify>
`bun run build` exits 0. `grep -n "'deliberation_surface'" src/benchmark/vesna/setup.ts` matches one line (the case). `grep -n "deliberation_surface" src/benchmark/vesna/types.ts` matches one line (the type union member). `grep -n "upsertChunk" src/benchmark/vesna/setup.ts` matches one line (the import).
  </verify>
  <done>
Vesna setup.ts has a working `deliberation_surface` dispatcher; types.ts allows the new kind; the existing 21 probes' setup paths are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 5 deliberation-engagement probe fixtures (kinds a-e)</name>
  <files>src/benchmark/vesna/probes/deliberation-engagement-a-001.json, src/benchmark/vesna/probes/deliberation-engagement-b-001.json, src/benchmark/vesna/probes/deliberation-engagement-c-001.json, src/benchmark/vesna/probes/deliberation-engagement-d-001.json, src/benchmark/vesna/probes/deliberation-engagement-e-001.json</files>
  <action>
Author one new probe per drift kind. Each follows `src/benchmark/vesna/probes/lesson-application-001.json` shape with the addition of a `setup_steps[]` entry of `kind='deliberation_surface'` and an `expected_recall.must_contain_phrase_pattern` that asserts both the advisory header and at least one span citation phrase.

**Common drift-kind taxonomy reference (from P9 09-CONTEXT.md / 09-RESULTS.md):**
- a: sample-size shift — past decision was made on data of one size; current case is materially smaller or larger
- b: threshold-source shift — past decision used a hardcoded threshold; the threshold's grounding has since changed
- c: scope-change — past decision was scoped to feature X; current case extends to feature Y
- d: dependency-change — past decision relied on dependency Z at version V; Z is now at V'
- e: assumption-drift — past decision assumed condition C; condition C no longer holds

**Probe template (use this shape for all five; substitute the drift specifics):**

```json
{
  "id": "deliberation-engagement-{a|b|c|d|e}-001",
  "category": "deliberation-engagement",
  "source_session_id": "claudex-v3:phase-10-deliberation-fixture-{kind}",
  "source_project": "claudex-v3",
  "scenario": "<one sentence describing the past decision and the drift the new prompt introduces>",
  "user_prompt": "<the agent-facing prompt simulating the drift>",
  "expected_recall": {
    "artifact_id_or_pattern": "decision:<topic>|deliberation-engagement-{kind}",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": [
      "## Deliberation Surfaced",
      "From session .* turn"
    ]
  },
  "lexical_exclusions": ["<terms the agent should not lean on lexically>"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "deliberation_surface",
      "payload": {
        "artifact": {
          "kind": "decision",
          "summary": "<the past deliberation's outcome — one sentence>",
          "project": "claudex-v3",
          "tags": ["<topic>", "deliberation-engagement", "kind-{a|b|c|d|e}"]
        },
        "transcript_chunks": [
          {
            "session_id": "phase-10-deliberation-fixture-{kind}",
            "project_id": "claudex-v3",
            "turn_index": 0,
            "sub_index": 0,
            "role": "user",
            "provenance": "organic",
            "body": "<original deliberation user-side context>",
            "created_at_epoch_ms": 1700000000000,
            "wrapper_redacted": false
          },
          {
            "session_id": "phase-10-deliberation-fixture-{kind}",
            "project_id": "claudex-v3",
            "turn_index": 1,
            "sub_index": 0,
            "role": "assistant",
            "provenance": "organic",
            "body": "<original deliberation assistant-side reasoning that named the locked condition>",
            "created_at_epoch_ms": 1700000000060,
            "wrapper_redacted": false
          }
        ]
      }
    }
  ]
}
```

**Concrete fixture content per kind (use these — they are CONTEXT-locked drift narratives, drawn from the v5/v6 project history):**

**deliberation-engagement-a-001 (sample-size shift, FLAT-in-P9 baseline):**
- scenario: "Past decision: 'multi-handle retrieval was killed at n=20 with Wilson CI lower -0.157 (Phase 2 KILL)'. New prompt asks whether to retry the multi-handle thesis at n=200."
- user_prompt: "We have a much bigger held-out corpus now. Should I rerun the multi-handle retrieval experiment at n=200 to see if the kill flips?"
- artifact summary: "Multi-handle retrieval thesis killed empirically at n=20 in Phase 2; corpus-expansion rerun (Phase 2.1) at n=20 strict + n=19 relaxed both confirmed KILL."
- transcript chunks: two-turn dialog naming the n=20 sample and the Wilson CI binding rule.
- lexical_exclusions: ["multi-handle", "n=20", "Wilson", "Phase 2.1"]
- must_contain_phrase_pattern: ["## Deliberation Surfaced", "From session .* turn", "(?i)sample.?size|n=\\d+"]

**deliberation-engagement-b-001 (threshold-source shift, LIFTY-in-P9):**
- scenario: "Past decision: 'cross-encoder fitness threshold set at 60% top-3 overlap (P9 CONTEXT decision 4)'. New prompt asks why a 56% fitness should not be promoted as the new operational threshold."
- user_prompt: "Cross-encoder fitness landed at 56% on the latest backfill — let's lower the threshold to 55% so we can flip cross-encoder primary."
- artifact summary: "Cross-encoder fitness threshold = 60% top-3 overlap; below threshold means bi-encoder fallback is the production primary (P9 CONTEXT decision 4)."
- transcript chunks: two-turn dialog where the 60% threshold was sourced from the P9 binding-measurement methodology, NOT the operational corpus.
- lexical_exclusions: ["56%", "60%", "cross-encoder", "fitness"]
- must_contain_phrase_pattern: ["## Deliberation Surfaced", "From session .* turn", "(?i)threshold|methodology"]

**deliberation-engagement-c-001 (scope-change, FLAT-in-P9 baseline):**
- scenario: "Past decision: 'V32 transcript_chunk_v6 substrate is opt-in per assembly site, default false (Plan 10-02 ASM-03)'. New prompt asks for global opt-in across all assembly sites."
- user_prompt: "Let's just turn deliberation surfacing on globally for every assembly call — easier to reason about."
- artifact summary: "Deliberation surfacing is opt-in per assembly site (default false) — sites that don't opt in see zero behavior change. Per CONTEXT decision 3 (token budget management)."
- transcript chunks: two-turn dialog naming the per-site opt-in discipline + the budget-management reasoning.
- lexical_exclusions: ["opt-in", "global", "deliberation surfacing"]
- must_contain_phrase_pattern: ["## Deliberation Surfaced", "From session .* turn", "(?i)opt.?in|per.?site"]

**deliberation-engagement-d-001 (dependency-change, LIFTY-in-P9):**
- scenario: "Past decision: 'sqlite-vec is the vector store; Qdrant was removed in session 47 (single-store design)'. New prompt asks to add a Pinecone external store for the new feature."
- user_prompt: "We need vector search for the new ABC feature — should I spin up Pinecone or Qdrant alongside?"
- artifact summary: "Qdrant removed in session 47; switched to sqlite-vec / vec0 virtual tables embedded in claudex.db. Single-store design — never add a separate vector DB."
- transcript chunks: two-turn dialog where the single-store decision was made + the sqlite-vec rationale.
- lexical_exclusions: ["sqlite-vec", "vec0", "single-store", "session 47"]
- must_contain_phrase_pattern: ["## Deliberation Surfaced", "From session .* turn", "(?i)single.?store|sqlite.?vec"]

**deliberation-engagement-e-001 (assumption-drift, LIFTY-in-P9):**
- scenario: "Past decision: 'Bi-encoder primary on the v6 transcript surface — cross-encoder fitness 56.0% < 60% threshold (P10 CONTEXT decision 1)'. New prompt suggests assuming cross-encoder is always better and flipping the default."
- user_prompt: "Cross-encoders are always better than bi-encoders on reranking — let's flip v6.routing.reranker_mode to cross_encoder_primary by default."
- artifact summary: "Bi-encoder is production primary for v6 transcript routing; cross-encoder remains code-reachable behind the v6.routing.reranker_mode flag (P10 CONTEXT decision 1)."
- transcript chunks: two-turn dialog naming the conversation-distribution-vs-web-distribution distinction + the P9 binding-measurement methodology discipline.
- lexical_exclusions: ["bi-encoder", "cross-encoder", "reranker_mode"]
- must_contain_phrase_pattern: ["## Deliberation Surfaced", "From session .* turn", "(?i)distribution|methodology|fitness"]

For each fixture, fill the `<placeholders>` with concrete sentences drawn from project history (CONTEXT.md, 09-RESULTS.md, the v5 reframe artifact). The fixture body must be self-contained — Vesna's setup writes the synthetic transcript chunks into `transcript_chunk_v6` BEFORE the runner invokes the agent under test, so the routing surface has real chunks to fan out to.

**Pre-commitment discipline:** The five new probes are committed BEFORE `bun run vesna` is run for the first time on the new suite. Mirrors Phase 9's pre-committed probe-set pattern. The fixtures + their `expected_recall.must_contain_phrase_pattern` are the binding contract.
  </action>
  <verify>
`ls src/benchmark/vesna/probes/deliberation-engagement-*.json | wc -l` returns 5. `node -e "for (const k of ['a','b','c','d','e']) { const j = require('./src/benchmark/vesna/probes/deliberation-engagement-' + k + '-001.json'); if (!j.id || !j.expected_recall || !j.setup_steps) throw new Error('bad fixture: ' + k); console.log(k, j.id, j.setup_steps.length, 'setup steps'); }"` prints five lines, all valid. `grep -l "deliberation_surface" src/benchmark/vesna/probes/*.json | wc -l` returns 5.
  </verify>
  <done>
Five deliberation-engagement probe fixtures land at production-shape scale, one per P9 drift kind a-e. Each fixture writes a synthetic past-deliberation artifact + companion transcript chunks via setup_steps[].kind='deliberation_surface'. P9 fixtures under .planning/phases/09-empirical-measurement/probes/ are untouched (byte-immutable lock honored).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update vesna probe-count assertion + run the extended suite</name>
  <files>src/benchmark/vesna/loader.ts, src/benchmark/vesna/runner.ts, src/tests/unit/vesna-loader.test.ts, src/tests/unit/vesna-setup.test.ts</files>
  <action>
**(a) Inspect `loader.ts`, `runner.ts`, and the existing vesna unit tests for any hardcoded probe-count assertion (`21` literally) — these will need to bump to `26`.**

`grep -n "21" src/benchmark/vesna/loader.ts src/benchmark/vesna/runner.ts src/benchmark/vesna/setup.ts src/tests/unit/vesna-*.test.ts` — flag every match for review. The probe count is most likely an assertion in the test files (`expect(probes.length).toBe(21)`) or a documentation string in the loader/runner (`/** 21 functional probes */`). Bump each occurrence to `26`.

If `loader.ts` uses a glob pattern that matches all `*.json` in `probes/`, no glob change is needed — the new fixtures are already discovered. If it instead uses an explicit allowlist or a manifest file, add the five new probe ids to that allowlist.

**(b) `src/tests/unit/vesna-setup.test.ts`:** add a new test case asserting that `kind='deliberation_surface'` is dispatched correctly:

```ts
it('dispatches kind=deliberation_surface to upsertChunk + createArtifact', async () => {
  const db = new Database(':memory:');
  initializeSchema(db);

  const step = {
    kind: 'deliberation_surface' as const,
    payload: {
      artifact: { kind: 'decision', summary: 'test', project: 'test-project', tags: [] },
      transcript_chunks: [
        {
          session_id: 'sess-test',
          project_id: 'test-project',
          turn_index: 0,
          sub_index: 0,
          role: 'user' as const,
          provenance: 'organic' as const,
          body: 'test body',
          created_at_epoch_ms: 1700000000000,
          wrapper_redacted: false,
        },
      ],
    },
  };

  await runSetupStep(db, step); // adapt to actual function name in setup.ts

  const chunk = db.prepare(`SELECT * FROM transcript_chunk_v6 WHERE session_id = 'sess-test'`).get();
  expect(chunk).toBeDefined();
  expect(chunk.body).toBe('test body');
  // Plus an assertion that the artifact landed via the existing artifact-creation helper
});
```

**(c) Run the extended Vesna suite end-to-end:**

```bash
bun run vesna
```

Expected: `26/26 PASS at 100%`, `gated=true`, `AGGREGATE: 100% — GATED PASS`. If any of the five new probes fails, debug:
- Check that `transcript_chunk_v6` rows landed before the agent run (query the test DB).
- Check that the routing surface returns spans for the synthetic `session_id` + time window.
- Check that the assembler's deliberation-surfacing integration is opted in for the Vesna runner — inspect the runner's assembler invocation and ensure `deliberationSurfacing: true` is passed (or set as the default for the Vesna context). If the runner doesn't currently flip this flag, add the opt-in there.
- Check that the `must_contain_phrase_pattern` regexes match the rendered output — adjust the regex literal (not the rendered output) if the format text differs in casing/whitespace.

If a regex needs adjustment for any fixture, document the adjustment in the SUMMARY — pre-commitment discipline allows regex tightening for shape-correctness, but NOT loosening that would let a fundamentally-failed probe pass. Each fixture must still test the locked drift narrative.
  </action>
  <verify>
`bun run vesna` reports `26/26 PASS at 100%`, `gated=true`, `AGGREGATE: 100% — GATED PASS`. `bun run vitest run src/tests/unit/vesna-loader.test.ts src/tests/unit/vesna-setup.test.ts src/tests/unit/vesna-evaluator.test.ts` all pass. `grep -n "deliberation-engagement" src/benchmark/vesna/probes/*.json | wc -l` returns at least 5.
  </verify>
  <done>
Vesna probe count is 26; all 26 PASS at 100%; the five new deliberation-engagement probes exercise the wired routing+assembly path; existing 21 probes are unchanged; all vesna unit tests still pass.
  </done>
</task>

</tasks>

<verification>
- `bun run build` exits 0.
- `bun run vesna` reports `26/26 PASS at 100%`, `gated=true`, `AGGREGATE: 100% — GATED PASS`.
- `bun run vitest run src/tests/unit/vesna-*.test.ts` all pass.
- `bun run test` — no NEW regressions vs Plan 10-02 close-out baseline.
- Five new probe files exist under `src/benchmark/vesna/probes/deliberation-engagement-{a,b,c,d,e}-001.json`.
- The 21 pre-existing probe files in `src/benchmark/vesna/probes/` are untouched (file-content-hash verification optional but encouraged).
- P9 fixtures under `.planning/phases/09-empirical-measurement/probes/` are byte-immutable (byte-immutable lock honored — verify via `git diff .planning/phases/09-empirical-measurement/probes/` showing no changes).
</verification>

<success_criteria>
- Vesna grows from 21 to 26 functional probes per CONTEXT decision 4.
- One probe per P9 drift kind a-e at production-shape scale, fresh fixtures (P9 fixtures byte-immutable per probe-set pre-commitment lock).
- Probes for kinds a + c (FLAT in P9, Δ=0.000) included as non-regression baseline; b + d + e (LIFTY in P9) exercise the engagement signal.
- Each probe writes synthetic past-deliberation via the production write surfaces (upsertChunk + artifact-creation helper) — never via test-only shims.
- Each probe asserts the deliberation-surfacing section appears in the rendered output via must_contain_phrase_pattern regexes.
- Pass criterion landed: production retrieval surface fires deliberation-surfacing path correctly per probe.
- Foundation for Plan 10-04 (the WIR-01 wire test will run alongside `bun run vesna 26/26` as one of the 9 ship gates).
</success_criteria>

<output>
After completion, create `.planning/phases/10-conditional-ship/10-03-SUMMARY.md` per the standard template — include `requires: [10-02]`, `provides: [5 deliberation-engagement probes a-e, vesna setup.ts deliberation_surface dispatcher, types.ts kind extension, vesna 26/26 PASS]`, `affects: [10-04]`, key-files, and decision notes:

1. "P9 fixtures stay byte-immutable per probe-set pre-commitment lock — the five new probes are NEW files at production-shape scale targeting the wired retrieval path, not modifications of P9 fixtures."
2. "Kinds a + c (FLAT in P9, Δ=0.000) included as non-regression baseline per CONTEXT decision 4 — regression discipline requires both lifty and flat kinds covered."
3. "Pass criterion is mechanical wire correctness + behavioral engagement at the agent level (consistent with existing Vesna probe shape) — NOT engagement re-measurement (that would re-litigate the P9 verdict)."
4. "If any phrase-pattern regex needed adjustment for shape-correctness, document the adjustment here — pre-commitment discipline allows tightening but not loosening."
</output>
