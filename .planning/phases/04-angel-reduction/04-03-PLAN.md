---
phase: 04-angel-reduction
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/intelligence/experience-scoring.ts
  - src/intelligence/correction-detection.ts
  - src/tests/intelligence/experience-detection.test.ts
files_created:
  - src/tests/intelligence/correction-signal.test.ts
files_deleted:
  - src/tests/intelligence/experience-detection.test.ts
autonomous: true
requirements: [AR-01, AR-03]
must_haves:
  truths:
    - "applyExperienceFeedback no longer creates experience_patterns rows when correction_flagged=true"
    - "Step 2 (topic-aware score feedback) and step 3 (flag rotation) of applyExperienceFeedback survive verbatim"
    - "extractLessonFromUserCorrection and extractPatternFromAssistantText are no longer exported from correction-detection.ts"
    - "detectCorrectionSignal, findCausalEvent, storeCausalAttribution survive in correction-detection.ts"
    - "grep -r 'extractLessonFromUserCorrection\\|extractPatternFromAssistantText' src/ returns ZERO matches"
    - "src/tests/intelligence/correction-signal.test.ts contains the surviving behavioral-signal assertions; extraction-related cases dropped"
    - "bun run build succeeds; bun run test passes"
  artifacts:
    - path: "src/intelligence/experience-scoring.ts"
      provides: "applyExperienceFeedback minus step 1 (pattern extraction); imports trimmed"
      contains: "applyExperienceFeedback"
      not_contains: "extractLessonFromUserCorrection"
    - path: "src/intelligence/correction-detection.ts"
      provides: "detectCorrectionSignal + findCausalEvent + storeCausalAttribution; the two extraction functions deleted"
      contains: "export function detectCorrectionSignal"
      not_contains: "extractLessonFromUserCorrection"
    - path: "src/tests/intelligence/correction-signal.test.ts"
      provides: "Renamed/split test file with surviving correction-signal + behavioral-signal cases"
      min_lines: 200
  key_links:
    - from: "src/intelligence/experience-scoring.ts"
      to: "src/intelligence/correction-detection.ts"
      via: "import { findCausalEvent, storeCausalAttribution } from './correction-detection.js'"
      pattern: "correction-detection"
---

<objective>
Surgical deletion of Site B (hook-side, heuristic-driven extraction in `experience-scoring.ts`) and Site D (the two extraction helpers in `correction-detection.ts` that only Site B called). Step 1 of `applyExperienceFeedback` (the `correction_flagged=true` → `createPattern` block) dies; steps 2 and 3 (score feedback + flag rotation) survive verbatim because they read existing rows and update score/maturity columns — that's binding/indexing, not creation.

The split-rename of `experience-detection.test.ts` → `correction-signal.test.ts` preserves the behavioral-signal half (which tests `detectCorrectionSignal` + `withBehavioralBatch` etc.) and drops the extraction half.

Purpose: Site B is the lesser-known of the three deletion sites — CONTEXT.md surfaced it during survey expansion. Killing it here makes `applyExperienceFeedback` honest about what it does: scoring and rotation, not extraction.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-angel-reduction/04-CONTEXT.md
@.planning/phases/04-angel-reduction/04-RESEARCH.md
@src/intelligence/experience-scoring.ts
@src/intelligence/correction-detection.ts
@src/tests/intelligence/experience-detection.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim experience-scoring.ts step 1 + imports</name>
  <files>src/intelligence/experience-scoring.ts</files>
  <action>
1. Update the import at line 26 from:

   ```ts
   import { extractPatternFromAssistantText, extractLessonFromUserCorrection, findCausalEvent, storeCausalAttribution } from './correction-detection.js';
   ```

   To:

   ```ts
   import { findCausalEvent, storeCausalAttribution } from './correction-detection.js';
   ```

2. Update the import block at lines 17–23. Remove `createPattern` from the destructured list — the surviving step 2 only uses `updatePatternScore`, `incrementUsefulCount`, `escalatePattern`, plus `classifyPatternScope`. Keep those.

3. Delete the entire step-1 block at lines 64–112 — the `if (correction_flagged && lastUserText) { try { ... extracted ... createPattern ... findCausalEvent ... storeCausalAttribution ... } catch { ... } }` block.

   **Important:** the `findCausalEvent` and `storeCausalAttribution` calls inside the deleted block (lines 102–107) ALSO die — they only made sense paired with the just-created `patternId`. Without `createPattern`, there's no pattern to attribute the causal event to. The two functions remain exported from `correction-detection.ts` because Phase 7 retirement work may use them; they just have no live caller in production after this plan.

4. After deletion, line 64 (the `try { expFlags = getExperienceFlags(db, sessionId); ...` block) flows directly into what was line 114 (the score feedback block). Verify the `try`/`catch` structure of `applyExperienceFeedback` is still well-formed — re-read the function end-to-end.

5. The `enrichmentProvider` detection (lines 82–94) was used only by the deleted step 1. Delete those lines too.

6. Remove now-unused imports: `detectEnrichmentProvider` from `'./enrichment.js'` (line 24), and `CC_CAPABILITIES` from `'../shared/constants.js'` (line 15) IF only step 1 used them. Re-grep the file — if survivors reference them, leave them.

**Avoid:**
- Touching step 2 (lines 114–171) — it survives verbatim.
- Touching step 3 (the `finally { setExperienceFlags(...) }` block, lines 189–209) — survives verbatim.
- Touching the outcome-tracker call (lines 173–186) — uses surviving `inferOutcomeFromSession`.
- Renaming `applyExperienceFeedback` — the function's contract narrowed (no longer extracts) but its name still describes what it does (scores + rotates flags).
  </action>
  <verify>
1. `bun run build` succeeds.
2. `grep -n "createPattern\|extractLessonFromUserCorrection\|extractPatternFromAssistantText" src/intelligence/experience-scoring.ts` returns ZERO matches.
3. `grep -n "updatePatternScore\|incrementUsefulCount\|escalatePattern" src/intelligence/experience-scoring.ts` returns at least 3 matches (step 2 survives).
4. `grep -n "setExperienceFlags" src/intelligence/experience-scoring.ts` returns at least one match (step 3 survives).
  </verify>
</task>

<task type="auto">
  <name>Task 2: Delete extractLessonFromUserCorrection and extractPatternFromAssistantText from correction-detection.ts</name>
  <files>src/intelligence/correction-detection.ts</files>
  <action>
1. Read `src/intelligence/correction-detection.ts` end-to-end. Identify the bodies of:
   - `export function extractLessonFromUserCorrection(...)`
   - `export function extractPatternFromAssistantText(...)`

2. Delete both function bodies and any helper consts/types ONLY they reference (e.g., regex patterns specific to their scope — but NOT the shared `CORRECTION_PATTERNS` const which `detectCorrectionSignal` uses).

3. Re-grep:

   ```bash
   grep -rn "extractLessonFromUserCorrection\|extractPatternFromAssistantText" src/
   ```

   Expected matches AFTER deletion: only in `src/tests/intelligence/correction-signal.test.ts` if a stale reference survives (correct in task 3); else zero.

4. The surviving exports are: `CORRECTION_PATTERNS`, `detectCorrectionSignal`, `findCausalEvent`, `storeCausalAttribution`. Confirm the file's top docblock (lines 1–18) still accurately describes what survives — narrow the description if it claims to also extract lessons. Replace lines 4–9 (the "Two extraction paths" paragraph) with:

   ```
   * Linguistic correction-signal detection.
   *
   * Surfaces "user is correcting" linguistic markers from a user prompt
   * (e.g., "no, that's not right", "stop doing X"). The detection is read-only —
   * it does NOT create new patterns. Pattern creation was deleted in Phase 4
   * (see .planning/reframes/2026-05-05-multi-handle-kill.md). The surviving
   * downstream uses are: hook-side score feedback (scores existing patterns)
   * and causal attribution (links a session event to a previously-created
   * pattern when one exists).
   ```

**Avoid:**
- Deleting `detectCorrectionSignal` or `CORRECTION_PATTERNS` — `detectCorrectionSignal` is used by hook-side correction signal detection.
- Deleting `findCausalEvent` or `storeCausalAttribution` — they remain available for Phase 7 retirement tooling and the surviving direct-causal-attribution path.
- Renaming the file — `correction-detection.ts` still describes what it does.
  </action>
  <verify>
1. `bun run build` succeeds.
2. `grep -rn "extractLessonFromUserCorrection\|extractPatternFromAssistantText" src/` returns ZERO matches outside the renamed test file (which task 3 cleans).
3. `grep -n "export function detectCorrectionSignal" src/intelligence/correction-detection.ts` returns one match (preserved).
4. `grep -n "export function findCausalEvent\|export function storeCausalAttribution" src/intelligence/correction-detection.ts` returns 2 matches (preserved).
  </verify>
</task>

<task type="auto">
  <name>Task 3: Split experience-detection.test.ts into correction-signal.test.ts</name>
  <files>src/tests/intelligence/experience-detection.test.ts, src/tests/intelligence/correction-signal.test.ts</files>
  <action>
1. Read `src/tests/intelligence/experience-detection.test.ts` (683 lines) carefully. Identify the test sections:
   - Tests for `detectCorrectionSignal` (linguistic detection)
   - Tests for `extractLessonFromUserCorrection` (deleted symbol — drop these)
   - Tests for `extractPatternFromAssistantText` (deleted symbol — drop these)
   - Tests for `withBehavioralBatch`, `applyFileEditIncrement`, `applyToolCallPattern`, `getBehavioralCounters` (behavioral signals — keep)
   - Tests for `buildToolSignature` (kept — uses `behavioral-signals.ts`)

2. Create `src/tests/intelligence/correction-signal.test.ts` containing:
   - The imports from `correction-detection.ts` LIMITED to `detectCorrectionSignal` only (drop `extractPatternFromAssistantText`, `extractLessonFromUserCorrection`)
   - The behavioral-signal imports unchanged (`withBehavioralBatch`, `applyFileEditIncrement`, `applyToolCallPattern`, `getBehavioralCounters`)
   - The `buildToolSignature` import unchanged
   - All test cases EXCEPT those that exercise the two deleted functions
   - Update top-of-file JSDoc to note: *"Phase 4 split: extraction tests dropped (extractLessonFromUserCorrection / extractPatternFromAssistantText were deleted along with extraction-time pattern creation). Surviving cases test correction-signal detection + behavioral-signal counters."*

3. Delete the original `src/tests/intelligence/experience-detection.test.ts`.

4. The new file MUST be ≥200 lines (the surviving content is substantial — behavioral-signal tests alone span dozens of cases). If the resulting file is shorter than expected, audit whether you over-deleted.

**Git note:** Renaming preserves blame via `git log --follow` automatically. To make the rename even more obvious to reviewers, consider committing the move as one commit ("rename → correction-signal.test.ts") followed by a separate commit ("drop extraction-tests") — the planner may decide otherwise based on commit-boundary preferences.

**Avoid:**
- Deleting cases that exercise `detectCorrectionSignal` — that function survives.
- Deleting behavioral-signal tests — the `behavioral-signals.ts` module survives unchanged.
- Adding new assertions in this plan — Layer 2 regression guard lives in plan 06.
  </action>
  <verify>
1. `bun run build` succeeds.
2. The old file is gone: `test ! -f src/tests/intelligence/experience-detection.test.ts`.
3. The new file exists: `test -f src/tests/intelligence/correction-signal.test.ts`.
4. `grep -n "extractLessonFromUserCorrection\|extractPatternFromAssistantText" src/tests/intelligence/correction-signal.test.ts` returns ZERO matches.
5. `bun run test src/tests/intelligence/correction-signal.test.ts` PASSES.
6. `bun run test` — full suite passes.
  </verify>
</task>

</tasks>

<post_conditions>
- `bun run build` clean.
- `bun run test` passes.
- `applyExperienceFeedback` is now a pure scoring/rotation function — its name still fits, its contract narrowed.
- Site B is structurally gone.
- The two extraction helpers in `correction-detection.ts` are deleted; the file's docblock honestly describes the narrowed surface.
</post_conditions>
