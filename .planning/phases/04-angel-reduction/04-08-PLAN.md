---
phase: 04-angel-reduction
plan: 08
type: execute
wave: 4
depends_on: [01]
files_modified:
  - src/assembly/assembler.ts
  - src/angel/heartbeat.ts
  - src/intelligence/trigger-engine.ts
  - src/intelligence/contradiction-detector.ts
  - src/intelligence/outcome-tracker.ts
  - src/embeddings/sqlite-vec-backend.ts
  - src/embeddings/embed-pipeline.ts
  - src/mcp/recall-server.ts
  - src/adapters/cc-hooks/stop.ts
autonomous: true
requirements: [AR-02]
must_haves:
  truths:
    - "Each of the 9 reader files contains the uniform legacy comment near its experience_patterns SELECT"
    - "grep -rn 'Phase 4 stopped new INSERTs' src/ returns ≥9 matches (one per reader file in the locked checklist)"
    - "No reader is re-pointed at episodic_events — read paths stay legacy per Phase 7 retirement decision"
    - "bun run build succeeds; bun run test passes; bun run vesna 18/18 PASS"
---

<objective>
Add the uniform legacy comment from CONTEXT.md item 5 to every reader of `experience_patterns` in the locked consumer table. The comment is a single source of truth: greppable via `"Phase 4 stopped new INSERTs"` returns the full surface map. Phase 7 retirement work uses this audit trail.

The comment is COMMENT-ONLY — no logic changes. No reader is re-pointed at `episodic_events` (that was Phase 3, dropped 2026-05-05). All UPDATE/DELETE/SELECT calls stay live; only INSERTs are blocked by V28.

Purpose: AR-02 deliverable. After Phase 4, when a Phase 7 contributor decides retirement direction (drop / project / keep), they need an exhaustive list of consumers. This plan produces that list as in-code comments.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-angel-reduction/04-CONTEXT.md
@.planning/phases/04-angel-reduction/04-RESEARCH.md
@src/assembly/assembler.ts
@src/angel/heartbeat.ts
@src/intelligence/trigger-engine.ts
@src/intelligence/contradiction-detector.ts
@src/intelligence/outcome-tracker.ts
@src/embeddings/sqlite-vec-backend.ts
@src/embeddings/embed-pipeline.ts
@src/mcp/recall-server.ts
@src/adapters/cc-hooks/stop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add uniform legacy comment to each reader</name>
  <files>src/assembly/assembler.ts, src/angel/heartbeat.ts, src/intelligence/trigger-engine.ts, src/intelligence/contradiction-detector.ts, src/intelligence/outcome-tracker.ts, src/embeddings/sqlite-vec-backend.ts, src/embeddings/embed-pipeline.ts, src/mcp/recall-server.ts, src/adapters/cc-hooks/stop.ts</files>
  <action>
The uniform comment text (copy-paste ready):

```
// Reads experience_patterns (pre-Phase-4 legacy table). Phase 4 stopped
// new INSERTs (V28 trigger blocks them). Phase 7 owns retirement direction
// — drop / project / keep. See .planning/reframes/2026-05-05-multi-handle-kill.md.
```

Add it directly above the relevant SELECT/UPDATE statement at each location:

1. **`src/assembly/assembler.ts`** — above the SELECT for the P1.5/P4.1 injection tier (around lines 200–248, the experience-tier reader). Comment goes above the prepared statement that reads `experience_patterns`.

2. **`src/angel/heartbeat.ts`** — TWO locations:
   - Above the pruning + auto-verification SELECT (around line 380–403; the `SELECT id FROM experience_patterns WHERE times_triggered >= 5 AND harmful_count > helpful_count`)
   - Above the retrieval-mode promotion SELECT (around line 1030–1089)

   The Site C merge loop (lines 1090–1149) was already trimmed in plan 04 with its own narrower comment; do NOT add the uniform comment there too — the Site-C-specific comment is more accurate.

3. **`src/intelligence/trigger-engine.ts`** — locate the trigger-time injection decision SELECT(s). Comment above each.

4. **`src/intelligence/contradiction-detector.ts`** — locate the contradiction SELECT. Comment above.

5. **`src/intelligence/outcome-tracker.ts`** — locate the outcome reinforcement SELECT and the score UPDATE. Comment above the SELECT (one place is sufficient — the UPDATE is adjacent).

6. **`src/embeddings/sqlite-vec-backend.ts:386–396`** — comment above the SELECT for the embedding pipeline read.

7. **`src/embeddings/embed-pipeline.ts:208`** — comment above the UPDATE on the `embedding` column.

8. **`src/mcp/recall-server.ts`** — locate the FTS5 SELECT against `experience_patterns_fts` (or against `experience_patterns` directly). Comment above.

9. **`src/adapters/cc-hooks/stop.ts:323`** — comment above the `applyExperienceFeedback` invocation site (note: the function inside survives — see plan 03; this comment marks the call site as a legacy reader).

**Process per file:**
- Read the file FIRST to locate the exact line of the SELECT/UPDATE.
- Identify the indentation of that line.
- Indent the comment to match.
- Place it on the lines immediately above (no blank line in between).

**Avoid:**
- Re-pointing any of these readers at `episodic_events` — that was Phase 3 (dropped). Reads stay legacy.
- Modifying the SQL or the surrounding logic — comment-only.
- Adding the comment to file-level docblocks — it goes at the SELECT site so it's discoverable when reading the relevant code.
- Adding the comment to UPDATE-only paths that don't INSERT (e.g., the FTS5 sync triggers in schema.ts) — those are infrastructure, not readers in the consumer sense.
- Adding it to surviving callers of `createPattern` in test fixtures — those call sites are legitimate (with the pragma); they don't need the legacy comment.
  </action>
  <verify>
1. `bun run build` succeeds.
2. `grep -rn "Phase 4 stopped new INSERTs" src/` returns at least 9 matches (one per file in the locked checklist; some files may have 2 matches if they have multiple reader sites — heartbeat.ts has 2, others have 1).
3. `grep -rn "2026-05-05-multi-handle-kill.md" src/` returns at least 11 matches (9 reader-comment sites + 2 tombstones from plan 05 + plan 04's Site-C narrower comment).
4. `bun run test` passes.
5. `bun run vesna` 18/18 PASS.
  </verify>
</task>

</tasks>

<post_conditions>
- `bun run build` clean.
- `bun run test` passes.
- `bun run vesna` 18/18 PASS.
- Greppable single-source surface map exists: `grep -rn "Phase 4 stopped new INSERTs" src/` returns the complete consumer list. Phase 7 contributors use this as the audit trail for the retirement direction decision.
- AR-02 is shipped: every reader is either re-pointed (NONE — Phase 3 was dropped) or marked legacy with explicit follow-up (ALL 9 sites).
</post_conditions>
