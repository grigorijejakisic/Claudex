---
phase: 02.1-corpus-expansion-rerun
plan: 05
type: execute
wave: 3
depends_on: [02.1-03, 02.1-04]
files_modified:
  - src/benchmark/episodic-density/results-renderer.ts
  - src/benchmark/episodic-density/aggregator-renderer.ts
  - src/benchmark/episodic-density/runner-tiered.ts
  - .planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md
  - .planning/aggregates/multi-handle.md
  - .planning/aggregates/README.md
  - src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts
  - src/tests/benchmark/episodic-density/density-language-lint.test.ts
autonomous: true
requirements: [IDX-02, IDX-04]
must_haves:
  truths:
    - "02.1-RESULTS.md is the human-readable per-phase report. It contains: decision rule cited verbatim (CONTEXT.md decision 5 binding), TWO parallel verdict tables (strict + relaxed), per-tier criterion checks with observed values, per-tier metric tables (pooled + per-corpus_origin from the three-tier scheme), per-tier latency table, per-tier audit precision joined from Plan 02.1-03's audit artifacts, per-tier corpus_origin distribution, density signal, and the bound-experience descriptions per tier"
    - "02.1-RESULTS.md uses CONTEXT.md decision 6 outcomes-language EXACTLY — descriptive of conditions, NOT thesis-claims. Forbidden phrasings (auto-linted in Task 6): 'fusion works', 'fusion doesn't work', 'the multi-handle thesis is validated/invalidated', 'Phase 3 should/should not ship', 'this proves/disproves'. Permitted phrasings: 'verdict V at tier T (≥X frames), n=N, with auto-labeler precision P/N', 'Combined with Phase 2 we now have N bound experiences with breakdown ...'"
    - "02.1-RESULTS.md does NOT pick a winning tier or compute a combined verdict — both tiers ship as parallel bound experiences (CONTEXT.md decision 2a)"
    - ".planning/aggregates/multi-handle.md is the human-readable projection over the JSON event log. Shape: question header + chronological table (one row per bound experience: phase, date, labeler, n, delta_p5, Wilson CI, latency p99 ratio, intra-project share, verdict, full conditions block) + verdict-grouping summary (counts of GREEN_LIGHT / SCOPE_DOWN / KILL / BLOCKED bound experiences) + dated interpretive paragraph PREPENDED at each phase close (CONTEXT.md decision 4b + 4c bindings)"
    - "Markdown aggregator: append-evolving interpretive history (CONTEXT.md decision 4c rule 3 binding). Each phase prepends a NEW dated section to the interpretive paragraph; prior interpretive paragraphs remain UNTOUCHED. Test (Task 5) snapshot-hashes the markdown's prior interpretive sections before runner runs; asserts byte-equal after"
    - "Markdown aggregator: density-language only (CONTEXT.md decision 4c rule 1 binding). Permitted phrasings: 'N bound experiences, M with verdict V', 'density of consistent X / mixed', 'no abstraction yet at this density of evidence', 'more measurements needed before milestone-level claim'. Forbidden phrasings: 'the thesis works/doesn't work', 'fusion is/isn't justified', any single-experience generalization, 'Phase 3 ships / does not ship'. Test (Task 6) lints the file's content for forbidden phrasings"
    - "Markdown aggregator: NO action-conditional language (CONTEXT.md decision 4c rule 2 binding). No 'if N=200 lands GREEN then Phase 3 ships'. The paragraph describes; it does not prescribe next steps"
    - ".planning/aggregates/README.md documents the aggregates/ directory pattern: one .md + one .json per empirical question; append-only event-sourced JSON; append-evolving prepended-paragraph markdown; density-language discipline; future empirical phases (Phase 5+) follow the same pattern (e.g., Phase 5's density-based abstraction will create .planning/aggregates/density-abstraction.{md,json})"
    - "02.1-RESULTS.md and .planning/aggregates/multi-handle.md ARE both written atomically by the runner (extending Plan 02.1-04's runner-tiered.ts) — not hand-authored. The renderer functions are pure and unit-tested (Tasks 1-2)"
    - "On the runner's first 2.1 run: aggregator markdown file is SEEDED with the question header, the chronological table seeded with Phase 2's row + Phase 2.1's two new rows, the verdict-grouping summary, and the FIRST dated interpretive paragraph (timestamped to the runner's ts_epoch). On subsequent re-runs: prior interpretive paragraphs remain byte-identical; the chronological table is rebuilt from the JSON event log (idempotent — same JSON entries → same rows); verdict-grouping summary recomputed; if a NEW dated interpretive paragraph would be added on re-run, the runner detects 'paragraph for date D already exists' and skips (idempotency)"
    - "Where Phase 2's published entry has the OLD two-tier corpus_origin breakdown (phase1_organic / v4_backfill — not the new three-tier scheme), the markdown aggregator faithfully renders Phase 2's row with its published shape; do NOT re-classify Phase 2's metrics under the three-tier scheme (CONTEXT.md `Deferred Ideas`: 'Re-running Phase 2's measurement with the new three-tier corpus_origin schema → operational follow-up, not part of 2.1 scope. The Phase 2 entry in the aggregator stays at the precision of 02-results.json as published')"
    - "The runner does NOT touch src/shared/constants.ts feature flag, does NOT move Vesna probes — same as Plan 02.1-04 (CONTEXT.md decision 7 binding)"
  artifacts:
    - path: "src/benchmark/episodic-density/results-renderer.ts"
      provides: "Pure renderer: TieredHarnessResult + two Verdicts + per-tier audit summary → 02.1-RESULTS.md content string"
      contains: "renderTieredResultsMarkdown"
      min_lines: 220
    - path: "src/benchmark/episodic-density/aggregator-renderer.ts"
      provides: "Pure renderer: AggregatorFile + new dated-paragraph fragment → multi-handle.md content string. Append-evolves prior dated paragraphs. Lints for forbidden phrasings"
      contains: "renderAggregatorMarkdown"
      min_lines: 200
    - path: "src/benchmark/episodic-density/runner-tiered.ts"
      provides: "Wire results-renderer into runFullPhase21Measurement (replace placeholder from Plan 02.1-04 Task 2). Wire aggregator-renderer to write multi-handle.md atomically alongside multi-handle.json"
      contains: "renderTieredResultsMarkdown"
    - path: ".planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md"
      provides: "Human-readable per-phase report with two parallel verdicts + audit precision conditions + outcomes-language discipline"
      min_lines: 120
    - path: ".planning/aggregates/multi-handle.md"
      provides: "Milestone-level human-readable aggregator: question + chronological table + verdict-grouping summary + dated interpretive paragraphs (append-evolving)"
      min_lines: 50
    - path: ".planning/aggregates/README.md"
      provides: "Aggregates/ directory pattern documentation for Phase 5+ replication"
      min_lines: 60
    - path: "src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts"
      provides: "Snapshot-hash prior interpretive paragraphs; assert byte-equal after re-run"
      min_lines: 80
    - path: "src/tests/benchmark/episodic-density/density-language-lint.test.ts"
      provides: "Lint multi-handle.md + 02.1-RESULTS.md for forbidden phrasings; allow permitted phrasings"
      min_lines: 80
  key_links:
    - from: "src/benchmark/episodic-density/runner-tiered.ts"
      to: "src/benchmark/episodic-density/results-renderer.ts"
      via: "imports renderTieredResultsMarkdown — invoked at the results-md write step"
      pattern: "renderTieredResultsMarkdown"
    - from: "src/benchmark/episodic-density/runner-tiered.ts"
      to: "src/benchmark/episodic-density/aggregator-renderer.ts"
      via: "imports renderAggregatorMarkdown — invoked after appendBoundExperiences"
      pattern: "renderAggregatorMarkdown"
---

<objective>
Author the per-phase results report (`02.1-RESULTS.md`) and the milestone-level aggregator (`.planning/aggregates/multi-handle.md` + `.planning/aggregates/README.md`) per CONTEXT.md decision 4 bindings:

1. **02.1-RESULTS.md (per-phase deliverable)**: cites the locked decision rule verbatim (CONTEXT.md decision 5), reports TWO parallel verdicts (strict + relaxed) with full per-tier conditions (n, deltas + Wilson CI, intra-project share, latency ratio, auto-labeler precision joined from Plan 02.1-03's audit), and respects CONTEXT.md decision 6 outcomes-language discipline (descriptive of conditions, NOT thesis-claims; specific forbidden phrasings auto-linted).

2. **multi-handle.md (milestone-level aggregator, NEW)**: chronological table primary (one row per bound experience: phase, date, labeler, n, delta_p5, Wilson CI, latency p99 ratio, intra-project share, verdict, conditions), verdict-grouping summary below, dated interpretive paragraph PREPENDED at each phase close. Append-evolving — each phase's paragraph survives byte-identical (CONTEXT.md decision 4c rule 3 binding). Density-language only (decision 4c rule 1). NO action-conditional language (decision 4c rule 2).

3. **aggregates/README.md (NEW pattern doc)**: documents the directory pattern so Phase 5+ can replicate. One `.md` + one `.json` per empirical question; append-only event-sourced JSON; append-evolving prepended-paragraph markdown.

The renderer functions for (1) and (2) are pure and unit-tested. The runner from Plan 02.1-04 is extended to invoke them at the appropriate ordering steps.

The dated interpretive paragraph at 2.1's close is the ONLY moment where Phase 2.1 contributes synthesis prose; it MUST stay descriptive — "N bound experiences, M with verdict V, density of consistent failure / mixed / single-green; more measurements may be needed before any milestone-level claim is warranted." NOT "the thesis works/doesn't work" / "Phase 3 ships / does not ship." Phase 3 readiness is decided at the user-approval gate by the orchestrator + user reading the aggregator, NOT by the aggregator's prose.

CONTEXT.md decision 7 binding REMAINS: NO side effects on feature flag or Vesna probes. Phase 2.1 stays at flag=false from Phase 2's verdict.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-corpus-expansion-rerun/02.1-CONTEXT.md
@.planning/phases/02.1-corpus-expansion-rerun/02.1-01-PLAN.md
@.planning/phases/02.1-corpus-expansion-rerun/02.1-02-PLAN.md
@.planning/phases/02.1-corpus-expansion-rerun/02.1-03-PLAN.md
@.planning/phases/02.1-corpus-expansion-rerun/02.1-04-PLAN.md
@.planning/phases/02-multi-modal-index-seeds-density-check/02-RESULTS.md
@.planning/phases/02-multi-modal-index-seeds-density-check/02-results.json
@src/benchmark/episodic-density/runner-tiered.ts
@src/benchmark/episodic-density/aggregator.ts
@src/benchmark/episodic-density/runner.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pure results renderer — TieredHarnessResult + 2× Verdict + 2× audit summary → 02.1-RESULTS.md content</name>
  <files>src/benchmark/episodic-density/results-renderer.ts</files>
  <action>
Create `src/benchmark/episodic-density/results-renderer.ts`:

```typescript
/**
 * Phase 2.1 — pure renderer for the per-phase RESULTS markdown.
 *
 * Outcomes-language discipline (CONTEXT.md decision 6 binding): every
 * narrative phrase is descriptive of conditions, not thesis-claims.
 * Forbidden phrasings (linted in Task 6 of this plan):
 *   - "fusion works" / "fusion doesn't work"
 *   - "the multi-handle thesis is validated/invalidated"
 *   - "Phase 3 should/should not ship"
 *   - "this proves/disproves"
 *
 * Permitted phrasings appear in the rendered conditions block:
 *   - "verdict V at tier T (≥X frames), n=N, with auto-labeler precision P/N"
 *   - "conditions={corpus_origin breakdown, audit precision per stratum, ...}"
 */

import type { TieredHarnessResult, HarnessRunResult } from './harness.js';
import type { Verdict } from './verdict.js';

export interface PerTierAuditSummary {
  tier: 'strict_3frame' | 'relaxed_2frame';
  sample_size: number;       // 20 or fewer if corpus-too-sparse
  per_stratum_precision: {
    v4_backfill?: { valid: number; sampled: number };
    phase1_organic_pre_phase2_close?: { valid: number; sampled: number };
    phase1_organic_post_phase2_close?: { valid: number; sampled: number };
  };
  tier_total_precision: { valid: number; sampled: number };
}

export function renderTieredResultsMarkdown(
  tiered: TieredHarnessResult,
  verdicts: { strict_3frame: Verdict; relaxed_2frame: Verdict },
  auditSummaries: { strict_3frame: PerTierAuditSummary | null; relaxed_2frame: PerTierAuditSummary | null },
  opts: { seed: number },
): string;
```

Markdown layout (the renderer produces this content):

```markdown
# Phase 2.1 Results: Corpus-expansion rerun (second bound measurement)

**Generated:** {ISO date from generated_at_ts_epoch}
**Harness seed:** {seed}
**Tiers:** strict_3frame, relaxed_2frame (parallel — no combined verdict per CONTEXT.md decision 2a)

---

## Decision rule (Phase 2 CONTEXT.md item 5 — REUSED VERBATIM, no goalpost shift)

> {paste verdicts.strict_3frame.decision_rule_quote verbatim — same string in relaxed; assert byte-equal across both tiers}

The same locked rule is applied INDEPENDENTLY to each tier (CONTEXT.md decision 2a + 5).

---

## Per-tier verdicts

### Strict tier (≥3 frames overlap)

**Verdict:** **{strict.kind}**
**Reasoning:** {strict.reasoning}

| # | Criterion | Threshold | Observed | Passed | Evidence |
|---|-----------|-----------|----------|--------|----------|
| 1 | Fusion improvement (max(Δp@5,Δr@10) ≥ +5pp AND CI lower ≥ 0 on the same metric) | 0.05 / CI≥0 | {c1.observed} | {c1.passed} | {c1.evidence} |
| 2 | Density signal (intra-project share ≥ 30%) | 0.30 | {c2.observed} | {c2.passed} | {c2.evidence} |
| 3 | Latency budget (p99 fused / p99 semantic < 2.0) | 2.0 | {c3.observed} | {c3.passed} | {c3.evidence} |

**Bound experience conditions:**
- n (held-out test set): {strict.harness.pairs.test}
- corpus_origin distribution (sampled events feeding the tier): see "corpus" section below
- auto-labeler precision (CONTEXT.md decision 3b descriptive): {audit.tier_total_precision.valid}/{audit.tier_total_precision.sampled} pooled; per-stratum: v4={...}, pre={...}, post={...}

### Relaxed tier (≥2 frames overlap)

**Verdict:** **{relaxed.kind}**
**Reasoning:** {relaxed.reasoning}

(same table format; same condition shape)

---

## Quality metrics — held-out test set

For each tier, three sub-tables (pooled + per-corpus_origin from the three-tier scheme):

### Strict tier

#### Pooled
| Variant | precision@5 (Wilson 95% CI) | recall@10 (Wilson 95% CI) | MRR (mean ± bootstrap CI) | n |
|---------|------------------------------|----------------------------|----------------------------|---|
| A semantic-only | ... | ... | ... | ... |
| B fingerprint-only | ... | ... | ... | ... |
| C RRF-fused (k=60) | ... | ... | ... | ... |

#### v4_backfill only
{same shape}

#### phase1_organic_pre_phase2_close only
{same shape}

#### phase1_organic_post_phase2_close only
{same shape}

#### Deltas vs A (Newcombe 95% CI)
| Comparison | Δ precision@5 (CI) | Δ recall@10 (CI) | Origin split |
|------------|--------------------|------------------|--------------|
| C - A | ... | ... | pooled |
| C - A | ... | ... | v4_backfill |
| C - A | ... | ... | phase1_organic_pre_phase2_close |
| C - A | ... | ... | phase1_organic_post_phase2_close |

### Relaxed tier
{same nested structure}

---

## Latency

For each tier:

| Variant | p50 (ms) | p95 (ms) | p99 (ms) |
|---------|----------|----------|----------|
| A | ... | ... | ... |
| B | ... | ... | ... |
| C | ... | ... | ... |

p99(C)/p99(A) per tier — strict={...}; relaxed={...} (criterion 3 threshold=2.0)

---

## Density signal (CONTEXT.md item 4)

Density is corpus-wide (same for both tiers) — single block:
- Random-pair sample size: {density.random_pair_sample_size}
- Noise floor (95th percentile): {density.noise_floor}
- σ: {density.noise_sigma}
- Cluster threshold: {density.cluster_threshold}
- Weak clusters (K=2..4): {density.cluster_count.weak_K2}
- Strong clusters (K≥5): {density.cluster_count.strong_K5}
- Intra-project share: {density.intra_project_share} (CONTEXT.md item 4 threshold=0.30)
- Density meaningful: {density.density_meaningful}

---

## Corpus

- Total fingerprinted episodes: {harness.corpus_size.total}
- v4_backfill: {harness.corpus_size.v4_backfill}
- phase1_organic_pre_phase2_close: {harness.corpus_size.phase1_organic_pre_phase2_close}
- phase1_organic_post_phase2_close: {harness.corpus_size.phase1_organic_post_phase2_close}
- Projects covered: {harness.corpus_size.projects.join(', ')}

Pair counts per tier:
- Strict (≥3 frames): {strict.pairs.total} total, {strict.pairs.test} held-out
- Relaxed (≥2 frames): {relaxed.pairs.total} total, {relaxed.pairs.test} held-out

See:
- 02.1-03-strict-audit.md (auto-labeler precision per stratum, descriptive)
- 02.1-03-relaxed-audit.md (same, relaxed tier)

---

## Bound-experience description

{Branch on (strict.kind, relaxed.kind) tuple. Use CONTEXT.md decision 6 outcomes-language. Examples:}

- Both GREEN_LIGHT: "Both tiers cleared the locked decision rule's bar at strict n=X / relaxed n=Y. Combined with Phase 2's KILL at n=20, the aggregator now contains 3 bound experiences with mixed verdicts (1 KILL, 2 GREEN_LIGHT) under different conditions. Per the parable, density at this evidence level is mixed; more measurements may be needed before any milestone-level claim is warranted."
- Both KILL: "Both tiers failed the locked decision rule. Combined with Phase 2's KILL at n=20, the aggregator contains 3 bound experiences all with KILL verdicts under different labeler-strictness and corpus-size conditions — emerging density of consistent failure. This is much stronger evidence to escalate at milestone level than any single measurement."
- Mixed (strict GREEN, relaxed KILL or vice versa): "Verdict is sensitive to labeler strictness — itself a finding about the methodology. Aggregator contains 3 bound experiences with mixed verdicts; the strict-vs-relaxed asymmetry is itself a bound-experience condition."
- Either tier BLOCKED (corpus-too-sparse sentinel): "Even {tier} labeling produced n=X<200 at this corpus size; corpus is too sparse for the held-out methodology at the requested precision. The corpus-sparsity finding is the primary outcome of this bound experience."

NO "fusion works", "fusion doesn't work", "thesis is validated/invalidated", "Phase 3 should ship". Density-language ONLY. The lint test in Task 6 enforces.

---

## Notes for the orchestrator + user (next user-approval gate)

This phase produced TWO bound experiences (strict + relaxed). Combined with Phase 2's earlier bound experience, the aggregator at `.planning/aggregates/multi-handle.json` now contains 3 entries. Whether more bound experiences are needed before considering Phase 3 (or any milestone-level conclusion) is owned by the user-approval gate, NOT by this RESULTS.md.

The aggregator's interpretive paragraph at 2.1 close describes what the density of evidence looks like at this point — see `.planning/aggregates/multi-handle.md`.
```

**Important constraints**:
- Pure function — no fs, no DB, no clock dependency. `opts.seed` is the only dynamic input besides the harness/verdict/audit blocks.
- Does NOT include any verdict synthesis logic — the renderer just formats; the verdict objects come pre-computed from `runFullPhase21Measurement`.
- The bound-experience description block at the bottom uses the (strict.kind, relaxed.kind) tuple to branch into one of the documented descriptive paragraphs — pre-written templates per outcome combination, NOT free-form generation.
- Audit summaries may be `null` (Plan 02.1-03 may not have run yet at the time the runner is first invoked, in which case the renderer omits per-stratum audit precision and the bound-experience description notes "audit pending — re-render after audit completes").

**Avoid:**
- Embedding any "winning tier" or "primary verdict" — both tiers are parallel.
- Free-form interpretation in the bound-experience description block — use the pre-written templated paragraphs branched on the verdict tuple.
- Any forbidden phrasing — Task 6's lint test will catch.
- Reading from disk — keep the renderer pure.
  </action>
  <verify>
1. `bun run build` clean.
2. Smoke unit test (write a small test in this plan's test file): renderer with a synthetic TieredHarnessResult + Verdicts produces a string containing the expected section headers; passes the density-language lint (Task 6).
  </verify>
  <done>renderTieredResultsMarkdown is pure; produces the documented Markdown layout; respects outcomes-language discipline; tested in Tasks 5/6.</done>
</task>

<task type="auto">
  <name>Task 2: Pure aggregator-markdown renderer — append-evolving dated paragraphs</name>
  <files>src/benchmark/episodic-density/aggregator-renderer.ts</files>
  <action>
Create `src/benchmark/episodic-density/aggregator-renderer.ts`:

```typescript
/**
 * Phase 2.1 — pure renderer for `.planning/aggregates/multi-handle.md`.
 *
 * Layout:
 *   1. Question header (constant from aggregator.ts).
 *   2. Chronological bound-experience table (rebuilt from JSON event log
 *      every render — idempotent).
 *   3. Verdict-grouping summary (recomputed every render).
 *   4. Append-evolving dated interpretive paragraph history. Each phase
 *      that ships PREPENDS a new dated section; prior content is byte-
 *      identical (CONTEXT.md decision 4c rule 3 binding).
 *
 * Density-language only (decision 4c rule 1):
 *   Permitted: "N bound experiences, M with verdict V"; "density of
 *     consistent X / mixed"; "no abstraction yet at this density of
 *     evidence"; "more measurements needed before milestone-level claim".
 *   Forbidden: "the thesis works/doesn't work"; "fusion is/isn't
 *     justified"; any single-experience generalization; "Phase 3 ships /
 *     does not ship".
 *
 * NO action-conditional language (decision 4c rule 2): no "if N=200 lands
 * GREEN then Phase 3 ships". The paragraph describes; it does not
 * prescribe.
 */

import type { AggregatorFile, BoundExperience } from './aggregator.js';

export interface NewParagraphFragment {
  /** ISO date (YYYY-MM-DD) — the heading of the new dated section. */
  date: string;
  /** The phase this paragraph closes (e.g. '2.1'). */
  phase_closing: string;
  /** Density-language descriptive prose. Multiple paragraphs allowed. */
  body: string;
}

/**
 * Render the full aggregator markdown.
 *
 * - `aggregator`: the loaded `.planning/aggregates/multi-handle.json` (post-
 *   append from Plan 02.1-04).
 * - `priorMarkdown`: the existing markdown file's content (or empty string
 *   if it doesn't exist). Used to extract prior dated interpretive
 *   paragraphs verbatim and prepend the new one above them.
 * - `newParagraph`: the new dated paragraph fragment to prepend. If null,
 *   re-renders without adding a new paragraph (idempotent re-runs).
 */
export function renderAggregatorMarkdown(
  aggregator: AggregatorFile,
  priorMarkdown: string,
  newParagraph: NewParagraphFragment | null,
): string;

/**
 * Extract the prior dated interpretive paragraphs from the existing
 * markdown file. The interpretive paragraphs live below a `## Interpretive
 * History` heading, each as `### {date} — phase {phase_closing} close`
 * sections. Returns the prior history string verbatim. If no `##
 * Interpretive History` heading exists (first run), returns empty string.
 */
export function extractPriorInterpretiveHistory(priorMarkdown: string): string;

/**
 * Pre-written density-language paragraph templates branched on
 * (verdict-grouping summary, current phase) — used by the runner to build
 * NewParagraphFragment.body without free-form generation.
 *
 * Templates use the same outcome-tuple branching as the per-phase
 * RESULTS.md bound-experience description (Task 1), but at the milestone
 * level (talks about TOTAL bound experiences across phases, not just the
 * closing phase's verdicts).
 */
export function pickDensityLanguageTemplate(
  aggregator: AggregatorFile,
  closing_phase: string,
): string;
```

Markdown layout produced:

```markdown
# Multi-handle aggregator

**Question:** does multi-handle retrieval (semantic + non-semantic) improve recall over semantic-only at our scale?

**Bound experiences:** {N total} (rebuilt from `.planning/aggregates/multi-handle.json` event log)

---

## Chronological table

| Phase | Date | Labeler | n | Δp@5 (Wilson CI) | Δr@10 (Wilson CI) | latency p99 ratio | intra-project share | Verdict | Conditions |
|-------|------|---------|---|------------------|-------------------|-------------------|---------------------|---------|------------|
| 2 | 2026-05-04 | strict_3frame | 20 | +0.10 [-0.16, 0.38] | -0.05 [-0.27, 0.17] | 0.89 | 0.234 | KILL | 02-RESULTS.md (audit pre-2.1: not stratified) |
| 2.1-strict | {date} | strict_3frame | {n} | ... | ... | ... | ... | {verdict} | 02.1-RESULTS.md, 02.1-03-strict-audit.md |
| 2.1-relaxed | {date} | relaxed_2frame | {n} | ... | ... | ... | ... | {verdict} | 02.1-RESULTS.md, 02.1-03-relaxed-audit.md |

(rows added at the bottom by future empirical phases; never modified.)

---

## Verdict-grouping summary

- GREEN_LIGHT: {count}
- SCOPE_DOWN: {count}
- KILL: {count}
- BLOCKED: {count}

Total bound experiences: {N}.

---

## Interpretive History

(Each phase that closes prepends a new dated section. Prior content is preserved byte-identical per CONTEXT.md decision 4c rule 3.)

### {date_2.1} — phase 2.1 close

{Density-language paragraph from pickDensityLanguageTemplate. Examples:
 - "At 2.1 close, the aggregator contains 3 bound experiences. Verdict-grouping: 1 KILL (Phase 2), {strict.kind} (2.1 strict), {relaxed.kind} (2.1 relaxed). Density at this evidence level is {consistent failure / consistent success / mixed}. More measurements may be needed before any milestone-level claim is warranted."
 - "The strict-vs-relaxed asymmetry within Phase 2.1 is itself a bound-experience condition: verdict is sensitive to labeler strictness."
 - "Phase 2.1 strict-tier audit precision was P/N stratified; relaxed-tier audit precision was P/N. These descriptive conditions accompany the verdicts in the bound-experience entries."}

(future dated sections from Phase 2.2, Phase 5, etc., would prepend HERE — above this 2.1 section, never below.)
```

**Append-evolving discipline (decision 4c rule 3)**:

The renderer's prepend logic:
1. Extract prior dated sections from `priorMarkdown` via `extractPriorInterpretiveHistory`.
2. If `newParagraph` is null (idempotent re-run): keep the prior history verbatim; the rest of the document (header, table, summary) is rebuilt fresh from the JSON.
3. If `newParagraph` is provided (new phase close): check whether a `### {date} — phase {phase_closing} close` heading already exists in priorMarkdown. If yes, treat as idempotent re-run (skip the prepend; the paragraph is already there). If no, render the new section ABOVE all prior dated sections under `## Interpretive History`.

The function does NOT modify any prior dated section — it only prepends. The append-only test in Task 5 enforces.

**Density-language template selection** (`pickDensityLanguageTemplate`):
- Compute verdict-grouping counts from `aggregator.bound_experiences`.
- Branch on the count tuple (GREEN_LIGHT count, SCOPE_DOWN count, KILL count, BLOCKED count). Each branch returns a pre-written density-language paragraph string.
- ALL branches use permitted phrasings only. The lint test in Task 6 enforces.

**Avoid:**
- Free-form prose generation — use templates.
- Mutating prior dated sections in any way — only prepend.
- Including thesis-level phrasings — density-language only.
- Embedding any "Phase 3 ships / does not ship" branch — action-conditional language is forbidden (decision 4c rule 2).
  </action>
  <verify>
1. `bun run build` clean.
2. Tests in Tasks 5 + 6 pass.
  </verify>
  <done>aggregator-renderer.ts exports renderAggregatorMarkdown (pure) + extractPriorInterpretiveHistory (pure) + pickDensityLanguageTemplate (pure); append-evolving prepend semantics correct; density-language template branched on verdict-grouping counts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire renderers into runner-tiered.ts (replacing Plan 02.1-04's placeholder MD write + adding aggregator-md write)</name>
  <files>src/benchmark/episodic-density/runner-tiered.ts</files>
  <action>
Edit `src/benchmark/episodic-density/runner-tiered.ts`:

1. Replace the placeholder 02.1-RESULTS.md writer (Plan 02.1-04 Task 2 step 4) with a call to `renderTieredResultsMarkdown(tiered, verdicts, auditSummaries, { seed })`.

2. Compute `auditSummaries` by reading the per-tier audit markdown files (Plan 02.1-03's outputs) at fixed paths:
   - `.planning/phases/02.1-corpus-expansion-rerun/02.1-03-strict-audit.md`
   - `.planning/phases/02.1-corpus-expansion-rerun/02.1-03-relaxed-audit.md`

   Parse the precision figures from the markdown's "Per-stratum precision" table at the bottom (the table the auditor agent fills in). If a file doesn't exist (audit hasn't been run yet), pass `null` for that tier's audit summary; the renderer omits per-stratum audit precision and notes "audit pending."

3. After the aggregator JSON append step (Plan 02.1-04 Task 2 step 5), add:
   - Read existing `.planning/aggregates/multi-handle.md` if it exists (else empty string).
   - Build `NewParagraphFragment`: date = today (YYYY-MM-DD from `ts_epoch`), phase_closing = '2.1', body = `pickDensityLanguageTemplate(aggregator, '2.1')`.
   - If a section heading `### {date} — phase 2.1 close` already exists in the prior markdown (re-run case), skip; pass null instead.
   - Render: `renderAggregatorMarkdown(aggregator, priorMarkdown, newParagraph)`.
   - Atomic write to `.planning/aggregates/multi-handle.md`.

4. ORDERING: aggregator-md write happens AFTER aggregator-json append. Ordering test (in Task 4 of Plan 02.1-04, extended here) is updated to assert the aggregator-md write also lands after the json append.

5. The runner-tiered.ts ordering remains: harness → 2× verdict → 02.1-results.json write → 02.1-RESULTS.md write → multi-handle.json append → multi-handle.md write. The test is updated to assert all five writes in this order.

**Avoid:**
- Reading audit MD files synchronously in a way that fails the runner if they're missing — graceful null fallback.
- Re-writing prior dated sections of multi-handle.md — append-evolving only.
- Touching feature flags or probe locations — decision 7 binding remains.
  </action>
  <verify>
1. `bun run build` clean.
2. Plan 02.1-04's existing tests still pass (extended for the aggregator-md ordering).
3. Manual: `measure-tiered` against operator DB → produces 02.1-RESULTS.md (full content, not placeholder) + multi-handle.md alongside multi-handle.json.
  </verify>
  <done>Runner wires renderers; ordering preserved; audit summaries joined when audit MDs exist; aggregator-md write is the LAST write step.</done>
</task>

<task type="auto">
  <name>Task 4: Author aggregates/README.md — pattern documentation for Phase 5+</name>
  <files>.planning/aggregates/README.md</files>
  <action>
Hand-author `.planning/aggregates/README.md`. Content:

```markdown
# .planning/aggregates/

Milestone-level event-sourced aggregators for empirical questions.

This directory aggregates bound-experience evidence across multiple phases. The pattern was introduced in Phase 2.1 (commit-pending-2.1-close) and is intended to be replicated by future empirical phases.

## Pattern

Each empirical question that produces multiple bound experiences across phases gets:

- `<question-slug>.json` — strictly append-only event-sourced log of bound-experience entries.
- `<question-slug>.md` — human-readable projection over the JSON: chronological table + verdict-grouping summary + append-evolving dated interpretive paragraphs.

Current files:

- `multi-handle.json` + `multi-handle.md` — does multi-handle retrieval (semantic + non-semantic) improve recall over semantic-only at our scale?

Anticipated future files (per ROADMAP.md):

- `density-abstraction.json` + `density-abstraction.md` — Phase 5 empirical work: does retrieval-time density-clustering subsume `experience_patterns` injection at our scale?

## JSON event log discipline

```json
{
  "schema_version": 1,
  "question": "...",
  "bound_experiences": [
    {"phase": "...", "labeler": "...", "date": "...", "n": ..., "verdict": "...", "conditions": {...}, "metrics": {...}}
  ]
}
```

Rules:
- **Strictly append-only.** Existing entries are NEVER modified. Each phase appends exactly the entries it produced.
- **No `winning` / `current_consensus` / `primary` / `combined` key at any level.** Aggregate IS the array; consumers iterate.
- **Schema is forward-compatible**: adding new fields to `conditions` or `metrics` per-entry is fine; renaming/removing fields breaks the parable invariant (history must remain readable as written).
- **Idempotent re-runs**: append callers (currently `src/benchmark/episodic-density/aggregator.ts`'s `appendBoundExperiences`) must skip entries whose `(phase, labeler)` tuple already exists.

## Markdown projection discipline

The markdown file is regenerated from the JSON every render, EXCEPT for the dated interpretive paragraphs under `## Interpretive History`.

Rules:
- **Density-language only.** Permitted: "N bound experiences, M with verdict V"; "density of consistent X / mixed"; "no abstraction yet at this density of evidence"; "more measurements needed before milestone-level claim". Forbidden: "the thesis works/doesn't work"; "fusion is/isn't justified"; any single-experience generalization; "Phase 3 ships / does not ship".
- **No action-conditional language.** No "if N=200 lands GREEN then Phase 3 ships". The paragraph describes; it does not prescribe.
- **Append-evolving interpretive history.** Each phase that closes PREPENDS a new dated section; prior content remains byte-identical. The history of how synthesis evolved is itself a bound experience.

The lint test at `src/tests/benchmark/episodic-density/density-language-lint.test.ts` enforces the forbidden-phrasing rule on the markdown file.

## Why this pattern, not "RESULTS-thesis.md"

Per the v5 framing doc (`.planning/research/2026-05-04-v5-bound-episodes-framing.md`), abstraction emerges from density of bound experiences, not from any single experience. Naming a file `RESULTS-thesis.md` implies a pass/fail framing the parable rejects — `aggregates/` is parable-aligned and the directory pattern is reusable.

The aggregator markdown's interpretive history is the human-readable projection of bound-experience density over time. Each new dated section is itself a bound experience (the synthesis state at that point in time); prior dated sections record how density looked previously. This matches the framing doc's "raw transcripts append-only; synthesis is a projection" principle applied to our engineering process.

## How a future empirical phase replicates this pattern

1. Pick a slug for the question (kebab-case, descriptive, e.g. `density-abstraction`).
2. In the phase's renderer module (analogous to `src/benchmark/episodic-density/aggregator.ts`):
   - Define the BoundExperience shape for this question's domain.
   - Implement `appendBoundExperiences(newEntries, opts)` with the same idempotent + append-only semantics.
3. In the phase's results renderer (analogous to `src/benchmark/episodic-density/aggregator-renderer.ts`):
   - Implement `renderAggregatorMarkdown(aggregator, priorMarkdown, newParagraph)`.
   - Implement `pickDensityLanguageTemplate` with question-domain-specific templates (still respecting density-language and no-action-conditional rules).
4. The phase runner appends + re-renders at close; the lint test guards forbidden phrasings.

A possible future extension is a generic GSD primitive (`gsd-tools.cjs aggregate-append <slug> <entry-json>`) — captured in CONTEXT.md decision `Deferred Ideas`. Until then, each empirical phase implements its own thin renderer module.
```

This is hand-authored, NOT runner-generated.

**Avoid:**
- Including any specific tied-to-multi-handle prose — keep the README pattern-focused so Phase 5 can copy.
- Implying that aggregates/ files are gating — they're descriptive milestone-level evidence; gating happens at user-approval gates.
- Writing forbidden phrasings even in the README — keep the example phrasings within permitted set.
  </action>
  <verify>
1. The file exists at `.planning/aggregates/README.md` with the documented content.
2. Run the density-language lint test (Task 6) against the README too — passes (no forbidden phrasings).
  </verify>
  <done>aggregates/ pattern documented for Phase 5+ replication; density-language compliant.</done>
</task>

<task type="auto">
  <name>Task 5: Append-evolving markdown test — prior interpretive paragraphs are byte-identical after re-render</name>
  <files>src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts</files>
  <action>
Create `src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts`:

1. **Empty-prior + new paragraph (first render)**:
   - Pass `priorMarkdown=''`, `aggregator` with 3 entries, `newParagraph={date: '2026-05-04', phase_closing: '2.1', body: '...'}`.
   - Assert rendered output contains `## Interpretive History` heading + `### 2026-05-04 — phase 2.1 close` heading + the body verbatim.
   - Assert chronological table has 3 rows (one per bound experience).
   - Assert verdict-grouping summary computed from the 3 entries' verdicts.

2. **Prior history preserved byte-identical (CONTEXT.md decision 4c rule 3)**:
   - Pre-existing `priorMarkdown` containing a `### 2026-05-01 — phase 2 close\n\n{prior body}` section. SHA-256 the prior dated section block.
   - Pass `aggregator` with 4 entries (added Phase 2.1 entries on top of Phase 2's prior single entry), and `newParagraph={date: '2026-05-04', phase_closing: '2.1', body: '...new body...'}`.
   - Assert rendered output contains BOTH the new section (date 2026-05-04) AND the old section (date 2026-05-01) under `## Interpretive History`.
   - Assert the OLD dated section block (extracted from the rendered output) is byte-identical to the pre-snapshot. (Same hash.)
   - Assert the NEW dated section appears ABOVE the old one (chronological reverse: newest first), per the prepend semantics.

3. **Idempotent re-render (newParagraph=null)**:
   - Same setup as case 2 but `newParagraph=null`.
   - Assert prior dated section is byte-identical (no changes).
   - Assert chronological table is regenerated from the same JSON → same content.
   - Assert no new dated section was added.

4. **Idempotent re-render (newParagraph for already-existing date+phase)**:
   - priorMarkdown contains `### 2026-05-04 — phase 2.1 close\n\n{prior 2.1 body}`.
   - Pass `newParagraph={date: '2026-05-04', phase_closing: '2.1', body: 'different body'}` (re-run on same date).
   - Caller logic in runner-tiered.ts (Task 3) detects the heading already exists and passes `null` instead. The renderer's contract: if `newParagraph` is non-null but the heading already exists in priorMarkdown, take whichever path is documented (likely: skip the prepend; behavior is the same as if newParagraph were null). Test asserts that path.

5. **Chronological table rebuilt from JSON every render** (idempotency on table):
   - priorMarkdown's table has stale data (e.g. wrong delta_p5 for Phase 2's row).
   - Render with current aggregator. Assert table in rendered output reflects aggregator's CURRENT data (not the stale priorMarkdown). The chronological table is REGENERATED, not preserved — it's a projection over the JSON event log.
   - This contrasts with the dated interpretive paragraphs which ARE preserved byte-identical.

**Avoid:**
- Tests that depend on the live `.planning/aggregates/multi-handle.md` — use synthetic `priorMarkdown` strings.
- Snapshotting the entire rendered output — assert specific invariants.
  </action>
  <verify>
1. `bun run test src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts` — full green.
2. `bun run test` — full suite green.
  </verify>
  <done>5 cases covering: first render, prior history byte-equality, idempotent re-render, repeat-date detection, table regeneration discipline. CONTEXT.md decision 4c rule 3 mechanically enforced.</done>
</task>

<task type="auto">
  <name>Task 6: Density-language lint test — forbidden phrasings rejected; permitted phrasings accepted</name>
  <files>src/tests/benchmark/episodic-density/density-language-lint.test.ts</files>
  <action>
Create `src/tests/benchmark/episodic-density/density-language-lint.test.ts`:

1. Define the forbidden phrasings (case-insensitive substring matches; CONTEXT.md decision 4c rule 1 + decision 6):
   - `"the thesis works"` / `"the thesis doesn't work"` / `"thesis is validated"` / `"thesis is invalidated"` / `"thesis is dead"`
   - `"fusion works"` / `"fusion doesn't work"` / `"fusion is justified"` / `"fusion isn't justified"`
   - `"this proves"` / `"this disproves"`
   - `"Phase 3 ships"` / `"Phase 3 does not ship"` / `"Phase 3 should ship"` / `"Phase 3 shouldn't ship"`
   - `"if N=200 lands GREEN then"` (action-conditional pattern; relaxed glob check)

2. Define permitted-phrasing samples (the test asserts these pass the lint):
   - `"N bound experiences, M with verdict V"`
   - `"density of consistent failure"`
   - `"no abstraction yet at this density of evidence"`
   - `"more measurements may be needed before any milestone-level claim"`
   - `"verdict V at tier T (≥X frames), n=N, with auto-labeler precision P/N"`

3. Implement a `lintForbiddenPhrasings(markdown: string): { ok: boolean; violations: string[] }` helper (test-internal — does not need to be exported as production code).

4. Test cases:
   - **Forbidden phrasing detection**: for each forbidden phrasing, build a synthetic markdown containing it; assert `lintForbiddenPhrasings` returns `ok: false` with the violation.
   - **Permitted phrasing acceptance**: build a markdown using only permitted phrasings; assert `lintForbiddenPhrasings` returns `ok: true`.
   - **Production-file lint**: read the actual rendered `.planning/aggregates/multi-handle.md` (after the runner has produced it — the test depends on Task 3 having been run; otherwise create a fixture by calling `renderAggregatorMarkdown` with a representative aggregator + paragraph). Assert `lintForbiddenPhrasings(content).ok === true`.
   - **Production-file lint**: same against `.planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md`. Assert clean.
   - **Production-file lint**: same against `.planning/aggregates/README.md` (Task 4 hand-authored). Assert clean.
   - **Production-file lint**: same against `.planning/phases/02.1-corpus-expansion-rerun/02.1-03-strict-audit.md` and the relaxed counterpart (Plan 02.1-03's outputs after auditor-agent fill-in). Assert clean.

5. **Whitelist for the decision rule's verbatim quote**: the `decision_rule_quote` text from Phase 2 CONTEXT.md item 5 contains `"GREEN-LIGHT Phase 3"` and `"Phase 3 plan is rewritten"` etc. — the lint must ALLOW the verbatim decision-rule quote (because it's part of the locked rule, not interpretive prose). Implement: skip lint inside `<blockquote>` blocks (or markdown `>` lines). Test: a markdown file containing the decision_rule_quote inside a blockquote passes; the same content outside a blockquote fails. (The verbatim rule is the FOUNDATION the bindings protect; the bindings forbid USING the rule's outcome words OUTSIDE the verbatim quote in interpretive prose.)

6. **Per-RESULTS-md-section discipline**: the bound-experience description block at the bottom of 02.1-RESULTS.md (Task 1) MUST pass the lint. The pre-written templated paragraphs are clean by construction; the lint is the regression gate.

**Avoid:**
- Over-strict regex that rejects benign phrasings ("works" alone is fine; "fusion works" specifically is not).
- Skipping the blockquote whitelist — the decision rule's verbatim quote must be allowed wherever it appears verbatim.
- Skipping the production-file lint — the test value is in catching regressions in the rendered files, not just synthetic ones.
  </action>
  <verify>
1. `bun run test src/tests/benchmark/episodic-density/density-language-lint.test.ts` — full green.
2. `bun run test` — full suite green.
  </verify>
  <done>Lint test mechanically enforces CONTEXT.md decision 4c rule 1 + decision 6 outcomes-language discipline; production .md files pass; blockquote whitelist correctly handles the locked decision rule's verbatim quote.</done>
</task>

<task type="auto">
  <name>Task 7: Final close-out run — produce 02.1-RESULTS.md (full content), multi-handle.md (with first dated paragraph), commit per-tier audit if not already</name>
  <files>.planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md, .planning/aggregates/multi-handle.md</files>
  <action>
After Tasks 1–6 are complete and tests are green:

1. Confirm the inputs are in place:
   - Plan 02.1-01 backfill ran (per-tier corpus_origin assigned).
   - Plan 02.1-02 dual-tier harness wired.
   - Plan 02.1-03 auditor-agent has filled in `02.1-03-{strict,relaxed}-audit.md` with per-pair judgments + per-stratum + tier-total precision.
   - Plan 02.1-04 runner-tiered ran once + appended to multi-handle.json.

2. Re-run `bun run src/benchmark/episodic-density/cli.ts measure-tiered --seed 42`. (Idempotent on the JSON aggregator from Plan 02.1-04; this run renders the full markdown for both files.)

3. Verify outputs:
   - `02.1-RESULTS.md` contains: full decision rule verbatim block, two parallel verdict tables, per-tier criterion checks, per-tier metric tables (pooled + 3-way corpus_origin), per-tier latency, per-tier audit precision joined from the audit MDs, density signal, corpus block, bound-experience description.
   - `multi-handle.md` contains: question header, chronological table (Phase 2 row + Phase 2.1 strict row + Phase 2.1 relaxed row), verdict-grouping summary, `## Interpretive History` heading, ONE dated section `### {today} — phase 2.1 close` with the templated density-language paragraph.
   - `aggregates/README.md` exists.

4. Run the lint test (Task 6) one more time as a final gate:
   ```bash
   bun run test src/tests/benchmark/episodic-density/density-language-lint.test.ts
   ```
   Green.

5. Run the full episodic-density test suite:
   ```bash
   bun run test src/tests/benchmark/episodic-density/
   ```
   Green.

6. Run the full project test suite:
   ```bash
   bun run test
   ```
   Green.

7. Run Vesna probe gate:
   ```bash
   bun run vesna
   ```
   17 probes (unchanged from Phase 2's KILL — probes still in `.disabled/` per CONTEXT.md decision 7).

8. Commit. Suggested commit message follows the project's pattern:
   ```
   phase(02.1): close — corpus-expansion rerun (second bound measurement)

   Two parallel bound experiences:
   - strict_3frame: verdict={kind}, n={n}, ...
   - relaxed_2frame: verdict={kind}, n={n}, ...

   Aggregator at .planning/aggregates/multi-handle.{md,json} now contains
   3 bound experiences (Phase 2 + 2.1 strict + 2.1 relaxed). Interpretive
   paragraph (descriptive) at 2.1 close added; prior content unmodified.

   Vesna remains at 17 probes (CONTEXT.md decision 7 — probes stay in
   .disabled/ until aggregator density of consistent green-light warrants).
   src/shared/constants.ts feature flag remains false from Phase 2.
   ```

**Avoid:**
- Editing the rendered MDs by hand — re-run the runner if the content needs changing; the renderers are pure.
- Modifying the verdict in 02.1-results.json post-hoc — moving goalposts is the named failure mode.
- Touching the feature flag or probes — CONTEXT.md decision 7 binding.
  </action>
  <verify>
1. All five test invocations above (lint, episodic-density, full project, vesna) green.
2. `02.1-RESULTS.md` contains the full layout per Task 1's template.
3. `multi-handle.md` contains the full layout per Task 2's template + ONE dated section under interpretive history (the 2.1 close paragraph).
4. `aggregates/README.md` exists.
5. `02.1-results.json` and `multi-handle.json` are unchanged from the Plan 02.1-04 run (or, if re-run added the same entries idempotently, byte-identical at the entry level).
6. Vesna probe count: 17.
7. `src/shared/constants.ts` `error_fingerprint` flag: still `false` from Phase 2's KILL.
  </verify>
  <done>Phase 2.1 closes: per-phase RESULTS.md + milestone aggregator MD + aggregates README all on disk; density-language compliant; Vesna unchanged; flag unchanged; ready for user-approval gate.</done>
</task>

</tasks>

<verification>
- `bun run build` clean.
- `bun run test src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts` — green; CONTEXT.md decision 4c rule 3 enforced.
- `bun run test src/tests/benchmark/episodic-density/density-language-lint.test.ts` — green; outcomes-language discipline enforced.
- `bun run test` — full suite green.
- `bun run vesna` — 17 probes green (CONTEXT.md decision 7 unchanged).
- `02.1-RESULTS.md` contains the decision rule verbatim block (`grep -F '+5pp on either precision@5 OR recall@10'`) and both tiers' criterion check tables.
- `.planning/aggregates/multi-handle.md` contains the question header, chronological table, verdict-grouping summary, and dated 2.1 interpretive paragraph; passes the lint.
- `.planning/aggregates/README.md` documents the directory pattern; passes the lint.
- `src/shared/constants.ts` `error_fingerprint` flag unchanged (CONTEXT.md decision 7).
</verification>

<success_criteria>
- [ ] 02.1-RESULTS.md authored: decision rule verbatim, two parallel verdicts, per-tier criterion + metric + latency + audit precision tables, bound-experience description in CONTEXT.md decision 6 outcomes-language.
- [ ] multi-handle.md authored: question header + chronological bound-experience table + verdict-grouping summary + ONE dated interpretive paragraph (the 2.1 close paragraph) under `## Interpretive History`.
- [ ] aggregates/README.md authored documenting the pattern for Phase 5+ replication.
- [ ] Append-evolving discipline mechanically enforced: prior dated paragraphs are byte-identical across re-renders (CONTEXT.md decision 4c rule 3).
- [ ] Density-language discipline mechanically enforced via lint test (CONTEXT.md decision 4c rule 1 + decision 6).
- [ ] No action-conditional language in any rendered file (CONTEXT.md decision 4c rule 2).
- [ ] No "winning tier" / "primary verdict" / "combined verdict" anywhere in 02.1-RESULTS.md or multi-handle.md.
- [ ] Phase 2's published `02-results.json` is unchanged on disk (append-only across the entire phase's deliverables).
- [ ] Vesna remains at 17 probes (CONTEXT.md decision 7).
- [ ] `src/shared/constants.ts` `error_fingerprint` flag remains false from Phase 2 (CONTEXT.md decision 7).
- [ ] IDX-02 (auto-pair-labeling correctness) and IDX-04 (density at scale) addressed via the dual-tier conditions reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-corpus-expansion-rerun/02.1-05-SUMMARY.md` documenting:
- Both tier verdicts (kind + n + headline metrics) — copied from 02.1-RESULTS.md.
- Aggregator state at close: total bound experiences (3), verdict-grouping count, dated interpretive paragraph headline.
- Whether either tier hit corpus-too-sparse sentinel and how it was rendered.
- Confirmation: feature flag unchanged + probes unchanged + Phase 2's results JSON unchanged.
- Pointer to the next user-approval gate: orchestrator + user read `.planning/aggregates/multi-handle.md` to decide whether more bound experiences are needed before considering Phase 3 (per CONTEXT.md decision 6 / decision 7).
</output>
</content>
</invoke>