# Phase 2.1 Plans 04 + 05 — Verification

**Verifier:** gsd-plan-checker (goal-backward)
**Scope:** Plans 02.1-04 (verdict runner + binding tests), 02.1-05 (renderers + density-language lint + append-evolving discipline).
**Sources read:** 02.1-CONTEXT.md, both plans, src/benchmark/episodic-density/verdict.ts, 02-results.json, 02.1-02-PLAN.md.

## Verdict: PASS WITH NOTES

Four CONTEXT bindings are mechanically un-leakable as planned. Density-language lint is sound in concept but the blockquote whitelist needs explicit pseudocode and one spy disambiguation is owed. None are blockers.

## Bindings — sound

**1. Ordering (02.1-04 Task 4).** verdict.ts exports computeVerdict AND blockedVerdict as separate top-level symbols (verdict.ts:122, :206). Total verdict-module calls = 2 even on the BLOCKED-tier path: one computeVerdict + one blockedVerdict. The runner's computePerTierVerdict is an or-not-and dispatch, so the discipline holds. fs.writeFileSync spy captures actual paths because atomicWrite goes writeFileSync(.tmp) → renameSync — both default node:fs symbols the spy intercepts. No-flag-mutation and no-probe-mutation assertions (paths contain src/shared/constants.ts / vesna/probes/) are sound.

**2. Schema (02.1-04 Task 5).** Depth-first walk for combined/winning/primary at every level catches a key buried at depth 3+ (Task 5 step 2 explicit). Object.keys(verdicts).sort() === ['relaxed_2frame','strict_3frame'] is exact two-key. decision_rule_quote byte-equality holds because DECISION_RULE_QUOTE is a single module-level const (verdict.ts:81); both computeVerdict and blockedVerdict emit it. Phase 2's 02-results.json was generated from the same const → byte-equal by construction.

**3. Append-only (02.1-04 Task 6).** Plan specifies "JSON-serialized version of each entry"; pre- and post-snapshot must use identical JSON.stringify args (NOTE 1). (phase, labeler) tuple uniqueness drives idempotency; new entries push to tail. Atomic-write leak case is self-flagged in the plan (NOTE 2).

**4. Decision rule verbatim (02.1-04 Tasks 2+5).** verdict.ts is in files_modified of NO plan in 02.1 — confirmed unchanged. Test asserts byte-equal across Phase 2 and Phase 2.1 verdicts via the same const. Editing the const breaks all tests — desired regression behavior.

## Density-language lint (02.1-05 Task 6) — sound but underspecified

DECISION_RULE_QUOTE contains "Phase 3 plan is rewritten" (verdict.ts:96), a forbidden phrasing that must be ALLOWED inside `>` blockquotes (where the verbatim rule appears) and REJECTED outside.

- `> 5pp delta` (greater-than comparison): line starts with `>` but CommonMark treats as blockquote. Not a leak path.
- Multi-line blockquote with lazy-continuation lines (non-blank, no `>` prefix): per CommonMark these STAY in blockquote-mode. A naive per-line starts-with-`>` check FALSE-POSITIVE flags forbidden phrasings on lazy-continuation lines (NOTE 3).
- Fenced code blocks: lint plan does NOT mention skipping them; renderer code samples could trip the lint (NOTE 4).

Append-evolving (02.1-05 Task 5): snapshot-hash + byte-equality on re-render. Sound. Section extraction by `### {date} — phase {phase} close` heading is robust.

## Notes (resolve before execute, not blockers)

1. (02.1-04 Task 6) Hash canonicalization: sha256(JSON.stringify(entry)) with NO spacing arg, identical pre/post.
2. (02.1-04 Task 6 step 5) Pick one .tmp.PID-on-failure semantic. Recommend try/finally cleanup of .tmp on rename throw.
3. (02.1-05 Task 6) Pseudocode blockquote detection as a state machine: `>` enters blockquote; blank line exits; non-blank non-`>` continuation STAYS (CommonMark lazy-continuation).
4. (02.1-05 Task 6) Add fenced-code-block skip (lines between matching ``` fences excluded), same as blockquote skip.
5. (02.1-04 Task 4 step 1) Disambiguate strict-vs-relaxed in spy call records: assert held_out_test_n differs OR add a tier discriminant; pin in the test fixture.

## What the plans, executed faithfully, deliver

computeVerdict + blockedVerdict total = 2 per runner. 02.1-results.json verdicts has exactly {strict_3frame, relaxed_2frame}; zero combined/winning/primary at any depth. Phase 2's seeded aggregator entry byte-identical before/after; idempotent re-run is a no-op. decision_rule_quote byte-equal across Phase 2 and Phase 2.1. Density-language discipline enforced across 02.1-RESULTS.md, multi-handle.md, aggregates/README.md, both audit MDs; pending Notes 3+4 the lint cannot be silently bypassed.

Recommendation: address Notes 1-5 inline in the plan files (one-line clarifications), then execute.