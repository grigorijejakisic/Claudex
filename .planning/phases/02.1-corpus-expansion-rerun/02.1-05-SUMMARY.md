---
phase: 02.1-corpus-expansion-rerun
plan: 05
subsystem: episodic-density-benchmark
tags: [empirical, results-renderer, aggregator-renderer, density-language-lint]
requires: [02.1-03, 02.1-04]
provides:
  - "src/benchmark/episodic-density/results-renderer.ts — pure renderer for 02.1-RESULTS.md"
  - "src/benchmark/episodic-density/aggregator-renderer.ts — pure renderer for multi-handle.md with append-evolving interpretive history"
  - ".planning/aggregates/README.md — aggregates/ pattern documentation for Phase 5+ replication"
  - "Density-language lint test (CommonMark blockquote + fenced-code state machine)"
  - "Append-evolving markdown test (prior dated sections survive byte-identical)"
  - "02.1-RESULTS.md (full content with audit precision joined)"
  - "multi-handle.md (chronological table + verdict-grouping summary + dated 2.1 close paragraph)"
affects:
  - "src/benchmark/episodic-density/results-renderer.ts (new)"
  - "src/benchmark/episodic-density/aggregator-renderer.ts (new)"
  - "src/benchmark/episodic-density/runner-tiered.ts (renderers wired; audit MD parsing; aggregator MD step)"
  - "src/tests/benchmark/episodic-density/density-language-lint.test.ts (new)"
  - "src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts (new)"
  - ".planning/aggregates/README.md (new)"
  - ".planning/aggregates/multi-handle.md (new)"
  - ".planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md (full content)"
tech-stack:
  added: []
  patterns:
    - "CommonMark-aware lint state machine: blockquote (`>` enters; blank line exits; lazy-continuation stays); fenced code (```` ``` ```` toggle)"
    - "Append-evolving markdown: chronological table regenerated from JSON every render; interpretive history sections preserved byte-identical"
    - "Verdict-tuple-driven bound-experience description: pre-written templates per (strict.kind, relaxed.kind) outcome, no free-form generation"
patterns:
  - "Pure renderer + DB-attached runner + DB-attached audit MD parser, with separable concerns"
key-files:
  created:
    - "src/benchmark/episodic-density/results-renderer.ts"
    - "src/benchmark/episodic-density/aggregator-renderer.ts"
    - "src/tests/benchmark/episodic-density/density-language-lint.test.ts"
    - "src/tests/benchmark/episodic-density/aggregator-markdown-append.test.ts"
    - ".planning/aggregates/README.md"
    - ".planning/aggregates/multi-handle.md"
  modified:
    - "src/benchmark/episodic-density/runner-tiered.ts (Step 4 placeholder replaced by real renderer; audit MD parsing added; Step 6 aggregator MD write added)"
    - ".planning/phases/02.1-corpus-expansion-rerun/02.1-RESULTS.md (re-rendered with full content)"
    - ".planning/aggregates/multi-handle.json (audit precision now attached to 2.1-strict + 2.1-relaxed conditions)"
key-decisions:
  - decision: "Density-language template selection branches on the verdict-grouping count tuple of the FULL aggregator (not just the closing phase)"
    rationale: "CONTEXT.md decision 4c rule 3 binding: the aggregator's interpretive paragraph describes density across all bound experiences, not just the closing phase. Branching on full-aggregator counts (e.g. 'all 3 bound experiences are KILL — emerging consistent failure') keeps the synthesis honest."
  - decision: "parseAuditSummary anchors to '## Per-stratum precision' section start to disambiguate the TWO `| v4_backfill | ...` rows in the audit MD"
    rationale: "The stratification-table row at the top has shape `population/allocation/sampled` and the precision-table row at the bottom has shape `valid/sampled/precision`. A naive regex matches the first occurrence — wrong values. Section-anchored parsing is robust."
  - decision: "Aggregator markdown is the LAST write step (Step 6, after Step 5's JSON append)"
    rationale: "CONTEXT.md decision 4c rule 3 binding (append-evolving): the markdown is rebuilt from the JSON every render. The JSON must be durably on disk before the markdown render reads it. Step 5 -> Step 6 ordering captured by the runner-tiered.ts source order."
  - decision: "Bound-experience description is pre-written templated paragraphs branched on (strict.kind, relaxed.kind) tuple, NOT free-form generation"
    rationale: "CONTEXT.md decision 6 outcomes-language discipline. Pre-written templates are mechanically clean (no risk of forbidden phrasings) and the lint test catches any regression."
requirements-completed: [IDX-02, IDX-04]
duration: "18 min"
completed: "2026-05-05"
---

# Phase 2.1 Plan 05: Renderers + density-language lint + close

CONTEXT.md decision 4 (per-phase RESULTS + milestone aggregator with append-evolving interpretive history) and decision 6 (outcomes-language discipline) implemented. The phase closes with both `02.1-RESULTS.md` and `.planning/aggregates/multi-handle.md` rendered fully and lint-clean.

**Duration:** 18 min  **Completed:** 2026-05-05  **Tasks:** 7  **Files:** 6 new + 3 modified

## What changed

- **`results-renderer.ts`** — pure renderer for `02.1-RESULTS.md`. Layout: title + decision rule (verbatim blockquote) + per-tier section (verdict + criterion checks + bound-experience conditions + 4 metric tables: pooled, v4_backfill, phase1_organic_pre_phase2_close, phase1_organic_post_phase2_close + deltas + latency) + density signal (corpus-wide) + corpus block + bound-experience description (templated paragraph) + notes for orchestrator. The bound-experience description block branches on the (strict.kind, relaxed.kind) tuple — pre-written templates per outcome combination, no free-form prose.
- **`aggregator-renderer.ts`** — pure renderer for `.planning/aggregates/multi-handle.md`.
  - Layout: question header + chronological table (regenerated from JSON every render) + verdict-grouping summary + `## Interpretive History` heading + new dated section + prior dated sections preserved byte-identical.
  - `pickDensityLanguageTemplate(aggregator, closing_phase)`: branches on verdict-grouping counts of the FULL aggregator. Permitted phrasings only. CONTEXT.md decision 4c rules 1+2+3 enforced.
  - `extractPriorInterpretiveHistory(priorMarkdown)`: extracts everything below the `## Interpretive History` heading verbatim. Empty string if heading missing.
- **`runner-tiered.ts`** — Plan 02.1-04's placeholder MD writer replaced with `renderTieredResultsMarkdown` call. Audit MD parsing (`parseAuditSummary`) joins per-tier audit precision into the renderer + into the aggregator entry's `conditions.auto_labeler_precision`. Step 6 (aggregator MD write) added as the LAST write step.
- **`density-language-lint.test.ts`** — 12 tests covering forbidden-phrasing detection, blockquote whitelist (CommonMark lazy-continuation handled by state machine), fenced-code-block whitelist, and lint runs over actual rendered files (multi-handle.md, 02.1-RESULTS.md, aggregates/README.md, audit MDs).
- **`aggregator-markdown-append.test.ts`** — 5 tests covering first render, byte-identical prior history, idempotent re-render with newParagraph=null, idempotent re-render on already-existing date+phase, chronological table regenerated from JSON.

## Live close-out output

```
Strict (≥3 frames):  KILL  (n=20, delta_p5=+0.10 [CI -0.157, +0.376], intra=0.2418, p99r=0.83, audit=16/20)
Relaxed (≥2 frames): KILL  (n=19, delta_p5=+0.21 [CI -0.033, +0.491], intra=0.2418, p99r=1.31, audit=14/20)
Aggregator entries appended: 3 (Phase 2 seed + 2.1-strict + 2.1-relaxed)
```

The aggregator at `.planning/aggregates/multi-handle.json` contains 3 bound experiences, all KILL. The interpretive paragraph at 2.1 close (rendered into `multi-handle.md`):

> At phase 2.1 close, the aggregator contains 3 bound experiences (3 KILL). Phase 2.1 contributed: 2.1-strict=KILL, 2.1-relaxed=KILL. Density at this evidence level is consistent failure across conditions. More measurements may be needed before any milestone-level claim is warranted; emerging density of consistent KILL is much stronger evidence to escalate at milestone level than any single measurement.

## Verification

- `bun run build` — clean.
- `bun run test src/tests/benchmark/episodic-density/` — 121/121 pass (104 prior + 12 lint + 5 append-evolving).
- `bun run vesna` — 17 probes 100% pass (CONTEXT.md decision 7 binding: probes unchanged).
- `src/shared/constants.ts` content unchanged (CONTEXT.md decision 7 binding).
- `02.1-RESULTS.md` lint-clean; `multi-handle.md` lint-clean; `aggregates/README.md` lint-clean; both audit MDs lint-clean.

## CONTEXT.md decision 7 binding (single GREEN does not ship probes; all artifacts unchanged at 02.1 close)

- Vesna probes still in `src/benchmark/vesna/probes/.disabled/` (`episodic-fingerprint-fires.json`, `fusion-non-regression.json`).
- `src/shared/constants.ts` unchanged byte-for-byte.
- Phase 2's published `02-results.json` and `02-RESULTS.md` unchanged byte-for-byte (append-only across the entire phase).

## Issues Encountered

- Initial parseAuditSummary regex matched the WRONG `| v4_backfill | ... |` row (the stratification table's `population/allocation/sampled` row instead of the `## Per-stratum precision` table's `valid/sampled/precision` row). Fixed by anchoring parsing to the section start. Caught by visual inspection of the rendered RESULTS.md (line `auto-labeler precision: ...; per-stratum: v4_backfill=191/20`).

## Next

Phase 2.1 is closed. The aggregator at `.planning/aggregates/multi-handle.{md,json}` is the operator + user's read at the next user-approval gate. Per CONTEXT.md decision 6, NO milestone-level conclusion is owed by this phase; whether more bound experiences are needed before considering Phase 3 is decided at the user-approval gate.
