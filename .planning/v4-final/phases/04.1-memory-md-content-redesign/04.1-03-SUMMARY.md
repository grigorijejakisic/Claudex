---
phase: 04.1-memory-md-content-redesign
plan: "03"
subsystem: angel/memory-md-writer
tags: [writer, marker-bug, CUR-13, line-anchored-matcher]
requires: []
provides: [findUserTailStart helper, line-anchored CUR-13 fix]
affects: [src/angel/memory-md-writer.ts]
tech-stack:
  added: []
  patterns: [line-anchored matcher; LF-normalized fast-path comparison]
key-files:
  created:
    - src/tests/angel/memory-md-writer-marker-bug.test.ts
  modified:
    - src/angel/memory-md-writer.ts
key-decisions:
  - "findUserTailStart uses strict line equality (no whitespace tolerance)"
  - "Normalize CRLF to LF for offset arithmetic AND for fast-path idempotency comparison"
  - "Refusal path (sentinel_missing) preserved for marker-without-sentinel state"
requirements-completed:
  - CUR-13
duration: 20 min
completed: 2026-04-29
---

# Phase 04.1 Plan 03: Line-anchored marker matcher Summary

`findUserTailStart` helper added to `memory-md-writer.ts`; replaces the buggy `existing.indexOf('<!-- USER EDITABLE -->')` substring match that caused the duplicate-marker regression visible at production CLAUDEXv3 MEMORY.md. Idempotency fast-path also normalized for LF/CRLF differences.

## Files

**Created:**
- `src/tests/angel/memory-md-writer-marker-bug.test.ts` (9 tests, ~190 lines)

**Modified:**
- `src/angel/memory-md-writer.ts` — added `findUserTailStart` export, replaced indexOf call site, normalized fast-path comparison

## Test count

**9 new tests, 43 total memory-md-writer green:**

`findUserTailStart` unit tests (6):
1. Returns -1 for content with no marker
2. Returns offset of clean marker line
3. Does NOT match substring inside body text (CUR-13 bug case)
4. Matches a real line even when prior content mentions the marker as substring
5. Rejects marker line with trailing whitespace
6. Handles CRLF input correctly

`curateMemoryMd` integration tests (3):
7. Literal corrupted CLAUDEXv3 fixture → exactly one marker line, User Notes preserved at correct offset
8. sentinel_missing refusal still fires (marker present but no sentinel on line 1)
9. Fresh-file idempotent fast-path produces idempotent_noop on second call

## Live-fire note

Production CLAUDEXv3 MEMORY.md not yet repaired by this fix. The fix takes effect on the next Angel heartbeat after deploy that touches the writer. Once deployed:
- Heartbeat detects existing file with sentinel + line-anchored marker (now line ~38 of the corrupted file)
- Treats content above marker as managed (will be replaced); content below marker as user tail (preserved)
- The duplicated `## How to Query` block (which was above the real marker but below the substring marker) gets removed during managed-section regeneration
- Result: a single marker line, no duplicated headers

## Tasks Completed

| # | Task | Files | Commit |
|---|------|-------|--------|
| 1 | Add findUserTailStart helper + use in curateMemoryMd | memory-md-writer.ts | 4668aa3 |
| 2 | Regression test against literal corrupted CLAUDEXv3 fixture | memory-md-writer-marker-bug.test.ts | 4668aa3 |

## Verification

- `bun run build` passes
- `bun run test src/tests/angel/memory-md-writer*.test.ts` — 43 tests pass (34 existing + 9 new)
- The corrupted-fixture test inverts the failure when the indexOf revert is simulated (substring-vs-line semantics is the only difference)

## Deviations from Plan

None. Plan 03 executed as written.

**Total deviations: 0.**

## Issues Encountered

None.

## Next Phase Readiness

**Ready for Wave 2** (Plans 04 and 06). Plan 04 (lesson types/writer/reader) depends on Plan 01 only; Plan 06 (user-content migration fixtures) depends on Plan 03 only.

## Self-Check: PASSED

- 1 commit on master: `git log --oneline --grep="04.1-03"` → 4668aa3
- File created on disk: `ls src/tests/angel/memory-md-writer-marker-bug.test.ts` → exists
- Build passes; all 43 memory-md-writer tests pass
