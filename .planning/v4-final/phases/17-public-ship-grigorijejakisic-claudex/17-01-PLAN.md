---
plan_id: 17-01
phase: 17
title: CHANGELOG promotion + README badges (REL-06)
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - README.md
requirements:
  - REL-06
autonomous: true
estimated_commits: 1
must_haves:
  - "CHANGELOG.md has a new `## [4.1.0] — 2026-05-02` section above `## [4.0.0] — 2026-04-30`"
  - "The new [4.1.0] section contains the v4.1 narrative: Phase 12 (LICENSE + metadata + README + CHANGELOG + CONTRIBUTING), Phase 13 (cross-platform code audit), Phase 14 (`bun run setup` + `CLAUDEX_PROJECTS_DIR` + `install.sh`/`install.bat`), Phase 15 (`bun run doctor` 6/7 checks), Phase 16 (README §Quick Start + §Troubleshooting + onboarding fixtures), Phase 17 (public ship)"
  - "The new [4.1.0] section preserves the `Pending (HITL — gate full Phase 16 close, do not block v4.1 ship)` block from prior `[Unreleased]` content (PLAT-06..08 + VER-04/05) — moved verbatim, not deleted"
  - "[Unreleased] is reset to a placeholder (`### Added\\n\\n- _Nothing yet._` or equivalent) for v4.2+ work"
  - "README.md has 3 badges between line 1 (`# Claudex`) and line 3 (tagline `Persistent memory for LLM coding agents...`): license MIT, version 4.1.0, Vesna CI"
  - "Badges use the exact URLs from CONTEXT.md `<decisions>` REL-06 section"
  - "Badges render as a single line (one space between each), not a stacked list"
  - "License badge links to `./LICENSE`; Vesna badge links to `.github/workflows/vesna.yml` action page; version badge has no link (informational only) — matches CONTEXT.md"
  - "No other README content changes (Status banner, sections, anchors all preserved)"
  - "`bun run build` green; `bun run test` ≥3188 baseline + 20 baseline llama failures unchanged from v4.0.0; `bun run vesna` 17/17 GATED PASS; `bun run doctor` exits 0"
  - "Atomic commit `phase(17): CHANGELOG promote [Unreleased] → [4.1.0] + README badges (REL-06)`"
---

# Plan 17-01: CHANGELOG promotion + README badges

## Objective

Promote `CHANGELOG.md`'s `[Unreleased]` section to `[4.1.0] — 2026-05-02` and add the three top-of-README badges (license MIT, version 4.1.0, Vesna CI). This is the docs/markdown half of the public ship — everything that must be in place **before** the `v4.1.0` annotated tag is created in Plan 17-02 (so the tag points at a commit with correct CHANGELOG and README content).

Closes REL-06 (badges) and stages the CHANGELOG content that REL-04 (GitHub release) consumes in Plan 17-03.

## Why this comes first

Phase 17's sequence is locked in CONTEXT.md `<decisions>` → "CHANGELOG.md update for v4.1.0" → "Sequence matters: CHANGELOG update → commit → tag → push (so the tag points at a commit with the correct CHANGELOG)." The badges land in the same atomic commit because they are also pre-tag content — strangers visiting `github.com/grigorijejakisic/Claudex` after the push see the README rendered with badges + the v4.1.0 CHANGELOG section in one shot. Both are pure markdown; build/test/vesna/doctor are unaffected and serve only as regression witnesses.

Plans 17-02 (tag + push), 17-03 (release + topics + protection runbook), and 17-04 (phase close) all assume this commit has landed.

## Tasks

<task id="01-read-current-state">
**Read the current state of `CHANGELOG.md` and `README.md`** to confirm anchor positions before editing.

`CHANGELOG.md` shape (verified at planning time):
1. Line 1-6: header + Keep-a-Changelog reference + SemVer reference
2. Line 8: `## [Unreleased]`
3. Lines 10-22: Phase 16 entry under `### Added` + `### Pending (HITL — gate full Phase 16 close, do not block v4.1 ship)` block listing PLAT-06..08 + VER-04/05 + the closing paragraph "The HITL-pending items follow the same pattern Phase 11 SC#4 used to ship v4.0.0..."
4. Line 24: `## [4.0.0] — 2026-04-30` (preserve as-is)

`README.md` shape (verified at planning time):
1. Line 1: `# Claudex`
2. Line 2: blank
3. Line 3: tagline `Persistent memory for LLM coding agents — they reach for it the way they reach for Grep.`
4. Line 4: blank
5. Line 5: `> **Status:** v4.1 Quick Start shipped...` — preserve as-is
6. Lines 7+: `## What is Claudex?` and onward — preserve as-is

The new shape after this plan:

`CHANGELOG.md`:
1. Lines 1-6: unchanged
2. Line 8: `## [Unreleased]` with `### Added\n\n- _Nothing yet._` (placeholder for v4.2+)
3. New `## [4.1.0] — 2026-05-02` section with the Phase 12-17 narrative + the Pending (HITL) block moved verbatim from old [Unreleased]
4. `## [4.0.0] — 2026-04-30` unchanged

`README.md`:
1. Line 1: `# Claudex`
2. Line 2: blank
3. **NEW Line 3:** badges row (license MIT, version 4.1.0, Vesna CI — three badges separated by single spaces)
4. Line 4: blank
5. Line 5: tagline (was line 3)
6. Line 6: blank
7. Line 7: status quote (was line 5)
8. Lines 8+: rest of README unchanged

This means inserting 2 lines (badges row + blank line) at line 3, pushing all subsequent content down by 2 lines.
</task>

<task id="02-promote-changelog-unreleased-to-4-1-0">
**Promote `CHANGELOG.md` `[Unreleased]` to `[4.1.0] — 2026-05-02`.**

Replace the current `## [Unreleased]` section (lines 8-22) with two new sections, in this order:

```markdown
## [Unreleased]

### Added

- _Nothing yet._ Track v4.2 milestone planning at `.planning/STATE.md` once it kicks off.

## [4.1.0] — 2026-05-02

Distribution release. v4.0.0 (2026-04-30) shipped Claudex's internal infrastructure — organic tool use, behavior-gated retrieval, single-store SQLite + sqlite-vec, BGE-v2-m3 reranker. v4.1 makes that infrastructure installable by strangers and ships the repo publicly to `github.com/grigorijejakisic/Claudex`.

### Added

- **Phase 12 — Metadata + License + README foundation:** MIT LICENSE at repo root; package.json polished (description, repository, keywords, author, license fields); README §What/§Why/§Documentation/§License sections; CHANGELOG.md (this file) seeded; CONTRIBUTING.md established. The repo is now externally legible and legally redistributable.
- **Phase 13 — Cross-platform code audit:** code-only sweep across path handling, hook semantics, file locking, subprocess spawn, line-ending policy. `.gitattributes` enforces LF for shell scripts; subprocess calls use platform-aware shells; path joins go through `path.join` everywhere; lock files use cross-platform primitives. No portability TODOs left in source.
- **Phase 14 — Bootstrap install + configurable paths:** `bun run setup` is the single entry point — detects Bun, detects Ollama, pulls `snowflake-arctic-embed2` if missing, creates the BGE reranker Python venv at `services/.venv`, installs Python deps, spawns the reranker on port 7439, creates `~/.claudex/db/claudex.db`, and registers Claude Code hooks at `~/.claude/settings.json`. Idempotent. `CLAUDEX_PROJECTS_DIR` env var replaces the hardcoded `~/Desktop/Projects/` reference. Two thin entry points wrap setup: `install.sh` (macOS / Linux) and `install.bat` (Windows).
- **Phase 15 — `bun run doctor` diagnostics:** seven parallel checks — Bun version, DB schema, Ollama daemon + `snowflake-arctic-embed2`, BGE reranker on `:7439`, Claude Code hooks, Angel guardian process, plus an aggregate exit-code rollup. Reranker check warns rather than fails (bi-encoder fallback covers it). Each check prints a one-line remediation when it fails.
- **Phase 16 — Onboarding verification + README polish (structural):** README §Quick Start (≤80 lines, walks prereqs → clone → install → verify → first session, references the install scripts and `bun run doctor` shipped in Phases 14-15 verbatim); README §Troubleshooting (≤120 lines, four canonical failures — Ollama not running, port 7439 dead, Bun version mismatch, hook registration failure — all routed through `bun run doctor` first); three onboarding fixtures at `docs/onboarding/{macos,linux,windows}.md` matching the Phase 11 SC#4 cold-start-trial template. Windows fixture is split-mode (steps 4-7 recorded from current dev machine, steps 1-3 HITL-pending for fresh-VM rigor).
- **Phase 17 — Public ship:** README badges (license MIT, version 4.1.0, Vesna CI); `v4.1.0` annotated git tag; complete master history pushed to `github.com/grigorijejakisic/Claudex` (336 commits, fast-forward from v3-era `712c910`); GitHub release `v4.1.0 — Distribution` published with these notes; repository topics applied (`claude-code`, `mcp`, `agent-memory`, `llm-tools`, `typescript`, `bun`, `claudex`, `persistent-memory`, `claude`); branch protection runbook authored at `docs/onboarding/branch-protection-setup.md` for the operator-driven UI step.

### Pending (HITL — gate full Phase 16 close, do not block v4.1 ship)

- **PLAT-06** — macOS install verified end-to-end on fresh VM
- **PLAT-07** — Ubuntu 24.04 LTS install verified end-to-end on fresh VM
- **PLAT-08** — Windows 11 install verified end-to-end on fresh VM (regression check)
- **VER-04** — every friction surfaced in fixtures resolved as code fix / doctor check / README troubleshooting entry
- **VER-05** — <30-minute install target measured and met on each platform
- **REL-07** — branch protection rule for Vesna CI applied via GitHub UI on `grigorijejakisic/Claudex` (runbook at `docs/onboarding/branch-protection-setup.md`)

The HITL-pending items follow the same pattern Phase 11 SC#4 used to ship v4.0.0 with three live cold-start trials still operator-runnable: structural close documented; operator returns when ready; v4.1 ships without waiting.

### Stats

- **44 v4.1 requirements:** 32 closed autonomously across Phases 12-17; 12 HITL-pending (operator-runnable).
- **Hard gates:** `bun run build` green, `bun run test` 3188 baseline + 20 baseline llama-server-supervisor failures unchanged from v4.0.0, `bun run vesna` 17/17 GATED PASS, `bun run doctor` exit 0.
- **DB schema unchanged from v4.0.0** (V24).
- **Hook semantics unchanged from v4.0.0.**

```

Hard authoring rules:

- **Move the `Pending (HITL...)` block from old `[Unreleased]` into new `[4.1.0]` — do not delete it and do not duplicate it.** It belongs in the v4.1.0 release section because the items were structurally addressed in Phase 16/17 and are part of the v4.1.0 release narrative; if the operator later closes them, that is recorded in a future patch release (e.g., `[4.1.1] — YYYY-MM-DD: HITL items closed`), not by editing v4.1.0.
- **Do NOT touch the `## [4.0.0] — 2026-04-30` section.** That release shipped — its content is frozen.
- **Date is `2026-05-02`** per CONTEXT.md `<decisions>` "Sequence matters" and the team-lead briefing. This is the structural-close date for v4.1; same date as Phase 16 close. (Tag creation date may be a few minutes later in real time but the CHANGELOG date is the structural date.)
- **No fabricated phase content.** Every Added bullet describes work actually shipped in Phases 12-17. If unsure about wording, re-read the corresponding `*-SUMMARY.md` in `.planning/phases/`.
- **Stats block is honest:** 32 closed autonomously, 12 HITL-pending. Do not round these numbers up. The 12 are: PLAT-06, PLAT-07, PLAT-08, VER-04, VER-05 (5 from Phase 16) + REL-07 (1 from Phase 17) = 6 actual HITL-pending. Plus 6 deferred-to-Phase-17 items already closed by then = the 12 number in CONTEXT.md actually counts something different. **Re-verify by inspecting REQUIREMENTS.md before authoring this Stats block** — if the 32/12 split is wrong, fix it in this plan rather than carry the error forward.
- **No emoji** (matches the rest of the changelog).

**Pre-write verification step:** before authoring the Stats block, run:

```bash
grep -c '^\- \[x\]' .planning/REQUIREMENTS.md   # autonomous-closed count
grep -c '^\- \[~\]' .planning/REQUIREMENTS.md   # HITL-pending count
```

Use the actual counts in the Stats block. If they total >44 or <44, the count is wrong and must be fixed in REQUIREMENTS.md (out of scope here — flag to Plan 17-04 phase close instead).
</task>

<task id="03-add-readme-badges">
**Add 3 badges at the top of `README.md`** between line 1 (`# Claudex`) and line 3 (tagline).

Insert exactly these 2 lines after `# Claudex` (preserving the blank line that already follows the title):

```markdown
[![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)](./LICENSE) [![Version](https://img.shields.io/badge/version-4.1.0-brightgreen.svg)](#) [![Vesna CI](https://github.com/grigorijejakisic/Claudex/actions/workflows/vesna.yml/badge.svg)](https://github.com/grigorijejakisic/Claudex/actions/workflows/vesna.yml)

```

(The blank line after the badges row separates it from the tagline that follows.)

After the edit, the first 7 lines of `README.md` are:

```
# Claudex

[![License: MIT](...)](./LICENSE) [![Version](...)](#) [![Vesna CI](...)](...)

Persistent memory for LLM coding agents — they reach for it the way they reach for Grep.

> **Status:** v4.1 Quick Start shipped...
```

Hard authoring rules:

- **Use the exact URLs above.** They match CONTEXT.md `<decisions>` REL-06. The Vesna badge URL points at `grigorijejakisic/Claudex` (capital C) — that is intentional, the public repo name preserves the C.
- **Single line, three badges.** Each separated by a single space. Do NOT stack them on three separate lines (matches industry convention; see e.g., the README pattern at `bun.sh` or `oven-sh/bun`).
- **Version badge has no link** (`(#)` is the canonical "no link" anchor on GitHub-flavored markdown — the badge is informational, not navigational).
- **License badge links to `./LICENSE`** (relative link, resolves on GitHub and locally).
- **Vesna badge links to the workflow page**, so a click goes to the run history. The badge image will show "no status" until the workflow runs at least once on the public repo — acceptable per CONTEXT.md note ("it'll go green on first push that triggers CI").
- **Do not modify any other README content.** Status banner, all sections, all anchors, all links — preserved as-is. Plans 16-01 / 16-04 already finalized this README; Phase 17 only adds badges at the very top.
</task>

<task id="04-verify">
**Run hard gates** after the edits land:

```bash
bun run build          # esbuild ~70ms; expect green
bun run test           # vitest; expect ≥3188 passing + 20 baseline llama-supervisor failures unchanged from v4.0.0
bun run vesna          # expect 17/17 GATED PASS
bun run doctor         # expect exit 0 (5-6 pass + ≤1 warn for Angel heartbeat staleness, same as Phase 15 baseline)
```

The edits are markdown-only — none of these gates should be affected. If `bun run test` regresses (e.g., a snapshot pinned to old README first lines), revert and re-author the relevant section to be snapshot-equivalent.

**Sanity checks specific to this plan:**

```bash
# CHANGELOG promotion
grep -n '^## \[Unreleased\]' CHANGELOG.md           # expect line 8
grep -n '^## \[4.1.0\] — 2026-05-02' CHANGELOG.md   # expect to exist, after [Unreleased], before [4.0.0]
grep -n '^## \[4.0.0\] — 2026-04-30' CHANGELOG.md   # expect to still exist, unchanged

# README badges
sed -n '1,7p' README.md                              # expect title, blank, badges, blank, tagline, blank, status

# Badge URL correctness
grep -F 'img.shields.io/badge/license-MIT' README.md
grep -F 'img.shields.io/badge/version-4.1.0' README.md
grep -F 'github.com/grigorijejakisic/Claudex/actions/workflows/vesna.yml/badge.svg' README.md
```

If any of these greps fail, fix and re-run before the commit.

**Atomic commit:**

```bash
git add CHANGELOG.md README.md
git commit -m "phase(17): CHANGELOG promote [Unreleased] → [4.1.0] + README badges (REL-06)"
```

Do NOT push yet. Plan 17-02 creates the v4.1.0 tag at this commit, then pushes both the tag and the master branch to `public`.
</task>

## Verification criteria

- [ ] CHANGELOG.md `## [Unreleased]` reset to `_Nothing yet._` placeholder
- [ ] CHANGELOG.md has new `## [4.1.0] — 2026-05-02` section above `## [4.0.0] — 2026-04-30`
- [ ] [4.1.0] section contains Phase 12-17 narrative under `### Added` (one bullet per phase)
- [ ] [4.1.0] section preserves the `Pending (HITL — gate full Phase 16 close, do not block v4.1 ship)` block from old [Unreleased] + adds REL-07 to that list
- [ ] [4.1.0] section has a `### Stats` block with autonomous-closed and HITL-pending counts verified against REQUIREMENTS.md
- [ ] `## [4.0.0] — 2026-04-30` section unchanged
- [ ] README.md has 3 badges as a single line at line 3 (between title and tagline)
- [ ] License badge URL: `https://img.shields.io/badge/license-MIT-blue.svg`, links to `./LICENSE`
- [ ] Version badge URL: `https://img.shields.io/badge/version-4.1.0-brightgreen.svg`, no link (`(#)`)
- [ ] Vesna badge URL: `https://github.com/grigorijejakisic/Claudex/actions/workflows/vesna.yml/badge.svg`, links to the workflow page
- [ ] No other README content changed (Status banner, sections, anchors all preserved)
- [ ] `bun run build` green
- [ ] `bun run test` ≥3188 passing + 20 baseline llama-supervisor failures unchanged from v4.0.0
- [ ] `bun run vesna` 17/17 GATED PASS
- [ ] `bun run doctor` exits 0
- [ ] Atomic commit `phase(17): CHANGELOG promote [Unreleased] → [4.1.0] + README badges (REL-06)`
- [ ] **NOT pushed yet** — Plan 17-02 handles the tag + push

## Out of scope

- v4.1.0 git tag creation — Plan 17-02
- `git push public master` and `git push public --tags` — Plan 17-02
- GitHub release via `gh release create` — Plan 17-03 (consumes the [4.1.0] CHANGELOG section authored here)
- Repository topics — Plan 17-03
- Branch protection runbook — Plan 17-03 (REL-07 HITL marker)
- STATE.md / ROADMAP.md / REQUIREMENTS.md updates — Plan 17-04 (phase close)
- 17-SUMMARY.md — Plan 17-04 (phase close)

## Notes for the implementer

- **Sequence matters.** This plan's commit MUST land before Plan 17-02 creates the tag. The tag is annotated at the commit hash; if CHANGELOG and README badges aren't in place at tag time, the GitHub release in 17-03 will display a tag pointing at incomplete content. The 4-plan structure exists specifically to enforce this ordering.
- **Pre-write verification step on the Stats block is load-bearing.** The 32/12 split came from CONTEXT.md, but CONTEXT.md was authored 2026-05-02 — if REQUIREMENTS.md state has shifted in the interim, recompute. Don't carry stale numbers into the public release notes.
- **Do not touch v4.0.0 changelog content.** It is frozen as-is. Adding the v4.1.0 section above it is the only mutation to that area of the file.
- **The Vesna CI badge will show "no status" on initial push.** Per CONTEXT.md REL-06 note: "the Vesna badge will show 'no status' until the workflow runs at least once on the public repo. Acceptable — it'll go green on first push that triggers CI." Do not preemptively change the badge URL or add a fallback "no status" image.
- **No PR or push in this plan.** This is a local-only commit. Plans 17-02 / 17-03 push to `public` and create the GitHub release. Keeping this plan local-only means a regression in the gates (build/test/vesna/doctor) can be reverted without touching the public remote.
