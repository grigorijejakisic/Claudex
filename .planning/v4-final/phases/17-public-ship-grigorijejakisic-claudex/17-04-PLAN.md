---
plan_id: 17-04
phase: 17
title: Phase 17 close + final v4.1 milestone close
wave: 4
depends_on: [17-01, 17-02, 17-03]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - REL-05
  - REL-06
  - REL-07
autonomous: true
estimated_commits: 1
must_haves:
  - "REQUIREMENTS.md: REL-01..06 marked [x] (autonomous deliverables shipped); REL-07 marked [~] HITL-pending with reference to docs/onboarding/branch-protection-setup.md"
  - "REQUIREMENTS.md traceability table: REL-01..06 status flipped to Done; REL-07 status to `Pending (HITL — operator runs UI step; runbook at docs/onboarding/branch-protection-setup.md)`"
  - "ROADMAP.md Phase 17 row: marked [x] with completion date 2026-05-02"
  - "ROADMAP.md milestone status: v4.1 SHIPPED at v4.1.0 — 2026-05-02; next milestone v4.2 (TBD)"
  - "STATE.md current focus updated: v4.1 SHIPPED at v4.1.0; Phase 17 [x]; next milestone v4.2 (TBD)"
  - "STATE.md last activity updated to 2026-05-02 with description of public ship"
  - "17-SUMMARY.md created at .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md following the 14-SUMMARY.md / 15-SUMMARY.md / 16-SUMMARY.md template"
  - "17-SUMMARY.md captures: what shipped (REL-01..06), what's HITL-pending (REL-07 + carry-forward Phase 16 HITLs), hard-gate evidence, and explicit note that v4.1 is now publicly shipped"
  - "Hard gates re-run final time and recorded in 17-SUMMARY.md: bun run build green, bun run test ≥3188 + 20 baseline llama unchanged from v4.0.0, bun run vesna 17/17 GATED PASS, bun run doctor exits 0, DB schema unchanged, hook semantics unchanged"
  - "Phase-close commit follows phase(17): convention; commit message: `phase(17): close — v4.1 SHIPPED to grigorijejakisic/Claudex (REL-01..06 [x], REL-07 [~] HITL)`"
  - "Phase-close commit pushed to BOTH remotes: `git push origin master` (Corleanus) AND `git push public master` (grigorijejakisic) — both succeed as fast-forward"
  - "After push, `git ls-remote origin refs/heads/master` and `git ls-remote public refs/heads/master` BOTH return the same hash as local HEAD"
---

# Plan 17-04: Phase 17 close + final v4.1 milestone close

## Objective

Close Phase 17 structurally and close the v4.1 Distribution milestone overall. The autonomous deliverables (REL-01 through REL-06) flip to `[x]`; REL-07 stays `[~]` HITL-pending with the runbook reference. STATE / ROADMAP / REQUIREMENTS reflect "v4.1 SHIPPED at v4.1.0 — 2026-05-02; next milestone v4.2 (TBD)." The 17-SUMMARY.md records what shipped, what's HITL, and final hard-gate evidence.

The final commit lands in `phase(17):` convention and is pushed to BOTH remotes — `origin` (Corleanus/CLAUDEXv3) and `public` (grigorijejakisic/Claudex). After this plan, v4.1 is shipped publicly; subsequent commits flow normally to both remotes.

This is the bookkeeping wave. No new code, no new content beyond the SUMMARY itself. Closes REL-01..07 in REQUIREMENTS.md (with REL-07 as `[~]` HITL).

## Why this comes last

Plan 17-04 references all three prior plans' state:

- Plan 17-01's CHANGELOG + badges shipped (referenced in 17-SUMMARY.md)
- Plan 17-02's tag + push to public landed (REL-02 + REL-03 are now mark-able [x])
- Plan 17-03's release + topics applied OR fallback runbook authored (REL-04 + REL-05 + REL-07 status)

Authoring this plan before the prior plans land would mean drafting a SUMMARY of work that hasn't shipped — fabrication risk identical to 16-04's pattern. Wave 4 also re-runs the hard gates one final time as the v4.1-milestone-close witness.

## Tasks

<task id="01-update-requirements-md">
**Update `.planning/REQUIREMENTS.md`** in two places.

**(a) Top-level checklist (REL-01..07 entries):** flip the autonomous-closed deliverables to `[x]` and mark REL-07 as `[~]` HITL.

```markdown
- [x] **REL-01**: Public GitHub remote configured pointing to `github.com/grigorijejakisic/claudex`
- [x] **REL-02**: Initial push includes complete master history + all tags (v4.0.0 + v4.1.0)
- [x] **REL-03**: `v4.1.0` annotated git tag created and pushed
- [x] **REL-04**: GitHub release for v4.1.0 published with notes derived from `CHANGELOG.md`
- [x] **REL-05**: Repository topics set on GitHub for discoverability (final list: `claude-code`, `mcp`, `agent-memory`, `llm-tools`, `typescript`, `bun`, `claudex`, `persistent-memory`, `claude`)
- [x] **REL-06**: README badges (license, version, build status) display correctly on GitHub
- [~] **REL-07**: Branch protection rule for Vesna CI applied via GitHub UI (manual step from Phase 10 carry-forward) _(HITL — runbook at docs/onboarding/branch-protection-setup.md; operator runs the UI step post-ship)_
```

**Note on REL-04 + REL-05 if Plan 17-03 hit a permission issue:** if the autonomous run via Corleanus failed and the operator runs the gh fallback, REL-04 and REL-05 are technically "shipped via operator fallback" rather than "shipped autonomously." For REQUIREMENTS.md purposes, both states are `[x]` — the requirement is "release/topics exist on grigorijejakisic/Claudex," not "Corleanus applied them autonomously." If at the time this plan runs, Plan 17-03 logged that the operator fallback was needed, leave REL-04 + REL-05 as `[~]` HITL temporarily and note "awaiting operator gh-as-grigorijejakisic run" — they flip to `[x]` once the operator has run the fallback. For the typical case (autonomous succeeded), they are `[x]`.

**(b) Traceability table (REL-01..07 rows):** flip the Status column.

```markdown
| REL-01 | Phase 17 | Done |
| REL-02 | Phase 17 | Done |
| REL-03 | Phase 17 | Done |
| REL-04 | Phase 17 | Done |
| REL-05 | Phase 17 | Done |
| REL-06 | Phase 17 | Done |
| REL-07 | Phase 17 | Pending (HITL — operator runs UI step; runbook at docs/onboarding/branch-protection-setup.md) |
```

The Phase 16 HITL items (PLAT-06, PLAT-07, PLAT-08, VER-04, VER-05) remain `[~]` — Phase 17 does NOT close them. Their status is unchanged from Phase 16's close.

**Pre-write verification:** before authoring, count the current `[ ]`, `[x]`, `[~]` rows in REQUIREMENTS.md and confirm the targets match expectations:

```bash
# Before this task
grep -c '^\- \[ \] \*\*REL-' .planning/REQUIREMENTS.md   # expect 7 (all REL- still pending)
grep -c '^\- \[x\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 0
grep -c '^\- \[~\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 0

# After this task
grep -c '^\- \[ \] \*\*REL-' .planning/REQUIREMENTS.md   # expect 0
grep -c '^\- \[x\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 6 (REL-01..06)
grep -c '^\- \[~\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 1 (REL-07)
```

If pre-state diverges from expectations (e.g., a REL- was already marked done), investigate before editing. CONTEXT.md says all 7 REL- items are pending entering Phase 17.
</task>

<task id="02-update-roadmap-md">
**Update `.planning/ROADMAP.md`** in two places.

**(a) Phase 17 row:** mark `[x]` with completion date.

Locate the line:

```markdown
- [ ] **Phase 17: Public ship to grigorijejakisic/claudex** — remote + history + tags + GitHub release + topics + badges + branch protection (UNBLOCKED — HITL-pending Phase 16 items do not gate Phase 17 per Phase 11 SC#4 precedent)
```

Replace with:

```markdown
- [x] **Phase 17: Public ship to grigorijejakisic/claudex** — SHIPPED 2026-05-02. v4.1.0 tagged at content-complete commit; complete master history (336 commits + Phase 17 admin) pushed to `grigorijejakisic/Claudex`; GitHub release v4.1.0 — Distribution published with CHANGELOG-derived notes; 9 repository topics applied; README badges live; branch protection runbook at `docs/onboarding/branch-protection-setup.md` (REL-07 HITL — operator runs UI step).
```

**(b) Phase 17 detail block (the section starting `### Phase 17: Public ship to grigorijejakisic/claudex`):** add a closing summary at the end of the existing block.

Find the existing detail block (currently ends with the success criteria #1-6 enumeration and "**Plans:** TBD"). After the success criteria, **before** any subsequent table or section, add:

```markdown

**Closed:** 2026-05-02 with 4 plans / 4 atomic implementation commits + this close commit. REL-01..06 [x] (autonomous); REL-07 [~] HITL (operator runs branch protection UI step). v4.1 milestone SHIPPED publicly.
```

Update the milestone-level `**Plans:** TBD` to:

```markdown
**Plans:** 17-01 (CHANGELOG + badges) → 17-02 (v4.1.0 tag + push) → 17-03 (GitHub release + topics + branch protection runbook) → 17-04 (close + push to BOTH remotes)
```

**(c) Phase 17 row in the milestone progress table** (the row currently reading `| 17. Public ship to grigorijejakisic/claudex | 0/0 | Not started (UNBLOCKED — HITL-pending Phase 16 items do not gate Phase 17) | - |`): replace with:

```markdown
| 17. Public ship to grigorijejakisic/claudex | 4/4 | Closed 2026-05-02 (REL-01..06 [x], REL-07 [~] HITL) | 2026-05-02 |
```

**(d) Milestone-level header (top of v4.1 section, wherever the milestone status appears):** update v4.1 to SHIPPED state:

```markdown
**v4.1 — Distribution: SHIPPED 2026-05-02 at v4.1.0** — public on `github.com/grigorijejakisic/Claudex`. 32/44 reqs closed autonomously across Phases 12-17 + REL-04..05 in Phase 17; 12/44 HITL-pending (PLAT-06..08, VER-04..05 from Phase 16; REL-07 from Phase 17). Next milestone: v4.2 (TBD).
```

The exact location of the milestone header depends on ROADMAP.md's current shape. If the ROADMAP currently has a "Current milestone: v4.1" status block at the top, update that block. If not, add this line near the v4.1 section header.

**Pre-write verification:**

```bash
# Phase 17 currently checkbox-pending
grep '^\- \[ \] \*\*Phase 17:' .planning/ROADMAP.md
# Expected: 1 line

# Phase 17 currently 0/0 in progress table
grep '17. Public ship' .planning/ROADMAP.md
# Expected: 1 line, "0/0 ... Not started"
```

After edits:

```bash
grep '^\- \[x\] \*\*Phase 17:' .planning/ROADMAP.md
# Expected: 1 line (the new SHIPPED line)

grep '17. Public ship' .planning/ROADMAP.md
# Expected: 1 line, "4/4 ... Closed 2026-05-02"
```
</task>

<task id="03-update-state-md">
**Update `.planning/STATE.md`** to reflect v4.1 SHIPPED.

The current STATE.md (per the snapshot from CONTEXT.md gather time) has:

```markdown
**Current focus:** **Phase 16 STRUCTURAL CLOSE 2026-05-02.** ... Next: Phase 17 (Public ship to grigorijejakisic/Claudex, REL-01..07).
```

Replace with:

```markdown
**Current focus:** **v4.1 SHIPPED at v4.1.0 — 2026-05-02.** Phase 17 closed: REL-01..06 [x] (autonomous — public remote, fast-forward push 336+1 commits, v4.1.0 annotated tag, GitHub release with CHANGELOG-derived notes, 9 repo topics, README badges); REL-07 [~] HITL-pending (branch protection runbook at `docs/onboarding/branch-protection-setup.md` — operator runs the UI click). 32/44 v4.1 reqs done autonomously; 12/44 HITL-pending (PLAT-06..08 + VER-04..05 from Phase 16; REL-07 from Phase 17 — all operator-runnable). Next milestone: v4.2 (TBD).
```

Update the Current Position block:

```markdown
## Current Position

**Current Milestone:** v4.1 — Distribution **SHIPPED at v4.1.0**
**Phase:** 17 — closed 2026-05-02
**Plan:** —
**Status:** v4.1 SHIPPED publicly on `github.com/grigorijejakisic/Claudex`. 4 plans / 4 atomic implementation commits + this close commit pushed to BOTH remotes. v4.0.0 + v4.1.0 tags both visible on the public repo. GitHub release v4.1.0 — Distribution published. 9 topics applied. README badges live. REL-07 (branch protection) HITL-pending.
**Last activity:** 2026-05-02 — Phase 17 closed; v4.1 milestone shipped publicly
```

Update the v4.1 Phase Structure table to mark Phase 17 SHIPPED:

```markdown
| 17 — Public ship to grigorijejakisic/claudex | Tag + push + GitHub release + topics + branch protection | REL-01..07 (7 reqs) — **SHIPPED 2026-05-02 (REL-01..06 [x], REL-07 [~] HITL)** |
```

Update **Coverage:**

```markdown
**Coverage:** 44/44 v4.1 requirements closed structurally (32 autonomous [x] + 12 HITL [~]). v4.1 milestone is SHIPPED — HITL items are deferred to operator follow-up, not blockers.
```

Update **Last Activity Description:**

```markdown
**Last Activity:** 2026-05-02
**Last Activity Description:** Phase 17 CLOSE. v4.1 SHIPPED publicly to `github.com/grigorijejakisic/Claudex`. 4 plans landed atomically: **17-01:** CHANGELOG promote [Unreleased] → [4.1.0] — 2026-05-02 (Phase 12-17 narrative + Pending HITL block + Stats); README badges (license MIT + version 4.1.0 + Vesna CI) at top of README. **17-02:** v4.1.0 annotated tag created at 17-01's commit with locked Phase 12-17 + 32/12 message body; `git push public master` succeeded as fast-forward (337-commit jump from 712c910); `git push public --tags` sent v4.0.0 + v4.1.0. **17-03:** GitHub release `v4.1.0 — Distribution` published via `gh release create` with notes extracted from CHANGELOG; 9 topics applied via `gh repo edit`; runbook authored at `docs/onboarding/branch-protection-setup.md` for the REL-07 HITL UI step. **17-04 (this commit):** STATE/ROADMAP/REQUIREMENTS updated for final v4.1 close; 17-SUMMARY.md captures what shipped + HITL deferrals + hard-gate evidence; phase-close commit pushed to BOTH remotes. Hard gates: bun run build green, bun run test 3188 + 20 baseline llama unchanged, bun run vesna 17/17 GATED PASS, bun run doctor exit 0. DB schema (V24) unchanged. Hook semantics unchanged. v4.0.0 + v4.1.0 tags both visible on `grigorijejakisic/Claudex/tags`. Release page reachable. v4.1 milestone closed. Next: v4.2 milestone definition (TBD).
```

(The exact wording can be tightened, but every clause above corresponds to verifiable state — no fabrication.)
</task>

<task id="04-author-17-summary">
**Author `.planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md`** following the template established by 14-SUMMARY.md, 15-SUMMARY.md, 16-SUMMARY.md.

Required content (target ≤200 lines):

```markdown
# Phase 17 Summary — Public Ship to grigorijejakisic/Claudex

**Closed:** 2026-05-02
**Status:** SHIPPED — v4.1 publicly on `github.com/grigorijejakisic/Claudex`
**Requirements:** REL-01..07 (7 total) — 6 [x] autonomous + 1 [~] HITL

## What shipped

### REL-01: Public GitHub remote configured

`public` remote points to `git@github.com:grigorijejakisic/Claudex.git` (verified pre-phase; no setup needed). Used SSH auth (key registered for grigorijejakisic).

### REL-02: Complete master history pushed (337 commits)

`git push public master` succeeded as fast-forward from `712c910` (the v3-era last public push, session 41) to current HEAD. 336 commits from v4.0.0 milestone work + 1 commit from Plan 17-01 = 337 total. NO force-push needed.

`git push public --tags` sent both `v4.0.0` (created Phase 11 close at `f8f617c`, never previously pushed to public) and `v4.1.0` (created Plan 17-02 at Plan 17-01's commit).

### REL-03: v4.1.0 annotated tag

Annotated tag at the CHANGELOG/badges commit (Plan 17-01) — content-complete state, not phase-admin state. Tag message body matches CONTEXT.md `<decisions>` REL-03 verbatim:

```
v4.1.0 — Distribution

Phase 12: MIT LICENSE + package.json polish + README + CHANGELOG + CONTRIBUTING
Phase 13: Cross-platform code audit (path/hooks/locks/subprocess/.gitattributes)
Phase 14: bun run setup bootstrap + CLAUDEX_PROJECTS_DIR env var + install.sh + install.bat
Phase 15: bun run doctor diagnostics (7 checks)
Phase 16: README Quick Start + Troubleshooting + onboarding fixtures (HITL-pending fresh-VM trials)
Phase 17: Public ship

44 v4.1 requirements: 32 closed autonomously; 12 HITL-pending (fresh-VM trials operator-runnable).
All hard gates green: build, test (3188 + 20 baseline llama unchanged), vesna 17/17, doctor.
```

Verified via `git cat-file -p v4.1.0`.

### REL-04: GitHub release v4.1.0 — Distribution

Published via `gh release create v4.1.0 --repo grigorijejakisic/Claudex --title "v4.1.0 — Distribution" --notes-file <extracted-changelog-section> --verify-tag`. Notes are byte-identical to CHANGELOG.md `[4.1.0] — 2026-05-02` section (extracted programmatically via awk, not hand-rewritten). Visible at `https://github.com/grigorijejakisic/Claudex/releases/tag/v4.1.0`.

[If autonomous run hit gh permission issue and operator ran fallback: note that here, e.g., "Created via operator fallback (gh auth as grigorijejakisic) — Corleanus lacked write permission on the public repo."]

### REL-05: Repository topics applied

9 topics on `grigorijejakisic/Claudex` via `gh repo edit --add-topic`:

- `claude-code`
- `mcp`
- `agent-memory`
- `llm-tools`
- `typescript`
- `bun`
- `claudex`
- `persistent-memory`
- `claude`

Verified via `gh repo view grigorijejakisic/Claudex --json repositoryTopics`.

### REL-06: README badges

3 badges added at top of README (between title and tagline):

- `[![License: MIT](...)](./LICENSE)` — links to LICENSE
- `[![Version](...)](#)` — informational, no link
- `[![Vesna CI](...)](https://github.com/grigorijejakisic/Claudex/actions/workflows/vesna.yml)` — links to workflow page

The Vesna badge will show "no status" until the Vesna workflow runs at least once on the public repo. The first push from Plan 17-02 should trigger that run; the badge resolves on its own.

### REL-07 (HITL): Branch protection runbook

Authored at `docs/onboarding/branch-protection-setup.md` describing the GitHub UI click path for the operator to apply the branch protection rule (Vesna CI required on `master`). Phase closes structurally with this requirement marked `[~]` HITL — operator runs the UI step post-ship. Pattern matches Phase 11 SC#4 cold-start trials and Phase 16 PLAT-06..08 HITL deferrals.

## What's HITL-pending after Phase 17

| Req | Source phase | Description | Runbook |
|-----|--------------|-------------|---------|
| PLAT-06 | Phase 16 | macOS install verified end-to-end on fresh VM | docs/onboarding/macos.md |
| PLAT-07 | Phase 16 | Ubuntu 24.04 LTS install verified on fresh VM | docs/onboarding/linux.md |
| PLAT-08 | Phase 16 | Windows 11 install verified on fresh VM (regression) | docs/onboarding/windows.md (split-mode autonomous half PASS) |
| VER-04 | Phase 16 | Friction resolution as code/doctor/README entry | (depends on PLAT-06..08) |
| VER-05 | Phase 16 | <30-min install target measured per platform | (depends on PLAT-06..08) |
| REL-07 | Phase 17 | Branch protection UI step | docs/onboarding/branch-protection-setup.md |

These six items follow the same flexibility pattern v4.0.0 used at Phase 11 SC#4 — structural close ships; operator-driven verification deferred. v4.1 milestone is closed despite these deferrals.

## Hard-gate evidence

Run on the close commit (Plan 17-04's commit) before the final BOTH-remotes push:

```
$ bun run build
[esbuild green, ~70ms]

$ bun run test
[≥3188 passing + 20 baseline llama-server-supervisor failures unchanged from v4.0.0]

$ bun run vesna
[17/17 GATED PASS, 100% aggregate, 100% per non-empty category]

$ bun run doctor
[exit 0; 5-6 pass + ≤1 warn for Angel heartbeat staleness, same as Phase 15 baseline]
```

DB schema (V24) unchanged from v4.0.0. Hook semantics unchanged.

## Plans

- **17-01** — CHANGELOG promote `[Unreleased]` → `[4.1.0] — 2026-05-02` + README badges. Atomic commit (REL-06 closed; CHANGELOG content staged for REL-04).
- **17-02** — v4.1.0 annotated tag at 17-01's commit + `git push public master` (fast-forward) + `git push public --tags`. No commit (tag is metadata). REL-02, REL-03 closed.
- **17-03** — GitHub release via `gh release create --verify-tag` + 9 topics via `gh repo edit` + branch-protection runbook authored at `docs/onboarding/branch-protection-setup.md`. Atomic commit (REL-04, REL-05 closed; REL-07 runbook half closed).
- **17-04 (this plan)** — STATE/ROADMAP/REQUIREMENTS updated for final v4.1 close + 17-SUMMARY.md (this file) + phase-close commit pushed to BOTH remotes. REL-01 (already verified pre-phase) marked [x]; REL-07 marked [~] HITL.

## Public-ship verification (visual on GitHub)

After push:

- `https://github.com/grigorijejakisic/Claudex` → master at the new HEAD; README renders with 3 badges at top
- `https://github.com/grigorijejakisic/Claudex/tags` → v4.0.0 and v4.1.0 listed
- `https://github.com/grigorijejakisic/Claudex/releases/tag/v4.1.0` → release page with v4.1 — Distribution title and CHANGELOG-derived notes
- `https://github.com/grigorijejakisic/Claudex` (sidebar) → 9 topics visible
- `https://github.com/grigorijejakisic/Claudex/settings/branches` → empty (operator runs REL-07 to populate)

## Notes

- **The v4.1.0 tag is at Plan 17-01's commit, not at this close commit.** This is intentional: the tag points at content-complete v4.1.0 state (CHANGELOG narrative + badges), not at planning admin (STATE updates). Matches v4.0.0 precedent (tag at `f8f617c`, the v4.0.0 release-content commit).
- **Phase 17 is the FINAL phase of v4.1.** v4.2 milestone is TBD; will be defined in a future planning session.
- **All four close commits use the `phase(17):` convention** — matches Phase 11/12/13/14/15/16 close commit format. The atomic chain is: 17-01 CHANGELOG/badges → 17-02 (no commit, tag + push) → 17-03 runbook + GH release/topics → 17-04 close.
- **The push to BOTH remotes happens only in this plan.** Plan 17-02's push to `public` only sent commits 1..N (where N is 17-01's commit); Plan 17-03's commit is local-only; this plan pushes commits 17-03 + 17-04 to `public` and the entire `phase(17):` chain to `origin` (Corleanus/CLAUDEXv3).
```

Hard authoring rules:

- **Match the established summary template.** Compare against `15-SUMMARY.md` and `16-SUMMARY.md` — same section headers, same level of detail. Do not innovate format here.
- **Cite verifiable state only.** Every claim (commit count, hash, gate output, topic list) must correspond to actual git/gh output. If a fact can't be verified at runtime, drop it rather than guess.
- **The "If autonomous run hit gh permission issue and operator ran fallback" line in REL-04 is conditional.** If Plan 17-03 succeeded autonomously, delete that bracketed paragraph. If it failed and the runbook fallback was triggered, replace the bracket with the actual fallback note.
- **Length:** target ≤200 lines.
- **No emoji.** No marketing language.
</task>

<task id="05-final-verify-and-commit">
**Run hard gates one final time** as the v4.1-milestone-close witness:

```bash
bun run build
bun run test
bun run vesna
bun run doctor
```

Capture the exit codes / pass counts and cite them in 17-SUMMARY.md (Task 04). If any gate regresses, fix before commit; do NOT commit a failing state into a v4.1 close.

**Final pre-commit sanity:**

```bash
# REQUIREMENTS.md state correct
grep -c '^\- \[x\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 6
grep -c '^\- \[~\] \*\*REL-' .planning/REQUIREMENTS.md   # expect 1

# ROADMAP.md state correct
grep '^\- \[x\] \*\*Phase 17:' .planning/ROADMAP.md      # expect 1 line

# 17-SUMMARY.md exists
test -f .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md && echo OK

# STATE.md current focus updated
grep -F 'v4.1 SHIPPED at v4.1.0' .planning/STATE.md       # expect non-empty match

# Branch protection runbook exists (from 17-03)
test -f docs/onboarding/branch-protection-setup.md && echo OK

# Working tree shows only the four expected modifications
git status --short
# Expected:
#  M .planning/STATE.md
#  M .planning/ROADMAP.md
#  M .planning/REQUIREMENTS.md
# ?? .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md
```

**Atomic commit:**

```bash
git add \
  .planning/STATE.md \
  .planning/ROADMAP.md \
  .planning/REQUIREMENTS.md \
  .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md

git commit -m "phase(17): close — v4.1 SHIPPED to grigorijejakisic/Claudex (REL-01..06 [x], REL-07 [~] HITL)"
```
</task>

<task id="06-push-to-both-remotes">
**Push the final phase-close chain to BOTH remotes.** This is the last action of v4.1 ship.

After Plan 17-02 pushed commits up through 17-01's commit to `public`, two more commits are local-only on `master`:

- 17-03's commit (branch-protection runbook + GH release/topics applied — though release/topics state is GitHub-side, the runbook file is local)
- 17-04's commit (this commit — STATE/ROADMAP/REQUIREMENTS/SUMMARY)

Push them to BOTH remotes:

```bash
# Push to public (grigorijejakisic/Claudex)
git push public master

# Push to origin (Corleanus/CLAUDEXv3)
git push origin master
```

**Both must succeed as fast-forward.** Expected output (each):

```
To <remote-url>
   <prev-public-or-origin-HEAD>..<new-HEAD>  master -> master
```

**Failure modes:**

- `git push public master` fails with permission error → SSH key issue OR Plan 17-02's push didn't fully land. Run `git ls-remote public refs/heads/master` to check current public state. SendMessage team-lead, HALT.
- `git push public master` fails as non-fast-forward → public/master advanced since Plan 17-02 (someone else pushed). SendMessage team-lead, HALT, do NOT use `--force`.
- `git push origin master` fails → origin (Corleanus/CLAUDEXv3) auth issue. Less critical than public, but still HALT and report.

**Verify after push:**

```bash
LOCAL_HEAD=$(git rev-parse HEAD)
PUBLIC_HEAD=$(git ls-remote public refs/heads/master | awk '{print $1}')
ORIGIN_HEAD=$(git ls-remote origin refs/heads/master | awk '{print $1}')

[ "$LOCAL_HEAD" = "$PUBLIC_HEAD" ] && echo "public: OK" || echo "public: MISMATCH"
[ "$LOCAL_HEAD" = "$ORIGIN_HEAD" ] && echo "origin: OK" || echo "origin: MISMATCH"
```

Both must report OK. CONTEXT.md `<acceptance>` #10 ("phase-close commit pushed to both remotes") is now satisfied.

**v4.1 is SHIPPED.** Strangers can clone `git@github.com:grigorijejakisic/Claudex.git`, see the v4.1.0 tag and release, run `bun run setup`, and start using Claudex.
</task>

## Verification criteria

- [ ] REQUIREMENTS.md: REL-01..06 [x]; REL-07 [~] HITL with runbook reference
- [ ] REQUIREMENTS.md traceability table: REL-01..06 status Done; REL-07 status `Pending (HITL — operator runs UI step; runbook at docs/onboarding/branch-protection-setup.md)`
- [ ] ROADMAP.md Phase 17 row marked [x] with completion date 2026-05-02
- [ ] ROADMAP.md milestone status: v4.1 SHIPPED at v4.1.0 — 2026-05-02; next milestone v4.2 (TBD)
- [ ] ROADMAP.md progress table: Phase 17 row 4/4 / Closed 2026-05-02
- [ ] STATE.md current focus: v4.1 SHIPPED at v4.1.0
- [ ] STATE.md current position: Milestone v4.1 SHIPPED, Phase 17 closed
- [ ] STATE.md last activity 2026-05-02 with public-ship description
- [ ] STATE.md v4.1 Phase Structure table: Phase 17 marked SHIPPED 2026-05-02 (REL-01..06 [x], REL-07 [~] HITL)
- [ ] STATE.md coverage: 44/44 v4.1 requirements closed structurally (32 [x] + 12 [~])
- [ ] 17-SUMMARY.md exists at .planning/phases/17-public-ship-grigorijejakisic-claudex/17-SUMMARY.md
- [ ] 17-SUMMARY.md follows the 14/15/16-SUMMARY template (sections: What shipped, HITL-pending, hard-gate evidence, plans, public-ship verification, notes)
- [ ] 17-SUMMARY.md captures hard-gate output (build, test, vesna, doctor) as last witness
- [ ] `bun run build` green; `bun run test` ≥3188 + 20 baseline llama unchanged; `bun run vesna` 17/17; `bun run doctor` exit 0
- [ ] DB schema unchanged (V24); hook semantics unchanged
- [ ] Atomic commit `phase(17): close — v4.1 SHIPPED to grigorijejakisic/Claudex (REL-01..06 [x], REL-07 [~] HITL)`
- [ ] `git push public master` succeeded as fast-forward; public/master at local HEAD
- [ ] `git push origin master` succeeded as fast-forward; origin/master at local HEAD
- [ ] `git ls-remote public refs/heads/master` and `git ls-remote origin refs/heads/master` both match local HEAD

## Out of scope

- v4.1.0 tag (already created in Plan 17-02)
- GitHub release object (already created in Plan 17-03)
- Repository topics (already applied in Plan 17-03)
- README badges (already added in Plan 17-01)
- CHANGELOG promotion (already done in Plan 17-01)
- Operator running the branch protection UI step (REL-07 HITL — operator does post-ship)
- Operator running fresh-VM PLAT-06..08 trials (Phase 16 HITL — unchanged by Phase 17)
- v4.2 milestone definition — separate planning cycle

## Notes for the implementer

- **This is the close witness.** Plan 17-04's commit is the only commit in the entire phase that pushes to BOTH remotes. Keep the file modifications focused — STATE, ROADMAP, REQUIREMENTS, 17-SUMMARY. Anything else creeping into this commit is scope drift.
- **The phase-close commit message wording matches CONTEXT.md `<acceptance>` #10 expectation.** "v4.1 SHIPPED to grigorijejakisic/Claudex" is the headline. Don't soften it ("close") or hedge ("structural close"). v4.1 is SHIPPED — operator-driven HITL items don't change that.
- **If the BOTH-remotes push fails on public, do NOT use force.** CONTEXT.md `<plan_authorization>` is explicit. Operator can finish the public push themselves; the `phase(17):` close commit is on origin and reproducible.
- **The 32/12 split in 17-SUMMARY.md and STATE.md must match REQUIREMENTS.md.** Compute it at runtime via `grep -c '^\- \[x\] ' .planning/REQUIREMENTS.md` and `grep -c '^\- \[~\] ' .planning/REQUIREMENTS.md`. If the counts diverge from 32/12, the divergence is real — fix REQUIREMENTS.md or correct the SUMMARY claim.
- **Authoring 17-SUMMARY.md before running the gates is OK.** The SUMMARY's hard-gate evidence section can be filled in after Task 05 runs the gates. Just leave a placeholder during Task 04 authoring and finalize after Task 05.
- **After this plan: v4.1 is shipped publicly.** The next session that opens this repo should see STATE.md indicating "v4.1 SHIPPED" and route to v4.2 milestone definition. There is no Phase 18.
