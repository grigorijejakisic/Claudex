---
phase: 11-p9-final-validation
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - .planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md
  - .planning/phases/11-p9-final-validation/11-04-vesna-report.json
autonomous: true
requirements:
  - VESN
must_haves:
  truths:
    - "Vesna full 20-probe suite ran against current v4 main"
    - "Aggregate pass rate ≥80% AND every non-empty category ≥80% (per-category enforcement, no aggregate masking)"
    - "Per-project verification: representative subset of probes also run against each of 5+ active projects' real DB state"
    - "11-04-SC1-RESULT.md records aggregate, per-category, and per-project pass rates with named failing probes"
  artifacts:
    - path: .planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md
      provides: "SC#1 measurement result — Vesna full-suite verdict"
      contains: "## Per-category pass rates"
    - path: .planning/phases/11-p9-final-validation/11-04-vesna-report.json
      provides: "Raw Vesna SuiteReport JSON"
      contains: "\"gated\""
  key_links:
    - from: .planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md
      to: .planning/phases/11-p9-final-validation/11-04-vesna-report.json
      via: "Result file is the Markdown rendering of the JSON report"
      pattern: "11-04-vesna-report.json"
---

<objective>
Run the full Vesna 20-probe suite against v4 main as the SC#1 ship-gate measurement and record the per-category + per-project verdict.

SC#1 per CONTEXT.md (lines 38-44):
- Run full Phase 10 probe suite (~20 probes) against current v4 codebase
- Per-category pass rate must clear ≥80% (aggregate is not enough — prevents weak-category masking)
- Per-project: run subset against each of 5+ active projects to confirm cross-project organic recall works in practice
- Failures: document the failing probe + investigate; revert problematic phase if needed

Why this is Wave 2 (depends on 11-01/02/03): per CONTEXT line 92, recommendation is SC#3 first (mechanical, fast), SC#2 in parallel (nearly free — runs the cache-stability test), SC#4 in parallel (live trials), then SC#1 last because Vesna is the slowest gate. If SC#3/SC#2/SC#4 reveal issues that require repairs to MEMORY.md curation or assembler sections, those repairs may shift Vesna behavior — so SC#1 measures the post-repair state. If 11-01/02/03 all PASS clean, SC#1 measures the same state, which is fine.

This plan is the official SC#1 measurement that goes in the v4 ship commit. The CI gate (`.github/workflows/vesna.yml`, Phase 10) provides per-PR enforcement; SC#1 is the named ship moment.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-p9-final-validation/11-CONTEXT.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md
@src/benchmark/vesna/README.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full Vesna suite and capture the report</name>
  <files>.planning/phases/11-p9-final-validation/11-04-vesna-report.json</files>
  <action>
Run the full suite (with the production reranker if available — run `services/reranker.py` first if not already up):

```bash
# 1. Confirm reranker is up (load-bearing per CLAUDE.md SAFE-08)
curl -s http://localhost:7439/health || echo "WARN: reranker not running — Vesna will fall back to bi-encoder"

# 2. Build first to ensure Phase 9 deletions / Phase 10 additions compile
bun run build

# 3. Full Vesna run with JSON output
bun run vesna -- --json > .planning/phases/11-p9-final-validation/11-04-vesna-report.json
echo "Exit code: $?" | tee -a /tmp/sc1-phase-11.log
```

Inspect the JSON for:
- `gated`: true iff aggregate ≥80% AND every non-empty category ≥80%
- `aggregate`: overall pass rate
- `per_category`: object keyed by category, each with `pass_rate`, `passed`, `total`, `probe_ids`
- Failing probes: list with `probe_id`, `category`, `failure_reason` (e.g. "no expected phrase matched in 3/3 trials")

**If `gated === false`**:
1. Identify which category(ies) are below 80%.
2. For each failing probe, decide:
   - Genuine recall failure → file as a Phase 11 sub-plan to investigate the responsible component (assembler section, retrieval channel weight, embedding drift). Do NOT mask by widening must_contain_phrase_pattern.
   - Probe was over-tuned in Phase 10 → re-tune the probe in this phase, document the change in 11-04-SC1-RESULT.md ("probe X relaxed because expected phrase was unrealistically narrow"), and re-run.
   - Reranker fallback issue → if bi-encoder mode produced the failure, ensure reranker is up and re-run; if reranker IS up but result is still flaky, note in caveat section but do NOT block ship if cross-encoder mode is used in production hooks (which it is per CLAUDE.md).

**If reranker is unavailable**: per CONTEXT, the bi-encoder fallback is a degraded mode. The SC#1 ship-gate run MUST use the cross-encoder. Block this plan until reranker is up.
  </action>
  <verify>
- `.planning/phases/11-p9-final-validation/11-04-vesna-report.json` exists with full SuiteReport JSON
- Run was performed with cross-encoder reranker (not bi-encoder fallback) — confirmed via reranker health check
- Exit code captured in log
  </verify>
  <done>Vesna full-suite report captured against v4 main with cross-encoder reranker.</done>
</task>

<task type="auto">
  <name>Task 2: Run per-project subset verification</name>
  <files>.planning/phases/11-p9-final-validation/11-04-per-project-verification.md</files>
  <action>
Per CONTEXT line 41: "Per-project: run subset against each of 5 active projects (claudex-v3, lacuna-betting, oracle, big-mozzy-v2, desktop-01dcc792, nexus-e53c6c93) — confirms cross-project organic recall works in practice."

Vesna's harness uses `claudex-vesna-test.db` (isolated test DB per Plan 10-01) — it does not exercise the real per-project DB state. So "per-project verification" here is a SEPARATE check: run a small representative subset of probes (specifically the 3 cross-project probes from `src/benchmark/vesna/probes/cross-project-*.json` plus 1-2 entity-recall probes) against each project's CWD-active context.

**Procedure**:

For each of the 5+ active projects, in a fresh executor context:
1. Set CWD to that project's directory (via the registry `~/.claudex/projects.json`).
2. Run the cross-project + 2 entity-recall probes through the Vesna runner with the project's CWD set as the operating context. The runner already supports CWD scoping via the existing harness (Plan 10-01) — re-use, don't re-implement. If it doesn't, document this as a "fix in v4.1" and run a manual equivalent: invoke `claudex_search` and `claudex_recall` with the project CWD set, paste the top-k results, and verify against probe expectations.

Document per project in `11-04-per-project-verification.md`:

```markdown
# SC#1 Per-Project Verification

| Project | Probes run | Pass rate | Notes |
|---|---|---|---|
| claudex-v3 | cross-project-001, entity-001, entity-002 | N/3 | <one-liner> |
| lacuna-betting | ... | ... | ... |
| oracle | ... | ... | ... |
| big-mozzy-v2 | ... | ... | ... |
| desktop-01dcc792 | ... | ... | ... |
| nexus-e53c6c93 | ... | ... | ... |
```

Per-project bar: each project should show ≥80% on the subset. If a project is below, capture which probes failed and why — typical failure mode is "this project doesn't have the artifacts the probe assumes" (legitimate — not all projects have rich histories), in which case the probe should be skipped for that project, not failed. Document the skip-or-fail decision.

**If the harness does NOT support CWD-scoped runs and a manual equivalent is too expensive**: document this as a v4.1 follow-up (probe-set per-project scoping) and accept the global Vesna run from Task 1 as the SC#1 evidence, with explicit note in 11-04-SC1-RESULT.md that per-project verification was not separately run. CONTEXT lock allows this — the per-project requirement is "confirms cross-project organic recall works in practice" and the cross-project category in the global suite already exercises it. The DECISION must be explicit, not implicit.
  </action>
  <verify>
- 11-04-per-project-verification.md exists with per-project results OR an explicit "deferred to v4.1, accepted-substitution" note
- If results are present, each project ≥80% on its subset OR the failure modes are documented and accepted
  </verify>
  <done>Per-project verification is either complete or explicitly deferred with justification.</done>
</task>

<task type="auto">
  <name>Task 3: Synthesize the SC#1 result file</name>
  <files>.planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md</files>
  <action>
Render the Vesna report JSON + per-project verification into `.planning/phases/11-p9-final-validation/11-04-SC1-RESULT.md`:

```markdown
# SC#1 Result — Vesna Probe Suite Pass Rate

**Run date:** YYYY-MM-DD
**Commit:** <git rev-parse HEAD>
**Reranker:** cross-encoder (BGE-v2-m3 on 7439) / bi-encoder fallback
**Verdict:** PASS / FAIL

## Aggregate

- Total probes: 20
- Passed: NN
- **Aggregate pass rate: NN%**
- Bar: ≥80% — PASS / FAIL

## Per-category pass rates

| Category | Total | Passed | Pass rate | Bar | Verdict |
|---|---|---|---|---|---|
| entity-recall | 3 | N | NN% | ≥80% | PASS/FAIL |
| constraint-recall | 3 | N | NN% | ≥80% | PASS/FAIL |
| handoff-pickup | 3 | N | NN% | ≥80% | PASS/FAIL |
| cross-project | 3 | N | NN% | ≥80% | PASS/FAIL |
| lesson-application | 3 | N | NN% | ≥80% | PASS/FAIL |
| self-instrumented | 2 | N | NN% | ≥80% | PASS/FAIL |
| buffer | 0 or filled | ... |

## Per-project pass rates (subset)

[link to 11-04-per-project-verification.md or paste the table]

## Failing probes (if any)

For each failing probe:
- probe_id
- category
- expected_recall snippet
- actual top-k from Vesna runner
- diagnosis
- decision (re-tune / investigate / accept-flaky)

## Phase 10 baseline cross-reference

Phase 10 closed at 17/17 = 100% per the team-lead brief. If Phase 11 SC#1 shows regression vs that baseline, the regressing commit(s) must be identified — bisect with `git log --oneline fe7a83b..HEAD` over `src/`.

## Decision

If PASS → SC#1 cleared, ready for SC verification roll-up (Plan 07).
If FAIL → open Phase 11.x sub-plan; do NOT ship v4 until per-category bar is met. Reverting Phase 9 deletions or Phase 10 promotions is on the table per CONTEXT line 44.
```
  </action>
  <verify>
- 11-04-SC1-RESULT.md exists with all sections populated
- Aggregate AND per-category verdicts are explicit
- Failing probes are named, not summarized
- Phase 10 baseline cross-reference is documented (regression check)
  </verify>
  <done>SC#1 verdict is recorded; PASS/FAIL is explicit; if FAIL, the diagnosis path is named.</done>
</task>

</tasks>

<verification>
- 11-04-vesna-report.json captured with cross-encoder reranker run
- 11-04-per-project-verification.md complete or explicitly deferred
- 11-04-SC1-RESULT.md aggregates the verdict with per-category rigor
- bun run vesna exits 0 (gated true) — or the failing probes are named and a remediation path is open
</verification>

<success_criteria>
- SC#1 has been measured against v4 main with the cross-encoder reranker
- Per-category bar (≥80% each) is verified — no aggregate masking
- Per-project verification is complete OR explicitly deferred to v4.1 with justification
- Failures (if any) trigger investigation, not masking
</success_criteria>

<output>
On completion, write a thin `11-04-SUMMARY.md` pointing to `11-04-SC1-RESULT.md`.
</output>
