---
phase: 11-p9-final-validation
plan: 06
type: execute
wave: 4
depends_on: ["11-04", "11-05"]
files_modified:
  - .planning/phases/11-p9-final-validation/11-06-BENCHMARK-VIBE-CHECK.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "LongMemEval Oracle run executed once against v4 main; aggregate score recorded"
    - "LoCoMo run executed once against v4 main; aggregate score recorded"
    - "11-06-BENCHMARK-VIBE-CHECK.md records both numbers, the prior baselines (90.6% LongMemEval / 55.5% LoCoMo), and the dramatic-regression assessment"
    - "Numbers are ARCHIVAL ONLY — they do NOT gate v4 ship per CONTEXT.md axiom"
  artifacts:
    - path: .planning/phases/11-p9-final-validation/11-06-BENCHMARK-VIBE-CHECK.md
      provides: "Archival benchmark numbers for v4 ship commit"
      contains: "## LongMemEval"
---

<objective>
Run LongMemEval Oracle and LoCoMo once against v4 main, record the numbers, and document them as the ship commit's archival vibe-check.

Per CONTEXT.md (lines 20 and 31):
- "One-shot benchmark vibe-check: run LongMemEval and LoCoMo once, record numbers in commit message for archival reference."
- "Numbers do not gate ship. If they regress dramatically, investigate; otherwise log and proceed."

Per CONTEXT.md generative axiom (line 5): "v4 ships when SC#1-#4 pass. Benchmarks are an archival vibe-check at ship time, never a gate. The audit's diagnosis was that 'green numbers feel like progress' — Phase 11 codifies the corrective."

This plan exists explicitly to NORMALIZE benchmarks as records, not gates. The numbers go in the v4.0.0 ship commit body for posterity. If they regress dramatically, this plan opens an investigation — but unless they do, the SC gates rule.

Prior baselines (from CLAUDE.md "Benchmarks" section):
- **LongMemEval Oracle**: 90.6% (426/470, deepseek-coder-v2:16b local) — competitive with Hindsight
- **LoCoMo**: 55.5% (855/1540, claude-sonnet-4-6) — known WIP, below published competitors

The "dramatic regression" threshold per the project's voice: roughly >10pp drop on either bench is dramatic and worth investigating. <10pp is noise / acceptable drift / target for v4.1 work.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-p9-final-validation/11-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run LongMemEval Oracle one-shot</name>
  <files>/tmp/longmemeval-phase-11.log</files>
  <action>
Step 1 — locate the LongMemEval harness:

```bash
find src/benchmark/longmemeval src/benchmarks/longmemeval . -maxdepth 4 -name "*.ts" -path "*longmem*" 2>/dev/null | head
ls src/benchmark/ 2>/dev/null
grep -l "longmem\|LongMem" package.json 2>/dev/null
```

Identify the canonical entry — look for a `bun run longmem*` script in package.json, or a CLI under `src/benchmark/longmemeval/`.

Step 2 — run Oracle mode (per CLAUDE.md baseline: only evidence sessions ingested, not full 500-session haystack — that's the canonical claudex-v3 baseline shape):

```bash
bun run <oracle-entry> 2>&1 | tee /tmp/longmemeval-phase-11.log
echo "Exit: $?"
```

Step 3 — extract the aggregate score from the log. Capture:
- Aggregate pass rate (e.g. 89.6%)
- Total questions / passed
- Model used (should be deepseek-coder-v2:16b or whichever is the canonical baseline target — match the CLAUDE.md baseline mode)
- Wall-clock time

If the harness has multiple modes (Oracle, Long, etc.), run Oracle only — that's the named v4 baseline. Long mode is a v4.1 target.

**If the harness is broken / missing**: per CONTEXT line 20 "log and proceed" — record "harness unavailable on Phase 11 close, deferred to v4.1" in 11-06-BENCHMARK-VIBE-CHECK.md. Do NOT block ship on harness availability — benchmarks are archival.
  </action>
  <verify>
- /tmp/longmemeval-phase-11.log exists
- Aggregate score is extracted
- Exit code captured
  </verify>
  <done>LongMemEval Oracle run captured (or explicitly noted as harness-unavailable).</done>
</task>

<task type="auto">
  <name>Task 2: Run LoCoMo one-shot</name>
  <files>/tmp/locomo-phase-11.log</files>
  <action>
Step 1 — locate the LoCoMo harness:

```bash
find src/benchmark/locomo src/benchmarks/locomo . -maxdepth 4 -path "*locomo*" 2>/dev/null | head
grep -l "locomo\|LoCoMo" package.json
```

Step 2 — run with the canonical baseline model (claude-sonnet-4-6 per CLAUDE.md):

```bash
bun run <locomo-entry> 2>&1 | tee /tmp/locomo-phase-11.log
echo "Exit: $?"
```

Step 3 — extract aggregate score from the log. Capture:
- Aggregate pass rate
- Total questions / passed
- Model used
- Wall-clock time

**If the harness uses Claude API**: ensure auth is current (OAuth at `~/.claude/.credentials.json` per CLAUDE.md SAFE-01). MAX subscription means no API cost concerns.

**If the harness is broken / missing**: same posture as Task 1 — log and proceed.
  </action>
  <verify>
- /tmp/locomo-phase-11.log exists
- Aggregate score is extracted
- Exit code captured
  </verify>
  <done>LoCoMo run captured (or explicitly noted as harness-unavailable).</done>
</task>

<task type="auto">
  <name>Task 3: Write the vibe-check archival record</name>
  <files>.planning/phases/11-p9-final-validation/11-06-BENCHMARK-VIBE-CHECK.md</files>
  <action>
Synthesize Tasks 1-2 into `.planning/phases/11-p9-final-validation/11-06-BENCHMARK-VIBE-CHECK.md`:

```markdown
# Phase 11 — Benchmark Vibe-Check (Archival, Non-Gating)

**Date:** YYYY-MM-DD
**Commit:** <git rev-parse HEAD>
**Status:** Vibe-check only — does NOT gate v4 ship.

Per CONTEXT.md: "Numbers do not gate ship. If they regress dramatically, investigate; otherwise log and proceed."

## LongMemEval Oracle

- **Run mode:** Oracle (evidence sessions only)
- **Model:** <as captured>
- **Score:** NN.N% (NN/NN)
- **Prior baseline (CLAUDE.md):** 90.6% (426/470, deepseek-coder-v2:16b)
- **Delta vs baseline:** ±N.Npp
- **Verdict:** within drift / dramatic regression / improvement

## LoCoMo

- **Model:** <as captured>
- **Score:** NN.N% (NN/NN)
- **Prior baseline (CLAUDE.md):** 55.5% (855/1540, claude-sonnet-4-6) — known WIP below competitors
- **Delta vs baseline:** ±N.Npp
- **Verdict:** within drift / dramatic regression / improvement (LoCoMo is a v4.1 active improvement target — small movement either direction is expected)

## Dramatic regression check (per CONTEXT.md line 31)

Threshold: >10pp drop on either benchmark = dramatic; investigate before ship.
- LongMemEval: <within / DRAMATIC>
- LoCoMo: <within / DRAMATIC>

If DRAMATIC: open Phase 11.x investigation sub-plan; do NOT ship until either (a) regression is understood and accepted, or (b) the regressing commit is reverted.

If within drift: log this file's results in the v4.0.0 ship commit body and proceed. The numbers are archival, not gating.

## Commit body excerpt (for Plan 07 to copy)

```
LongMemEval Oracle: NN.N% (vs 90.6% baseline, ±N.Npp — within drift)
LoCoMo: NN.N% (vs 55.5% baseline, ±N.Npp — within drift; LoCoMo remains a v4.1 target)
Vibe-check only; does not gate ship per CONTEXT.md axiom.
```

## Caveats

- Benchmarks were dropped as ship gates per the v4 audit (2026-04-27). The behavioral suite (Vesna, SC#1) is the canonical ship gate.
- LoCoMo's 55.5% known-WIP baseline reflects honesty about current state; the earlier 90.8% from a previous harness was misleading and is NOT the baseline (per CLAUDE.md "don't cite the old number" warning).
```
  </action>
  <verify>
- 11-06-BENCHMARK-VIBE-CHECK.md exists with both benchmarks' actual numbers (or harness-unavailable notes)
- Drift verdict is explicit per benchmark
- The commit-body excerpt is included verbatim for Plan 07 to lift
- The non-gating axiom is restated in the file header
  </verify>
  <done>Both benchmarks recorded; drift assessed; non-gating posture is unmistakable.</done>
</task>

</tasks>

<verification>
- LongMemEval Oracle ran (or harness-unavailable noted)
- LoCoMo ran (or harness-unavailable noted)
- 11-06-BENCHMARK-VIBE-CHECK.md exists with archival record + drift verdict + commit-body excerpt
- The "non-gating" axiom is restated to forestall future confusion
</verification>

<success_criteria>
- v4.0.0 commit body has authentic, archived benchmark numbers (or explicit harness-unavailable note)
- Dramatic regressions trigger investigation; within-drift drift is logged and accepted
- The corrective from the audit is enforced: benchmarks document, do not gate
</success_criteria>

<output>
On completion, write `11-06-SUMMARY.md` pointing to the vibe-check file.
</output>
