---
plan: 06-06
phase: 06-p5-retrieval-simplification-multiplier-ablation
wave: 5
depends_on: [06-03, 06-04, 06-05]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-p5-retrieval-simplification-multiplier-ablation/06-SUMMARY.md
  - .planning/phases/06-p5-retrieval-simplification-multiplier-ablation/06-VESNA-RESULT.md
autonomous: true
---

# Plan 06-06 — Final SC#1 Vesna gate + STATE/ROADMAP/REQUIREMENTS update

**Wave:** 5 (depends on all of W3, W4, W5).
**Goal:** Verify Phase 6 closes against the v4 success-criteria gates, update tracking docs.

## Requirements addressed
- All Phase 6 RETR-* IDs flip from `[ ]` to `[x]` in `REQUIREMENTS.md`.
- ROADMAP Phase 6 status flips from `[ ]` to `[x]`.
- STATE reflects "v4 P5 simplification done; reranker hard-required surface live".

## must_haves
- SC#1 — Vesna pass rate ≥80% maintained on the post-deletion baseline.
- SC#2 — token budget: no regression (the new `rerankerHealthSection` only fires on degradation; unchanged in production state).
- SC#4 — handoff-pickup probe passes (covered by the ablation harness probe set).
- No SC gates use benchmarks (LongMemEval/LoCoMo) — confirmed.
- Full test suite (`bun run test`) passes; no new test failures vs. Phase 5.5 close (2851 / 76 baseline).
- All RETR-01..05, RETR-08 boxes ticked in REQUIREMENTS.md.
- ROADMAP Phase 6 row marked `[x]`.
- `06-SUMMARY.md` enumerates: multipliers dropped, multipliers kept, ablation result table, telemetry surface live, Vesna baseline.
- `06-VESNA-RESULT.md` follows the `05.5-VESNA-RESULT.md` template.

## Tasks

<task id="06-06-01">
<title>SC#1 Vesna gate run on the simplified pipeline</title>
<verification>
`runs/06-06-vesna-gate.json` exists with the post-deletion + post-RETR-08 pipeline pass rate. Pass rate ≥80% across all categories. If not, the phase does not close.
</verification>
<steps>
1. Run `bun run test src/tests/integration/phase-6-multiplier-ablation.test.ts` with `multiplierFlags = undefined` (production config).
2. Save the per-probe outcomes to `runs/06-06-vesna-gate.json`.
3. Assert overall pass rate ≥80% AND every category's pass rate ≥80%.
4. If any category fails, write the failure detail to `06-VESNA-RESULT.md` with verdict FAIL and BLOCK the close. Surface to team-lead.
</steps>
</task>

<task id="06-06-02">
<title>SC#2 token budget regression check</title>
<verification>
`bun run test src/tests/integration/phase-5-full-gate.test.ts` (or whichever Phase 5 cache-stability + token-budget gate is the canonical) passes with no degradation.
</verification>
<steps>
1. Run the Phase 5 gate test file (the cache-stability + token-budget harness from Plan 05-09).
2. The new `rerankerHealthSection` from W4 should be zero-impact when the cross-encoder is healthy (returns `null` and contributes 0 tokens).
3. If the test fails because the new section pushed the assembly over budget when fallback fired, that's a real signal — note it in `06-SUMMARY.md` (degraded mode does inflate budget by ~1 line; document and accept).
</steps>
</task>

<task id="06-06-03">
<title>Update REQUIREMENTS.md, ROADMAP.md, STATE.md</title>
<verification>
Every Phase 6 RETR-* requirement flipped to `[x]`. ROADMAP Phase 6 row flipped `[ ]` → `[x]` with completion date. STATE.md reflects v4 P5 close.
</verification>
<steps>
1. In `.planning/REQUIREMENTS.md`:
   - Flip RETR-01, RETR-02, RETR-03, RETR-04, RETR-05, RETR-08 from `[ ]` to `[x]`.
   - Update the table at the bottom: Phase 6 row flips to "Done" with date 2026-04-XX.
2. In `.planning/ROADMAP.md`:
   - Phase 6 entry: `- [ ] **Phase 6: P5 …**` → `- [x] **Phase 6: P5 …**` with `(completed YYYY-MM-DD)`.
   - Update the narrative paragraph above the phase table to acknowledge Phase 6 close.
3. In `.planning/STATE.md`:
   - Update "Current phase" to Phase 6.5 (next).
   - Add a Phase 6 entry under "Recent phases" with the multipliers dropped/kept and the SC#1 result.
   - Note that the cross-encoder is now hard-required infra.
</steps>
</task>

<task id="06-06-04">
<title>Write 06-SUMMARY.md and 06-VESNA-RESULT.md</title>
<verification>
Both files exist in the phase directory.
- `06-SUMMARY.md`: enumerates plans, multipliers dropped/kept, telemetry surface, gate results, deferred items.
- `06-VESNA-RESULT.md`: follows the `05.5-VESNA-RESULT.md` shape — pass rate per category, total runtime, deferred items.
</verification>
<steps>
1. `06-SUMMARY.md` template:
   ```markdown
   # Phase 6 — P5 Retrieval Simplification + Per-Multiplier Ablation — SUMMARY

   **Closed:** YYYY-MM-DD
   **Plans:** 06-01..06-06 (6 plans, 5 waves)

   ## Outcome

   - Multipliers ablated: 7 (recency, importance, relevance, retrieval, novelty, activation, qvalue)
   - Multipliers DROPPED: ⟨list from 06-MULTIPLIER-ABLATION.md⟩
   - Multipliers KEPT: ⟨list⟩
   - Channels: ⟨FTS5+vec0+recency baseline; graph/temporal verdict⟩
   - Cross-encoder reranker hard-required: telemetry counter `reranker_fallback_fired` live; observational warning surface in session-start; CLAUDE.md/README.md updated.
   - RIF + spread activation retained per RETR-03; verified in `phase-6-rif-spread-retained.test.ts`.
   - MCP surface unchanged per RETR-04; verified in `phase-6-mcp-surface-unchanged.test.ts`.

   ## Gate results

   - SC#1 Vesna pass rate (post-deletion): ⟨X%⟩ ≥80% PASS
   - SC#2 token budget: PASS (no regression)
   - SC#4 handoff-pickup: PASS (probe coverage in ablation harness)
   - Benchmarks: NOT USED (per v4 rebind)

   ## Deferred (non-blocking)

   - q_value column on `artifacts` table — kept; column-drop tracked as future hygiene.
   - Bi-encoder fallback-of-fallback telemetry (Ollama down) — non-blocking TODO.
   - Reranker model swap — out of scope.

   ## Files of record

   - `.planning/phases/06-.../06-CONTEXT.md`
   - `.planning/phases/06-.../06-RESEARCH.md`
   - `.planning/phases/06-.../06-MULTIPLIER-ABLATION.md`
   - `.planning/phases/06-.../06-03-DELETION-LIST.md`
   - `.planning/phases/06-.../06-VESNA-RESULT.md`
   - `.planning/phases/06-.../runs/*.json`
   ```
2. `06-VESNA-RESULT.md`: paste the structured table from `runs/06-06-vesna-gate.json`. Use `05.5-VESNA-RESULT.md` for the format.
</steps>
</task>

<task id="06-06-05">
<title>Atomic close commit</title>
<verification>
A single commit closes Phase 6: STATE/ROADMAP/REQUIREMENTS updates + SUMMARY + VESNA-RESULT in one commit. `git log --oneline -1` matches the expected message format.
</verification>
<steps>
1. `git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/06-p5-retrieval-simplification-multiplier-ablation/06-SUMMARY.md .planning/phases/06-p5-retrieval-simplification-multiplier-ablation/06-VESNA-RESULT.md`
2. Commit message: `phase(06): P5 close — simplified hybrid-retrieval, K multipliers dropped, reranker hard-required, SC#1 X%`.
3. Do NOT push to remote unless team-lead instructs.
</steps>
</task>

## Wave-end gate
- All RETR-* requirements ticked.
- ROADMAP Phase 6 marked done.
- STATE updated; Phase 6.5 is next.
- Final commit landed.
- Notify team-lead via SendMessage with the SC#1 result and a one-line phase summary.

## Out of scope
- Phase 6.5 (cross-project task-pattern recall).
- Phase 7 (framing rewrite).
- Phase 10 (full Vesna probe suite — current 10-probe set is the proxy used here).
