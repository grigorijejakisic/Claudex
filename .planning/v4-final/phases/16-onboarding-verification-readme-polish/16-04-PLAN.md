---
plan_id: 16-04
phase: 16
title: Phase 16 close — HITL-marking + CHANGELOG + 16-SUMMARY + close commit
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - CHANGELOG.md
  - .planning/phases/16-onboarding-verification-readme-polish/16-SUMMARY.md
requirements:
  - DOC-03
  - DOC-04
  - VER-01
  - VER-02
  - VER-03
autonomous: true
estimated_commits: 1
must_haves:
  - "REQUIREMENTS.md: DOC-03 + DOC-04 + VER-01 + VER-02 + VER-03 marked [x] (autonomous deliverables shipped)"
  - "REQUIREMENTS.md: PLAT-06 + PLAT-07 + PLAT-08 + VER-04 + VER-05 marked Pending (HITL) in traceability table — NOT [x]"
  - "ROADMAP.md Phase 16 row: 'Structural complete, HITL-pending — 2026-05-02 (operator runs gate full close)'"
  - "STATE.md current focus updated to reflect Phase 16 structural close + Phase 17 unblocked for autonomous parts"
  - "CHANGELOG.md [Unreleased] section populated with Phase 16 onboarding work (DOC-03/04 + VER-01/02/03 structural; PLAT-06..08 + VER-04/05 HITL-pending)"
  - "16-SUMMARY.md created at .planning/phases/16-onboarding-verification-readme-polish/16-SUMMARY.md following the 14-SUMMARY.md / 15-SUMMARY.md template"
  - "16-SUMMARY.md explicitly notes that Phase 17 (public ship) is NOT blocked by HITL-pending PLAT-06..08 — same flexibility pattern as Phase 11 SC#4"
  - "All cross-links validated: README §Quick Start ↔ §Troubleshooting ↔ docs/onboarding/{macos,linux,windows}.md all resolve"
  - "Hard gates re-run final time and recorded in 16-SUMMARY.md: bun run build green, bun run test ≥3188 + 20 baseline llama unchanged from v4.0.0, bun run vesna 17/17 GATED PASS, bun run doctor exits 0, DB schema unchanged, hook semantics unchanged"
  - "Phase-close commit message follows phase(16): convention used by 11/12/13/14/15 close commits"
---

# Plan 16-04: Phase 16 close — HITL-marking + CHANGELOG + 16-SUMMARY + close commit

## Objective

Close Phase 16 structurally. The autonomous deliverables (DOC-03, DOC-04, VER-01, VER-02, VER-03) shipped in Plans 16-01 / 16-02 / 16-03 are marked `[x]`; the HITL deliverables (PLAT-06, PLAT-07, PLAT-08, VER-04, VER-05) are marked `Pending (HITL)` — NOT `[x]` — in REQUIREMENTS.md, ROADMAP.md, and STATE.md. CHANGELOG.md `[Unreleased]` section gets a Phase 16 entry. `16-SUMMARY.md` captures what shipped and what's deferred. The phase-close commit lands in the `phase(16):` convention.

This plan is the only one in Wave 3. It must run after Plans 16-01, 16-02, and 16-03 have all landed — it depends on the README sections existing, the three fixture files existing, and the cross-links resolving. Its content is bookkeeping: no new code, no new docs beyond the SUMMARY itself.

The HITL-pending pattern matches Phase 11 SC#4 cold-start trials. Plan-phase pre-authorized this in `16-CONTEXT.md` `<plan_authorization>`. Phase 17 (public ship) is NOT blocked by the HITL items per `16-CONTEXT.md` decisions: v4.1 ships to GitHub with HITL items deferred, same flexibility Phase 11 used to ship v4.0.0 with three cold-start trials still HITL-pending.

## Why this comes last

Plan 16-04 references all three prior plans by file path and confirms cross-links resolve. Authoring it before the prior plans land would mean drafting a SUMMARY of work that hasn't shipped — fabrication risk. Wave 3 also re-runs the hard gates one final time as the phase-close witness, which only makes sense after every code/docs change in the phase has settled.

## Tasks

<task id="01-update-requirements-md">
**Update `.planning/REQUIREMENTS.md`** in two places:

**(a) Top-level checklist (lines 22-23, 34-36, 61-65 per the grep audit):** flip the autonomous deliverables to `[x]` and leave the HITL ones as `[ ]` (with a marker that distinguishes them from "not yet attempted"):

```markdown
- [x] **DOC-03**: README has a Quick Start section: clone → `bun run setup` → open Claude Code → working session, all in <30 minutes
- [x] **DOC-04**: README has a Troubleshooting section covering Ollama not running, port 7439 dead, Bun version mismatch, hook registration failure
...
- [~] **PLAT-06**: Install verified end-to-end on macOS (latest stable) on a fresh VM; friction captured as test fixtures _(HITL — fixture authored at docs/onboarding/macos.md, awaiting fresh-VM run)_
- [~] **PLAT-07**: Install verified end-to-end on Linux (Ubuntu 24.04 LTS) on a fresh VM; friction captured as test fixtures _(HITL — fixture authored at docs/onboarding/linux.md, awaiting fresh-VM run)_
- [~] **PLAT-08**: Install verified end-to-end on Windows 11 on a fresh VM; regression check (current development platform) _(HITL — fixture authored at docs/onboarding/windows.md split-mode, autonomous half PASS, fresh-VM half awaiting operator)_
...
- [x] **VER-01**: Onboarding fixture document at `docs/onboarding/macos.md` records every friction encountered installing on a fresh macOS VM _(structural — file shipped, friction blocks await operator)_
- [x] **VER-02**: Onboarding fixture at `docs/onboarding/linux.md` for Ubuntu 24.04 LTS VM _(structural — file shipped, friction blocks await operator)_
- [x] **VER-03**: Onboarding fixture at `docs/onboarding/windows.md` for Windows 11 VM (regression check) _(structural — file shipped, autonomous half recorded, HITL half awaits operator)_
- [~] **VER-04**: Each friction point in the three fixtures is resolved as: a code fix (preferred), a doctor check, or a README troubleshooting entry — none left as open _(HITL — friction resolution depends on PLAT-06/07/08 actually running)_
- [~] **VER-05**: <30 minute install target measured and met on each platform; total elapsed time recorded in fixture _(HITL — measurement depends on PLAT-06/07/08 actually running)_
```

The `[~]` marker matches the convention already in use elsewhere in this project (e.g., Phase 3 partial-with-followups, Phase 4 partial-corrective-pending) for "shipped at reduced acceptance with explicit follow-up tracked." Phase 16's HITL deliverables are exactly that: shipped structurally, awaiting operator-driven completion.

**(b) Traceability table (lines ~123-154):** flip the Status column for the autonomous deliverables and refine the HITL marker:

```markdown
| DOC-03 | Phase 16 | Done |
| DOC-04 | Phase 16 | Done |
...
| PLAT-06 | Phase 16 | Pending (HITL — macOS fresh VM; fixture at docs/onboarding/macos.md) |
| PLAT-07 | Phase 16 | Pending (HITL — Ubuntu 24.04 fresh VM; fixture at docs/onboarding/linux.md) |
| PLAT-08 | Phase 16 | Pending (HITL — Windows 11 fresh VM; fixture at docs/onboarding/windows.md split-mode, autonomous half PASS) |
...
| VER-01 | Phase 16 | Done (structural — fixture shipped HITL-pending) |
| VER-02 | Phase 16 | Done (structural — fixture shipped HITL-pending) |
| VER-03 | Phase 16 | Done (structural — fixture shipped split-mode, recorded half PASS) |
| VER-04 | Phase 16 | Pending (HITL — depends on PLAT-06/07/08 runs) |
| VER-05 | Phase 16 | Pending (HITL — depends on PLAT-06/07/08 runs) |
```

The locked decision per `16-CONTEXT.md`: VER-01..03 close `Done` because the fixture files exist with the agreed structure. The HITL fields inside those files (Friction / Elapsed / Verdict) are operator-fillable but the fixture deliverable itself is the file shape, which IS shipped.

(c) Coverage line at the bottom: update the count.

```
- Phase 16: 5 reqs done structurally (DOC-03/04 + VER-01/02/03), 5 reqs HITL-pending (PLAT-06/07/08 + VER-04/05)
```

If the existing line says `Phase 16: 10 reqs (PLAT-06..08, VER-01..05, DOC-03, DOC-04)`, update to clarify shipped vs HITL split.
</task>

<task id="02-update-roadmap-md">
**Update `.planning/ROADMAP.md`** in two places:

**(a) Phases (v4.1) checklist line ~64:** flip Phase 16 to a structural-complete-with-HITL marker:

```markdown
- [~] **Phase 16: Onboarding verification + README polish** — STRUCTURAL COMPLETE 2026-05-02 (DOC-03/04 + VER-01/02/03 shipped; PLAT-06..08 + VER-04/05 HITL-pending — fresh-VM operator runs gate full close)
- [ ] **Phase 17: Public ship to grigorijejakisic/claudex** — remote + history + tags + GitHub release + topics + badges + branch protection (UNBLOCKED — HITL-pending Phase 16 items do not gate Phase 17 per Phase 11 precedent)
```

**(b) Progress table at the bottom (line ~517):** update the Phase 16 row:

```markdown
| 16. Onboarding verification + README polish | 4/4 | Structural complete (HITL-pending: PLAT-06..08 + VER-04/05) | 2026-05-02 |
```

(4 plans = 16-01 README + 16-02 mac/linux fixtures + 16-03 Windows fixture + 16-04 close.)

**(c) Optional but recommended — add a "HITL-pending pattern" footnote** under the v4.1 progress table referencing Phase 11 SC#4 as precedent:

```markdown
**HITL-pending pattern note:** Phase 16's structural close mirrors Phase 11 SC#4 — autonomous deliverables (README sections + fixture file shapes) ship; operator-driven verification (fresh-VM installs, friction resolution, <30-min measurement) deferred to a follow-up gate. Phase 17 is NOT blocked by these deferrals (locked per Phase 16 CONTEXT.md). The operator returns the three fixtures filled in when ready; PLAT-06..08 + VER-04/05 flip to `[x]` at that point.
```

Place the footnote immediately after the progress table.
</task>

<task id="03-update-state-md">
**Update `.planning/STATE.md`** in three places:

**(a) "Current focus" paragraph (line 9):** rewrite to reflect Phase 16 structural close.

Old text (Phase 15 focus):
> **Current focus:** **Phase 15 SHIPPED 2026-05-02.** DIAG-01..08 closed; ... Next: Phase 16 (Onboarding verification + README polish, HITL).

New text:
> **Current focus:** **Phase 16 STRUCTURAL CLOSE 2026-05-02.** DOC-03/04 + VER-01/02/03 shipped (README §Quick Start ≤80 lines + §Troubleshooting ≤120 lines, both referencing Phase 14/15 verbatim; three onboarding fixtures at `docs/onboarding/{macos,linux,windows}.md` matching the Phase 11 SC#4 cold-start-trial template). PLAT-06..08 + VER-04/05 marked Pending (HITL) — operator-driven fresh-VM runs on macOS / Ubuntu 24.04 LTS / Windows 11 gate full close, not blocking Phase 17. Windows fixture is split-mode: steps 4-7 recorded autonomously from current dev machine state (doctor exit 0, 5 pass + 1 warn for Angel heartbeat per 15-SUMMARY baseline), steps 1-3 HITL-pending for fresh-VM rigor. 32/44 v4.1 reqs done (27 from Phase 12-15 + 5 structural from Phase 16). Next: Phase 17 (Public ship to grigorijejakisic/Claudex, REL-01..07).

**(b) "Current Position" block (lines 13-17):** update Phase + Status + Last activity:

```markdown
**Current Milestone:** v4.1 — Distribution
**Phase:** 17 (Public ship to grigorijejakisic/Claudex, REL-01..07) — next
**Plan:** —
**Status:** Phase 16 STRUCTURAL CLOSE 2026-05-02 (4 plans / 4 atomic implementation commits + this close commit). DOC-03/04 + VER-01/02/03 [x]. PLAT-06..08 + VER-04/05 [~] HITL-pending (operator-driven fresh-VM runs).
**Last activity:** 2026-05-02 — Phase 16 structurally closed
```

**(c) "Last Activity" + "Last Activity Description" (lines 35-36):** update to reflect the close.

Old block (Phase 15 description):
> **Last Activity:** 2026-05-02
> **Last Activity Description:** Phase 15 SHIPPED. DIAG-01..08 closed in 5 plans. ...

New block (replace, do NOT delete the Phase 15 paragraph that follows — that lives on as historical record):

> **Last Activity:** 2026-05-02
> **Last Activity Description:** Phase 16 STRUCTURAL CLOSE. 4 plans shipped: **16-01:** README §Quick Start (≤80 lines, walks prereqs → clone → install → verify → first session, references `./install.sh` + `install.bat` + `bun run setup` + `CLAUDEX_PROJECTS_DIR` from Phase 14 and `bun run doctor` + the 6 checks from Phase 15 verbatim) + §Troubleshooting (≤120 lines, four canonical install failures Ollama/7439/Bun/hooks, every entry routes through `bun run doctor` first); status banner updated to reflect v4.1 Quick Start shipped + HITL fresh-VM verification pending; `## Installation` placeholder "Coming in v4.1." removed. **16-02:** `docs/onboarding/macos.md` + `docs/onboarding/linux.md` matching the Phase 11 SC#4 cold-start-trial template (status header HITL-PENDING, 7 numbered steps each with Command/Expected/Friction/Elapsed, Friction summary + Resolutions + Verdict sections at end, all `[OPERATOR FILL-IN]` slots preserved); Linux fixture explicitly scoped to Ubuntu 24.04 LTS only (other distros out of scope per CONTEXT.md). **16-03:** `docs/onboarding/windows.md` split-mode — steps 1-3 (fresh Bun/Ollama/Python install) HITL-pending, steps 4-7 (clone / install.bat / `bun run doctor` / first session) recorded from current dev machine state; verbatim doctor table pasted into Step 6 (5 pass + 1 warn for Angel heartbeat staleness per 15-SUMMARY.md baseline); dual-Verdict at bottom (autonomous half PASS, HITL half HITL-PENDING) — does NOT close PLAT-08; only an operator-driven fresh-Win11-VM run does that. **16-04:** this close commit — REQUIREMENTS.md/ROADMAP.md/STATE.md updated to mark DOC-03/04 + VER-01/02/03 [x] / Done and PLAT-06..08 + VER-04/05 [~] / Pending (HITL); CHANGELOG.md [Unreleased] populated; 16-SUMMARY.md authored. Hard gates: `bun run build` ~70ms green; `bun run test` 3188+ baseline (matches Phase 15 close, +0 from this phase since edits are docs-only); 20 baseline llama-supervisor failures unchanged from v4.0.0; `bun run vesna` **17/17 GATED PASS**; `bun run doctor` exits 0; DB schema unchanged; hook semantics unchanged. Phase 17 (Public ship to grigorijejakisic/Claudex, REL-01..07) is the next concrete unit — UNBLOCKED per Phase 11 SC#4 precedent (HITL-pending PLAT/VER items do not gate public ship; operator returns the fixtures filled in when ready).

(Preserve the existing Phase 15 paragraph that follows — STATE.md keeps a running historical record, not just the most recent phase.)

**(d) "Phase: X of 17" line (line 39):** update.

Old: `Phase: 15 of 17 — Phase 15 SHIPPED (5/5 plans), Phase 16 next (HITL)`
New: `Phase: 16 of 17 — Phase 16 STRUCTURAL CLOSE (4/4 plans), Phase 17 next`

**(e) Progress bar line (line 43):** update to reflect 5 of 6 phases complete.

Old: `Progress: [████████████████░░░░░░] ~67% (v4.1 — 4 of 6 phases complete)`
New: `Progress: [██████████████████░░░░] ~83% (v4.1 — 5 of 6 phases structurally complete; Phase 17 = 6/6)`
</task>

<task id="04-update-changelog">
**Update `CHANGELOG.md` `[Unreleased]` section** (currently a placeholder reading "(v4.1 phases will populate this section as they land.)").

The format follows Keep-a-Changelog (already established in v4.0.0 entry). Add subsections only for actually-shipped categories. Phase 16 is docs-only — `### Added` covers everything.

Replace the placeholder line with:

```markdown
## [Unreleased]

### Added

- **Phase 16 — Onboarding verification + README polish (structural):** README §Quick Start (clone → `bun run setup` → working session, ≤80 lines, references the install scripts and `bun run doctor` shipped in Phases 14-15 verbatim); README §Troubleshooting (≤120 lines, four canonical failures — Ollama not running, port 7439 dead, Bun version mismatch, hook registration failure — all routed through `bun run doctor` first); three onboarding fixtures at `docs/onboarding/{macos,linux,windows}.md` matching the Phase 11 SC#4 cold-start-trial template. Windows fixture is split-mode (steps 4-7 recorded from current dev machine, steps 1-3 HITL-pending for fresh-VM rigor).

### Pending (HITL — gate full Phase 16 close, do not block v4.1 ship)

- **PLAT-06** — macOS install verified end-to-end on fresh VM
- **PLAT-07** — Ubuntu 24.04 LTS install verified end-to-end on fresh VM
- **PLAT-08** — Windows 11 install verified end-to-end on fresh VM (regression check)
- **VER-04** — every friction surfaced in fixtures resolved as code fix / doctor check / README troubleshooting entry
- **VER-05** — <30-minute install target measured and met on each platform

The HITL-pending items follow the same pattern Phase 11 SC#4 used to ship v4.0.0 with three live cold-start trials still operator-runnable: structural close documented; operator returns when ready; v4.1 ships without waiting.
```

Do NOT touch the v4.0.0 entry below. Do NOT add a `### Changed` or `### Fixed` section — Phase 16 is documentation-only and there are no code changes to call out.
</task>

<task id="05-write-16-summary">
**Create `.planning/phases/16-onboarding-verification-readme-polish/16-SUMMARY.md`** following the `15-SUMMARY.md` template (which itself followed the 14-SUMMARY.md template — both are now canonical for v4.1 phase closes).

Required structure:

```markdown
# Phase 16 — Onboarding Verification + README Polish — STRUCTURAL CLOSE

**Shipped:** 2026-05-02 (structural — autonomous deliverables on disk)
**HITL-pending close:** PLAT-06..08 + VER-04/05 awaiting operator-driven fresh-VM runs
**Generative axiom:** Strangers picking up `git clone` on Mac, Linux, or Windows can install Claudex in <30 minutes following `README.md` alone, with `bun run doctor` confirming health when something's off. The README half is shipped; the fresh-VM half awaits operator.

---

## What shipped (autonomous half)

| # | Deliverable | Location | Length |
|---|-------------|----------|--------|
| 1 | README §Quick Start (DOC-03) | `README.md` between §Why Claudex and §Diagnostics | ≤80 lines |
| 2 | README §Troubleshooting (DOC-04) | `README.md` after §Diagnostics | ≤120 lines |
| 3 | macOS onboarding fixture (VER-01) | `docs/onboarding/macos.md` | ~150-200 lines |
| 4 | Linux (Ubuntu 24.04 LTS) onboarding fixture (VER-02) | `docs/onboarding/linux.md` | ~150-200 lines |
| 5 | Windows 11 onboarding fixture (VER-03, split-mode) | `docs/onboarding/windows.md` | ~250-300 lines |

## Requirements closed (autonomous)

- [x] **DOC-03** — README §Quick Start exists; references Phase 14 commands (`./install.sh`, `install.bat`, `bun install --frozen-lockfile`, `bun run build`, `bun run setup`, `CLAUDEX_PROJECTS_DIR`) and Phase 15 commands (`bun run doctor`, the 6 diagnostic checks) verbatim
- [x] **DOC-04** — README §Troubleshooting covers Ollama not running / port 7439 dead / Bun version mismatch / hook registration failure; every entry routes through `bun run doctor` before listing manual fixes; reranker entry uses `⚠` (warn) not `✗` (fail), matching doctor's actual output
- [x] **VER-01** — `docs/onboarding/macos.md` exists with the agreed fixture template (status header HITL-PENDING, 7 numbered steps with Command/Expected/Friction/Elapsed each, Friction summary / Resolutions / Verdict sections, cross-links to README sections and to linux.md / windows.md)
- [x] **VER-02** — `docs/onboarding/linux.md` exists with same template, Ubuntu 24.04 LTS specifics (`apt install python3.11 python3.11-venv`, `curl -fsSL https://ollama.com/install.sh | sh`, `systemctl --user start ollama` or `ollama serve &` for headless), distro scope explicitly Ubuntu 24.04 only
- [x] **VER-03** — `docs/onboarding/windows.md` exists in split-mode: steps 1-3 (fresh Bun/Ollama/Python install) HITL-pending, steps 4-7 (clone / install.bat / `bun run doctor` / first session) recorded from current dev machine state with verbatim doctor table pasted in Step 6

## Requirements deferred (HITL — Phase 16 close conditional)

- [~] **PLAT-06** — macOS install verified end-to-end on fresh VM (operator runs `docs/onboarding/macos.md`)
- [~] **PLAT-07** — Ubuntu 24.04 LTS install verified end-to-end on fresh VM (operator runs `docs/onboarding/linux.md`)
- [~] **PLAT-08** — Windows 11 install verified end-to-end on fresh VM (operator runs steps 1-3 of `docs/onboarding/windows.md` from a clean Win11 VM; steps 4-7 already pre-recorded from dev machine, autonomous half PASS)
- [~] **VER-04** — friction-resolution audit (operator-driven; depends on PLAT-06/07/08 runs surfacing real friction first)
- [~] **VER-05** — <30-minute install target measured per platform (operator-driven; baseline is total elapsed time recorded in each fixture's Status header)

These deferrals follow the Phase 11 SC#4 pattern: structural autonomous + HITL operator-runnable. Phase 17 (public ship) is NOT blocked.

## Files added

- `docs/onboarding/macos.md` — VER-01 fixture
- `docs/onboarding/linux.md` — VER-02 fixture
- `docs/onboarding/windows.md` — VER-03 fixture (split-mode: autonomous half recorded, HITL half pending)

## Files modified

- `README.md` — §Quick Start added, §Troubleshooting added, status banner updated, `## Installation` placeholder removed
- `CHANGELOG.md` — `[Unreleased]` section populated with Phase 16 entry
- `.planning/STATE.md` — Phase 16 structural close status; Phase 17 unblocked
- `.planning/ROADMAP.md` — Phase 16 row updated to `[~]` Structural complete (HITL-pending); Phase 17 row marked unblocked; HITL-pending pattern footnote added
- `.planning/REQUIREMENTS.md` — DOC-03/04 + VER-01/02/03 marked `[x]` / Done; PLAT-06..08 + VER-04/05 marked `[~]` / Pending (HITL); coverage count updated

## Hard gates (autonomous half)

- `bun run build` — green (esbuild ~70ms; docs-only edits unaffected)
- `bun run test` — ≥3188 passing + 20 baseline llama-supervisor failures unchanged from v4.0.0 (no test changes; matches Phase 15 close exactly)
- `bun run vesna` — **17/17 GATED PASS** (entity-recall 3/3 · constraint-recall 3/3 · handoff-pickup 3/3 · cross-project 3/3 · lesson-application 3/3 · self-instrumented 2/2)
- `bun run doctor` — exit 0 (5 pass + 1 warn for Angel heartbeat staleness per 15-SUMMARY.md baseline; warn self-clears on next session-start)
- DB schema — unchanged
- Hook semantics — unchanged
- README final length — within ≤2000 line budget set by `16-CONTEXT.md`

## Cross-link audit

All links resolve at phase close:

- `README.md#quick-start` → §Quick Start in README ✓
- `README.md#troubleshooting` → §Troubleshooting in README ✓
- `docs/onboarding/macos.md` → from README status banner + Troubleshooting footer + Linux/Windows fixture cross-references ✓
- `docs/onboarding/linux.md` → from same set ✓
- `docs/onboarding/windows.md` → from same set + cross-references in macos.md / linux.md ✓
- `../../.planning/phases/14-bootstrap-install-configurable-paths/14-SUMMARY.md` → from Windows fixture ✓
- `../../.planning/phases/15-claudex-doctor-diagnostics/15-SUMMARY.md` → from Windows fixture ✓

## Notable decisions made during execution

(Plan-phase agent fills this in if any decision arose during 16-01..16-04 execution that wasn't pre-locked in `16-CONTEXT.md`. Examples of what would land here: "had to drop the Windows-specific Bun install line from Quick Start to fit ≤80 lines," "doctor's current output diverged from 15-SUMMARY.md baseline at fixture-recording time — re-recorded shape," etc. If no such decisions arose, the section says "No mid-execution decisions; all decisions pre-locked in 16-CONTEXT.md.")

## What's next

Phase 17 — Public ship to `grigorijejakisic/Claudex`. REL-01..07 (remote configuration + history push + tag + GitHub release + topics + badges + branch protection). Unblocked per Phase 11 SC#4 precedent — HITL-pending Phase 16 items do not gate public ship.

When the operator runs the three fixtures and reports back, PLAT-06..08 + VER-04/05 flip to `[x]` in REQUIREMENTS.md and ROADMAP.md, closing Phase 16 fully. That update can land any time, doesn't block v4.1.0 release.
```

The "Notable decisions made during execution" section is a placeholder — the agent running this plan inspects the prior three plans' commits and fills in any divergences from the locked decisions. Most likely there will be none, in which case the section says so.
</task>

<task id="06-verify-and-commit">
**Re-run hard gates one final time** as the phase-close witness, recording results in 16-SUMMARY.md if anything diverges from the recorded baseline:

```bash
bun run build          # esbuild ~70ms; expect green
bun run test           # vitest; expect ≥3188 + 20 baseline llama-supervisor failures unchanged from v4.0.0
bun run vesna          # expect 17/17 GATED PASS
bun run doctor         # expect exit 0; 5 pass + ≤1 warn matching 15-SUMMARY.md baseline
```

**Cross-link audit:** open each of the following files and confirm every relative link resolves to the expected target:

- `README.md` (anchors: §Quick Start, §Diagnostics, §Troubleshooting, §Documentation, §License; outbound: docs/onboarding/, CHANGELOG.md, CONTRIBUTING.md, LICENSE)
- `docs/onboarding/macos.md` (outbound: ../../README.md anchors, ./linux.md, ./windows.md, ../../.planning/phases/11-...)
- `docs/onboarding/linux.md` (same)
- `docs/onboarding/windows.md` (same + ../../.planning/phases/14-... + ../../.planning/phases/15-...)

If any link 404s, fix it before commit.

**File-list sanity:**

```bash
ls README.md CHANGELOG.md
ls docs/onboarding/macos.md docs/onboarding/linux.md docs/onboarding/windows.md
ls .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-SUMMARY.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-CONTEXT.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-01-PLAN.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-02-PLAN.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-03-PLAN.md
ls .planning/phases/16-onboarding-verification-readme-polish/16-04-PLAN.md
```

All must exist. If `16-SUMMARY.md` is missing, Task 05 didn't run — back up.

**Commit:**

Stage every file modified or created in this plan plus the artifacts from prior plans not yet committed (in practice, prior plans should have committed atomically; this commit is bookkeeping only — STATE/ROADMAP/REQUIREMENTS/CHANGELOG + 16-SUMMARY.md):

```bash
git add README.md CHANGELOG.md docs/onboarding/ .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/16-onboarding-verification-readme-polish/16-SUMMARY.md
git commit -m "phase(16): close — Onboarding verification + README polish (structural; HITL-pending PLAT-06..08 + VER-04/05)"
```

**Atomic commit message convention** (matches Phase 11 / 12 / 13 / 14 / 15 close commits):

```
phase(16): close — Onboarding verification + README polish (structural; HITL-pending PLAT-06..08 + VER-04/05)
```

After commit:

```bash
git log --oneline -5    # confirm phase-close commit at HEAD
git status              # confirm clean tree
```
</task>

## Verification criteria

- [ ] REQUIREMENTS.md: DOC-03, DOC-04, VER-01, VER-02, VER-03 marked `[x]` in checklist; status `Done` in traceability table
- [ ] REQUIREMENTS.md: PLAT-06, PLAT-07, PLAT-08, VER-04, VER-05 marked `[~]` in checklist with HITL annotation; status `Pending (HITL — ...)` in traceability table
- [ ] REQUIREMENTS.md: coverage line updated to reflect 5 done structurally + 5 HITL-pending in Phase 16
- [ ] ROADMAP.md: Phase 16 row marked `[~]` Structural complete with HITL-pending annotation; date 2026-05-02
- [ ] ROADMAP.md: Phase 17 row notes UNBLOCKED status; HITL-pending pattern footnote added
- [ ] ROADMAP.md: progress table Phase 16 row shows 4/4 plans Structural complete
- [ ] STATE.md: current focus paragraph reflects Phase 16 structural close
- [ ] STATE.md: Current Position block shows Phase 17 next, Plan —, Status reflects close
- [ ] STATE.md: Last Activity Description block has Phase 16 entry; preserves Phase 15 historical paragraph below
- [ ] STATE.md: Phase X of 17 line shows 16 of 17, structural close
- [ ] STATE.md: progress bar updated to ~83% / 5 of 6 phases
- [ ] CHANGELOG.md: `[Unreleased]` section populated with Phase 16 `### Added` block + HITL-pending list; v4.0.0 entry untouched
- [ ] `16-SUMMARY.md` created with structure matching 14/15-SUMMARY.md templates
- [ ] All cross-links in README and fixtures resolve (§Quick Start, §Troubleshooting, docs/onboarding/{mac,linux,win}.md, Phase 14/15 summaries)
- [ ] `bun run build` green
- [ ] `bun run test` ≥3188 + 20 baseline llama-supervisor failures unchanged
- [ ] `bun run vesna` 17/17 GATED PASS
- [ ] `bun run doctor` exits 0 (matches 15-SUMMARY.md baseline; any divergence noted in 16-SUMMARY.md "Notable decisions" section)
- [ ] DB schema unchanged
- [ ] Hook semantics unchanged
- [ ] Phase-close commit lands with the agreed message format
- [ ] `git status` clean after commit

## Out of scope

- Code changes — Phase 16 is docs-only beyond fixture authoring
- Marking PLAT-06..08 or VER-04/05 as `[x]` — those close only after operator-driven runs
- Public push, tagging, GitHub release — Phase 17
- Future-phase planning — separate `/gsd:` invocation after this close

## Notes for the implementer

- **Phase 11's close commit message format** is the closest precedent for split-mode close: see `.planning/phases/11-p9-final-validation/11-CLOSE-SUMMARY.md` and the close commit. Phase 16's commit follows the same pattern but at smaller scope (docs-only, 4 plans not 7).
- **STATE.md preserves history.** When updating "Last Activity Description," do NOT delete the Phase 15 paragraph that follows the new Phase 16 paragraph — STATE.md keeps a running record. This convention is visible in the existing Phase 15 paragraph which itself preserves the Phase 14 (and prior) descriptions below it.
- **The `[~]` partial marker** is the project's existing convention for "shipped at reduced acceptance with explicit follow-up." It's used elsewhere in ROADMAP.md (Phase 3 partial-with-followups; Phase 4 partial-corrective-pending). Phase 16 uses it identically: structural autonomous half shipped, operator-driven HITL half explicitly tracked.
- **The HITL-pending coverage count** is intentionally honest — REQUIREMENTS.md should not pretend Phase 16 is 10/10 done when only 5 deliverables actually shipped autonomously. The audit habit ("green numbers feel like progress while artifacts regress" — Phase 11 close) calls for honest tracking. This is the same discipline.
- **Cross-link audit** is part of phase close, not optional. A 404 link to `docs/onboarding/macos.md` from the README would be a phase-close failure even if all other gates pass. Validate explicitly.
- **If `bun run doctor` shape changes between Plan 16-03's recording and this plan's gate**, update the Windows fixture's Step 6 in a follow-up commit (`phase(16): re-record Windows fixture doctor table`) before the close commit. Document the divergence in 16-SUMMARY.md "Notable decisions" section. Do NOT silently let the fixture diverge from real doctor output — that turns the fixture into a fabrication.
