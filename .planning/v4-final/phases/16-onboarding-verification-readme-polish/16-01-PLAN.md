---
plan_id: 16-01
phase: 16
title: README Quick Start + Troubleshooting (DOC-03 + DOC-04)
wave: 1
depends_on: []
files_modified:
  - README.md
requirements:
  - DOC-03
  - DOC-04
autonomous: true
estimated_commits: 1
must_haves:
  - "README has a §Quick Start section between §Why Claudex and §Diagnostics"
  - "Quick Start ≤80 lines and walks: prereqs → clone → install → verify (doctor) → first session"
  - "Quick Start references real Phase 14 commands verbatim: install.sh, install.bat, bun run setup, CLAUDEX_PROJECTS_DIR"
  - "Quick Start references real Phase 15 command verbatim: bun run doctor (6 checks)"
  - "README has a §Troubleshooting section after §Diagnostics (or replaces / extends the existing Diagnostics block — see decisions below)"
  - "Troubleshooting ≤120 lines and covers: Ollama not running, port 7439 dead, Bun version mismatch, hook registration failure"
  - "Troubleshooting pattern per entry: Symptom → run bun run doctor → if [check] fails: [fix]"
  - "No fabricated CLI output (no invented Linux/Mac shell shapes); no emoji; no marketing"
  - "The placeholder text 'Coming in v4.1.' under §Installation is removed (superseded by §Quick Start)"
  - "bun run build green; bun run test ≥3188 baseline + 20 baseline llama failures unchanged from v4.0.0; bun run vesna 17/17; bun run doctor exits 0"
---

# Plan 16-01: README Quick Start + Troubleshooting

## Objective

Replace the README's `## Installation` placeholder ("Coming in v4.1.") with a real Quick Start that walks a stranger from `git clone` → working session, then add a Troubleshooting section that triages the four canonical install failures (Ollama down, port 7439 dead, Bun mismatch, hook reg failure) by routing every entry through `bun run doctor` first.

This is the README half of Phase 16. Both sections reference artifacts shipped in Phase 14 (install.sh / install.bat / `bun run setup` / `CLAUDEX_PROJECTS_DIR`) and Phase 15 (`bun run doctor` + the six diagnostic checks) verbatim — no fabrication of command shapes, no invented expected output. Closes DOC-03 and DOC-04.

## Why this comes first

Plans 16-02 / 16-03 (the runbook fixtures) reference README sections by anchor — fixtures will say "if Ollama isn't running, see README §Troubleshooting → Ollama not running." Authoring the README sections first means the fixtures can link into stable anchors. Plan 16-04 (phase close) then verifies all hard gates against a final README shape that won't shift again.

## Tasks

<task id="01-read-current-state">
**Read the current README.md** to confirm anchor positions before editing.

The current shape (`README.md` at repo root, 60 lines):

1. `# Claudex`
2. Header tagline + status banner (lines 1-5)
3. `## What is Claudex?` (lines 7-13)
4. `## Why Claudex?` (lines 15-23)
5. `## Installation` — **placeholder "Coming in v4.1."** (lines 25-27) — **REPLACE**
6. `## Diagnostics` — Phase 15's existing block on `bun run doctor` (lines 29-46) — **KEEP, used as anchor**
7. `## Documentation` (lines 48-54)
8. `## License` (lines 56-59)

The new shape after this plan:

1. `# Claudex`
2. Header tagline + status banner — **status banner updated**: "v4.1 Quick Start shipped; Phase 16 fresh-VM verification HITL-pending. See [docs/onboarding/](./docs/onboarding/) for per-platform fixtures."
3. `## What is Claudex?` (unchanged)
4. `## Why Claudex?` (unchanged)
5. `## Quick Start` — **NEW** (replaces `## Installation`)
6. `## Diagnostics` — unchanged (Phase 15)
7. `## Troubleshooting` — **NEW** (added after Diagnostics)
8. `## Documentation` (unchanged)
9. `## License` (unchanged)

**Order rationale:** Quick Start before Diagnostics so a first-time reader sees the install path before the diagnostic surface. Troubleshooting after Diagnostics so it can reference `bun run doctor` as the entry point without re-explaining what doctor is.
</task>

<task id="02-write-quick-start">
**Add `## Quick Start` to `README.md`** between `## Why Claudex?` and `## Diagnostics`. Length budget: ≤80 lines.

Required content shape (action-first, no tutorials):

```markdown
## Quick Start

Claudex runs entirely on your machine. Bring `bun >=1.3`, `ollama`, and Python `3.11+` (the BGE reranker uses a Python venv). Then:

### 1. Prereqs

| Tool | Minimum version | Install |
|------|-----------------|---------|
| Bun | 1.3 | `curl -fsSL https://bun.sh/install | bash` (macOS / Linux) · `powershell -c "irm bun.sh/install.ps1 | iex"` (Windows) |
| Ollama | latest | https://ollama.com/download |
| Python | 3.11 | `brew install python@3.11` (macOS) · `apt install python3.11 python3.11-venv` (Ubuntu 24.04) · https://www.python.org/downloads/ (Windows) |

`bun run doctor` (after install) verifies each of these — no need to memorize floors.

### 2. Clone

```bash
git clone https://github.com/grigorijejakisic/Claudex.git
cd Claudex
```

(Note the capital `C` in the URL — the repo is `grigorijejakisic/Claudex`.)

### 3. Install

```bash
./install.sh        # macOS / Linux
install.bat         # Windows (cmd.exe or PowerShell)
```

Either entry point pre-flights Bun, then runs `bun install --frozen-lockfile && bun run build && bun run setup`. The `bun run setup` step is the substantive bootstrap — it detects Bun, detects Ollama, pulls `snowflake-arctic-embed2` if missing, creates the BGE reranker Python venv at `services/.venv`, installs Python deps, spawns the reranker on port 7439, creates `~/.claudex/db/claudex.db`, and registers Claude Code hooks at `~/.claude/settings.json`. Idempotent — re-running it on a working install is a no-op.

### 4. Verify

```bash
bun run doctor
```

Six parallel checks: Bun version, DB schema, Ollama daemon + `snowflake-arctic-embed2`, BGE reranker on `:7439`, Claude Code hooks, Angel guardian process. Exit 0 means healthy; the reranker check warns rather than fails (bi-encoder fallback covers it). If anything fails, the doctor names the broken check and prints a one-line fix.

### 5. First session

By default Claudex tracks projects under `~/Projects/` (override via `CLAUDEX_PROJECTS_DIR=/some/other/path`). Open Claude Code in any subdirectory of that root — the SessionStart hook injects assembled context within the first user turn. No manual priming needed.

If your projects already live elsewhere, set the env var before launching Claude Code; setup writes the value into `~/.claudex/projects.json` on first run.
```

Hard authoring rules:

- **No emoji** (matches the rest of the README).
- **No marketing language** ("just minutes!", "blazing fast", etc.). Tone is direct technical-literate.
- **Use the exact commands shipped in Phase 14:** `./install.sh`, `install.bat`, `bun install --frozen-lockfile`, `bun run build`, `bun run setup`, `CLAUDEX_PROJECTS_DIR`. Do NOT paraphrase them. Read `install.sh` and `install.bat` at repo root if uncertain — those files are the source of truth.
- **Use the exact command shipped in Phase 15:** `bun run doctor`. The 6 checks must be named in the same order as the doctor's output (Bun version → DB schema → Ollama → Reranker → CC hooks → Angel) so a reader running doctor sees what the README described.
- **No fabricated expected output.** Phase 15's README §Diagnostics already shows the doctor table; do not duplicate it here. Quick Start describes what the steps DO; §Diagnostics shows what the output looks like.
- **Honest gaps:** if you write anything platform-specific (e.g., Linux `apt` line for Python 3.11), it is OK because the prereq install URLs are official upstream pages, not Claudex commands. Do NOT fabricate behavior for `bun run setup` on Linux/Mac — we have not run it on a fresh non-Windows VM yet (that is the HITL half of Phase 16).
- **Length:** target ≤80 lines (counting the heading line + table + code blocks + prose).
</task>

<task id="03-update-status-banner">
**Update the status banner** at lines 4-5 of `README.md`. Current text:

> **Status:** v4.0 shipped (internal infrastructure). v4.1 — Distribution — is in flight to make Claudex installable by strangers. Until v4.1 lands, install paths are not yet stable; track [CHANGELOG.md](./CHANGELOG.md) for ship dates.

New text (one line, no list):

> **Status:** v4.1 Quick Start shipped (Phase 16 structural). Fresh-VM verification on macOS / Ubuntu 24.04 / Windows 11 is HITL-pending — see [docs/onboarding/](./docs/onboarding/) for per-platform runbooks. Track [CHANGELOG.md](./CHANGELOG.md) for the v4.1 release.

Rationale: the install path IS stable now (`./install.sh` / `install.bat` / `bun run setup` shipped in Phase 14, `bun run doctor` shipped in Phase 15) — the only thing pending is operator-driven fresh-VM regression on Mac/Linux/Windows. The banner should match reality.
</task>

<task id="04-write-troubleshooting">
**Add `## Troubleshooting` to `README.md`** after `## Diagnostics` and before `## Documentation`. Length budget: ≤120 lines.

Required content shape (pattern: Symptom → run doctor → if check fails, fix it):

```markdown
## Troubleshooting

When something feels off, **always run `bun run doctor` first** — it pinpoints which subsystem is broken in under a second and prints a one-line remediation. The entries below cover the four canonical install failures and what each one looks like through the doctor lens.

### Ollama not running

**Symptom:** SessionStart assembly empty or stale; `claudex_search` calls return nothing; `bun run setup` hangs at "model-pull".

**Diagnostic:** `bun run doctor` reports `✗ Ollama` with one of:
- `daemon not reachable on 127.0.0.1:11434`
- `binary missing from PATH`
- `snowflake-arctic-embed2 not pulled`

**Fix:**
- macOS / Linux: start the daemon with `ollama serve &`. If the binary is missing, install per https://ollama.com/download.
- Windows: launch the Ollama app from Start Menu (it runs as a tray service).
- Once the daemon is up, run `bun run setup` again — the model-pull step is idempotent and will pull `snowflake-arctic-embed2` if it isn't already present.

### Port 7439 dead (BGE reranker)

**Symptom:** retrieval feels noticeably worse; SessionStart shows a "Reranker Health" line counting `reranker_fallback` events.

**Diagnostic:** `bun run doctor` reports `⚠ Reranker` (warn, not fail — the bi-encoder fallback keeps retrieval working). Common causes:
- `:7439/health` not reachable
- Python venv at `services/.venv` missing or broken
- The reranker process crashed and Angel hasn't restarted it yet

**Fix:**
- Re-run `bun run setup`. Step 4 (`reranker-bootstrap`) recreates the venv if missing and respawns the reranker on `:7439`.
- If the venv exists but the service won't start, delete `services/.venv/` and re-run `bun run setup` — the Python step is idempotent.
- If `bun run doctor` still warns after re-setup, check `~/.claudex/logs/reranker.log` for the underlying error (typically a Python version mismatch — Phase 14 requires Python 3.11+).

### Bun version mismatch

**Symptom:** `bun run setup` exits 1 immediately with a version-floor message; or hooks fail at session-start with `bun: command not found`.

**Diagnostic:** `bun run doctor` reports `✗ Bun version` with either:
- `Bun not found` (binary missing)
- `Bun X.Y.Z (<1.3 required)`

**Fix:**
- macOS / Linux: `curl -fsSL https://bun.sh/install | bash` (re-run installs the latest).
- Windows: `powershell -c "irm bun.sh/install.ps1 | iex"`.
- Verify with `bun --version`. The floor is `1.3.0` — anything below fails the doctor check.

### Hook registration failure

**Symptom:** SessionStart hook doesn't fire; first user turn shows no assembled context; PostToolUse events not recorded.

**Diagnostic:** `bun run doctor` reports `✗ CC hooks` with `N of 25 registered (M missing)`. The doctor names the missing hook(s).

**Fix:**
- Re-run `bun run setup`. Step 7 (hooks) patches `~/.claude/settings.json` to register every Claudex hook. Idempotent — safe to run on a working install.
- If a hook still won't register after re-setup, your `~/.claude/settings.json` may have a syntax error from a manual edit. Validate with any JSON parser; the doctor will refuse to register hooks into invalid JSON.
- After re-running setup, restart Claude Code. Hooks are loaded at session-start and don't hot-reload.

---

If `bun run doctor` exits 0 but something still feels broken, see [docs/onboarding/](./docs/onboarding/) for per-platform runbooks recording known friction points encountered on fresh VMs.
```

Hard authoring rules:

- **Every entry routes through `bun run doctor` first.** Doctor IS the diagnostic surface; this section's job is to map doctor output → fix, not to teach manual diagnostics.
- **Match doctor's actual output.** The check names ("Bun version", "DB schema", "Ollama", "Reranker", "CC hooks", "Angel") and pass/warn/fail symbols (`✓` / `⚠` / `✗`) come from `src/diagnostics/format.ts` shipped in Phase 15. Do not invent labels. If unsure, re-read `15-SUMMARY.md` lines 70-83 (live verification block) for the canonical output shape.
- **Reranker = warn, not fail** — locked in `15-CONTEXT.md` and respected by the doctor. The Troubleshooting entry must reflect this (`⚠ Reranker`, not `✗ Reranker`) or the agent will lie about what doctor prints.
- **Cite real file paths** (`services/.venv/`, `~/.claudex/logs/reranker.log`, `~/.claude/settings.json`). All shipped — don't invent log paths that don't exist. If `~/.claudex/logs/reranker.log` doesn't exist on this Windows machine, drop that line rather than fabricate it. Verify with `ls "$HOME/.claudex/logs/"` before authoring.
- **No emoji** (other than the doctor's own `✓`/`⚠`/`✗` glyphs, which are part of the diagnostic output and quoted as such).
- **Length:** target ≤120 lines.

**Before-write check on the reranker.log path:** if it doesn't exist on this machine after a full `bun run setup` cycle, change the line to "check the reranker process logs (location depends on how `bun run setup` started it)" rather than naming a fictional path.
</task>

<task id="05-verify">
**Run hard gates** after the README edits land:

```bash
bun run build          # esbuild ~70ms; expect green
bun run test           # vitest; expect ≥3188 passing + 20 baseline llama-supervisor failures unchanged
bun run vesna          # expect 17/17 GATED PASS
bun run doctor         # expect exit 0 (5 pass + ≤1 warn for Angel heartbeat staleness, same as Phase 15 baseline)
```

The README is markdown-only — none of these gates should be affected by the edits. If `bun run test` regresses (e.g., a snapshot test pinned to a stale README), revert and re-author the relevant section to be snapshot-equivalent.

**Length sanity check:**

```bash
awk '/^## Quick Start/,/^## Diagnostics/' README.md | wc -l   # expect ≤80
awk '/^## Troubleshooting/,/^## Documentation/' README.md | wc -l   # expect ≤120
```

(On Windows use `powershell -c "(Get-Content README.md | Select-String -Pattern '...').Count"` or just count by hand from line numbers.)

Atomic commit message: `phase(16): README Quick Start + Troubleshooting (DOC-03 + DOC-04)`
</task>

## Verification criteria

- [ ] `## Quick Start` exists in README between `## Why Claudex?` and `## Diagnostics`; ≤80 lines
- [ ] Quick Start uses verbatim commands from Phase 14 (`./install.sh`, `install.bat`, `bun install --frozen-lockfile`, `bun run build`, `bun run setup`, `CLAUDEX_PROJECTS_DIR`)
- [ ] Quick Start names `bun run doctor` (Phase 15) as the verify step and lists the 6 checks in the same order as doctor's output
- [ ] Status banner (lines 4-5) updated to reflect v4.1 Quick Start shipped + HITL-pending fresh-VM verification
- [ ] `## Installation` placeholder text "Coming in v4.1." is gone (superseded by Quick Start)
- [ ] `## Troubleshooting` exists after `## Diagnostics`; ≤120 lines
- [ ] Troubleshooting covers all 4 required topics: Ollama not running, port 7439 dead, Bun version mismatch, hook registration failure
- [ ] Every Troubleshooting entry routes through `bun run doctor` before listing manual fixes
- [ ] Reranker entry uses `⚠` (warn) not `✗` (fail) — matches doctor's actual behavior
- [ ] No emoji in the new sections (doctor output glyphs are quoted output, not authored emoji)
- [ ] No fabricated CLI output for commands not run on this Windows machine
- [ ] `bun run build` green
- [ ] `bun run test` ≥3188 passing + 20 baseline llama-supervisor failures unchanged
- [ ] `bun run vesna` 17/17 GATED PASS
- [ ] `bun run doctor` exits 0
- [ ] Atomic commit `phase(16): README Quick Start + Troubleshooting (DOC-03 + DOC-04)`

## Out of scope

- Onboarding fixtures at `docs/onboarding/{macos,linux,windows}.md` — Plans 16-02 and 16-03
- STATE / ROADMAP / REQUIREMENTS / CHANGELOG updates — Plan 16-04 (phase close)
- Code changes to `src/diagnostics/` or `src/cli/setup.ts` — Phase 16 is docs-only beyond fixture authoring (per `16-CONTEXT.md` plan_authorization)
- Public-ship copy edits (release notes, social blurbs) — Phase 17

## Notes for the implementer

- **The README is the public-ship artifact.** Strangers will read this in Phase 17. Tone matters: no marketing, no hedging, no apology for HITL-pending status. State what shipped, point at the diagnostic surface, list the four canonical failures. Done.
- **Re-read `15-SUMMARY.md` if unsure about doctor's output shape.** The "Live verification on this Windows machine" block (lines 70-83) is the canonical example of what users will see. Any Troubleshooting entry that contradicts that block is wrong.
- **Keep the existing `## Diagnostics` block intact.** Phase 15 shipped it; Phase 16 wraps Quick Start and Troubleshooting around it. Do not rewrite Phase 15's content.
- **The `[docs/onboarding/](./docs/onboarding/)` link in the status banner and Troubleshooting footer is forward-looking** — Plans 16-02 and 16-03 create the directory. The link will 404 until those plans land. That is acceptable mid-phase; Plan 16-04 verifies the link resolves at phase close.
- **If you run out of length budget**, drop the prereq install commands' Windows variant (Bun + Python only; Ollama Windows install is a graphical installer with no CLI shape worth quoting) before dropping any of the four Troubleshooting topics. The four topics are required by REQUIREMENTS.md DOC-04.
