---
plan: 09-07
phase: 9
sub_phase: 9.7
title: Delete data-quality.ts (sub-phase 9.7)
wave: 7
depends_on: [09-06]
files_modified:
  - src/angel/data-quality.ts
  - src/angel/heartbeat.ts
  - src/tests/angel/heartbeat.test.ts
  - src/tests/angel/guardian.test.ts
autonomous: true
requirements:
  - CUR-07
---

# Plan 09-07 — Delete data-quality.ts (sub-phase 9.7)

## Goal

Delete `src/angel/data-quality.ts` (424 lines) and its sole heartbeat
callsite (Phase 4d). Per CONTEXT.md `<artifacts>` — *"`src/core/data-quality.ts`
(or wherever it lives — planner verifies path) — 9.7 target"*. Verified:
the file lives at `src/angel/data-quality.ts`, NOT `src/core/`.

The CONTEXT.md `<decisions>.### Module dependencies` notes consumers in
"assembler quality-warning surface (Phase 5 deletes)". That assembler
section was deleted by Phase 5; the heartbeat tick is the only remaining
caller.

Also delete the `runDataQualityChecks` test surface in
`src/tests/angel/guardian.test.ts` (which has a dedicated
`describe('runDataQualityChecks', ...)` block and three test cases).

## Scope

| File | Change |
|---|---|
| `src/angel/data-quality.ts` | DELETE (whole file, 424 lines) |
| `src/angel/heartbeat.ts` | Drop the line-50 import; delete Phase 4d block at lines ~991-1004 (the `runDataQualityChecks` callsite). |
| `src/tests/angel/heartbeat.test.ts` | Drop `runDataQualityChecks: vi.fn().mockReturnValue({...})` mock entry (line ~82-87). |
| `src/tests/angel/guardian.test.ts` | Drop the `runDataQualityChecks` import (line ~28) and the `describe('runDataQualityChecks', ...)` block (lines ~678-710). |

## Tasks

<task id="09-07-01">
  <subject>Verify path + zero non-test, non-self consumers</subject>
  <description>
First, confirm path:
```bash
test -f src/angel/data-quality.ts && echo "found at src/angel"
test -f src/core/data-quality.ts && echo "found at src/core"
```

Expected: `src/angel/`. CONTEXT.md hedged on the location; planner verifies.

Then grep for consumers:
```bash
grep -rnE "data-quality|runDataQualityChecks" src/ \
  --include="*.ts" --exclude-dir=node_modules
```

Expected matches:
- `src/angel/data-quality.ts` — self
- `src/angel/heartbeat.ts:50, 991-1004` — import + Phase 4d
- `src/tests/angel/heartbeat.test.ts:82-87` — mock
- `src/tests/angel/guardian.test.ts:28, 678-710` — test surface

Verify NO match in:
- `src/assembly/` (CONTEXT.md said the assembler quality-warning consumer
  was deleted by Phase 5 — confirm)
- `src/intelligence/`, `src/core/`, MCP handlers
- Other test files

If `src/assembly/` still references data-quality, Phase 5's deletion was
incomplete; STOP and amend.
  </description>
  <verification>
- Path confirmed at `src/angel/data-quality.ts`
- Grep returns only the four expected files
- `src/assembly/` and other surfaces have zero matches
  </verification>
</task>

<task id="09-07-02">
  <subject>Delete data-quality.ts</subject>
  <description>
`rm src/angel/data-quality.ts`. Whole-file deletion — 424 lines.

```bash
find src/tests -name "data-quality*"
```
Returns empty at plan-write time. If a dedicated test file exists at
execution time, delete it too.
  </description>
  <verification>
- `test ! -f src/angel/data-quality.ts`
- No orphan test files (the surface lives in guardian.test.ts, handled separately)
  </verification>
</task>

<task id="09-07-03">
  <subject>Remove heartbeat Phase 4d block + import</subject>
  <description>
In `src/angel/heartbeat.ts`:

1. Drop line 50 import:
   ```ts
   import { runDataQualityChecks } from './data-quality.js';
   ```

2. Delete the Phase 4d block (around lines 991-1004):
   ```ts
       // Phase 4d: Data quality & integrity checks.
       try {
         const qualityResult = runDataQualityChecks(ctx.db, ctx.config.retention);
         // ... result.data_quality_* fields ...
       } catch { /* non-critical */ }
   ```

3. Remove any `result.data_quality_*` or similar fields written only by this
   block from the heartbeat result-type interface.

Sibling blocks (Phase 4d2 codebase index refresh, Phase 4d3 MemRL temporal
decay — note 4d3 is part of the RL stack and is handled in Plan 09-08; do
NOT touch it here) stay intact.
  </description>
  <verification>
- `grep -n "data-quality\|runDataQualityChecks\|Phase 4d\b" src/angel/heartbeat.ts` returns zero matches (the `\b` excludes Phase 4d2 / 4d3)
- `grep -n "Phase 4d2\|Phase 4d3" src/angel/heartbeat.ts` returns matches (siblings preserved)
- `bun run build` exits 0
  </verification>
</task>

<task id="09-07-04">
  <subject>Update heartbeat.test.ts mock</subject>
  <description>
Drop `runDataQualityChecks: vi.fn().mockReturnValue({...})` from the
data-quality mock object (line ~82-87). If the whole
`vi.mock('../../angel/data-quality.js', ...)` block becomes empty, delete
the entire `vi.mock(...)` call.
  </description>
  <verification>
- `grep -n "data-quality\|runDataQualityChecks" src/tests/angel/heartbeat.test.ts` returns zero matches
- `bun run test src/tests/angel/heartbeat.test.ts` exits 0
  </verification>
</task>

<task id="09-07-05">
  <subject>Update guardian.test.ts</subject>
  <description>
In `src/tests/angel/guardian.test.ts`:

1. Drop `runDataQualityChecks` from the import block at line ~28:
   ```ts
   import { ..., runDataQualityChecks, ... } from '../../angel/data-quality.js';
   ```
   If the import line is now empty (only `runDataQualityChecks` was
   imported from data-quality.js), delete the entire import statement.

2. Delete the `describe('runDataQualityChecks', ...)` block at lines
   ~678-710. The block contains three test cases (`r1 = runDataQualityChecks(db, config1)`,
   `r2 = runDataQualityChecks(db, config1)`, `result = runDataQualityChecks(db, disabledConfig)`).
   Delete the entire `describe` block.

3. Verify the rest of guardian.test.ts is independent of data-quality —
   surviving describes test other guardian surfaces (retention sweep,
   memory monitor, etc.). Don't touch them.
  </description>
  <verification>
- `grep -n "data-quality\|runDataQualityChecks" src/tests/angel/guardian.test.ts` returns zero matches
- `bun run test src/tests/angel/guardian.test.ts` exits 0
  </verification>
</task>

<task id="09-07-06">
  <subject>Vitest pass + Vesna spot-check + commit</subject>
  <description>
```bash
bun run test
bun run test src/tests/integration/phase-6-multiplier-ablation.test.ts \
              src/tests/integration/phase-6-5-cross-project-vesna.test.ts
```

Pass rate ≥80%.

Atomic commit:

```
chore(angel): delete data-quality (Phase 9.7)

Dead infra cleanup — assembler quality-warning consumer was deleted by
Phase 5; heartbeat Phase 4d was the only remaining inbound. Vesna
spot-check N/M ≥80%.

- src/angel/data-quality.ts (deleted, 424 lines)
- src/angel/heartbeat.ts: drop Phase 4d block + import (~16 lines)
- src/tests/angel/heartbeat.test.ts: drop mock entry
- src/tests/angel/guardian.test.ts: drop import + runDataQualityChecks
  describe block (3 test cases)

Phase 9.7 of 9 (sub-phased per-module deletion). Last non-RL sub-phase;
9.8 (RL stack) and 9.9 (phase close) follow.
```

Append STATE.md sub-phase entry.
  </description>
  <verification>
- `bun run build` + `bun run test` exit 0
- Vesna ≥80%
- Single commit
- STATE.md updated
  </verification>
</task>

## must_haves

- [ ] `src/angel/data-quality.ts` does not exist
- [ ] Heartbeat Phase 4d block + import gone (Phase 4d2 / 4d3 untouched)
- [ ] heartbeat.test.ts mock cleaned
- [ ] guardian.test.ts data-quality import + describe block removed
- [ ] `bun run build` + `bun run test` exit 0
- [ ] Vesna spot-check ≥80%
- [ ] Single atomic commit
- [ ] STATE.md updated

## Out of scope

- Phase 4d2 codebase indexer (`codebase-indexer.ts`) — stays
- Phase 4d3 RL temporal decay block — Plan 09-08 (sub-phase 9.8) handles it
- `data_quality` table or any persistence schema — none exists; this module
  was a pure-function quality-check pass over existing tables
- Other 9.X sub-phases

## Risk + rollback

Risk: low. The data-quality checks were observability-only — they wrote
flags / metrics rather than mutating core retrieval state. With Phase 5
having already deleted the assembler section that surfaced them, the
remaining heartbeat tick produced output nothing read.

Rollback: `git revert HEAD`.
