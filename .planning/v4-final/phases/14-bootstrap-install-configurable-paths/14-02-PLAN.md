---
phase: 14-bootstrap-install-configurable-paths
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/shared/scope-detector.ts
  - src/shared/content-router.ts
  - src/mcp/recall-server.ts
  - src/assembly/sections.ts
  - src/core/session-events.ts
  - src/angel/memory-md-writer.ts
  - src/angel/curated-context-extractor.ts
  - "{any other code-fix files identified in 14-01-CALLSITE-AUDIT.md}"
autonomous: true
requirements:
  - INST-05
  - INST-06
must_haves:
  truths:
    - "Every code-fix entry from 14-01-CALLSITE-AUDIT.md is applied — replaced with getProjectsDir() or equivalent"
    - "Every string-text-fix entry is applied — runtime interpolation via getProjectsDir() or generic phrasing"
    - "Every comment-update entry is applied — references CLAUDEX_PROJECTS_DIR and the new ~/Projects default"
    - "src/mcp/recall-server.ts MCP instructions text uses getProjectsDir() at runtime so the registered server reflects the user's configured directory (INST-06)"
    - "After this plan, `grep -rn 'Desktop/Projects' src/` returns only documented kept-with-reason hits (test fixtures, llama-server-supervisor's separate-concern defaults, intentional historical-comment references)"
    - "scope-detector.ts and content-router.ts use getProjectsDir() for the directory scan — no hardcoded path.join(os.homedir(), 'Desktop', 'Projects') remains"
    - "Existing project registry at ~/.claudex/projects.json is NOT migrated automatically (per CONTEXT.md decision); registered entries continue to resolve correctly because their absolute paths are stored, not relative-to-base"
    - "Existing tests still pass — including memory-md-writer.test.ts and memory-md-verify.test.ts which contain Desktop/Projects fixtures (rewrite via getProjectsDir() override OR update fixtures to match new default)"
    - "bun run build green"
    - "bun run test passes baseline + 14-01 new tests; no regression"
    - "bun run vesna 17/17 PASS"
    - "DB schema unchanged"
    - "Hook semantics unchanged"
  artifacts: []
  key_links:
    - from: 14-02-PLAN.md
      to: 14-01-CALLSITE-AUDIT.md
      via: "Consumes the code-fix, string-text-fix, comment-update, and test-fixture rows"
      pattern: "code-fix"
---

<objective>
Apply every fix from `14-01-CALLSITE-AUDIT.md` so the codebase stops hardcoding `~/Desktop/Projects/` and instead resolves the projects directory through `getProjectsDir()` (env-var aware). This satisfies INST-05 and INST-06.

The MCP server's instructions text (`src/mcp/recall-server.ts`) is the one acceptance gate that requires runtime interpolation — when a user sets `CLAUDEX_PROJECTS_DIR=~/code/work` and registers the server, the instructions string the agent sees at session start must say "All projects live in ~/code/work" not "~/Desktop/Projects/". This is what INST-06 means in practice.

Existing project registry data at `~/.claudex/projects.json` is left alone (CONTEXT.md decision: no automatic data migration). Registered entries store absolute paths, so they keep resolving correctly regardless of base-dir change.
</objective>

<execution_context>
**Inputs:**
- `.planning/phases/14-bootstrap-install-configurable-paths/14-01-CALLSITE-AUDIT.md` — the classified list

**Fix patterns (from CONTEXT.md `<decisions>` INST-05):**

For **code-fix**:
```ts
// Before
const baseDir = path.join(os.homedir(), 'Desktop', 'Projects');

// After
import { getProjectsDir } from '../shared/projects-dir.js';
const baseDir = getProjectsDir();
```

For **string-text-fix** in MCP instructions and assembly sections:
The instruction text is a template literal built once at module load. Convert it to a function that builds the string at call time, OR keep it template-y but lazily resolve via `${getProjectsDir()}`:

```ts
// Before (recall-server.ts)
const INSTRUCTIONS = `... All projects live in ~/Desktop/Projects/ ...`;

// After — option A: build at registration time
function buildInstructions(): string {
  return `... All projects live in ${getProjectsDir()} ...`;
}

// After — option B: keep static but use a generic phrasing
const INSTRUCTIONS = `... All projects live under your configured CLAUDEX_PROJECTS_DIR (default ~/Projects/) ...`;
```

Prefer option A for `recall-server.ts` (the user wants their actual configured path shown, not generic) and option B for `assembly/sections.ts` (less load-bearing; generic wording is cleaner).

For **comment-update** in JSDoc:
```ts
// Before
// Scans ~/Desktop/Projects/ for unregistered project directories

// After
// Scans the configured projects directory (CLAUDEX_PROJECTS_DIR, default ~/Projects/)
// for unregistered project directories.
```

For **test-fixtures** (`memory-md-writer.test.ts`, `memory-md-verify.test.ts`):
- If the test sets `os.homedir()` to a controlled path and expects `Desktop/Projects` joined onto it, override the env var to match the test's expected base:
  ```ts
  process.env.CLAUDEX_PROJECTS_DIR = path.join(homedirMock, 'Desktop', 'Projects');
  ```
  This keeps the test's intent (verifying scan logic for derived IDs) without rewriting test data.
- If the fixture is a literal string assertion that doesn't go through `getProjectsDir()`, leave it but document why in the audit's keep-with-reason section.

**Authority (per CONTEXT.md `<plan_authorization>`):**
- The executor may downgrade an audit-classified fix from code-fix to keep-with-reason if inspection proves it's intentional (rare; flag in commit message).
- The executor may NOT migrate `~/.claudex/projects.json` data automatically — leave it.
- The executor may NOT replace the existing RerankerSupervisor (delegate, don't replace) — but RerankerSupervisor isn't touched by this plan anyway.

**Out of scope:**
- `src/angel/llama-server-supervisor.ts:26,46,48` — these reference `~/Desktop/Projects/llama-cpp/...` for the llama-server binary path, NOT the projects scan. They have their own env vars (`LLAMA_SERVER_EXE`, `LLAMA_MODEL_PATH`) and are a separate concern. Leave them; document in keep-with-reason.
- `src/cli/setup.ts` bootstrap — that's 14-03's territory.
- `install.sh` / `install.bat` — that's 14-04's territory.

</execution_context>

<tasks>

<task id="14-02-01">
**Subject:** Apply code-fix replacements in scope-detector.ts and content-router.ts

**Action:**
Edit `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\shared\scope-detector.ts` line ~133 and `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\shared\content-router.ts` line ~100. Replace:

```ts
const baseDir = path.join(os.homedir(), 'Desktop', 'Projects');
```

with:

```ts
const baseDir = getProjectsDir();
```

Add the import at the top of each file:
```ts
import { getProjectsDir } from './projects-dir.js';
```

Update the JSDoc/inline comments above each replaced line to match (per audit's comment-update entries):
- `scope-detector.ts:108` JSDoc — change `Scans ~/Desktop/Projects/` to `Scans the configured projects directory (CLAUDEX_PROJECTS_DIR, default ~/Projects/)`
- `content-router.ts:9` JSDoc — same wording

**Verification:**
- `grep -n "Desktop/Projects" src/shared/scope-detector.ts` returns no hits
- `grep -n "Desktop/Projects" src/shared/content-router.ts` returns no hits
- `getProjectsDir` import is present and used in both files
- `bun run build` green
- `bun run test` passes (scope-detector and content-router unit tests, if any, still green; no behavior regression because both old + new resolve to a real directory at scan time)

**Commit:** `phase(14-02): INST-05 — scope-detector + content-router use getProjectsDir()`
</task>

<task id="14-02-02">
**Subject:** Update MCP server instructions to use runtime projects-dir (INST-06)

**Action:**
Edit `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\mcp\recall-server.ts`. The instructions text around line 127 contains:

```
All projects live in ~/Desktop/Projects/. The project registry is at ~/.claudex/projects.json.
```

The current implementation builds the instructions string at module-load time as a const template literal (search for `## Navigation Rule` in the file to find the exact construction). Refactor so the projects-dir portion is resolved at instruction-build time, not module-load time.

Two options:
- **A (preferred):** wrap the instructions string in a function `buildClaudexInstructions(): string` called by the MCP server at registration. The function calls `getProjectsDir()` and interpolates.
- **B:** if the existing structure makes A invasive, keep it a const but defer evaluation by calling `getProjectsDir()` inside a getter or initializing the const after a small refactor.

Pick whichever produces the minimal diff. Add the import:
```ts
import { getProjectsDir } from '../shared/projects-dir.js';
```

Update the existing test seam (`getClaudexInstructions()` or whatever the file already exposes for tests — see `narration-directive integration tests` referenced at line ~134) so it produces the runtime-resolved string. Existing assertion tests for the instructions text may need updating to inject `CLAUDEX_PROJECTS_DIR` or to assert against the dynamic-result.

**Verification:**
- `grep -n "Desktop/Projects" src/mcp/recall-server.ts` returns no hits
- `getProjectsDir()` is called when the instructions string is constructed (verify by `grep -n getProjectsDir src/mcp/recall-server.ts`)
- Setting `CLAUDEX_PROJECTS_DIR=/tmp/test-projects` and re-building the instructions string shows `/tmp/test-projects` in the output (manual sanity check via small test or REPL)
- All existing recall-server tests pass — particularly `narration-directive` tests; update assertions if they hardcoded the old path
- `bun run build` green
- `bun run test` passes baseline + projects-dir tests

**Commit:** `phase(14-02): INST-06 — MCP recall-server instructions use getProjectsDir()`
</task>

<task id="14-02-03">
**Subject:** Apply string-text-fix and comment-update replacements in remaining src/ files

**Action:**
Per the 14-01 audit's string-text-fix and comment-update rows, edit:

- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\assembly\sections.ts` (~line 71): the system-reminder text says `All projects are in \`~/Desktop/Projects/\``. Change to a generic phrasing: `All projects are under your configured CLAUDEX_PROJECTS_DIR (default \`~/Projects/\`)`. (Generic is fine here; the agent doesn't need the literal path interpolated.)
- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\core\session-events.ts` (~line 332): the path-simplification regex hardcodes `Desktop/Projects/CLAUDEXv3/`. Update so it handles the configured value too — either a regex built from `getProjectsDir()` at simplify-time, or a regex that matches both old and new defaults: `(?:Desktop\/Projects|Projects)`. Choose whichever the audit recommends; the test in `session-events.test.ts` (if any) should still pass.
- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\angel\memory-md-writer.ts` (~line 94): JSDoc comment. Replace mention of `~/Desktop/Projects/` with `the configured projects directory (CLAUDEX_PROJECTS_DIR, default ~/Projects/)`.
- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\angel\curated-context-extractor.ts` (~line 119): the comment is an example in JSDoc (`"Code lives at ~/Desktop/Lacuna, docs at ~/Desktop/Projects/Lacuna-Betting."`) — leave the example as-is OR change `~/Desktop/Projects/` to `~/Projects/`; this is illustrative content, not actionable. Document the chosen fix in the commit message.

For each file, only touch the specific line(s) called out by the audit. Do not opportunistically edit adjacent code.

**Verification:**
- After this task, `grep -rn "Desktop/Projects" src/` returns only:
  - `src/angel/llama-server-supervisor.ts:26,46,48` (kept-with-reason: separate llama-cpp concern, has own env vars)
  - Test fixtures handled by 14-02-04 (next task)
- `bun run build` green
- `bun run test` passes — particularly `session-events.test.ts` if it asserts on the simplified-path output (update test fixture if needed)
- Assembly section text change is reflected in section-snapshot tests if any (`src/tests/assembly/sections.test.ts`)

**Commit:** `phase(14-02): INST-05 — string-text + comment updates for projects-dir`
</task>

<task id="14-02-04">
**Subject:** Reconcile test fixtures that reference the old default

**Action:**
Two test files contain fixtures with the literal old path:
- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\tests\angel\memory-md-writer.test.ts` line ~695 — `// No projects.json, no matching Desktop/Projects scan entry.`
- `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\src\tests\core\memory-md-verify.test.ts` line ~183 — `const cwd = 'C:/Users/Test/Desktop/Projects/alpha';`

For `memory-md-writer.test.ts:695` — comment only. Update to `// No projects.json, no matching projects-dir scan entry.`.

For `memory-md-verify.test.ts:183` — fixture path. Two options:
- **Preferred:** set `process.env.CLAUDEX_PROJECTS_DIR = 'C:/Users/Test/Desktop/Projects'` in the test's `beforeEach` so `getProjectsDir()` returns the same base the test expects, then keep `cwd` as-is. The test is asserting derive-project-id behavior, not the literal default; an env-var override preserves intent.
- **Alternative:** rewrite `cwd` to `'C:/Users/Test/Projects/alpha'` and update any other Desktop/Projects fixtures in the same test to match.

Pick the minimal-diff option. After the fix, `grep -rn "Desktop/Projects" src/tests/` should either return zero hits OR only documented kept-with-reason hits (e.g., a test that explicitly tests the legacy-default fallback path, if such a test exists).

**Verification:**
- `grep -rn "Desktop/Projects" src/tests/` returns no hits, OR only kept-with-reason hits documented in the audit
- `bun run test` passes — both files' tests still green
- `bun run build` green

**Commit:** `phase(14-02): INST-05 — test fixtures use getProjectsDir-aware paths`
</task>

<task id="14-02-05">
**Subject:** Final grep audit and per-plan SUMMARY

**Action:**
Run final greps to verify cleanup:

```bash
grep -rn "Desktop/Projects" src/
grep -rn "'Desktop', 'Projects'" src/
grep -rn '"Desktop", "Projects"' src/
grep -rn 'path\.join(os\.homedir(), .Desktop' src/
```

Every remaining hit must be:
- An entry in `src/angel/llama-server-supervisor.ts` (separate concern), OR
- A documented kept-with-reason entry in `14-01-CALLSITE-AUDIT.md`'s keep section

Write `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\phases\14-bootstrap-install-configurable-paths\14-02-SUMMARY.md` with:
- Files modified (count + names)
- Replacements applied (counts per category: code-fix, string-text-fix, comment-update, test-fixture)
- Final grep output (the kept hits, with reasons)
- Confirmation that `bun run build`, `bun run test`, `bun run vesna` all green

**Verification:**
- 14-02-SUMMARY.md exists
- Final greps return only kept-with-reason hits
- `bun run build` green
- `bun run test` passes 3128 (3123 baseline + 5 from 14-01) + 20 baseline llama failures unchanged
- `bun run vesna` 17/17 PASS

**Commit:** `phase(14-02): close — projects-dir refactor complete`
</task>

</tasks>

<verification>
After this plan completes:
- All code-fix, string-text-fix, comment-update entries from 14-01 are applied
- MCP server's instructions text uses `getProjectsDir()` so the agent sees the user's configured path (INST-06)
- `grep -rn "Desktop/Projects" src/` returns only kept-with-reason hits
- `bun run build` green
- `bun run test` passes baseline + projects-dir tests; no regression
- `bun run vesna` 17/17 PASS
- DB schema unchanged
- Hook semantics unchanged
- ~/.claudex/projects.json registry data is untouched (no auto-migration)
</verification>
