---
phase: 14-bootstrap-install-configurable-paths
plan: 05
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
  - 14-03
  - 14-04
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/14-bootstrap-install-configurable-paths/14-SUMMARY.md
autonomous: true
requirements:
  - INST-01
  - INST-02
  - INST-03
  - INST-04
  - INST-05
  - INST-06
  - INST-07
must_haves:
  truths:
    - "Live first-session UX verified: after bun run setup, opening Claude Code in any project under getProjectsDir() produces working assembly within 1 user turn — session-start fires, Angel auto-spawns, MCP serves (INST-07)"
    - "Hard gate: bun run build green"
    - "Hard gate: bun run test passes baseline 3123 + new tests from 14-01 (5) + 14-03 (~17) ≈ 3145; 20 baseline llama failures unchanged from v4.0.0; nothing else regressed"
    - "Hard gate: bun run vesna 17/17 PASS"
    - "Hard gate: DB schema unchanged (verify by inspecting migrations module — no new migration registered for Phase 14)"
    - "Hard gate: hook semantics unchanged (verify by inspecting src/adapters/cc-hooks/ — no behavioral changes from 14-01..04)"
    - "Hard gate: `bun run setup` is idempotent — two consecutive runs both exit 0 with same end state"
    - "Hard gate: existing v4 install on this Windows machine is not regressed — Angel still alive, reranker still on 7439, hooks still registered, MCP still serving"
    - "STATE.md updated: Phase 14 marked [x]; INST-01..07 marked [x] in traceability"
    - "ROADMAP.md updated: Phase 14 status changed to closed; Plans line lists the actual 5 plans"
    - "REQUIREMENTS.md updated: INST-01..07 checkboxes flipped to [x]"
    - "14-SUMMARY.md exists at phase root with cross-plan synthesis"
    - "All plans 14-01..04 have SUMMARY.md files documenting their per-plan close"
    - "Atomic per-task commits using phase(14-XX): convention; final phase-close commit at end"
  artifacts:
    - path: .planning/phases/14-bootstrap-install-configurable-paths/14-SUMMARY.md
      provides: "Cross-plan phase summary for STATE.md decision history"
      contains: "Phase 14"
---

<objective>
Verify Phase 14's six functional requirements (INST-01..06) live on this Windows machine, demonstrate INST-07's first-session UX in a fresh CC session, and close the phase by updating STATE.md, ROADMAP.md, REQUIREMENTS.md, and writing 14-SUMMARY.md.

This plan does not write new functional code. It runs the hard gates, observes live behavior, updates planning docs, and produces the phase-close commit. If any hard gate fails, this plan stops and reports — it does not patch.
</objective>

<execution_context>
**Hard gates (CONTEXT.md `<acceptance>`):**
1. `bun run setup` exits 0 end-to-end on this Windows machine
2. Idempotency: re-running setup also exits 0
3. `install.sh` has executable bit; `install.bat` exists; both call `bun run setup`
4. Ollama-missing path: setup exits 1 with platform-specific link (verify via mocked unit test from 14-03; OR temporary `mv` of `ollama` binary if practical — optional live test)
5. Model `snowflake-arctic-embed2` listed by `ollama list`
6. BGE reranker venv at `services/.venv/`; `/health` returns 200
7. `src/shared/projects-dir.ts` exports `getProjectsDir()` with passing unit tests
8. All projects-dir callsites updated (grep returns only kept-with-reason hits)
9. MCP server's instructions text uses runtime projects-dir
10. `bun run build` + `bun run test` + `bun run vesna` all pass per defined baselines
11. Atomic per-task commits already in place from 14-01..04

**INST-07 verification (the deliverable's reason for being):**
After all the above pass, open a fresh Claude Code session in a project under `getProjectsDir()`. Within the first user turn:
- Session-start hook fires (visible: `<system-reminder>` block contains assembly content; or check `~/.claudex/db/claudex.db` for a fresh `session_events` row of type `session_start`)
- Angel spawns (visible: `~/.claudex/angel.pid` is fresh, or process exists with the expected entrypoint)
- MCP server is reachable (visible: a quick `claudex_search "test"` call returns without error)

If any of those three fail in the live session, INST-07 is not met and this plan reports the failure rather than closing the phase.

**STATE.md / ROADMAP.md / REQUIREMENTS.md updates:**
Read each file, locate the Phase 14 entries, change `[ ]` → `[x]`. Add the close date. For STATE.md, append a one-paragraph decision-history entry summarizing what shipped.

For ROADMAP.md's Phase 14 `Plans:` line, replace `TBD` with:
```
**Plans:** 14-01 (projects-dir helper + audit), 14-02 (callsite refactor), 14-03 (bootstrap steps), 14-04 (install wrappers), 14-05 (verify + close)
```

**Out of scope:**
- Fresh-VM cross-platform testing (Phase 16 HITL)
- README polish (Phase 16)
- Public push (Phase 17)
- Doctor diagnostics (Phase 15)

</execution_context>

<tasks>

<task id="14-05-01">
**Subject:** Run all hard gates and capture results

**Action:**
Execute the following commands in this order. Capture stdout/stderr for each. Stop and report if any fails.

```bash
bun run build                  # expect exit 0
bun run test                   # expect 3145 pass + 20 baseline llama failures unchanged
bun run vesna                  # expect 17/17 PASS
bun run setup                  # expect exit 0, steps 1-8 visible
bun run setup                  # second run; expect exit 0 with "already present" / "already healthy" markers
ollama list | grep snowflake-arctic-embed2  # expect a hit
curl -s http://localhost:7439/health        # expect 200 OK
git ls-files --stage install.sh             # expect mode 100755
```

For the final grep audit:
```bash
grep -rn "Desktop/Projects" src/ | grep -v llama-server-supervisor  # expect zero hits, OR only documented kept-with-reason
```

Capture each command's output. If any command fails or returns unexpected output, do NOT proceed to 14-05-02; instead, surface the failure as a SendMessage to team-lead with the failing command + output.

**Verification:**
- All commands ran
- Output captured (in scratch / a temp file under the phase dir)
- No unexpected failures; baseline-expected failures (20 llama tests) confirmed unchanged

**Commit:** None (intermediate; output consumed by 14-05-03)
</task>

<task id="14-05-02">
**Subject:** INST-07 live first-session verification

**Action:**
1. Confirm `getProjectsDir()` resolves to a directory containing this CLAUDEXv3 project (or any project): `node -e "console.log(require('./dist/shared/projects-dir.cjs').getProjectsDir())"`. Note the path.
2. From that path, open a fresh Claude Code session (close any existing one first).
3. In the very first user turn, observe:
   - **Session-start hook fired** — the assembly system-reminder is present in the session's first injected context. Either confirm by inspecting the visible session-start output, or by querying the DB:
     ```bash
     sqlite3 ~/.claudex/db/claudex.db "SELECT created_at, kind, session_id FROM session_events ORDER BY created_at DESC LIMIT 5;"
     ```
     A row with `kind='session_start'` from the last minute confirms the hook fired.
   - **Angel auto-spawned** — check `ls ~/.claudex/angel.pid` exists with a recent mtime, OR check process list for `node dist/angel/index.cjs`.
   - **MCP server reachable** — invoke `claudex_search "phase 14"` from within the session. Should return without error (results may be empty; that's fine — just no exception).

If any of the three fails, document the failure mode in 14-05-SUMMARY notes and stop.

**Verification:**
- All three live observations pass within the first user turn of a fresh session
- Notes captured for 14-05-03

**Commit:** None (intermediate; observations consumed by 14-05-03)
</task>

<task id="14-05-03">
**Subject:** Update STATE.md, ROADMAP.md, REQUIREMENTS.md to mark Phase 14 closed

**Action:**

1. **REQUIREMENTS.md** at `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\REQUIREMENTS.md`:
   Find INST-01..07 section. Change each `- [ ]` to `- [x]`. Append a brief note next to each ID:
   ```
   - [x] **INST-01**: ... (closed 2026-05-XX, plan 14-03/04)
   - [x] **INST-02**: ... (closed 2026-05-XX, plan 14-03)
   ...
   ```
   Use the actual close date.

2. **ROADMAP.md** at `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\ROADMAP.md`:
   Find `### Phase 14: Bootstrap install + configurable paths`. Update:
   - Status indicator (if any) → closed
   - `**Plans:** TBD` → `**Plans:** 14-01 (projects-dir helper + audit), 14-02 (callsite refactor), 14-03 (bootstrap steps), 14-04 (install wrappers), 14-05 (verify + close)`
   - Append a `**Closed:** {date}` line

3. **STATE.md** at `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\STATE.md`:
   Find the Phase 14 entry (or the v4.1 milestone progress section). Mark Phase 14 as `[x]` complete. Append a one-paragraph decision-history entry:
   ```
   **Phase 14 closed {date}.** Bootstrap is now one command (`bun run setup` or `./install.sh` / `install.bat`) end-to-end: Bun version check, Ollama detect+daemon-running, snowflake-arctic-embed2 pull, BGE reranker venv+spawn (best-effort fallback), projects-dir ensure via getProjectsDir() (CLAUDEX_PROJECTS_DIR env var, default ~/Projects/), then existing dirs/DB/config/hooks logic. MCP server's instructions text now resolves projects-dir at runtime so registered servers reflect the user's configured directory (INST-06). Reranker fallback is best-effort — bi-encoder degraded mode is documented, not a hard fail. No DB schema change; no hook semantic change.
   ```

**Verification:**
- All three planning docs reflect Phase 14 closed
- INST-01..07 all marked [x] in REQUIREMENTS.md
- ROADMAP.md Phase 14 plans line lists the five plans
- STATE.md has a decision-history entry for Phase 14
- `bun run build` + `bun run test` + `bun run vesna` still green (no source changes from this task)

**Commit:** `phase(14): close — Phase 14 STATE/ROADMAP/REQUIREMENTS updated`
</task>

<task id="14-05-04">
**Subject:** Write 14-SUMMARY.md and final phase-close commit

**Action:**
Write `C:\Users\Grigorije\Desktop\Projects\CLAUDEXv3\.planning\phases\14-bootstrap-install-configurable-paths\14-SUMMARY.md`:

```markdown
# Phase 14 — Bootstrap install + configurable paths

**Closed:** {YYYY-MM-DD}
**Milestone:** v4.1 Distribution
**Requirements satisfied:** INST-01, INST-02, INST-03, INST-04, INST-05, INST-06, INST-07

## What shipped

1. **One-command bootstrap** — `bun run setup` (and the new `./install.sh` / `install.bat` wrappers) is end-to-end: Bun version → Ollama detect+daemon → model pull → reranker venv+spawn → projects-dir ensure → DB/config/hooks. Idempotent; re-running exits 0 with no-op markers.
2. **Ollama detection** — bootstrap exits 1 with platform-specific install link if Ollama is missing or daemon unreachable (INST-02).
3. **Model pull** — `snowflake-arctic-embed2` pulled via `ollama pull`, idempotent (INST-03).
4. **Reranker bootstrap** — Python 3.11+ detection, `services/.venv/` venv creation, `pip install -r services/requirements.txt`, detached service spawn, `/health` poll (60s budget). Best-effort fallback: warn but do not hard-fail; bi-encoder degraded mode documented (INST-04).
5. **Configurable projects directory** — new `src/shared/projects-dir.ts` exports `getProjectsDir()` honoring `CLAUDEX_PROJECTS_DIR` env var (default `~/Projects/` cross-platform). All scope-detector / content-router / MCP-instructions / assembly-section callsites updated to use it. No automatic data migration of `~/.claudex/projects.json` (per CONTEXT.md decision). (INST-05)
6. **MCP runtime path** — `recall-server.ts` instructions text resolves `getProjectsDir()` at registration so the agent sees the user's actual configured directory (INST-06).
7. **First-session UX verified** — fresh CC session opened in a project under `getProjectsDir()` produces session-start assembly + Angel auto-spawn + working `claudex_search` tool within 1 user turn (INST-07).

## Plans

- 14-01 — `getProjectsDir()` helper + callsite audit
- 14-02 — apply audit replacements (code-fix, string-text-fix, comment-update, test-fixture)
- 14-03 — bootstrap steps (Bun/Ollama/model/reranker) wired into `bun run setup`
- 14-04 — `install.sh` POSIX + `install.bat` Windows wrappers
- 14-05 — verify + close

## Hard gates passed

- `bun run build` green
- `bun run test` 3145 pass + 20 baseline llama failures unchanged
- `bun run vesna` 17/17 PASS
- DB schema unchanged
- Hook semantics unchanged
- `bun run setup` idempotent on this Windows machine

## Notable decisions

- **Standard `venv` + `pip`, not uv** — minimum compatibility wins over speed for first install; uv revisitable in Phase 16+.
- **No project-registry data migration** — `~/.claudex/projects.json` is left alone; absolute paths keep resolving regardless of base-dir change.
- **Reranker is best-effort in setup** — Python on Windows can be brittle; a degraded-mode warning is acceptable. Vesna SC#1 still requires the reranker; this is documented as a known limitation for the bootstrap path, not the steady-state path.
- **Delegate to RerankerSupervisor** — setup spawns the reranker as a one-shot externally-managed process; Angel's existing supervisor detects and reuses it on first heartbeat (no duplicated lifecycle logic).

## Out of scope (deferred)

- Doctor diagnostics → Phase 15
- Fresh-VM verification on Mac/Linux → Phase 16 HITL
- README Quick Start polish → Phase 16
- Public push to grigorijejakisic/Claudex → Phase 17

## Files of interest

- `src/shared/projects-dir.ts` — single source of truth for the projects directory
- `src/cli/setup.ts` — orchestrator (enhanced, not rewritten)
- `src/cli/bootstrap-steps/` — modular, testable bootstrap step library
- `services/requirements.txt` — pinned reranker dependencies
- `install.sh`, `install.bat` — first-touch wrappers
```

Use the actual close date (today). Pull plan-by-plan details from the per-plan SUMMARY.md files written in 14-01-SUMMARY through 14-04-SUMMARY.

**Verification:**
- 14-SUMMARY.md exists in the phase directory
- Cross-references the five plans
- Hard-gate confirmations recorded
- Decision-history paragraph parallels the STATE.md entry

**Commit:** `phase(14): close — Phase 14 SUMMARY and final close`
</task>

</tasks>

<verification>
After this plan completes:
- All hard gates pass on this Windows machine
- INST-07 first-session UX confirmed live
- STATE.md, ROADMAP.md, REQUIREMENTS.md mark Phase 14 closed and INST-01..07 [x]
- 14-SUMMARY.md exists with cross-plan synthesis
- v4.1 Distribution milestone is one phase closer to complete
- Ready to advance to Phase 15 (claudex doctor diagnostics)
</verification>
