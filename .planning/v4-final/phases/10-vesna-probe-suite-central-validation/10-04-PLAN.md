---
phase: 10-vesna-probe-suite-central-validation
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/benchmark/vesna/probes/cross-project-001.json
  - src/benchmark/vesna/probes/cross-project-002.json
  - src/benchmark/vesna/probes/cross-project-003.json
  - src/benchmark/vesna/probes/lesson-application-001.json
  - src/benchmark/vesna/probes/lesson-application-002.json
  - src/benchmark/vesna/probes/lesson-application-003.json
autonomous: true
requirements:
  - VESN-01
  - VESN-02
must_haves:
  truths:
    - "3 cross-project probes exist; each has lesson stored under project A and tested via session in project B; advisory voice expected"
    - "3 lesson-application probes exist; each has past lesson with concrete decision; agent's response cites and applies the lesson"
    - "Every probe's user_prompt is perceptually-similar but lexically-distinct from the expected recall"
    - "Cross-project probes set source_project = the SOURCE project (where lesson lives) and scenario describes the TARGET project where the test runs"
    - "Canonical cross-project probe shape: shadowban-from-Lacuna applies to Mozzart-like rate-limit issue in another project (CONTEXT line 81)"
  artifacts:
    - path: src/benchmark/vesna/probes/cross-project-001.json
      provides: "Canonical cross-project probe — lesson from project A surfaces in project B"
      contains: "\"category\": \"cross-project\""
    - path: src/benchmark/vesna/probes/cross-project-002.json
      provides: "Cross-project probe — second domain pair"
      contains: "\"category\": \"cross-project\""
    - path: src/benchmark/vesna/probes/cross-project-003.json
      provides: "Cross-project probe — third domain pair"
      contains: "\"category\": \"cross-project\""
    - path: src/benchmark/vesna/probes/lesson-application-001.json
      provides: "Lesson-application probe — past architectural decision applied to current question"
      contains: "\"category\": \"lesson-application\""
    - path: src/benchmark/vesna/probes/lesson-application-002.json
      provides: "Lesson-application probe — past failure mode avoided in current decision"
      contains: "\"category\": \"lesson-application\""
    - path: src/benchmark/vesna/probes/lesson-application-003.json
      provides: "Lesson-application probe — past best-practice applied to current implementation"
      contains: "\"category\": \"lesson-application\""
  key_links:
    - from: src/benchmark/vesna/probes/cross-project-*.json
      to: claudex DB cross_project_index OR claudex_search hybrid retrieval
      via: "lesson surfaces from project A when active project is B"
      pattern: "source_project.*target"
    - from: src/benchmark/vesna/probes/lesson-application-*.json
      to: decision/learning artifacts in claudex DB
      via: "past lesson cited and applied in agent response"
      pattern: "scenario|expected_recall"
---

<objective>
Author 6 new probes (3 cross-project + 3 lesson-application) by mining real session histories where a lesson from one project would have benefitted another, OR where a past concrete decision should influence a current similar decision (CONTEXT lines 100-112).

Purpose: Fill the final 6 of the 12 core slots. Cross-project probes test Phase 6.5's HYBRID equivalence — telemetry-handle overlap + shape vocabulary firing on perceptual similarity (CONTEXT line 209). Lesson-application probes test the "agent cites prior lesson and applies it" behavior, which is downstream of OBS-02's narration directive.

Output: 6 probe JSON files. After this plan, total probe count = 14 (Plans 02 + 03) + 6 = **20 = full corpus per CONTEXT lock (17 core + 3 buffer)**.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-vesna-probe-suite-central-validation/10-CONTEXT.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mine + author 3 cross-project probes</name>
  <files>src/benchmark/vesna/probes/cross-project-001.json, src/benchmark/vesna/probes/cross-project-002.json, src/benchmark/vesna/probes/cross-project-003.json</files>
  <action>
**Mining methodology** (CONTEXT lines 102-107): Find a lesson stored in project A. Construct a probe that runs in project B, where B's user_prompt is perceptually-similar but lexically-different from A's lesson surface. Pass criterion: project A's lesson surfaces in project B's session within first 2 turns; advisory voice; lexical exclusions hold.

**Canonical example (CONTEXT line 81)**: shadowban-from-Lacuna → Mozzart-like rate-limit issue in another project.

**Suggested mining queries**:

1. `claudex_search("Mozzart 429 shadowban rate-limit per-IP auto-heal")` — lacuna-betting lesson. Apply to: a different project's rate-limit-shaped problem (oracle backend rate limits, big-mozzy-v2 API throttling, etc.).
2. `claudex_search("WebSocket polling react milliseconds")` — real-time-arch lesson. Apply to: another project's "should I poll or push?" decision.
3. `claudex_search("verify percentiles p50 p90 p99 not median")` — performance-measurement lesson (currently tagged for lacuna-betting per memory). Apply to: another project's latency reporting.
4. `claudex_search("OAuth alongside API key not just API key")` — auth lesson (memory-promoted rule). Apply to: another project's auth design.
5. `claudex_search("monitor all sports not just football")` — scope lesson. Apply to: scope-creep moments in other domains.

Pick 3 SOURCE/TARGET pairs that are PERCEPTUALLY similar but LEXICALLY distinct.

**cross-project-001.json template** (canonical shadowban example):

```json
{
  "id": "cross-project-001",
  "category": "cross-project",
  "source_session_id": "<session id where Mozzart shadowban was first decided>",
  "source_project": "lacuna-betting",
  "scenario": "Lesson lives in lacuna-betting: 'Mozzart 429 = per-IP, 15-min auto-heal — back off, rotate exit, retry after window.' Target session in big-mozzy-v2 (different project) hits a similar rate-limit shape on a different bookmaker. Probe tests cross-project HYBRID equivalence: shape vocabulary should fire and surface the Lacuna lesson advisorily.",
  "user_prompt": "Our bookmaker source started returning 429s — what's the right way to back off?",
  "expected_recall": {
    "artifact_id_or_pattern": "decision:mozzart-shadowban|project:lacuna-betting",
    "must_surface_within_turns": 2,
    "must_contain_phrase_pattern": [
      "lacuna|mozzart|per-IP|auto-heal|15.?min|rotate"
    ]
  },
  "lexical_exclusions": ["lacuna", "Mozzart", "per-IP", "auto-heal", "15-min", "shadowban"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "artifact",
      "payload": {
        "kind": "decision",
        "summary": "rate-limit shadowban detected — Mozzart returns 429 per-IP, 15-min auto-heal. Back off, rotate exit IP, retry after window.",
        "project": "lacuna-betting",
        "tags": ["rate-limit", "shadowban", "bookmaker"]
      }
    },
    {
      "kind": "artifact",
      "payload": {
        "kind": "decision",
        "summary": "Active project is big-mozzy-v2 (target project for this probe).",
        "project": "big-mozzy-v2",
        "tags": ["active-project-marker"]
      }
    }
  ]
}
```

Pre-flight check: user_prompt = "Our bookmaker source started returning 429s — what's the right way to back off?" Exclusions `["lacuna", "Mozzart", "per-IP", "auto-heal", "15-min", "shadowban"]` — none in user_prompt. NOTE: "429" is in user_prompt; do NOT exclude it (it's the perceptual signal, not a lexical leak — 429 is a generic HTTP status, not a Mozzart-specific token).

**cross-project-002.json**: pick a DIFFERENT lesson + DIFFERENT source/target pair. Example pair: source = a real-time architecture decision in claudex-v3 (e.g., "use WebSocket not polling for sub-second reactivity"); target = a question about reactivity in lacuna-betting / oracle / big-mozzy-v2 / desktop-01dcc792 / nexus.

**cross-project-003.json**: pick a third lesson + third source/target pair. Distribution rule: across the 3 cross-project probes, cover ≥3 distinct SOURCE projects (CONTEXT VESN-01 spirit: every active project should be exercised by SOMETHING in the suite).

**Hard pre-flight rule** (run mentally for EACH probe before saving): for every word in `lexical_exclusions`, verify it does NOT appear (case-insensitive substring) in `user_prompt`. Loader throws otherwise.

**Anti-pattern**: a probe whose user_prompt mentions the source project name or any exclusion word is testing keyword search, not perceptual recall. Reject and rewrite.
  </action>
  <verify>bun run build succeeds. `bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; const ps=loadProbes().filter(p=>p.category==='cross-project'); const sps=new Set(ps.map(p=>p.source_project)); console.log(ps.length, [...sps])"` prints 3 with ≥3 distinct source projects (or ≥2 if mining didn't yield clean third). No LexicalLeakageError.</verify>
  <done>3 cross-project probes exist; each has SOURCE/TARGET pair from real session lessons; each tests perceptual recall with lexical exclusions; advisory-voice surfacing expected within 2 turns.</done>
</task>

<task type="auto">
  <name>Task 2: Mine + author 3 lesson-application probes</name>
  <files>src/benchmark/vesna/probes/lesson-application-001.json, src/benchmark/vesna/probes/lesson-application-002.json, src/benchmark/vesna/probes/lesson-application-003.json</files>
  <action>
**Mining methodology** (CONTEXT lines 110-112): Find a past lesson with a CONCRETE decision attached (e.g., "used WebSocket not polling — react in milliseconds"). Construct a probe whose user_prompt is about to make a similar decision. Pass criterion: agent's response cites the prior lesson AND applies it (decision direction, not just acknowledgment).

**Difference from entity-recall**: entity probe tests "does the agent NAME the entity?". Lesson-application tests "does the agent CITE the lesson AND APPLY ITS DIRECTION to the current decision?".

**Difference from cross-project**: lesson-application can be same-project. The discriminator is decision-direction-application, not project-boundary-crossing.

**Suggested mining queries**:

1. `claudex_search("WebSocket polling react milliseconds")` — apply to a NEW reactivity decision in same project.
2. `claudex_search("V15 schema sqlite-vec qdrant removed")` — apply to a NEW vector-store decision.
3. `claudex_search("hook deadlock CLIProxyAPI Ollama")` — apply to a NEW hook design decision.
4. `claudex_search("snowflake-arctic-embed2 1024d Ollama")` — apply to a NEW embedding decision.
5. `claudex_search("V23 migration RL stack removed")` — apply to a NEW "should we add ranking layer X" decision.
6. `claudex_search("session-start hooks 26 ephemeral")` — apply to a NEW hook addition.

Pick 3 distinct decisions where past lesson clearly DIRECTS the current decision.

**lesson-application-001.json template** (vector store decision):

```json
{
  "id": "lesson-application-001",
  "category": "lesson-application",
  "source_session_id": "<session 47 id — where Qdrant was removed for sqlite-vec>",
  "source_project": "claudex-v3",
  "scenario": "Past decision: 'Qdrant was removed in session 47 for sqlite-vec — single-store design, vec0 virtual tables.' (See context/specs/SQLITE_VEC_MIGRATION.md.) New session asks whether to introduce a separate vector DB for a new feature. Agent should cite the prior decision and apply its direction (stay single-store).",
  "user_prompt": "We need vector search for the new ABC feature — should I spin up Pinecone or Qdrant alongside?",
  "expected_recall": {
    "artifact_id_or_pattern": "context/specs/SQLITE_VEC_MIGRATION.md|decision:single-store",
    "must_surface_within_turns": 2,
    "must_contain_phrase_pattern": [
      "sqlite-vec|single.?store|vec0|already in.*claudex.db"
    ]
  },
  "lexical_exclusions": ["sqlite-vec", "vec0", "single-store", "session 47", "MIGRATION.md"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "artifact",
      "payload": {
        "kind": "decision",
        "summary": "Qdrant removed in session 47; switched to sqlite-vec / vec0 virtual tables embedded in claudex.db. Single-store design — never add a separate vector DB.",
        "project": "claudex-v3",
        "tags": ["architecture", "vector-store", "single-store"]
      }
    }
  ]
}
```

Pre-flight check: user_prompt mentions "Pinecone" and "Qdrant" — exclusions `["sqlite-vec", "vec0", "single-store", "session 47", "MIGRATION.md"]` — none in user_prompt. Pass.

NOTE: `must_contain_phrase_pattern` is a single regex with alternation, so passing requires ANY of: `sqlite-vec`, `single-store`/`single store`, `vec0`, or `already in claudex.db`. The agent has multiple correct ways to cite-and-apply.

**lesson-application-002.json**: pick a different decision. Example: "use WebSocket not polling" lesson → new question about a similar reactivity gap. Source project can be lacuna-betting or whichever owns the original decision.

**lesson-application-003.json**: pick a third decision. Variety: one architecture, one tech-stack, one process/safety decision (e.g., "verify before claiming done" applied to a current "is this done?" question — but that overlaps with constraint-recall; pick a DIFFERENT shape — e.g., "always use OAuth alongside API key" applied to a current auth design question).

**Categorical pitfall**: lesson-application probes that DON'T have a concrete decision in the lesson devolve into entity-recall. Each probe MUST have a directional decision the agent is expected to apply, not just an entity to name.

Pre-flight rule: same — `lexical_exclusions ∩ user_prompt = ∅`. Loader will throw otherwise.
  </action>
  <verify>bun run build succeeds. `bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; const ps=loadProbes().filter(p=>p.category==='lesson-application'); console.log(ps.length, ps.map(p=>p.id))"` prints 3 with ids `[ 'lesson-application-001', 'lesson-application-002', 'lesson-application-003' ]`. No LexicalLeakageError. Total probe count `loadProbes().length` should now be 20 (5 migrated + 3 buffer + 6 from Plan 03 + 6 from Plan 04 = 20).</verify>
  <done>3 lesson-application probes exist; each based on a real concrete decision; each tests cite-and-apply (not just naming); pre-flight passes; total corpus reaches the 20-probe lock from CONTEXT.</done>
</task>

</tasks>

<verification>
- After this plan, total probe count: 5 + 3 + 6 + 6 = **20** (matches CONTEXT lock: 17 core + 3 buffer)
- Per-category distribution: entity-recall 3, constraint-recall 3, handoff-pickup 3, cross-project 3, lesson-application 3, self-instrumented 2, buffer 3 → matches CONTEXT lines 76-85 EXACTLY
- All 6 new probes load via `loadProbes()` without error
- All 6 pass lexical pre-flight
- Cross-project: ≥2 distinct source projects covered across the 3 probes
- Lesson-application: each has a directional decision (not just an entity)
</verification>

<success_criteria>
- VESN-01 fully satisfied: probes mined from real session histories across multiple projects (claudex-v3 + lacuna-betting + ≥1 others via cross-project)
- VESN-02 fully satisfied: ~20 probes covering all 6 categories per the locked distribution
- "Perceptual not lexical" rigor holds across the entire corpus
</success_criteria>

<output>
After completion, create `.planning/phases/10-vesna-probe-suite-central-validation/10-04-SUMMARY.md` listing: each new probe's id, source project pair (for cross-project) or source decision (for lesson-application), the perceptual signal it tests, and the final 20-probe corpus distribution table.
</output>
