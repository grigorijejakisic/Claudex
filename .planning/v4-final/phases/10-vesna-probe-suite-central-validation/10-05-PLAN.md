---
phase: 10-vesna-probe-suite-central-validation
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - .github/workflows/vesna.yml
  - src/benchmark/vesna/README.md
  - .planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md
autonomous: true
requirements:
  - VESN-04
must_haves:
  truths:
    - "GitHub Actions workflow at .github/workflows/vesna.yml runs `bun run vesna` on every PR"
    - "PR fails to merge if Vesna gate fails (aggregate <80% OR any non-empty category <80%)"
    - "Probe authoring README at src/benchmark/vesna/README.md documents the 7 sections from CONTEXT lines 160-167"
    - "Phase close summary documents the 20-probe corpus, schema, and how downstream phases (6.5, 8.5, 11) consume the harness"
  artifacts:
    - path: .github/workflows/vesna.yml
      provides: "GitHub Actions workflow — runs Vesna on every PR; gate-blocking required check"
      contains: "bun run vesna"
    - path: src/benchmark/vesna/README.md
      provides: "Probe authoring guide per CONTEXT lines 158-167"
      contains: "## Probe JSON schema"
    - path: .planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md
      provides: "Phase 10 close summary"
      contains: "20-probe corpus"
  key_links:
    - from: .github/workflows/vesna.yml
      to: package.json scripts.vesna
      via: "bun run vesna invocation"
      pattern: "bun run vesna"
    - from: src/benchmark/vesna/README.md
      to: src/benchmark/vesna/types.ts (Plan 01 schema)
      via: "README documents the canonical schema"
      pattern: "Probe JSON schema"
---

<objective>
Close Phase 10 with the two remaining deliverables: (1) CI integration via GitHub Actions so `bun run vesna` runs on every PR with the per-category + aggregate ≥80% gate as a required check; (2) probe authoring guide at `src/benchmark/vesna/README.md` per CONTEXT.md lines 158-167 so downstream phases (6.5, 8.5, 11) can author their own probes without ambiguity.

Purpose: Without CI integration, VESN-04 is half-shipped (harness exists but no merge gate). Without the authoring guide, downstream phases will drift from the canonical schema. Both deliverables are gating to phase closure.

Output: GitHub Actions workflow file, README authoring guide, phase close summary, ROADMAP / STATE / REQUIREMENTS check-marks updated.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-vesna-probe-suite-central-validation/10-CONTEXT.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-01-PLAN.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-02-PLAN.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-03-PLAN.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-04-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions workflow for Vesna gate</name>
  <files>.github/workflows/vesna.yml</files>
  <action>
Create `.github/workflows/vesna.yml`. The workflow must:

1. Trigger on `pull_request` against the default branch (and `push` to default branch for post-merge sanity).
2. Run on `ubuntu-latest`.
3. Steps:
   - actions/checkout@v4
   - oven-sh/setup-bun@v2 with bun-version: latest (or pin to the version in package.json if specified)
   - Install dependencies: `bun install --frozen-lockfile`
   - Build: `bun run build`
   - Run Vesna: `bun run vesna -- --json > vesna-report.json`
     - The `--json` flag emits machine-readable SuiteReport (per Plan 01 cli.ts spec)
     - Exit code 0 iff `gated === true` (per Plan 01 cli.ts spec)
   - Upload `vesna-report.json` as a workflow artifact (actions/upload-artifact@v4) for diagnostic visibility on failure.
   - Post a workflow summary table (using `$GITHUB_STEP_SUMMARY`): aggregate pass rate + per-category pass rates + count of flaky probes + list of failing probe IDs with diagnostics.

4. Use `permissions: contents: read` (no write needed).
5. Use `concurrency: group: vesna-${{ github.ref }}, cancel-in-progress: true` so superseded PR runs cancel.
6. Set `timeout-minutes: 30` — Vesna with 20 probes × 3 trials × ~30s/trial ≈ 30 min upper bound. Tune down after first real run.

**Critical**: do NOT skip the workflow on docs-only PRs unless we explicitly carve out a path filter. Per CONTEXT line 37 "Pass rate ≥80% required to merge ANY PR" — the gate is unconditional. (If docs-only PRs become a friction problem post-shipping, the path filter can be added in a follow-up — not in Phase 10.)

**Test DB note**: The harness uses a separate `claudex-vesna-test.db` per Plan 01. CI environment has no `~/.claudex/` directory by default — verify the harness creates the directory + test DB on first run, or add a workflow step that does it explicitly:

```yaml
- name: Initialize test DB directory
  run: mkdir -p ~/.claudex/db
```

**Reranker note (Plan 01 cross-encoder dependency)**: The reranker service runs on port 7439 locally but is NOT available in CI. Plan 01's runner.ts uses bi-encoder fallback per CLAUDE.md spec ("bi-encoder path is only used as a fallback"). Document in the workflow that bi-encoder fallback is the CI mode — this is acceptable for v4 probes since the lexical-exclusion rigor + bi-encoder cosine similarity should still cover perceptual recall. Add a workflow comment explaining this trade-off.

Branch protection setup is OUT OF SCOPE for this plan (requires repo admin GitHub UI action; not Claude-automatable). Document it in the close summary as a follow-up.
  </action>
  <verify>YAML lints cleanly: `bunx js-yaml .github/workflows/vesna.yml >/dev/null` (or any YAML validator). `git status` shows the new file. (Cannot test the workflow itself without pushing to GitHub, but the YAML is structurally valid.)</verify>
  <done>.github/workflows/vesna.yml exists; triggers on PRs; runs bun run vesna with --json output; uploads report artifact; emits step summary; uses concurrency cancellation.</done>
</task>

<task type="auto">
  <name>Task 2: Probe authoring guide README</name>
  <files>src/benchmark/vesna/README.md</files>
  <action>
Create `src/benchmark/vesna/README.md` per CONTEXT lines 158-167. The README must contain SEVEN sections in this order:

1. **How to mine candidate probes from session history** — query `claudex_events` and `claudex_search` to find retrieval moments; describe what makes a "good" candidate (perceptual recall opportunity, not lexical match).
2. **Probe JSON schema** — full TypeScript-shaped reference matching `src/benchmark/vesna/types.ts` (Plan 01). Include the canonical example from CONTEXT lines 53-70 and explain each field.
3. **Per-category authoring conventions** — copy CONTEXT lines 89-118 verbatim (the 6-category authoring guide), with one or two real probe examples per category (cite the actual probes shipped in Plans 02-04).
4. **Lexical exclusion rules** — restate CONTEXT line 207: probes whose user_prompt shares text with the expected recall are testing keyword search, not memory. Include the loader's pre-flight assertion behavior (LexicalLeakageError) and how to author exclusions: "list the most specific tokens from the expected memory that an agent would 'leak' if memory weren't doing real work; verify by hand that none appear in user_prompt".
5. **Setup-step DSL** — document the four `kind` values (artifact / handoff / critical_rule / narration_directive) and their payloads, citing `src/benchmark/vesna/setup.ts`. Include the test DB isolation guarantee: production `~/.claudex/db/claudex.db` is NEVER touched.
6. **Evaluation mode selection** — auto vs semi-auto. CONTEXT line 156 says "auto-evaluation where deterministic; semi-auto for behavioral nuance with first 2 turns logged for human review on borderline cases". v4 corpus is auto-only; semi-auto is reserved for future probes.
7. **Adding a probe to CI** — extremely short: "drop a `*.json` file into `src/benchmark/vesna/probes/`; CI picks it up on next PR via `bun run vesna`. No code changes needed. Update probe id following `<category>-<3-digit>` convention."

Add a closing **"Hand-forward to other phases"** section per CONTEXT lines 171-174:
- Phase 6.5 authors 3 cross-project probes (or claims buffer slots)
- Phase 8.5 authors 2 self-instrumented probes (already done in Plan 02)
- Phase 11 runs full ~20-probe suite as SC#1 final validation
- Future phases that ship behavioral changes SHOULD author probes for them (encouraged, not enforced — per CONTEXT line 174)

Keep the README ≤300 lines. Reference Plan 01 / Plan 02 / Plan 03 / Plan 04 SUMMARY files for deeper provenance, but DON'T duplicate their content.

Anti-pattern to avoid: the README is for ENGINEERS authoring probes, not a phase report. Make it actionable — every section should answer "what do I do?".
  </action>
  <verify>`wc -l src/benchmark/vesna/README.md` ≤ 300. README has all 7 sections + hand-forward + closing. Probes referenced match what was shipped (entity-001, constraint-001, etc.).</verify>
  <done>src/benchmark/vesna/README.md exists with 7 sections per CONTEXT lock + hand-forward; documents canonical schema, lexical exclusion rules, setup-step DSL, evaluation mode, and how to add a probe to CI.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — full-suite run + summary + REQUIREMENTS / ROADMAP / STATE updates</name>
  <files>.planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md</files>
  <action>
**Step 1 — Run the full suite locally and capture the report:**

```bash
bun run vesna -- --json > /tmp/vesna-phase-10-close-report.json
echo "Exit code: $?"
```

If exit code = 0 (gate pass) → phase ready to close. Capture the JSON for the close summary.

If exit code != 0 (gate fail) → INVESTIGATE before closing. Possibilities:
- A probe's lexical_exclusions are mis-tuned → loosen and document in the SUMMARY for that plan, then re-run.
- A probe's expected_recall is unrealistic for the current pipeline → tag the probe as flaky in its JSON (add `flaky: true` field — Plan 01 should support this; if not, fall back to widening must_contain_phrase_pattern).
- The harness has a real bug → file as a follow-up; do NOT mask.

**A failing close run is not a phase failure** — Phase 10's deliverable is the SURFACE, not the green-bar score. The score is Phase 11's gate. Phase 10 closes when (a) the 20-probe corpus exists, (b) `bun run vesna` runs without crashing, (c) CI workflow exists, (d) authoring guide exists. The CLOSE-SUMMARY documents the actual pass rate honestly — green or yellow — without spinning it.

**Step 2 — Write the close summary at `.planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md`:**

```markdown
# Phase 10 Close Summary — Vesna Probe Suite as Central Validation

**Status:** Closed YYYY-MM-DD
**Plans:** 5 (10-01 through 10-05)
**Requirements satisfied:** VESN-01, VESN-02, VESN-04
**Requirements deferred:** VESN-03 (Phase 11 gate runs the suite)

## Corpus Distribution

| Category | Count | Probe IDs |
|---|---|---|
| entity-recall | 3 | entity-001/002/003 |
| constraint-recall | 3 | constraint-001/002/003 |
| handoff-pickup | 3 | handoff-001/002/003 |
| cross-project | 3 | cross-project-001/002/003 |
| lesson-application | 3 | lesson-application-001/002/003 |
| self-instrumented | 2 | self-instrumented-001/002 |
| buffer (unallocated) | 3 | buffer-001/002/003 |
| **Total** | **20** |  |

Matches CONTEXT.md lock (lines 76-85): 17 core + 3 buffer = 20.

## First Full-Suite Result

[paste exit code + aggregate pass rate + per-category breakdown from /tmp/vesna-phase-10-close-report.json]

## Schema (canonical, src/benchmark/vesna/types.ts)

[paste the Probe interface from types.ts as the spec record]

## Harness

- Loader, setup-step DSL, evaluator, three-trial runner, CLI: `src/benchmark/vesna/`
- CLI entry: `bun run vesna` (and `bun run vesna -- --json` for machine-readable)
- Production DB untouched: harness uses isolated `~/.claudex/db/claudex-vesna-test.db`

## CI

- Workflow: `.github/workflows/vesna.yml`
- Trigger: every PR + push to default branch
- Gate: aggregate ≥80% AND every non-empty category ≥80%
- Follow-up: branch-protection rule to make Vesna a required check (manual GitHub UI step; not Claude-automatable)

## Hand-forward to Downstream Phases

- Phase 6.5: 3 cross-project probes already shipped; if 6.5 wants its own canonical probe, claim a buffer slot.
- Phase 8.5: 2 self-instrumented probes already shipped (recall-observability-gap-detection, recall-observability-empty-surface, migrated to canonical schema in Plan 10-02).
- Phase 11: runs the full 20-probe suite as SC#1.
- Future phases authoring own probes: see `src/benchmark/vesna/README.md` Section 7.

## Behavioral Discipline (the why)

Vesna is BEHAVIORAL, not benchmark. Every probe traces to a real retrieval moment in real session history. The "perceptual not lexical" rigor (lexical_exclusions enforced at load time) means probes test memory, not keyword search. Per CONTEXT axiom: "Behavioral validation is a first-class deliverable. Benchmarks were dropped because green numbers feel like progress while artifacts regress."

## Known Caveats

- CI runs in bi-encoder fallback mode (BGE-reranker port 7439 unavailable in CI). v4 acceptable trade-off; revisit if cross-encoder access in CI becomes feasible.
- Branch protection is a manual follow-up.
- Probes are static — auto-mining from telemetry is deferred (CONTEXT line 224).
```

**Step 3 — Update REQUIREMENTS.md, ROADMAP.md, STATE.md:**

- `.planning/REQUIREMENTS.md`: tick `[x]` on VESN-01, VESN-02, VESN-04. Leave VESN-03 `[ ]` (Phase 11 owns).
- `.planning/ROADMAP.md`: under Phase 10, set status to closed (consistent with how Phase 9 was marked closed); add commit hash placeholder for the phase-close commit (filled by execute-phase).
- `.planning/STATE.md` (or wherever STATE-equivalent lives): update last-completed-phase to 10.

**Step 4 — Phase close commit** (handled by execute-phase tooling, but document expected message):

```
phase(10): close — Vesna probe suite shipped as central validation

20-probe corpus (17 core + 3 buffer) covering 6 categories.
Harness at src/benchmark/vesna/; CI gate via .github/workflows/vesna.yml.
Authoring guide at src/benchmark/vesna/README.md.
VESN-01/02/04 satisfied. VESN-03 → Phase 11.
```

Do NOT run the commit yourself in this task — execute-phase wraps the phase-close commit per project convention. Just stage everything via git add and let the orchestrator commit.
  </action>
  <verify>`bun run vesna -- --json` runs to completion (exit code captured). `.planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md` exists with all 7 sections. REQUIREMENTS.md shows `[x]` on VESN-01/02/04. ROADMAP.md shows Phase 10 closed. `git status` shows the expected staged files.</verify>
  <done>Full Vesna suite ran (green or honestly-yellow); close summary written; REQUIREMENTS / ROADMAP / STATE updated; ready for phase-close commit.</done>
</task>

</tasks>

<verification>
- .github/workflows/vesna.yml exists, YAML valid, runs `bun run vesna --json`
- src/benchmark/vesna/README.md exists with 7 sections per CONTEXT
- 10-CLOSE-SUMMARY.md exists, includes corpus distribution + first-suite result + hand-forward
- REQUIREMENTS.md: VESN-01/02/04 = [x], VESN-03 = [ ] (Phase 11)
- ROADMAP.md: Phase 10 marked closed
- bun run vesna executes end-to-end (exit code documented in close summary regardless of green/yellow)
- bun run test still passes (no regressions from CI/README/SUMMARY changes)
</verification>

<success_criteria>
- VESN-04 fully satisfied: harness + CI + README all shipped
- Phase 10 corpus complete at 20 probes (17 core + 3 buffer)
- Downstream phases (6.5, 8.5, 11, future) have an actionable authoring path
- Phase closes with honest pass-rate disclosure, not theatrical green
</success_criteria>

<output>
After completion, the file `.planning/phases/10-vesna-probe-suite-central-validation/10-CLOSE-SUMMARY.md` IS the plan's summary; no separate `10-05-SUMMARY.md` needed (the close summary covers the full phase). If execute-phase tooling expects per-plan summaries, ALSO create `10-05-SUMMARY.md` as a thin pointer: "see 10-CLOSE-SUMMARY.md".
</output>
