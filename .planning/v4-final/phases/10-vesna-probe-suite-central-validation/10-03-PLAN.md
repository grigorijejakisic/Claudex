---
phase: 10-vesna-probe-suite-central-validation
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/benchmark/vesna/probes/entity-001.json
  - src/benchmark/vesna/probes/entity-002.json
  - src/benchmark/vesna/probes/entity-003.json
  - src/benchmark/vesna/probes/constraint-001.json
  - src/benchmark/vesna/probes/constraint-002.json
  - src/benchmark/vesna/probes/constraint-003.json
autonomous: true
requirements:
  - VESN-01
  - VESN-02
must_haves:
  truths:
    - "3 entity-recall probes exist, each mined from a real session in claudex-v3 / lacuna-betting / oracle / big-mozzy-v2 / desktop-01dcc792 / nexus"
    - "3 constraint-recall probes exist, each grounded in a real critical_rules entry or session-derived constraint"
    - "Every probe's user_prompt is perceptually-similar but lexically-distinct from the expected recall (lexical_exclusions enforce this)"
    - "Every probe has source_session_id traceable via claudex_search or claudex_recall"
    - "Each probe's scenario field documents what retrieval moment in real session history inspired it"
  artifacts:
    - path: src/benchmark/vesna/probes/entity-001.json
      provides: "entity-recall probe — agent surfaces named project/system from prior context"
      contains: "\"category\": \"entity-recall\""
    - path: src/benchmark/vesna/probes/entity-002.json
      provides: "entity-recall probe — agent surfaces named person/component"
      contains: "\"category\": \"entity-recall\""
    - path: src/benchmark/vesna/probes/entity-003.json
      provides: "entity-recall probe — agent surfaces named tool/library decision"
      contains: "\"category\": \"entity-recall\""
    - path: src/benchmark/vesna/probes/constraint-001.json
      provides: "constraint-recall probe — agent surfaces a hard rule before acting"
      contains: "\"category\": \"constraint-recall\""
    - path: src/benchmark/vesna/probes/constraint-002.json
      provides: "constraint-recall probe — agent surfaces a process constraint (verify-before-claim)"
      contains: "\"category\": \"constraint-recall\""
    - path: src/benchmark/vesna/probes/constraint-003.json
      provides: "constraint-recall probe — agent surfaces a safety rule (never X)"
      contains: "\"category\": \"constraint-recall\""
  key_links:
    - from: src/benchmark/vesna/probes/entity-*.json
      to: claudex DB conversation_turns table
      via: "source_session_id traceable to a real turn"
      pattern: "source_session_id"
    - from: src/benchmark/vesna/probes/constraint-*.json
      to: critical_rules table OR claudex_search-recoverable lesson
      via: "constraint surfaces from claudex memory channel"
      pattern: "critical_rules|decision|learning"
---

<objective>
Author 6 new probes (3 entity-recall + 3 constraint-recall) by mining real session histories across all active projects per CONTEXT.md line 14 and VESN-01. Each probe's user_prompt is perceptually-similar but lexically-distinct from the expected recall — lexical exclusions enforce this rigor (CONTEXT lines 207-209).

Purpose: Fill 6 of the 12 remaining core slots (Plan 04 fills the other 6). Establishes the "real-session-history" provenance discipline that makes Vesna behavioral rather than benchmark-shaped (CONTEXT line 199).

Output: 6 probe JSON files, all canonically-shaped, all loadable, all passing lexical pre-flight, all auditable via source_session_id.
</objective>

<execution_context>
@C:/Users/Grigorije/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Grigorije/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-vesna-probe-suite-central-validation/10-CONTEXT.md
@.planning/phases/10-vesna-probe-suite-central-validation/10-01-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mine + author 3 entity-recall probes</name>
  <files>src/benchmark/vesna/probes/entity-001.json, src/benchmark/vesna/probes/entity-002.json, src/benchmark/vesna/probes/entity-003.json</files>
  <action>
**Mining methodology** (CONTEXT lines 47-50): Use `claudex_events` to scan recent sessions across all 6 active projects. For each session, identify retrieval moments where a named entity (project, system, person, tool, library) was relevant. Then construct a probe whose user_prompt mentions the entity-shaped concept WITHOUT naming it explicitly (CONTEXT lines 89-92).

**Suggested mining queries** (run in order; pick the FIRST candidate that yields a clear entity + perceptually-distinct prompt):

1. `claudex_search("Mozzart cloudflare 429 shadowban")` → project_mozzart_cloudflare_429 entity from lacuna-betting. (Used in self-instrumented-001; do NOT reuse — pick a different entity for entity-recall.)
2. `claudex_search("WebSocket polling react milliseconds")` → real-time architecture decision. Possible entity: "WebSocket adapter" or specific lib.
3. `claudex_search("BGE reranker port 7439 cross-encoder")` → reranker entity in claudex-v3.
4. `claudex_search("oracle 3951898e architecture")` → an oracle subsystem.
5. `claudex_search("big-mozzy v2 stitching pipeline")` → entity in big-mozzy-v2.
6. `claudex_search("nexus e53c6c93")` → entity in nexus.

For each chosen entity, author a probe per CONTEXT lines 89-92:

**entity-001.json template** (concrete example — adapt entity per mining):

```json
{
  "id": "entity-001",
  "category": "entity-recall",
  "source_session_id": "<session id from claudex_events>",
  "source_project": "claudex-v3",
  "source_turn_idx": 0,
  "scenario": "Prior session decided BGE-reranker-v2-m3 on port 7439 is the cross-encoder for hybrid retrieval; bi-encoder is fallback. New session asks about 'the local cross-encoder service' without naming BGE/7439. Agent should name the actual service.",
  "user_prompt": "What's our local cross-encoder service for retrieval?",
  "expected_recall": {
    "artifact_id_or_pattern": "decision:bge-reranker-port-7439",
    "must_surface_within_turns": 2,
    "must_contain_phrase_pattern": [
      "BGE.*reranker|bge-reranker-v2-m3",
      "7439"
    ]
  },
  "lexical_exclusions": ["BGE", "7439", "bge-reranker", "v2-m3", "port"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "artifact",
      "payload": {
        "kind": "decision",
        "summary": "Use BGE-reranker-v2-m3 on port 7439 as cross-encoder; bi-encoder (snowflake-arctic-embed2) is fallback only",
        "project": "claudex-v3",
        "tags": ["retrieval", "reranker", "RETR-08"]
      }
    }
  ]
}
```

Pre-flight check: user_prompt = "What's our local cross-encoder service for retrieval?" — exclusions `["BGE", "7439", "bge-reranker", "v2-m3", "port"]` — none in user_prompt. Pass.

**entity-002.json**: pick a DIFFERENT entity from a DIFFERENT project. Suggested: a lacuna-betting or oracle named subsystem. Mining query: `claudex_search("lacuna-betting bookmaker adapter")` or `claudex_search("oracle ingest pipeline")`. Author per same template.

**entity-003.json**: pick a DIFFERENT entity from a DIFFERENT third project (big-mozzy-v2 / nexus / desktop-01dcc792 preferred — distribute across projects per VESN-01).

**Hard pre-flight rule** (run mentally for EACH probe before saving): for every word in `lexical_exclusions`, verify it does NOT appear (case-insensitive substring) in `user_prompt`. If it does, either rephrase the user_prompt or remove the word from exclusions. The loader will throw `LexicalLeakageError` at load time otherwise.

**Distribution rule (VESN-01)**: 3 entity-recall probes SHOULD cover ≥2 different active projects. Don't put all 3 in claudex-v3.
  </action>
  <verify>bun run build succeeds. `bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; const ps=loadProbes().filter(p=>p.category==='entity-recall'); console.log(ps.length, ps.map(p=>p.source_project))"` prints 3 with at least 2 distinct projects in the project list. No LexicalLeakageError.</verify>
  <done>3 entity-recall probes exist; each mined from a real session (source_session_id traceable); each tests perceptual-not-lexical recall; distribution covers ≥2 active projects.</done>
</task>

<task type="auto">
  <name>Task 2: Mine + author 3 constraint-recall probes</name>
  <files>src/benchmark/vesna/probes/constraint-001.json, src/benchmark/vesna/probes/constraint-002.json, src/benchmark/vesna/probes/constraint-003.json</files>
  <action>
**Mining methodology** (CONTEXT lines 95-98): Find rules in `critical_rules` table OR feedback memories that the agent must surface BEFORE acting. Then construct a probe whose user_prompt is about to do something the rule applies to.

**Suggested mining queries**:

1. `claudex_search("never use --no-verify")` → git-safety rule. Surfaces in CLAUDE.md project rules.
2. `claudex_search("verify before claiming done")` → engineering rule from `~/.claude/CLAUDE.md` Rule 7.
3. `claudex_search("Bun test vitest do not use bun test")` → CLAUDE.md project rule about Bun's test runner.
4. `claudex_search("hook deadlock CLIProxyAPI")` → CLAUDE.md project safety rule.
5. `claudex_search("scope lock no silent cleanup")` → ~/.claude/CLAUDE.md Rule 5.
6. `claudex_search("read before editing")` → ~/.claude/CLAUDE.md Rule 6.

Pick 3 distinct rules. Each probe sets up the rule (via `setup_steps: [{ kind: "critical_rule", payload: ... }]` OR ensures it's reachable through the user/project CLAUDE.md memory channel — note: critical_rules table is the cleaner setup path).

**constraint-001.json template** (Bun test rule):

```json
{
  "id": "constraint-001",
  "category": "constraint-recall",
  "source_session_id": "<session id where this rule was first surfaced/violated>",
  "source_project": "claudex-v3",
  "scenario": "Project rule: 'Do NOT use bun test — invokes Bun's native runner, not Vitest.' New session asks the agent to run the test suite. Agent should surface the rule and use vitest, not bun test.",
  "user_prompt": "Run the test suite to confirm nothing broke.",
  "expected_recall": {
    "artifact_id_or_pattern": "critical_rule:no-bun-test",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": [
      "bun run test|vitest|bun test.*native runner|do not use bun test"
    ]
  },
  "lexical_exclusions": ["bun test", "vitest", "Bun", "native runner"],
  "evaluation": "auto",
  "setup_steps": [
    {
      "kind": "critical_rule",
      "payload": {
        "rule": "Do NOT use `bun test` — invokes Bun's native runner, not Vitest. Use `bun run test`.",
        "project": "claudex-v3"
      }
    }
  ]
}
```

Pre-flight check: user_prompt = "Run the test suite to confirm nothing broke." Exclusions `["bun test", "vitest", "Bun", "native runner"]` — none in user_prompt. Pass.

Note one subtlety: `must_contain_phrase_pattern` uses regex alternation here so EITHER `vitest` OR `bun run test` OR a do-not-use-bun-test phrase counts as a pass. The rationale: the agent has multiple correct ways to surface the rule.

**constraint-002.json**: pick a different rule (e.g., "verify before claiming done" or "never --no-verify"). Different project context if available (use a lacuna-betting or oracle-specific rule if mining surfaces one; else claudex-v3 is fine — distribution requirement is softer for constraints since rules tend to be project-specific).

**constraint-003.json**: pick a third distinct rule. Aim for variety: one git/safety rule, one process rule, one engineering rule.

Pre-flight rule: same as Task 1 — `lexical_exclusions ∩ user_prompt = ∅`.

**Categorical pitfall**: do NOT make constraint-recall probes that are really just entity-recall in disguise. The hallmark of constraint-recall is the agent must surface a RULE (rule-shaped: "do/don't X", "always/never Y") *before acting*, not just name an entity.
  </action>
  <verify>bun run build succeeds. `bunx ts-node -e "import { loadProbes } from './src/benchmark/vesna/loader'; const ps=loadProbes().filter(p=>p.category==='constraint-recall'); console.log(ps.length, ps.map(p=>p.id))"` prints 3 with ids `[ 'constraint-001', 'constraint-002', 'constraint-003' ]`. No LexicalLeakageError.</verify>
  <done>3 constraint-recall probes exist; each based on a real rule (CLAUDE.md or critical_rules entry); each tests rule-surfacing-before-acting (not entity-naming); pre-flight passes.</done>
</task>

</tasks>

<verification>
- After this plan, total probe count: 5 migrated + 3 buffer + 6 new = 14
- All 6 new probes load via `loadProbes()` without error
- All 6 pass lexical pre-flight
- entity-recall: 3 distinct entities, ≥2 distinct projects (VESN-01)
- constraint-recall: 3 distinct rules, varied types (git/process/engineering)
- Every probe has a real source_session_id (queryable via claudex_recall) — no fabricated provenance
</verification>

<success_criteria>
- VESN-01 partial: 6 more probes mined from real session histories. (Plan 02 already migrated 5 from real Phase 7.5/8.5 sessions; Plan 04 will mine 6 more from cross-project + lesson-application moments.)
- VESN-02 partial: 6/12 remaining core slots filled. After Plan 04: 17/17 core + 3 buffer = 20 = COMPLETE per CONTEXT lock.
- "Perceptual not lexical" rigor (CONTEXT line 207) holds: every probe's exclusions are non-empty AND distinct from user_prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/10-vesna-probe-suite-central-validation/10-03-SUMMARY.md` listing: each probe's id, source_session_id, source_project, the entity/rule it tests, and the perceptual-vs-lexical surface used to test it.
</output>
