{
  "id": "self-instrumented-001",
  "category": "self-instrumented",
  "source_session_id": "phase-8.5-design",
  "source_project": "claudex-v3",
  "scenario": "Topic has rich prior experience in memory (Mozzart 429 / shadowban). User asks about that topic. Per OBS-02 narration directive, agent should narrate 'checking ... applying' on retrieval gold.",
  "user_prompt": "How should I handle the bookmaker rate-limit issue we hit last week?",
  "expected_recall": {
    "artifact_id_or_pattern": "decision:mozzart-shadowban",
    "must_surface_within_turns": 1,
    "must_contain_phrase_pattern": [
      "checking",
      "applying"
    ]
  },
  "lexical_exclusions": ["mozzart", "shadowban", "429", "auto-heal", "per-IP"],
  "evaluation": "auto",
  "setup_steps": [
    { "kind": "narration_directive", "payload": { "silent": false } },
    {
      "kind": "artifact",
      "payload": {
        "kind": "decision",
        "summary": "rate-limit shadowban detected — Mozzart returns 429 per-IP, 15-min auto-heal",
        "project": "lacuna-betting"
      }
    }
  ]
}
