---
schema: claudex/session-log
version: 1
session_id: 32464f85-7311-410e-85df-ead67e8c2a93
scope: project:claudex-v3
created_at: 2026-03-20T01:50:00Z
source_transcripts: []
handoff_id: null
---

# Session: MCP Server Fix + Full Wiring Audit + Dead Pipeline Repair
Date: 2026-03-20
Session: 20

## Flow
Started by verifying session 19's fixes after CC restart. MCP recall server wasn't connecting — traced to hand-rolled JSON-RPC stdio timing out after 30s (CC debug log confirmed). Rewrote with official @modelcontextprotocol/sdk — connected immediately. Then ran 5 parallel audit agents tracing every pipeline end-to-end. Found 2 real broken pipelines (retrieval_score inert, learnings invisible) and 3 dead code items. Also caught 2 false positives from audit agents (session summaries and trigger materialization were already wired correctly). Fixed everything, 1613 tests pass.

## Snapshot
None

## Where We Left Off
All fixes implemented, built, tested, deployed. MCP recall server connected (needs CC restart to pick up tools in session). All known wiring issues resolved.

## Work Completed
- Rewrote MCP recall server with official @modelcontextprotocol/sdk (was hand-rolled stdio, 30s timeout)
- Diagnosed CC MCP config: lives in ~/.claude.json via `claude mcp add`, not settings.json
- Fixed retrieval_score not used in search ORDER BY (was collected but inert)
- Added learnings section to full assembly (Priority 4, budget-gated)
- Removed dead code: duplicate import, unused import, never-produced event types
- Ran 5-agent parallel pipeline audit covering all 10+ data flows
- Corrected 2 false audit findings (session summaries + trigger materialization already wired)

## Files Changed
- src/mcp/recall-server.ts — rewrote with SDK (McpServer + StdioServerTransport + Zod schemas)
- src/core/artifacts.ts — added retrieval_score DESC to all 3 search ORDER BY clauses
- src/assembly/assembler.ts — added learnings section at Priority 4, removed duplicate getMaterializedArtifacts import
- src/assembly/sections.ts — added formatLearningsSection() formatter
- src/adapters/shared/lifecycle.ts — removed unused getObservationsByProject import
- src/core/session-events.ts — removed never-produced file_delete and error event types
- build.ts — added @modelcontextprotocol/sdk + zod to esbuild externals
- package.json / bun.lock — added @modelcontextprotocol/sdk dependency
- .claude/settings.json — cleaned up stale mcpServers key
- ~/.claude/settings.json — removed stale mcpServers key

## Decisions Made
- MCP recall server uses official SDK, not hand-rolled JSON-RPC (CC's stdio client expects SDK-compatible transport)
- CC stores MCP configs in ~/.claude.json via `claude mcp add`, NOT in settings.json
- Learnings injected at Priority 4 in full assembly (after checkpoint, before flow), max 5, budget-gated
- retrieval_score placed after importance in ORDER BY (importance is primary rank, score is tiebreaker)
- Dead event types removed rather than implemented (no extractors produce them, no DB rows exist)

## Learnings
- CC's MCP client uses official SDK transport — hand-rolled Content-Length framing times out silently
- `claude mcp list` shows health status; `claude mcp add -s user` is the correct way to register servers
- Audit agents can produce false positives — always verify findings against actual code before fixing
- The assembly waterfall (steps 1-6 in assembleRegularPrompt) is by design mutually exclusive

## Notes for Next Session
- Restart CC to pick up claudex-recall MCP tools (claudex_search, claudex_recall, claudex_store, claudex_events)
- All session 19 + 20 changes are uncommitted (19 files, +706/-269 lines) — commit when ready
- Session 19's verification items from handoff are mostly resolved: MCP ✓, wiring ✓. Remaining: live correction test
