---
schema: claudex/session-log
version: 1
session_id: ec83b7f5-a425-4955-b85e-a2a4d9c64616
scope: project:claudex-v3
created_at: 2026-03-17T00:00:00Z
source_transcripts: []
handoff_id: v3-post-session-14
---

# Session: Review Fix Mega-Team + User-Correction Extraction
Date: 2026-03-17
Session: 14

## Flow
Recovered dropped SSH session's state from Claudex DB — found unified review (Grade C, 7 critical) and architecture review (Grade C, 63/100) of Experience Patterns + Team Shared Memory features. 46 findings total. User said "fix ALL of them, not just critical." Presented full deduplicated finding set. Disagreed with review on 7 points (documented reasoning). User corrected my prioritization mindset: "we do it right or we don't do it." Deployed 3 PMs + 11 workers across 3 waves. 6 Wave 1 workers completed, then 3 Wave 2, then 2 Wave 3. CRITICAL DISCOVERY: 5 files' worker edits didn't persist (W1, W2 partial, W3, W5-sections). Meanwhile Echo died (OAuth 401) — fixed by pushing credentials + switching auth profile to oauth mode. User pointed out I SHOULD have known this (memory lesson exists). This exposed the REAL architectural gap: experience_patterns table was EMPTY — zero patterns ever created. Root cause: extraction only worked on rigid assistant self-reflective phrases that never occur naturally. Built user-correction extraction as PRIMARY source (ExpeL/Meta-Policy Reflexion inspired). Also fixed the misleading comment about dedup score thresholds.

## Snapshot
None

## Where We Left Off
17 files modified, 1 new. 79 test files, 1477/1477 passing. BUT 5 files need re-application (worker edits lost). Need to run /full-review on all changes before committing. User explicitly requested this.

## Work Completed

### Team Deployment (3 PMs, 11 workers, 3 waves)
- Deployed largest team ever for this project
- Full Claudex enrichment via worker-context CLI for each worker
- PMs verified worker output (though MCP messaging didn't connect — workers reported directly)

### Fixes That Landed (12 files modified, 1 new)
- `assembler.ts` — C3: Budget/side-effects split (applyEffects callback pattern)
- `worker-context.ts` — A6: async, A9: fileScope in queries, O3: heading level, O4: hard cap, O9: primer CWD
- `worker-observations.ts` — C5: DB dedup, C8: artifact promotion (TSM no longer write-only!), R3: session hash, R15: cachedPrepare, O7: paragraph preservation
- `artifact-claims.ts` — R4: TTL validation, O10: dedup IDs, A5: JSDoc deviation, schema alignment, O1: telemetry
- `file-leases.ts` — R4: TTL validation, R16: expired filter, O1: telemetry
- `migrations.ts` — C4: single-owner PK, C9: FTS trigger scoped, A1: INTEGER type, A2: worker index, A3: learnings FTS, A4: porter stemmer
- `stop.ts` — R12: extracted to experience-scoring.ts (149→~50 lines)
- `post-tool-use.ts` — R9: NotebookEdit path, O5: spec markers
- `experience-scoring.ts` — NEW: extracted scoring + wired user-correction as PRIMARY extraction
- `correction-detection.ts` — ARCHITECTURAL FIX: extractLessonFromUserCorrection() as primary source
- `cli/worker-context.ts` — async cascade
- All 6 test files updated (async, fixtures, imports, DB handles, e2e real hooks, fake timers)

### Fixes That Did NOT Land (need re-application)
- `behavioral-signals.ts` — C1: hash fallback, R5: secret regex, R18: content scanning
- `experience-patterns.ts` — C6/C7: global type guard, R17: verb filter, O2: comment, O6: redact enrichment, O1: telemetry, O5: spec markers
- `experience-flags.ts` — R1: JSON.parse guard, R2: Number cast, O1: telemetry
- `user-prompt-submit.ts` — R6: unconditional flag clear
- `sections.ts` — C2: complete XML escape

### Echo OAuth Fix
- Pushed ~/.claude/.credentials.json to server
- Switched auth profile from "token" to "oauth" mode
- Gateway restarted, no more 401 errors

### Architectural Discovery: User-Correction Extraction
- experience_patterns table was EMPTY — zero patterns ever created in production
- Root cause: extractPatternFromAssistantText requires rigid self-reflective phrases ("the fix is...", "going forward I will...")
- Assistant responses are practical, not self-reflective — regex never matches
- Built extractLessonFromUserCorrection() as PRIMARY extraction (user states lessons directly: "always X", "never Y")
- Wired into experience-scoring.ts: user extraction first, assistant extraction as fallback
- Backed by ExpeL and Meta-Policy Reflexion (2025) research

## Files Changed
- src/adapters/cc-hooks/stop.ts — extracted experience scoring to new file
- src/adapters/cc-hooks/post-tool-use.ts — R9 NotebookEdit, O5 spec markers
- src/assembly/assembler.ts — C3 budget/side-effects split with applyEffects
- src/assembly/worker-context.ts — async, fileScope, heading level, hard cap, primer CWD
- src/cli/worker-context.ts — async cascade
- src/core/migrations.ts — 6 schema fixes in new migration
- src/intelligence/artifact-claims.ts — TTL, dedup, JSDoc, schema alignment, telemetry
- src/intelligence/correction-detection.ts — NEW extractLessonFromUserCorrection()
- src/intelligence/experience-scoring.ts — NEW extracted scoring + user-correction wiring
- src/intelligence/file-leases.ts — TTL, expired filter, telemetry
- src/intelligence/worker-observations.ts — artifact promotion, dedup, session hash, cachedPrepare, paragraphs
- src/tests/assembly/worker-context.test.ts — async await cascade
- src/tests/integration/experience-patterns-e2e.test.ts — real hook calls
- src/tests/intelligence/artifact-claims.test.ts — DB handles, type fix
- src/tests/intelligence/experience-patterns.test.ts — global scope fixtures, fake timers, BM25 decoupling
- src/tests/intelligence/file-leases.test.ts — expired filter behavior
- src/tests/intelligence/worker-observations.test.ts — vitest imports

## Decisions Made
- User-correction extraction is PRIMARY, assistant extraction is SUPPLEMENTARY (not the other way around)
- Only discovery patterns may be promoted to global scope (corrections are project-specific)
- experience-scoring.ts extracted from stop.ts — stop becomes thin orchestrator
- artifact_claims uses single-owner PK (artifact_id INTEGER PRIMARY KEY)
- experience_patterns_fts uses porter stemmer (matching observations_fts)
- Side effects in assembly deferred via applyEffects callback (only committed after budget check)
- 5-token correction penalty cap stays (matches 3-word topic key granularity)

## Learnings
- Agent tool workers may not persist Edit operations — edits can silently fail. ALWAYS verify file contents after worker completion, not just their self-report.
- MCP team messaging doesn't connect to native Task-spawned agents — workers report directly to main agent.
- The experience pattern system was architecturally blind: designed to extract from assistant self-reflection that never happens in natural conversation. The user's correction IS the lesson.
- OAuth is ALWAYS the auth method. Never check for API keys, never ask. Push credentials + ensure oauth mode.

## Notes for Next Session
1. **RUN /full-review on all changes** — user explicitly requested this before committing
2. **Re-apply 5 lost files** — behavioral-signals.ts, experience-patterns.ts, experience-flags.ts, user-prompt-submit.ts, sections.ts. Detailed specs in ACTIVE.md handoff.
3. **After review + re-application: commit everything as one coherent commit**
4. Consider writing tests for extractLessonFromUserCorrection() — currently untested
5. 79 files, 1477 tests passing
