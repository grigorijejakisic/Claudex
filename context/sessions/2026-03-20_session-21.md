---
schema: claudex/session-log
version: 1
session_id: 15d6b515-6b24-4fe5-8ca3-2d06ca802fb1
scope: project:claudex-v3
created_at: 2026-03-20T01:22:40Z
source_transcripts: []
handoff_id: null
---

# Session: Comprehensive Audit + 10 Production Fixes
Date: 2026-03-20
Session: 21

## Flow
Full product audit across all 15 subsystems revealed 7 findings (2 bugs, 3 design gaps, 2 cosmetic). Fixed all 7, then user asked 10 deep architecture questions that revealed 3 more issues (CLAUDE.md enforcement, Flow structure, table rebuilds). Fixed those too. Total: 10 fixes across 20 files, +754/-87 lines. User asked about competitive landscape — searched the internet, confirmed Claudex has no equivalent (experience patterns, behavioral signals, artifact lifecycle unique). User considering open source vs commercial path. DB migrated through V5→V6→V7. Health check went from 8 constraint warnings to 0.

## Snapshot
None

## Where We Left Off
All fixes applied, built, tested (1613/1613 passing). DB at V7. Health: HEALTHY (1 warning — orphaned sessions, auto-cleaned next restart). Permissions updated with 7 new tools. Ready to move to Nexus.

## Work Completed
- Comprehensive 15-subsystem audit with live DB verification
- Fixed: MCP events default project fallback (latest active session across all projects)
- Fixed: Reasoning chain timestamp overflow (ms→s, V5 migration)
- Fixed: Assembly budget 4K→8K tokens (was exhausting on identity+project alone)
- Fixed: retrieval_feedback table missing (created in V5 migration)
- Fixed: Read extractor misclassification (title-only scan for Read/Grep/Glob)
- Fixed: Gauge loop detection flooding (30s cooldown via BehavioralCounters)
- Fixed: Test DB isolation (CLAUDEX_DB_PATH env var for wrapHook tests)
- Added: CLAUDE.md rule enforcement in post-compaction assembly (formatRulesReminderSection)
- Added: Flow restructuring with structured metadata (JournalMetadata, detectMilestone returns MilestoneResult)
- Added: Orphaned session recovery at SessionStart (auto-closes stale >1h sessions with synthesized summaries)
- Added: Bash command entity label cleanup (summarizeBashCommand)
- Full table rebuild: observations (20K rows + FTS5), sessions, pressure_scores, session_journal — all NOT NULL constraints fixed
- Non-standard observation categories fixed (change/discovery/feature → code/other)
- Permissions: added claudex-recall MCP tools + Agent + TaskCreate + TaskUpdate to settings.json
- Migration chain: V4→V5→V6→V7, initializeSchema targets V7

## Files Changed
- src/mcp/recall-server.ts — claudex_events fallback to latest active session
- src/shared/constants.ts — budget_tokens 4000→8000
- src/extraction/scoring.ts — Read/Grep/Glob scan title only, skip content keywords
- src/extraction/type-classifier.ts — Read/Grep/Glob always return 'routine'
- src/intelligence/experience-flags.ts — BehavioralCounters.last_loop_signal_epoch + 30s cooldown
- src/core/migrations.ts — V5 (retrieval_feedback + timestamp fix), V6 (journal metadata + pressure/session_journal rebuild), V7 (observations + sessions rebuild with NOT NULL)
- src/core/session-events.ts — summarizeBashCommand for clean entity labels
- src/core/journal.ts — JournalMetadata interface, addJournalEntry accepts metadata, parseJournalMetadata
- src/adapters/shared/lifecycle.ts — detectMilestone returns MilestoneResult with structured metadata
- src/adapters/cc-hooks/session-start.ts — orphaned session recovery, imports for session events + stmt cache
- src/assembly/sections.ts — formatRulesReminderSection (CLAUDE.md extraction), formatFlowSection (metadata hints)
- src/assembly/assembler.ts — import + wire formatRulesReminderSection in post-compaction assembly
- src/tests/adapters/cc-hooks/infrastructure.test.ts — CLAUDEX_DB_PATH isolation for wrapHook tests
- src/tests/adapters/cc-hooks/hooks.test.ts — detectMilestone tests updated for MilestoneResult
- src/shared/config.test.ts — budget_tokens 4000→8000
- src/tests/shared/config.test.ts — budget_tokens 4000→8000
- src/tests/core/migration-v2v3.test.ts — user_version 4→7
- src/tests/mcp/recall-server.test.ts — user_version 4→7
- ~/.claude/settings.json — 7 new tool permissions added

## Decisions Made
- Budget doubled to 8K: identity+project alone consumed ~4K, leaving nothing for learnings/checkpoint/flow/reference/materialization
- Read/Grep/Glob never scan content for error keywords: file content containing "error" in try/catch is not an error observation
- CLAUDE.md rules enforced post-compaction only: prevents drift without duplicating what CC already loads
- Table rebuild approach: probe-based (try inserting NULL, rebuild only if it succeeds) — idempotent
- Migration chain refactored: TARGET_VERSION constant + sequential loop instead of nested if/else
- Open source vs commercial: recommended open-source core + commercial intelligence layer

## Learnings
- SQLite CHECK constraints on categories: v2 data had 'change', 'discovery', 'feature' categories not in CHECK list — UPDATE before rebuild
- SQLite PRIMARY KEY columns don't report notnull=1 in PRAGMA table_info — false positive in verification scripts
- MCP servers are long-lived processes: code changes require CC restart to take effect
- The competitive landscape validates our architecture: boundary-only injection, quality gates, selective curation all backed by 2025 research

## Notes for Next Session
- CC restart needed: budget 8K, loop cooldown 30s, orphan recovery, entity labels, rules enforcement all in dist but not yet active
- After restart: verify health shows 0 warnings (orphans auto-cleaned)
- After restart: test claudex_events without params (fallback fix in new MCP server)
- All Claudex work verified — switch to Nexus per handoff plan
